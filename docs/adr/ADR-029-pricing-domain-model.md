# ADR-029 — Pricing Domain Model

| Campo | Valor |
|--------|-------|
| **ADR** | ADR-029 |
| **Título** | Pricing Domain Model |
| **Status** | Aprovado |
| **Data** | 2026-08-16 |
| **Decisores** | Project Mimikyu |
| **Decisão** | Pricing é um quarto domínio conceitual, independente de Catálogo Editorial, Patrimônio do Usuário (Ownership) e Analytics — nunca um apêndice de nenhum dos três. Modelado como dez entidades próprias (`pricing_source` até `pricing_sync_run_call`, ver `05f-pricing.md`), sem reaproveitar `asset_source`/`card_external_reference`/`card_set_external_reference`/`catalog_admin_action_log` do Catálogo Editorial. **Fundação física completa — dez de dez entidades `CONFIRMADO EXECUTADO`**: `pricing_source`, `card_condition`, `pricing_condition_mapping` no Incremento P1 (2026-08-16); `pricing_set_mapping`, `pricing_card_mapping` no Incremento P2 (2026-08-16, mesmo dia); `pricing_sync_run`, `pricing_sync_run_call` no Incremento P3 (2026-08-16, mesmo dia); `pricing_product` no Incremento P4 (2026-08-16, mesmo dia); `pricing_fx_rate` no Incremento P5 (2026-08-16, mesmo dia); `pricing_observation` no Incremento P6 (2026-08-16, mesmo dia). **Incremento P7 (2026-08-17)**: primeira fonte cadastrada — `JUSTTCG` em `pricing_source`, homologada para piloto técnico de produtos/printings/condições/preços em USD/histórico (Decisão A aprovada), não homologada para PT-BR nem "Valor Brasil" (Decisão B inconclusiva), `is_active = FALSE` (piloto condicionado, não produção). **Incremento P12 — Exibição de Preços no Frontend formalmente encerrado (2026-08-18)**: preço em BRL visível nos três grids do catálogo, sem N+1, com hierarquia de printing e sem nunca chamar a JustTCG no caminho de visualização (ver revisão `1.23`, abaixo). `JUSTTCG.is_active` permanece `FALSE`. Pricing ainda não é operacional em produção: seguem pendentes plano comercial contratado e agendamento automático de PTAX/conector (próximo incremento recomendado: P13). **Incremento P14.1 (2026-08-19)**: condição comercial satisfeita — `JUSTTCG.is_active` passa de `FALSE` para `TRUE` (Query `3911`), com piloto real controlado confirmando o caminho técnico (7 requests, 32 observações novas, zero duplicidade). Escopo ainda restrito a ME1/BASE1 (P8); agendamento automático (P14.2+) e ampliação de cobertura permanecem pendentes. **Incremento P14.2 (2026-08-19, mesmo dia)**: conector reescrito para descoberta paginada em lotes de até 100 cartas/requisição (`GET /v1/cards` com `limit`/`offset`), eliminando o padrão de uma chamada por carta de P8; resolução de Set passa a usar `release_date` exata (nunca nome — nenhuma tabela local tem nome em inglês confiável); correspondência de carta por número de coleção primário + nome só como verificação secundária, nunca confirmando ambiguidade automaticamente; nenhuma migration necessária (`match_status='PENDING'` já existente reaproveitado). Validado 100% offline (`tsc --strict` limpo, 50/50 asserções). **Incremento P14.2 formalmente encerrado (2026-08-19, mesmo dia)**: piloto real do Set `BASE4` validado — run `9df36424-b8f8-4ee8-a67f-2c04c74bd3fa`, `COMPLETED`, 130 mappings (127 `CONFIRMED`/3 `PENDING`), 635 `pricing_product`/635 `pricing_observation`. Achado de performance registrado: persistência sequencial no Supabase, ~156s. **Incremento P14.3 formalmente encerrado (2026-08-19, mesmo dia, ciclo seguinte)**: persistência em lotes via quatro funções `SECURITY INVOKER` restritas a `service_role` (Migrations `3912`–`3915`, `CONFIRMADO EXECUTADO`) — promoção de mapeamento só `PENDING`/`NOT_FOUND`→`CONFIRMED`, upsert em lote de `pricing_product`, pré-busca de observação corrigida de tupla exata para última observação por grupo (regra "histórico só por mudança real de preço"). Robustez de credencial corrigida (`resolveEntryDecision()`): `--fixture-check` sempre offline explícito, ausência de qualquer credencial fora desse modo encerra com código diferente de zero, sem rede prévia. `tsc --strict` limpo, 151/151 asserções offline. Piloto real aprovado: run `8e4367ad-f95d-41b6-bbbe-449bd7fb635f`, `COMPLETED`, 14,87s (contra ~156s do P14.2, ~10x mais rápido), 152 observações escritas (todas com mudança real de preço, zero redundante), `BASE4` inalterado (127/3). P14 como um todo permanece em andamento — próximo incremento recomendado: expandir para todos os Sets do catálogo. Ver `05f-pricing.md`, revisão `1.45`. |
| **Documentos Relacionados** | `ADR-006-separation-of-catalog-ownership-and-analytics.md`, `ADR-008-external-catalog-data-sources.md`, `ADR-013-collection-item-identity-model.md`, `ADR-028-card-variant-governance.md`, `../04-domain-model.md`, `../05f-pricing.md`, `../standards/STD-001-database-standards.md`, `../standards/STD-002-domain-modeling.md` |

---

# Context

Em 2026-08-16, Fabrício aprovou a sequência estratégica **Card Variant (fundação encerrada) → Pricing/Market Data → Collection → Analytics/Valuation** (`ROADMAP.md`), tornando Pricing a próxima frente comprometida do projeto. Um discovery anterior, na mesma data, já havia pesquisado fontes de mercado candidatas (JustTCG, TCGdex embutindo Cardmarket/TCGplayer), estratégia de conversão para BRL (PTAX do Banco Central) e estratégia de histórico (snapshots próprios) — mas era material de apoio à decisão, não uma modelagem aprovada, e permanece assim: a homologação técnica de qualquer fonte (JustTCG, especificamente) segue em paralelo, **sem concluir** no momento desta decisão — a prova técnica (`PROVA-TECNICA-JUSTTCG-PRICING-2026-08-16.md`, fora de `docs/`) foi interrompida por limitação operacional de cota, não por aprovação nem reprovação da fonte.

Esta ADR resolve uma pergunta diferente e anterior à homologação de qualquer fonte específica: **onde e como Pricing se encaixa na arquitetura já estabelecida do projeto**, de forma que a modelagem permaneça válida independentemente do resultado da homologação da JustTCG ou de qualquer outra fonte.

Havia uma hipótese inicial de nomes de tabela (`pricing_sources`, `pricing_set_mappings`, `pricing_card_mappings`, `pricing_products`, `pricing_condition_mappings`, `pricing_observations`, `pricing_sync_runs`, `item_valuation_snapshots`) fornecida como ponto de partida para confronto contra os padrões reais do repositório — não como modelo já aprovado.

---

# Decision

## Pricing é um quarto domínio, não um apêndice

`ADR-006` já separa o domínio em três responsabilidades — Catálogo Editorial, Patrimônio do Usuário (Ownership), Analytics. Pricing não pertence a nenhuma das três sem distorção (ver `05f-pricing.md`, seção "Por que Pricing é um Domínio Independente", para o racional completo): não é dado editorial oficial (Catálogo), não pertence a nenhum usuário especificamente (Ownership), e não é puramente derivado de Catálogo+Ownership (Analytics) — é, ele mesmo, um dado primário importado de terceiros, com seu próprio ciclo de staging/confirmação/auditoria, que Analytics consumirá depois.

`ADR-008` (fontes externas nunca são dependência estrutural em tempo real — sempre um pipeline `fonte → importação → catálogo interno soberano`) é estendida, pela primeira vez, para além do Catálogo Editorial: o mesmo princípio rege como Pricing consome fontes externas de preço.

## Dez entidades descritas, nove próprias de Pricing, modelo detalhado em `05f-pricing.md`

`pricing_source`, `card_condition` (referência compartilhada, não exclusiva de Pricing — corrigido em 2026-08-16, ver "Correção Arquitetural Pontual", abaixo), `pricing_condition_mapping`, `pricing_set_mapping`, `pricing_card_mapping`, `pricing_product`, `pricing_fx_rate`, `pricing_observation`, `pricing_sync_run`, `pricing_sync_run_call` — modelo lógico e físico completo (colunas, tipos, obrigatoriedade, PKs/FKs, uniques, checks, índices, cardinalidades, política de exclusão, RLS/grants, imutabilidade) em `05f-pricing.md`. As três primeiras (`pricing_source`, `card_condition`, `pricing_condition_mapping`) foram criadas no Supabase no Incremento P1 (2026-08-16, ver "Incremento P1 — Fundação Física", abaixo); as demais sete permanecem propostas.

## Nenhuma tabela do Catálogo Editorial é reaproveitada por Pricing

Apesar do padrão estrutural de `asset_source`/`card_external_reference`/`card_set_external_reference` (Catálogo Editorial, `05c-assets-e-importacao.md`) ser deliberadamente **repetido** por Pricing — mesma disciplina já validada em produção —, nenhuma dessas tabelas é referenciada fisicamente por uma tabela de Pricing. `pricing_source` é uma tabela própria, distinta de `asset_source`; `pricing_set_mapping`/`pricing_card_mapping` são tabelas próprias, distintas de `card_set_external_reference`/`card_external_reference`. Ver "Alternatives Considered" para o racional dessa escolha.

## Preço, condição, idioma e printing são dimensões independentes — nunca colapsadas

Herdado das correções conceituais já validadas durante a prova técnica da JustTCG (revisão 2 do documento de prova) e agora formalizado como decisão de modelagem permanente:

- **printing ≠ condição.** Só o acabamento/impressão (`printing`) tem relação com `Card Variant` (`ADR-028`) — condição de conservação nunca cria nem qualifica um `card_variant`; pertence à cotação (`pricing_observation.condition_id`, referenciando `card_condition` — ver nota de correção abaixo) e, futuramente, ao item físico do usuário (Ownership), nunca ao Catálogo. Reforça `ADR-006` diretamente.
- **moeda ≠ mercado.** `pricing_observation.currency_code` (a moeda em que o preço foi reportado) e `pricing_observation.market_label` (o mercado/mecanismo que originou o preço, ex.: TCGplayer vs. Cardmarket dentro de uma mesma fonte agregadora) são colunas independentes — uma fonte pode reportar mais de uma moeda para mercados diferentes que ela agrega (achado real do discovery: o campo `pricing` embutido da TCGdex combina Cardmarket em EUR e TCGplayer em USD).
- **cobertura de catálogo ≠ cobertura de idioma/impressão ≠ cobertura de mercado.** Modeladas como três perguntas distintas e independentes, nunca combinadas numa única resposta binária: `pricing_card_mapping.match_status` responde "esta é a mesma Card?" (`CONFIRMED`/`PENDING`/`NOT_FOUND`/`REJECTED` — ver correção de precisão abaixo); `pricing_product.language_status`/`language_id`/`card_variant_id` respondem "esta é a mesma impressão/idioma?"; `pricing_observation.market_scope`/`market_evidence` respondem "este preço específico vem de qual tipo de mercado, com que evidência?" (não mais `pricing_source.default_market_scope` isoladamente — ver correção abaixo). Uma Card pode estar `CONFIRMED` em `pricing_card_mapping` e ainda assim não ter nenhuma classificação de valuation por item, se a segunda ou a terceira pergunta não forem satisfeitas.

## Correção Arquitetural Pontual (2026-08-16, mesma data, ciclo seguinte)

Cinco pontos corrigidos a pedido explícito de Fabrício, sem reabrir a modelagem inteira nem as decisões já corretas acima. Detalhamento completo, por entidade, em `05f-pricing.md` revisão `1.1`. Resumo normativo:

1. **Idioma não pode ser modelado em função de PT-BR.** `pricing_product.language_status` generalizado de tri-estado binário (`CONFIRMED`=PT-BR / `NOT_CONFIRMED`=não-PT-BR / `UNDETERMINED`) para tri-estado neutro e multi-idioma: `CONFIRMED` (idioma explícito e confiável, qualquer idioma) / `INFERRED` (idioma inferido por heurística, sem declaração dedicada da fonte) / `UNDETERMINED`. `confirmed_language_id` renomeado para `language_id` (obrigatório em `CONFIRMED`/`INFERRED`, nulo em `UNDETERMINED`). Cobertura de um idioma específico (PT-BR ou qualquer outro) passa a ser sempre **derivada por comparação** — `pricing_product.language_id = collection_item.language_id` (futuro) —, nunca lida de um campo binário. Valuation direto exige `language_status = 'CONFIRMED'`; `INFERRED` não autoriza equivalência direta do item.
2. **"Valor Brasil" deve depender da evidência de mercado da observação, não só da fonte.** `pricing_source.market_scope` renomeado para `default_market_scope` — passa a ser classificação/capacidade declarada e default, nunca mais autoridade final. `pricing_observation` ganha `market_scope` (`INTERNATIONAL`/`BRAZIL`/`UNDETERMINED`), `market_label` (renomeado de `market`, mesmo propósito) e `market_evidence` (`JSONB`, evidência normalizada). Nova regra obrigatória: `BRAZIL_ITEM_VALUATION` só é autorizada quando a observação usada como base tiver `market_scope = 'BRAZIL'` **e** `market_evidence_confirmed = TRUE` — a classificação da fonte, isoladamente, nunca basta. Motivado por fontes agregadoras que reportam mais de um mercado ao mesmo tempo (ex.: JustTCG combina Cardmarket/TCGplayer, ambos internacionais); fixar a decisão só na fonte impediria representar corretamente uma futura fonte BR que também agregue um mercado internacional secundário, sem duplicar artificialmente o cadastro da fonte.
3. **Distinguir ausência confirmada de nunca testado.** `pricing_set_mapping`/`pricing_card_mapping` ganham um quarto estado, `NOT_FOUND` — consulta tecnicamente concluída sem correspondência localizada, distinta de (a) ausência de linha (nunca avaliado) e de (b) `REJECTED` (um candidato específico, encontrado e explicitamente rejeitado). A versão `1.0` continha exatamente essa contradição: tratava `REJECTED` como cobrindo simultaneamente "candidato rejeitado" e "busca concluída sem candidato algum" — corrigido. Falha técnica nunca gera `NOT_FOUND` (permanece só em `pricing_sync_run_call.outcome = 'TECHNICAL_FAILURE'`). `external_set_id`/`external_card_id` tornam-se opcionais, obrigatórios só em `CONFIRMED`; a `UNIQUE` simples de `(fonte, id externo)` foi substituída por um índice único parcial (`WHERE match_status = 'CONFIRMED'`), para não impedir que o mesmo candidato externo seja avaliado e descartado para mais de uma Card/Set ao longo do tempo. Novo campo `last_checked_at`, porque a cobertura de uma fonte externa pode mudar no futuro.
4. **Condição canônica deve ser referência compartilhada.** `pricing_condition` renomeada para `card_condition` e reclassificada como referência compartilhada e neutra — não pertence a Pricing nem ao Catálogo Editorial, para que `Collection Item` (Ownership, futuro) não precise depender de uma tabela nominalmente pertencente a Pricing só porque foi definida ali primeiro. `pricing_condition_mapping` permanece entidade própria de Pricing, agora referenciando `card_condition`. O total de entidades descritas em `05f-pricing.md` permanece 10, mas apenas 9 são exclusivas do domínio Pricing.
5. **Diagrama Mermaid corrigido.** Removidas as relações `CARD_VARIANT`↔`LANGUAGE` e `PRICING_FX_RATE`↔`PRICING_OBSERVATION` do ER — ambas já eram declaradas, no próprio texto da versão `1.0`, como não tendo FK física, o que as tornava contraditórias como relações desenhadas. Movidas para nota textual, fora do diagrama.

Nenhuma tabela criada, nenhuma migration, nenhuma chamada à API da JustTCG nesta correção. A condição da homologação da JustTCG permanece inalterada (pendente, não aprovada nem reprovada), assim como os critérios pré-registrados das Decisões A/B, intocados.

## "Valor Brasil" é uma propriedade da observação com evidência de mercado, nunca da fonte isolada nem da conversão de moeda

**Corrigido em 2026-08-16 (ver "Correção Arquitetural Pontual", acima)** — a versão original desta ADR atribuía essa autoridade exclusivamente a `pricing_source.market_scope`. `pricing_observation.market_scope` (`INTERNATIONAL`/`BRAZIL`/`UNDETERMINED`) combinado com `market_evidence_confirmed = TRUE` é o mecanismo que autoriza a classificação futura `BRAZIL_ITEM_VALUATION` (Analytics, ver `05f-pricing.md`, seção "Item Valuation") — `pricing_source.default_market_scope` é apenas a classificação/default declarado da fonte, nunca suficiente sozinha. Nenhuma conversão de `pricing_fx_rate` (USD→BRL) promove uma fonte ou uma observação internacional a "Valor Brasil" — a conversão é sempre informativa, nunca persistida sobre o preço original (`pricing_observation.price`/`currency_code` são imutáveis), e nunca muda `market_scope`/`market_evidence_confirmed` da observação que a originou.

## Preço é imutável — histórico nunca é sobrescrito

`pricing_observation` e `pricing_fx_rate` não têm coluna `updated_at` — divergência deliberada do "Padrão Mínimo" de auditoria do STD-001 (Seção 4: `id`/`created_at`/`updated_at`), justificada porque ambas são tabelas de fato de série temporal (log de eventos imutável), não entidades de negócio mutáveis. Nenhuma role de aplicação recebe `UPDATE`/`DELETE` nelas. Idempotência (nunca duplicar a mesma observação) é garantida por `UNIQUE` + `ON CONFLICT DO NOTHING`, mesmo padrão já exigido de Seeds pelo STD-001.

## Auditoria de sincronização em dois níveis, com precedente já validado em produção real

`pricing_sync_run` (execução, nível alto) e `pricing_sync_run_call` (cada chamada individual — endpoint, status HTTP, resultado lógico de três estados, cota restante, erro sempre sanitizado) não são uma invenção especulativa: espelham diretamente o mecanismo já **implementado e exercitado contra a API real da JustTCG** no script local da prova técnica (`Executar-ProvaJustTCG-Fase-A-B.ps1`) — contrato de três estados (`Sucesso`/`FalhaTecnica`/`OrcamentoInterrompido`), log duplo de retry após `429`, teto de segurança de cota, e redação defensiva de qualquer padrão de chave de API antes de persistir qualquer texto de erro. A modelagem eleva um mecanismo já comprovado a schema permanente, em vez de desenhá-lo do zero.

---

# Consequences

## Benefícios

- Pricing pode ganhar, perder ou substituir fontes (JustTCG, TCGplayer, uma futura fonte brasileira) sem reconstrução funcional — toda tabela referencia `pricing_source.id`, nunca o nome de uma fonte específica;
- a rejeição ou aprovação da JustTCG (ainda pendente) não bloqueia nem invalida nenhuma linha desta modelagem — o modelo é genérico por fonte desde a primeira entidade;
- a separação entre correspondência de Card (`pricing_card_mapping`), correspondência de impressão/idioma (`pricing_product`) e evidência de mercado por observação (`pricing_observation.market_scope`/`market_evidence_confirmed`, corrigido em 2026-08-16 — antes só `pricing_source.market_scope`) torna impossível, por construção, que uma impressão internacional receba automaticamente o rótulo "Valor Brasil" ou o valor de uma cópia de qualquer idioma específico do usuário;
- o histórico de preço nunca é perdido por sobrescrita, e toda conversão de moeda é rastreável até a taxa e a data exatas usadas;
- a auditoria de sincronização (execução + chamada individual) já nasce no mesmo padrão de rigor comprovado pela prova técnica real, incluindo a disciplina de nunca persistir segredo.

## Restrições / Pendências

- nenhuma tabela foi criada — a implementação física é um ciclo futuro próprio, condicionado à conclusão (aprovação, reprovação, ou aprovação parcial só para a Decisão A) da homologação de pelo menos uma fonte, para que a primeira seed real (`pricing_source`, `pricing_condition_mapping`) tenha dado real para se apoiar;
- a policy de leitura de Pricing para usuário final (não-admin) permanece em aberto — só será desenhada quando Collection existir e precisar exibir preço estimado ao colecionador, mesmo padrão de pendência já registrado em `ADR-028` para o seletor de Card Variant;
- `item_valuation_snapshot` (Analytics) é apenas esboçada conceitualmente em `05f-pricing.md` — sua modelagem física fica para quando Collection existir, por depender de `collection_item_id`, que ainda não tem tabela;
- o risco legal de redistribuição comercial de dado de preço (Cardmarket/TCGplayer, identificado no discovery) não é resolvido por esta ADR — `pricing_source.requires_commercial_agreement`/`terms_url`/`attribution_text` dão à modelagem um lugar para essa informação viver, sem decidir a questão jurídica.

## Incremento P1 — Fundação Física (2026-08-16, mesma data, ciclo seguinte à Correção Arquitetural Pontual)

Primeira implementação física do domínio, a pedido explícito de Fabrício, limitada às três entidades que não dependem da homologação de nenhuma fonte externa: `pricing_source`, `card_condition`, `pricing_condition_mapping` — `CONFIRMADO EXECUTADO` no Supabase, exatamente conforme o modelo aprovado em `05f-pricing.md` (estrutura, RLS, triggers, grants, validação de 12 itens). Milhar `3000`–`3999` formalizado como módulo Pricing em `STD-001` (subseção própria). Nenhuma fonte cadastrada, nenhuma condição semeada, nenhuma chamada à JustTCG, nenhuma das outras sete entidades implementada — permanecem propostas.

## Incremento P7 — Homologação Condicionada da JustTCG (2026-08-17, ciclo posterior — Incremento P6 encerrava a fundação física)

A pedido explícito de Fabrício, decorrente da conclusão (com ressalva) da prova técnica interrompida por limitação de cota em 2026-08-16. Introspecção obrigatória confirmou schema real de `pricing_source` (colunas, `CHECK`s, RLS, policy `pricing_admin_select`, grants `authenticated`=`SELECT`/`anon`=nenhum/`service_role`=`SELECT`) idêntico ao já `CONFIRMADO EXECUTADO` no Incremento P1, tabela ainda vazia; Termos de Uso vigentes da JustTCG consultados diretamente (`justtcg.com/terms`, "Last updated: 7/27/2026") — sem divergência material frente à decisão a registrar.

**Resultado da prova técnica, reportado por Fabrício**: 19/19 cartas e 7/7 Card Sets encontrados; preços em USD, printings, condições e histórico disponíveis; zero falha técnica; 15 requisições; nenhum segredo persistido. **Decisão A aprovada** (existência/qualidade de dado de produto/preço/histórico). **Decisão B inconclusiva** — idioma das 18 consultas novas ficou `UNDETERMINED`, não reprovada.

**Decisão formal**: JustTCG homologada para **piloto técnico** de produtos, printings, condições, preços originais em USD e histórico — **não homologada** para determinar PT-BR nem como preço do mercado brasileiro. Regra reforçada, não nova: idioma nunca é inferido pelo fato de o preço estar em USD; sem evidência, `pricing_product.language_status = 'UNDETERMINED'` e `language_id = NULL` (já previsto desde a "Correção Arquitetural Pontual", acima).

**Query `3700`** (`database/migrations/3700_seed_pricing_source_justtcg.sql`, primeiro arquivo físico a existir na faixa `3000`–`3999` do repositório) — `INSERT` único, `ON CONFLICT (code) DO NOTHING`, sem alterar estrutura/RLS/grants nem nenhuma migration histórica: `default_market_scope = 'INTERNATIONAL'` (fonte agrega Cardmarket/TCGplayer, ambos internacionais, sem evidência de mercado brasileiro); `requires_commercial_agreement = TRUE` (Termos, Seção 6/7.1: tier gratuito só pessoal/não comercial, uso comercial exige plano pago com licença comercial explícita — nenhum plano contratado neste incremento); **`is_active = FALSE`**, decisão deliberada — a única semântica real da coluna no modelo é "não excluída fisicamente" (`05f-pricing.md`, Regra de Negócio 3, mesmo padrão de `card_variant_type`/`asset_source`), sem nenhum consumidor operacional hoje (nenhuma Edge Function/cron/mapping/sincronização existe); mantida `FALSE` para não sinalizar, num futuro picker/consulta `WHERE is_active`, uma fonte pronta para uso operacional antes de existir plano pago contratado e sincronização real — reavaliação para `TRUE` fica para um incremento futuro. Detalhamento completo, coluna a coluna, em `05f-pricing.md`, seção "Incremento P7".

**Validação (8 itens)**: linha única confirmada (`code = 'JUSTTCG'`); reexecução do mesmo `INSERT` confirmada idempotente; RLS/policy e grants confirmados idênticos ao estado pré-migration; `get_advisors` (segurança e performance) sem achado novo referenciando `pricing_source`; varredura defensiva de padrão de segredo (`tcg_`/`Bearer `/`Authorization`/`x-api-key`) negativa; nenhuma outra tabela do domínio tocada — as outras nove entidades seguem vazias; nenhuma migration histórica alterada; escopo confirmado — chamadas à API, credencial, conector, mappings, sincronização, observações, PTAX, scheduler e frontend permanecem deliberadamente fora deste incremento. Nenhum commit/push realizado.

Decisão de numeração explícita de Fabrício: `card_condition`, referência conceitualmente compartilhada e neutra, permanece numerada dentro de `3000`–`3999` (`3010`–`3019`) — a posição registra apenas o ciclo que a implementou fisicamente pela primeira vez, não pertencimento de domínio. O módulo "Referências Compartilhadas" (candidato `4000`–`4999`) cogitado na Correção Arquitetural Pontual não foi criado neste incremento e permanece livre, não reservado. Ver `05f-pricing.md`, seção "Numeração", e `STD-001`, subseção "Módulo: Pricing (`3000`–`3999`)", para o mesmo registro em nível de documento físico e de Standard.

## Incremento P8 — Conector JustTCG e Piloto Controlado (2026-08-17, ciclo posterior ao Incremento P7)

A pedido explícito de Fabrício: primeiro fluxo real JustTCG → Pricing, exclusivamente server-side e acionado manualmente por administrador. `pricing_source.is_active` permanece `FALSE` — este incremento não reabre a homologação comercial decidida no Incremento P7, constrói apenas o caminho técnico que a exercitará quando aprovada.

**Decisão arquitetural**: script Deno standalone (`scripts/sync-justtcg-pricing.ts`), mesmo precedente de `scripts/import-manual-assets.ts` — nunca uma Edge Function, nunca chamado pelo frontend, nunca agendado. `getJustTcgSource()` é o único ponto do repositório autorizado a ler `pricing_source WHERE code = 'JUSTTCG'` apesar de `is_active = FALSE`, com um literal fixo — decisão deliberada de não criar uma versão genérica reaproveitável por outra fonte, para que este não seja um bypass silenciosamente extensível.

**Divergência material identificada e resolvida antes de prosseguir** (`AskUserQuestion`, aprovada por Fabrício): `card_condition` estava vazia e `service_role` tinha apenas `SELECT` em `pricing_set_mapping`/`pricing_card_mapping` — ambos já registrados como pendência explícita nos Incrementos P1/P2 ("capacidade de escrita adiada para incremento futuro de sincronização"; "seed real depende do vocabulário confirmado da fonte homologada"). Este é esse incremento futuro. Resolvido com três migrations: `3091` (`GRANT INSERT`, nunca `UPDATE`/`DELETE`, a `service_role` nas duas tabelas de mapeamento, com `REVOKE` explícito dos demais privilégios — mesmo padrão de defesa em profundidade de `pricing_product`/`pricing_fx_rate`); `3701` (seed de cinco condições canônicas confirmadas pela prova técnica real — `Near Mint`/`Lightly Played`/`Moderately Played`/`Heavily Played`/`Damaged` — não seis, como o texto de `05f-pricing.md` cogitava hipoteticamente até a versão `1.8`); `3702` (de-para das cinco strings exatas da JustTCG para `card_condition`, restrito a `pricing_source.code = 'JUSTTCG'`).

**Piloto controlado**: hardcoded no script, restrito a `ME1`/`BASE1` e até três cartas por Set — as mesmas nove já validadas na prova técnica de 2026-08-17; nunca amplia o catálogo. Resolve `pricing_set_mapping`/`pricing_card_mapping` e persiste `pricing_product`/`pricing_observation` por variante, preço original preservado em USD, idioma sempre `UNDETERMINED`/`NULL` sem sufixo explícito no campo `printing` — nunca inferido pelo fato de o preço estar em USD (mesma regra reforçada no Incremento P7). Toda escrita usa `INSERT ... ON CONFLICT DO NOTHING` contra a chave natural de cada tabela — idempotência obrigatória, mesmo padrão de P1–P6. Execução completa registrada em uma `pricing_sync_run`, cada chamada HTTP em `pricing_sync_run_call`; 401 aborta a execução, 429 aplica backoff e uma única retentativa, teto local de requisições conservador. Sanitização defensiva de padrão de segredo aplicada a todo texto antes de tocar campos logados/persistidos.

**Validação estrutural pós-migration**: `card_condition`/`pricing_condition_mapping` com 5 linhas cada; `service_role` com `INSERT`+`SELECT` exatos em `pricing_set_mapping`/`pricing_card_mapping` (confirmado via `information_schema.column_privileges`, não só `role_table_grants` — grants de coluna não aparecem na segunda); as oito tabelas tocadas com RLS habilitada e exatamente uma policy cada. Fora de escopo confirmado: PTAX, BRL, "Valor Brasil", scheduler, frontend, ativação comercial da fonte, ingestão em massa. Nenhum commit/push realizado.

## Correção pós-P8 — Validação de `confirmed_by` sem RPC de UUID Livre (2026-08-17, ciclo seguinte ao Incremento P8)

A pedido explícito de Fabrício, em resposta a um erro real de execução: o piloto local (`scripts/Executar-P8-JustTCG-Local.ps1`) falhou com `permission denied for table admin_user` — `service_role` nunca teve `SELECT` em `admin_user`, de propósito (`ADR-021-administrative-role-model.md`), e `requireAdmin()` no conector fazia exatamente esse `SELECT` direto.

**Divergência identificada e sinalizada antes de qualquer execução, não aplicada silenciosamente**: a correção inicialmente proposta — função `public.is_admin_user(uuid)`, `SECURITY DEFINER`, `EXECUTE` restrito a `service_role` — reproduz exatamente o padrão que `ADR-021` já registra como alternativa avaliada e rejeitada por Fabrício ("`is_admin(p_user_id UUID)` aceitando qualquer UUID... rejeitada... nenhuma função aceita um UUID arbitrário como parâmetro de checagem"). A diferença prática (só `service_role`, nunca alcançável por sessão de usuário, teria `EXECUTE` — o risco concreto que motivou a rejeição original, um usuário autenticado enumerando status administrativo de terceiros via API pública, não se reabriria) foi apresentada a Fabrício antes de qualquer DDL.

**Decisão de Fabrício: preservar `ADR-021` integralmente, sem nenhuma revisão** — nenhuma função RPC com parâmetro UUID livre foi criada. Em vez disso, `pricing_sync_run` ganha `confirmed_by UUID NOT NULL` (Query `3082`, sem `FK` — mesmo precedente de `confirmed_by` em `pricing_set_mapping`/`pricing_card_mapping`) e um trigger `BEFORE INSERT` `SECURITY DEFINER`/`search_path=''` (`validate_pricing_sync_run_confirmed_by()`, Query `3083`) que confirma `NEW.confirmed_by` contra `admin_user` como efeito colateral obrigatório do primeiro `INSERT` do piloto — nunca uma função isolada e chamável. `RETURNS TRIGGER` já impede invocação direta via SQL; `EXECUTE` revogado explicitamente de `PUBLIC`/`anon`/`authenticated`/`service_role` como defesa em profundidade (o disparo automático do trigger não depende desse privilégio — confirmado por teste real via `SET ROLE service_role`). Erro genérico (`PRICING_SYNC_RUN_CONFIRMED_BY_INVALID`, sem interpolar o UUID recebido) evita que a mensagem sirva de oráculo de enumeração de administradores.

`scripts/sync-justtcg-pricing.ts`: `requireAdmin()` removida (era a causa raiz do erro); `confirmed_by` passa a integrar o payload do `INSERT` em `pricing_sync_run`, que já era o primeiro write real do piloto (leituras de `pricing_source`/`pricing_condition_mapping` antecedem, sem escrever nada) — um `confirmed_by` inválido aborta a execução antes de qualquer Fase A/B rodar. Mensagem de ajuda ao operador (linha impressa quando `--confirmed-by` está ausente) corrigida — apontava incorretamente para um `SELECT id FROM admin_user` que nunca funcionou nem para sessão autenticada (RLS habilitado sem policy, deny-all).

**Validação**: transação `SET ROLE service_role` — UUID administrativo real insere com sucesso (`count = 1`); UUID aleatório falha com `ERROR P0001: PRICING_SYNC_RUN_CONFIRMED_BY_INVALID`, zero linha gravada; ambos os testes com `ROLLBACK`, zero resíduo real. `proacl` da nova função confirmado `{postgres=X/postgres}` (nenhum outro papel). Grants de `admin_user` inalterados (`service_role` continua sem `SELECT`, confirmado antes/depois idêntico). `get_advisors` (segurança e performance): zero achado novo referenciando `pricing_sync_run`/`admin_user`/a nova função — apenas achados pré-existentes não relacionados (`rls_enabled_no_policy` em `admin_user`, por design; `auth_rls_initplan` em `user_profile`, pré-existente). Runner PowerShell (`scripts/Executar-P8-JustTCG-Local.ps1`) intocado por esta correção. Nenhum commit/push realizado. Detalhamento completo em `05f-pricing.md`, revisão `1.11`.

## Fechamento do Incremento P8 — Evidência Real Final (2026-08-17, ciclo posterior às correções pós-P8)

Após as correções pós-P8 registradas acima (`confirmed_by`) e as duas seguintes, documentadas apenas em `05f-pricing.md`/`docs/log.md` por serem correções de implementação sem impacto arquitetural — bug de `findCard()`, finalização garantida de `pricing_sync_run`, telemetria de escrita (`05f-pricing.md` revisões `1.12`–`1.14`) — Fabrício executou o piloto real localmente com sucesso e reportou a evidência final, confirmada diretamente no Supabase pelo agente (não apenas relatada): quatro execuções `pricing_sync_run` `COMPLETED` no total, cada uma com `requests_made = 7`, `rate_limit_hits = 0`, resolvendo os `2` Sets e as `6` cartas do piloto; os totais de `pricing_product`/`pricing_observation` permaneceram estáveis em `32`/`32` através das quatro execuções — evidência direta de idempotência real, sem duplicidade, não uma coincidência de um único par. No par final da prova de idempotência, a segunda execução terminou com `productsWritten = 0` e `observationsWritten = 0`, exatamente o requisito reescrito na correção de telemetria. As 28 chamadas HTTP registradas (`pricing_sync_run_call`) têm `outcome = 'SUCCESS'` em todas, zero `429`, zero falha técnica. Varredura SQL direta sobre os campos de texto do domínio (`error_summary`/`error_detail`/`match_evidence`/`raw_payload`) não encontrou nenhum padrão de segredo. `get_advisors` (segurança e performance): zero achado novo referenciando o domínio Pricing.

`pricing_source.is_active` confirmado `FALSE` após as quatro execuções — este fechamento é estritamente técnico e **não autoriza** produção comercial, PT-BR, nem "Valor Brasil": a semântica de `is_active` permanece "não excluída fisicamente" (decisão do Incremento P7, preservada integralmente), reativação para uso operacional depende de plano comercial contratado (Termos da JustTCG) e de decisão própria futura. Sem mudança de dado, schema, migration ou código nesta rodada — fechamento documental sobre evidência já produzida. Nenhum commit/push realizado. Detalhamento completo em `05f-pricing.md`, revisão `1.15`.

## Incremento P14.1 — Ativação Comercial e Piloto Controlado da JustTCG (2026-08-19, a pedido explícito de Fabrício)

Primeira ativação operacional real da fonte JustTCG desde a homologação condicionada do Incremento P7 (2026-08-17) — `pricing_source.is_active` passa de `FALSE` para `TRUE`, mediante confirmação da condição comercial que a própria homologação já previa como pendência (Termos da JustTCG, Seção 6/7.1: uso comercial exige plano pago).

**Premissas comerciais confirmadas por Fabrício** (decisão de negócio dele, não reavaliada por este agente): assinatura ativa, 10.000 requests/mês, 1.000 requests/dia, 50 requests/minuto, até 100 cards/request.

**Introspecção prévia**: estado real inalterado desde o fechamento do Incremento P8 confirmado por leitura direta — 2 `pricing_set_mapping`/6 `pricing_card_mapping` (`CONFIRMED`), 32 `pricing_product`/32 `pricing_observation`, 5 `pricing_sync_run`, zero run ativo. `scripts/sync-justtcg-pricing.ts` (Incremento P8) confirmado suficiente para o piloto pedido, sem necessidade de alteração de código — já restrito a ME1/BASE1 + 6 cartas. Migration livre confirmada `3911`.

**Divergência material sinalizada antes de prosseguir**: o conector foi arquiteturado de propósito para rodar só localmente (Incremento P8) — nenhuma credencial existe no sandbox deste agente nem no Supabase Vault (que só guarda os segredos de PTAX). Apresentada a Fabrício via `AskUserQuestion`; decisão dele: executar localmente e reportar o resultado sanitizado — mesmo precedente de P8/P9.

**Migration `3911_activate_pricing_source_justtcg`** (`CONFIRMADO EXECUTADO`): `UPDATE` único, idempotente por `WHERE is_active = FALSE AND requires_commercial_agreement = TRUE`, sem alterar estrutura/RLS/grants/trigger — só aplicada depois de uma chamada real mínima e sem escrita (`GET /v1/sets?game=pokemon`, `HTTP 200`, `apiRequestsRemaining=9998`) confirmar a credencial e o contrato atual da API.

**Piloto real executado por Fabrício, escopo idêntico ao já mapeado desde P8 — sem ampliar catálogo/mappings/cobertura**: `requestsMade=7`, `rateLimitHits=0`, `status=COMPLETED`; `productsWritten=0` (32 produtos já existiam, conflito ignorado); `observationsWritten=32` — preços mais recentes que os de 2026-08-17, persistidos como histórico novo (nunca sobrescrita, `pricing_observation` imutável desde a versão `1.0` desta ADR). Validado por consulta direta ao Supabase: run terminal sem inconsistência, 7 `pricing_sync_run_call` todas `SUCCESS`, `pricing_observation` `32→64` sem duplicidade estrutural possível (identidade inclui `observed_at`), zero run ativo residual, nenhum segredo em texto persistido (varredura defensiva negativa), `get_advisors` sem achado novo referenciando o domínio.

**Fora de escopo, nada implementado nesta rodada**: nenhuma Edge Function/Cron/nova arquitetura para JustTCG (candidato a P14.2+, espelhando P13.2–P13.4 já feitos para PTAX); nenhuma alteração de PTAX, frontend ou outra frente; nenhuma ampliação de catálogo/mappings/cobertura além do escopo ME1/BASE1 já existente. Nenhum encerramento formal do Incremento P14 nesta rodada — só o P14.1. Detalhamento completo em `05f-pricing.md`, seção "Incremento P14.1". Nenhum commit/push realizado.

## Incremento P14.2 — Cobertura Escalável e Sincronização em Lotes da JustTCG (2026-08-19, mesmo dia, ciclo seguinte ao P14.1)

Objetivo pedido por Fabrício: eliminar o padrão de uma chamada HTTP por carta do conector P8, substituindo-o por descoberta paginada em lotes de até 100 cartas por requisição (plano Starter contratado), implementado e validado em um único Set ainda não mapeado — sem sincronizar o catálogo completo (~7.500 cartas).

**Dois achados materiais da introspecção, nenhum exigindo pausa**: (1) o endpoint de lote documentado (`POST /v1/cards`, até 100/200 conforme plano) exige identificadores já conhecidos por item — inútil para descobrir cartas ainda não mapeadas; o mecanismo real de "até 100 cartas por requisição" para cobrir um Set inteiro é a paginação de `GET /v1/cards` (`limit`/`offset`/`meta.hasMore`), documentada oficialmente como o próprio caso de uso "fetch every card in a set, paginated"; (2) nenhuma tabela local (`card_set`/`expansion`/`game`) guarda nome em inglês confiável — a resolução automatizada de Set passou a usar `release_date` exata como sinal primário, nunca nome (zero ou múltiplos candidatos na mesma data nunca são confirmados automaticamente).

**Fix P14.2.1 (2026-08-19, mesmo dia): correção de formato pós-piloto real.** O piloto real de Fabrício retornou `SET_NOT_FOUND(BASE4)` mesmo com `release_date` local batendo exatamente — causa raiz: a JustTCG retorna `release_date` como datetime ISO completo (`"2000-02-24T00:00:00.000Z"`), não como data pura, e a comparação de string exata nunca batia com o `date` puro do Postgres. Corrigido normalizando na fronteira de entrada (`normalizeExternalSetReleaseDate()`/`normalizeJustTcgSets()`, regex de prefixo, nunca `Date`/`toISOString()`); valor bruto preservado em `release_date_raw`; data ausente/inválida nunca confirma. 12 novas asserções (50→62), `tsc --strict` revalidado. Nenhuma migration. Ver `05f-pricing.md`, revisão `1.43`.

**Nenhuma migration necessária**: `match_status='PENDING'` (existente desde o Incremento P2, ver revisão `1.3`) reaproveitado para representar "candidato ambíguo, aguardando revisão humana" — o motivo específico fica em `match_evidence.resolution`, sem exigir um novo valor de enum. Migration livre confirmada `3912`, não usada.

**Set-piloto**: `BASE4` — Coleção Básica 2/Base Set 2 (`release_date=2000-02-24`, 130 cartas locais, mesma era Wizards já validada em P8 via `BASE1`). `ME1`/`BASE1` deliberadamente fora do escopo desta rodada.

**Mudanças estruturais em `scripts/sync-justtcg-pricing.ts`** (evoluído em lugar, mesmo padrão das correções P8): cliente HTTP com `fetchImpl` injetável (testabilidade offline, mesmo padrão de `_shared/pricing-ptax/core.ts`); paginação completa do Set via `GET /v1/cards?limit=100&offset=N`, substituindo a Fase B por-carta; correlação por número de coleção normalizado como identidade primária, nome só como verificação secundária (nunca a única evidência, nunca confirma ambiguidade); `upsertSetMapping()`/`upsertCardMapping()` com leitura prévia — corrige uma lacuna real de P8, cujo padrão insert-e-tolera-duplicata nunca conseguia promover uma linha `PENDING`/`NOT_FOUND` para `CONFIRMED` numa reexecução; uma linha já `CONFIRMED` nunca é rebaixada por uma nova classificação pior; escrita de `pricing_observation` agora distingue divergência real de preço (mesma identidade, `price` diferente) de conflito inofensivo (mesmo `price`) — divergência nunca sobrescreve, mesma disciplina já usada em PTAX/P9.

**Validação**: 100% offline (`tsc --strict` sem erro; 50/50 asserções via `runFixtureCheck()`, cobrindo as 11 categorias pedidas mais 17 de regressão de P8 — sandbox sem runtime Deno, mesma limitação/metodologia já registrada em P13.2/P13.3). Piloto real não executado nesta rodada — aguardando autorização e execução local de Fabrício; cobertura esperada: 3 requisições (1 `/sets` + 2 páginas de `/cards`) para as 130 cartas do Set `BASE4`, contra as 130 que o desenho de P8 exigiria.

**Fora de escopo, nada implementado nesta rodada**: nenhuma Edge Function/Cron/nova arquitetura; nenhuma sincronização do catálogo completo; nenhuma alteração de PTAX/frontend; nenhum `GRANT`/RLS novo. Nenhum encerramento formal do Incremento P14. Detalhamento completo em `05f-pricing.md`, seção "Incremento P14.2". Nenhum commit/push realizado.

**Encerramento do Incremento P14.2 (2026-08-19, mesmo dia) — piloto real validado, evidência confirmada por auditoria direta no Supabase.** Run `9df36424-b8f8-4ee8-a67f-2c04c74bd3fa` (`CARD_SYNC`/`MANUAL`/`COMPLETED`), 3 chamadas `HTTP 200`/`SUCCESS`; `BASE4` confirmado contra `base-set-2-pokemon` pela `release_date` normalizada (Fix P14.2.1). 130 mappings cobertos: 127 `CONFIRMED`, 3 `PENDING`, zero outros. 635 `pricing_product`/635 `pricing_observation`, zero produto ligado aos mappings pendentes, zero duplicidade de business key ou de observação, zero run residual, zero segredo nos payloads; `USD`/`market_scope=UNDETERMINED`/`JUSTTCG_AGGREGATE`/evidência não confirmada; todos os 635 produtos `language_status=UNDETERMINED`. Os 3 `PENDING` são comportamento fail-safe correto, não falha: número de coleção único bate em todos, mas o nome diverge o suficiente para `isNameCompatible()` rejeitar — `082` (`Nidoran♀` × `Nidoran F`), `083` (`Nidoran♂` × `Nidoran M`), `102` (`Impostor Professor Oak` × `Imposter Professor Oak`); nenhuma confirmação automática, aguardando revisão manual. **Achado de performance obrigatório**: run levou ~156s — o batching (P14.2) reduziu 130 chamadas potenciais à JustTCG para 3, mas a persistência sequencial de 635+635 linhas no Supabase é o novo gargalo dominante; catálogo completo e automação permanecem fora de escopo até essa correção. Próximo incremento recomendado: **P14.3 — Persistência em Lotes no Supabase**. Incremento P14 como um todo permanece em andamento — só o P14.2 encerra aqui. Nenhum código/migration/chamada externa nesta rodada de documentação. Ver `05f-pricing.md`, revisão `1.44`.

## Incremento P14.3 — Persistência em Lotes, Hardening de Segurança e Deduplicação por Mudança Real de Preço (2026-08-19, mesmo dia, ciclo seguinte ao encerramento do P14.2)

Objetivo pedido por Fabrício: corrigir o gargalo de performance registrado no encerramento do P14.2 (persistência sequencial de mappings/produtos/observações, ~156s) e, na mesma frente, a correção de dado descoberta durante o desenho — duplicidade de histórico quando o preço não muda entre execuções em dias diferentes.

**Migrations `3912`–`3915` (`CONFIRMADO EXECUTADO`)**, todas `SECURITY INVOKER`, `search_path` fixo, `REVOKE ALL FROM PUBLIC` + `GRANT EXECUTE` restrito a `service_role`: `3912` (`batch_upsert_pricing_card_mapping_promotion`) promove mapeamento exclusivamente `PENDING`/`NOT_FOUND`→`CONFIRMED`, nunca rebaixa nem troca a identidade de uma linha já `CONFIRMED`; `3913` (`batch_upsert_pricing_product`) upsert em lote, contornando a inviabilidade do `.upsert()` do PostgREST para a chave composta da tabela; `3914` (`batch_select_pricing_observation_by_identity`) pré-busca por tupla exata, corrigiu o produto cartesiano da primeira versão da pré-busca mas ainda criava histórico redundante por dia (mantida intacta por compatibilidade, remoção diferida até o código novo estar commitado); `3915` (`batch_select_latest_pricing_observation_by_identity`) a substitui na Fase 3 — busca a última observação conhecida por grupo (produto+condição+`price_type`+`currency_code`+`market_label`, sem `observed_at` na chave), via `LATERAL...ORDER BY observed_at DESC LIMIT 1`, deduplicação interna via CTE `SELECT DISTINCT`. Testada transacionalmente (5 cenários) antes e depois da aplicação real, resultados idênticos.

**Regra corrigida — "histórico só por mudança real de preço"**: a decisão em `persistBatchedResults()` passa a comparar cada variante contra a última observação conhecida do grupo, não contra a tupla exata anterior — preço igual reaproveita sem `INSERT`; mesmo `observed_at` com preço diferente preserva a colisão real, nunca sobrescreve; preço diferente do último conhecido grava observação nova. Bug de contagem duplicada em `observationsResolved` identificado e corrigido durante os testes.

**Hardening de robustez de credencial**: removido antipadrão em que a ausência isolada de `JUSTTCG_API_KEY` fazia o script cair silenciosamente em `--fixture-check`, mascarando ambiente mal configurado como validação bem-sucedida. Nova função pura `resolveEntryDecision()` (mesmo padrão de `planVariantProjection`): `--fixture-check` sempre offline explícito; fora dele, qualquer credencial ausente (`JUSTTCG_API_KEY`/`SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`) encerra com código diferente de zero antes de qualquer rede/Supabase, mensagem sanitizada (só nomes de variável).

**Validação**: `tsc --strict` limpo; suíte offline com 151/151 asserções.

**Piloto real aprovado**: run `8e4367ad-f95d-41b6-bbbe-449bd7fb635f`, `COMPLETED`, 3 chamadas HTTP, 13 operações Supabase, **14,87s (contra ~156s do P14.2, ~10x mais rápido)**. 635 produtos resolvidos/0 escritos (já existiam do piloto P14.2); 635 observações resolvidas/152 escritas, todas com preço genuinamente diferente da observação imediatamente anterior — zero snapshot redundante, confirmando empiricamente a correção da regra de histórico. `BASE4` inalterado (127 `CONFIRMED`/3 `PENDING`). Zero run residual.

**Fora de escopo**: remoção de `3914` (diferida até o código novo estar commitado); expansão além de `BASE4`; qualquer automação/agendamento do conector (permanece manual, `--confirmed-by` obrigatório).

**P14.3 formalmente encerrado.** Incremento P14 como um todo permanece em andamento — próximo incremento recomendado: expandir o mesmo padrão de descoberta paginada + persistência em lotes para todos os Sets restantes do catálogo. Detalhamento completo em `05f-pricing.md`, revisão `1.45`. Nenhum commit/push realizado.

## Incremento P2 — Correspondência Externa de Pricing (2026-08-16, mesmo dia, ciclo seguinte ao Incremento P1)

Segunda implementação física do domínio, a pedido explícito de Fabrício: `pricing_set_mapping` e `pricing_card_mapping` — `CONFIRMADO EXECUTADO` no Supabase (Queries `3030`/`3031`/`3040`/`3041`), ambas vazias, exatamente conforme o modelo aprovado em `05f-pricing.md`, com um requisito de integridade físico novo em relação ao texto original da versão `1.1`: `CHECK` garantindo que `match_status = 'NOT_FOUND'` exige `last_checked_at IS NOT NULL` — reforça por constraint uma regra que já era de negócio, sem alterar a semântica dos quatro estados de correspondência decidida na versão `1.1`. `service_role` recebeu apenas `SELECT` nas duas tabelas — decisão explícita de Fabrício de adiar a capacidade de escrita (`INSERT`/`UPDATE`, necessária para a futura Edge Function de sincronização gravar propostas) para um incremento futuro dedicado à sincronização, não concedida junto da fundação física. A regra hierárquica "`pricing_card_mapping` só quando `pricing_set_mapping` da mesma fonte estiver `CONFIRMED`" permanece responsabilidade de uma futura rotina de escrita, não de trigger — nenhum trigger/função criada neste incremento.

Validação consolidada de 15 itens executada de forma transacional (`BEGIN`/`ROLLBACK`), sem nenhum dado residual: estrutura completa, RLS/policies/grants, isolamento de `anon`, bloqueio de `authenticated` não-admin, leitura administrativa completa, ausência de dados e de referência à JustTCG, teste real dos quatro estados de correspondência, das duas falhas de `CHECK` esperadas e da unicidade parcial restrita a linhas `CONFIRMED`. Nenhuma fonte cadastrada, nenhuma chamada à JustTCG, nenhuma das cinco entidades restantes do domínio implementada — permanecem propostas. Nenhum commit/push realizado.

## Incremento P3 — Auditoria Operacional de Sincronização (2026-08-16, mesmo dia, ciclo seguinte ao Incremento P2)

Terceira implementação física do domínio, a pedido explícito de Fabrício: `pricing_sync_run` e `pricing_sync_run_call` — `CONFIRMADO EXECUTADO` no Supabase (Queries `3080`/`3081`/`3090`), ambas vazias. Dois requisitos de integridade físicos novos em relação ao texto original de `05f-pricing.md`: `CHECK` de `finished_at >= started_at` em `pricing_sync_run`; `CHECK` de `http_status_code` entre `100` e `599` em `pricing_sync_run_call`.

Índices reformulados por decisão explícita do incremento, deviando do par genérico de índices isolados originalmente cogitado, em favor do menor conjunto que atende às três consultas reais previstas (últimas execuções por fonte; execuções ativas; chamadas de uma execução em ordem) — todas as escolhas confirmadas por `EXPLAIN (ANALYZE, BUFFERS)` real sobre volume sintético (9.000 execuções, 1.000 chamadas), dentro de transação com `ROLLBACK`.

Grants de `service_role` mais restritivos que o padrão genérico "`SELECT`/`INSERT`/`UPDATE`" usado pelo texto original: em `pricing_sync_run`, `UPDATE` concedido só nas colunas operacionais do ciclo de vida da execução (`status`/`requests_made`/`requests_remaining_at_end`/`rate_limit_hits`/`error_summary`/`finished_at`) — colunas de identidade (`id`/`pricing_source_id`/`run_type`/`triggered_by`/`started_at`/`created_at`) permanecem inalteráveis pelo fluxo normal, sem `GRANT DELETE`; em `pricing_sync_run_call`, apenas `SELECT`/`INSERT` (tabela append-only). Ambas as restrições confirmadas por teste real (tentativas de `UPDATE`/`DELETE` fora do escopo concedido bloqueadas com `insufficient_privilege`).

Validação consolidada de 16 itens funcionais, executada de forma transacional (`BEGIN`/`ROLLBACK`, `pricing_source` temporária), sem nenhum dado residual: estrutura completa, todos os estados de `run_type`/`status`/`triggered_by`/`outcome`, as sete rejeições de integridade esperadas (contadores negativos, `sequence_number <= 0`, terminal sem `finished_at`, não-terminal com `finished_at`, `finished_at < started_at`, HTTP fora de `100`–`599`, duplicidade de `(sync_run_id, sequence_number)`), isolamento de `anon`, bloqueio de escrita de `authenticated`, leitura administrativa, e a capacidade exata de escrita da `service_role`. Verificação defensiva pós-teste não encontrou nenhum padrão de segredo nas tabelas (vazias). Advisors de segurança e performance sem achado novo relevante. Nenhuma fonte cadastrada, nenhuma chamada à JustTCG, nenhuma das três entidades restantes do domínio implementada — permanecem propostas. Nenhum commit/push realizado.

## Incremento P4 — Produto Externo de Pricing (2026-08-16, mesmo dia, ciclo seguinte ao Incremento P3)

Quarta implementação física do domínio, a pedido explícito de Fabrício: `pricing_product` — `CONFIRMADO EXECUTADO` no Supabase (Queries `3050`/`3051`/`3052`), vazia. Um requisito de integridade físico novo em relação ao texto original de `05f-pricing.md`: `CHECK` de não-branco em `external_product_id`, consistente com o padrão já usado em outros identificadores textuais do módulo.

Índices reformulados por decisão explícita do incremento: nenhum índice isolado em `pricing_card_mapping_id` (já coberto pelo prefixo da própria `UNIQUE (pricing_card_mapping_id, external_product_id)`); adicionados índices para identidade externa, vínculo com `card_variant` (parcial) e a combinação `card_variant`+idioma confirmado (composto, parcial) — todos confirmados por `EXPLAIN (ANALYZE, BUFFERS)` sobre volume sintético (5.000 produtos, 500 mappings), dentro de transação com `ROLLBACK`. Um quinto padrão de acesso cogitado ("produtos sem vínculo editorial") foi deliberadamente não indexado, por ainda não existir o fluxo administrativo que o justificaria. Um índice adicional na FK `language_id` foi incorporado em Query `3052`, após o advisor de performance apontar a ausência de cobertura — mesmo padrão de correção pós-validação já usado em Query `3002` no Incremento P1.

Grants de `service_role` mais restritivos que os três incrementos anteriores: além de não conceder `DELETE`, este incremento revoga explicitamente `TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` de `service_role` — privilégios que `pg_default_acl` concede por padrão em tabelas criadas por `postgres` e que P1–P3 haviam deixado intocados para esse papel; `UPDATE` restrito por coluna (`source_printing_label`/`language_status`/`language_id`/`card_variant_id`/`is_active`), com identidade (`id`/`pricing_card_mapping_id`/`external_product_id`/`created_at`) imutável após o insert. Ambas as restrições confirmadas por teste real de `insufficient_privilege`.

Validação consolidada de 19 itens funcionais, executada de forma transacional (`BEGIN`/`ROLLBACK`, `pricing_source`/`pricing_card_mapping` temporários, `card`/`card_variant`/`language` reais e somente lidos), sem nenhum dado residual: estrutura completa, os três estados de idioma, as três rejeições de `CHECK` esperadas, a duplicidade técnica bloqueada, isolamento de `anon`, bloqueio de escrita de `authenticated`, leitura administrativa, e a capacidade exata de `service_role` (inclusive `UPDATE` em colunas restritas, `DELETE` e `TRUNCATE`, todos bloqueados). Verificação defensiva pós-teste não encontrou nenhum padrão de segredo (tabela vazia). Nenhum trigger de consistência `card_variant_id`×`card_id` foi criado — permanece candidato futuro, decisão deliberada de não introduzir trigger cross-table sem necessidade concreta comprovada (mesmo raciocínio já aplicado à regra hierárquica `pricing_set_mapping`↔`pricing_card_mapping` nos incrementos anteriores). Nenhuma fonte cadastrada, nenhuma chamada à JustTCG, nenhuma das duas entidades restantes do domínio implementada — permanecem propostas. Nenhum commit/push realizado.

### Correção retroativa de segurança — sete tabelas de P1–P3 (2026-08-16, mesmo dia, mesmo ciclo do Incremento P4)

Auditoria somente leitura, a pedido explícito de Fabrício e explicitamente sem presumir que os relatórios anteriores refletiam os grants efetivos, confirmou via `has_table_privilege`/`information_schema.role_table_grants` que as sete tabelas dos Incrementos P1–P3 — `pricing_source`, `card_condition`, `pricing_condition_mapping`, `pricing_set_mapping`, `pricing_card_mapping`, `pricing_sync_run`, `pricing_sync_run_call` — nunca tiveram `TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` revogados de `service_role`: privilégios concedidos automaticamente por `pg_default_acl` em tabelas criadas pelo papel `postgres`, que P1–P3 haviam deixado intocados (mesmo achado que já motivara a revogação explícita em `pricing_product`, Query `3050`, Incremento P4). `anon` e `authenticated` confirmados sem excesso (zero privilégios e `SELECT`-only, respectivamente, em todas as sete).

Corrigido em uma única migration (Query `3053`), revogando apenas os quatro privilégios excedentes de `service_role` — `SELECT`/`INSERT`/`UPDATE`, incluindo os grants de coluna restritos de `pricing_sync_run` (Incremento P3), preservados integralmente; nenhuma alteração de RLS, policies, estrutura, índices, migrations históricas ou dados. Validado por `has_table_privilege` pós-correção (privilégios excedentes confirmados removidos nas sete tabelas, operacionais confirmados intactos) e por smoke test transacional real com `ROLLBACK` forçado: `service_role` continua fazendo `SELECT`/`INSERT`/`UPDATE` normalmente; `TRUNCATE` agora bloqueado por ausência real de grant (antes bloqueado só por não ter sido exercido); leitura administrativa de `authenticated` intacta — zero dado residual. Advisors de segurança e performance sem nenhum achado novo referenciando as sete tabelas. Nenhum commit/push realizado.

## Incremento P5 — Série Histórica de Taxas de Câmbio (2026-08-16, mesmo dia, ciclo seguinte à correção retroativa)

Quinta implementação física do domínio, a pedido explícito de Fabrício: `pricing_fx_rate` — `CONFIRMADO EXECUTADO` no Supabase (Query `3060`), vazia. Um requisito de integridade físico novo em relação ao texto original de `05f-pricing.md`: `CHECK` de formato técnico em `rate_source_code` (maiúsculo, `^[A-Z][A-Z0-9_]*$`, mesmo padrão de `pricing_source.code`), não fechado como enum — admite uma segunda fonte de câmbio futura sem migration.

Desenho de índice reformulado por decisão explícita do incremento: a `UNIQUE` de unicidade semântica teve suas colunas reordenadas de `(from_currency, to_currency, rate_date, rate_source_code)` (hipótese original) para `(from_currency, to_currency, rate_source_code, rate_date)` — mesma unicidade, mas agora a própria constraint serve integralmente como índice para os três padrões de consulta reais (taxa mais recente disponível até uma data; taxa de uma data exata; histórico de um par/fonte em intervalo), dispensando o índice adicional `(from_currency, to_currency, rate_date DESC)` cogitado no texto original — B-tree é percorrido nos dois sentidos, então `ORDER BY rate_date DESC` não exige coluna ou índice `DESC` dedicados. Confirmado por `EXPLAIN (ANALYZE, BUFFERS)` sobre volume sintético de 7.830 linhas (5 pares de moeda × 2 fontes × ~3 anos de dias úteis): os três planos usam `Index Scan`/`Index Scan Backward` sobre a `UNIQUE` reordenada, sem `Seq Scan` e sem `Sort` adicional.

RLS/Grants decididos como o padrão conservador já usado no restante do domínio, corrigindo a hipótese original de `05f-pricing.md` (que cogitava leitura pública, inclusive `anon`, "desde o início"): `anon` sem privilégio algum; `authenticated` apenas `SELECT`, com leitura efetiva restrita a administrador via policy `pricing_admin_select`; `service_role` apenas `SELECT`/`INSERT` (rotina agendada futura de ingestão diária da PTAX, ainda não implementada), com revogação explícita de `UPDATE`/`DELETE`/`TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` — não confiar nos defaults de `pg_default_acl`, mesmo padrão já usado em `pricing_product` (Query `3050`) e na correção retroativa (Query `3053`). Nenhum `is_active` criado — uma taxa histórica é um fato registrado, não um cadastro ativável; nenhum `updated_at`/trigger — tabela append-only por design, reforçando a imutabilidade já decidida na versão `1.0` desta ADR.

Validação consolidada de 22 itens funcionais/integridade/RLS/grants, executada de forma transacional (`BEGIN`/`ROLLBACK` forçado), sem nenhum dado residual: estrutura completa (colunas/tipos/precisão `NUMERIC(18,8)`/defaults/nulabilidade), ausência de `updated_at`/trigger, inserção válida, duplicidade rejeitada, `from_currency = to_currency` rejeitado, códigos de moeda fora do formato ou minúsculos rejeitados, `rate <= 0` rejeitado, `rate_source_code` vazio rejeitado, um novo código de fonte válido aceito sem migration, isolamento completo de `anon`, bloqueio de escrita de `authenticated`, leitura administrativa via RLS, e a capacidade exata de `service_role` (`SELECT`/`INSERT` funcionando; `UPDATE`/`DELETE`/`TRUNCATE` bloqueados por `insufficient_privilege` real; `REFERENCES`/`TRIGGER`/`MAINTAIN` confirmados ausentes por `has_table_privilege`). `get_advisors` (segurança e performance): zero achados novos referenciando `pricing_fx_rate`. Nenhuma chamada ao Banco Central, nenhuma chamada à JustTCG, nenhuma taxa real cadastrada, nenhuma ingestão PTAX implementada. Única entidade restante do domínio: `pricing_observation`. Nenhum commit/push realizado.

## Incremento P6 — Observação Histórica de Preço (2026-08-16, mesmo dia, ciclo seguinte ao Incremento P5)

Sexta e última implementação física do domínio, a pedido explícito de Fabrício: `pricing_observation` — `CONFIRMADO EXECUTADO` no Supabase (Query `3070`), vazia. Encerra a fundação física de Pricing: dez de dez entidades `CONFIRMADO EXECUTADO`.

**Correção obrigatória de identidade, exigida pelo próprio pedido que abriu este incremento.** A hipótese original de `05f-pricing.md` (`UNIQUE (pricing_product_id, condition_id, price_type, observed_at)`) era insuficiente para a própria semântica multi-mercado/multi-moeda já aprovada na versão `1.1` desta ADR: uma fonte agregadora pode reportar, para o mesmo produto/condição/tipo/instante, mercados subjacentes ou moedas originais diferentes — fatos distintos que a identidade original faria colidir indevidamente. Corrigida para `UNIQUE NULLS NOT DISTINCT (pricing_product_id, condition_id, price_type, currency_code, market_label, observed_at)`, com suporte a `NULLS NOT DISTINCT` confirmado no PostgreSQL 17.6 do projeto (disponível desde PostgreSQL 15) antes de qualquer DDL — garante que duas observações igualmente sem `market_label` também sejam tratadas como duplicatas, em vez de `NULL <> NULL` permitir linhas "iguais" infinitas. `market_scope`/`market_evidence_confirmed` permanecem deliberadamente fora da identidade: são classificação e evidência do fato, não o fato externo em si. Validada fisicamente por dois testes de regressão diretos: mesmo instante com mercado ou moeda diferentes é permitido (não há colisão indevida); duplicidade com `market_label IS NULL` em ambas as linhas é bloqueada (prova direta de `NULLS NOT DISTINCT`).

Desenho de índice reformulado por decisão explícita do incremento, evitando cópia mecânica do conjunto de índices originalmente cogitado em `05f-pricing.md`: nenhum índice isolado em `pricing_product_id` (já é a primeira coluna da própria identidade); `ix_pricing_observation_latest_lookup` `(pricing_product_id, condition_id, price_type, observed_at DESC)` cobre a leitura de "observação mais recente" quando moeda/mercado não são conhecidos previamente, padrão que a identidade não serve porque `currency_code`/`market_label` ficam entre `price_type` e `observed_at`; `ix_pricing_observation_condition_id` isolado, pois `condition_id` nunca ocupa posição utilizável em nenhum outro índice; `ix_pricing_observation_sync_run_id` parcial. Nenhum índice parcial dedicado à elegibilidade "Valor Brasil" foi criado — testado empiricamente contra o índice geral de leitura recente sobre 80.000 linhas sintéticas, com `Index Scan` e apenas 5 buffers, demonstrando que o índice geral já é suficiente (instrução explícita do pedido: só criar índice parcial dedicado se os planos demonstrassem benefício material sobre o geral).

**Particionamento — decisão explícita de não particionar neste incremento.** Checkpoint de capacidade registrado antes do DDL (volume por sincronização, frequência de ingestão, peso de `raw_payload`, extrapolação de tamanho a partir da amostra sintética de 80.000 linhas — detalhamento completo em `05f-pricing.md` revisão `1.8`) — constraints únicas em tabela particionada exigiriam incluir a chave de partição, alterando a própria estratégia de identidade recém-corrigida; decisão arquitetural própria, fora do escopo deste incremento. Histórico permanece de retenção permanente, sem política de descarte.

RLS/Grants no mesmo padrão conservador do restante do domínio: `anon` zero privilégios; `authenticated` apenas `SELECT`, com leitura efetiva restrita a administrador via `pricing_admin_select`; `service_role` apenas `SELECT`/`INSERT`, com revogação explícita de `UPDATE`/`DELETE`/`TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` — mesmo padrão já usado em `pricing_product`/`pricing_fx_rate`. `pricing_product_id`/`condition_id` em `ON DELETE RESTRICT` (histórico nunca perdido por cascata); `sync_run_id` em `ON DELETE SET NULL` — ambos validados fisicamente dentro da transação de teste (exclusão real de produto/condição referenciados bloqueada; exclusão real de uma execução de sincronização anula `sync_run_id` sem apagar a observação).

Validação consolidada de 32 itens funcionais/integridade/RLS/grants, executada de forma transacional (`BEGIN`/`ROLLBACK` forçado, fixtures temporárias em toda a cadeia de dependências, um `card` real usado somente para satisfazer a FK do mapping, Catálogo Editorial intocado), sem nenhum dado residual — cobrindo os seis valores de `price_type`, os três de `market_scope`, as quatro combinações de `market_scope`×`market_evidence`×`market_evidence_confirmed` (incluindo o teste de regressão direto de que `pricing_source.default_market_scope = 'BRAZIL'` sozinho nunca autoriza Valor Brasil — mesma regra já registrada na versão `1.1` desta ADR), duplicidade exata bloqueada, `ON CONFLICT DO NOTHING` idempotente, as duas regressões de identidade market-aware descritas acima, isolamento completo de `anon`, bloqueio de escrita de `authenticated`, leitura administrativa via RLS, e a capacidade exata de `service_role`. Validação de performance com 80.000 observações sintéticas: cinco planos de consulta reais usam `Index Scan` sobre os índices criados, sem `Seq Scan` em consulta seletiva de alto volume e sem índice duplicado com a `UNIQUE`. `get_advisors` (segurança e performance): zero achados novos referenciando `pricing_observation`. Nenhuma fonte real, nenhuma chamada à JustTCG, nenhuma condição real semeada, nenhum mapping/produto/preço real cadastrado, nenhuma ingestão PTAX. **Fundação física de Pricing completa.** Pricing ainda não é operacional: seguem pendentes fonte homologada (JustTCG permanece pendente, nem aprovada nem rejeitada), condições canônicas semeadas, mappings reais, integração/sincronização, ingestão PTAX, frontend e Analytics/Item Valuation. Nenhum commit/push realizado.

---

# Alternatives Considered

## Reaproveitar `asset_source`/`card_external_reference`/`card_set_external_reference` do Catálogo Editorial

Rejeitada. Embora estruturalmente quase idênticas, essas tabelas representam fontes de **sincronização de catálogo/imagem** (TCGdex, importação manual), um domínio já com seu próprio ciclo de vida, RLS e função administrativa. Pricing precisa, além disso, de campos que o Catálogo nunca precisou (`match_status`/`match_method`/`match_evidence`/`confirmed_at`/`confirmed_by` em nível de correspondência; `market_scope`/`base_currency`/`requires_commercial_agreement` em nível de fonte) — forçar esses campos dentro das tabelas do Catálogo Editorial misturaria dois domínios que `ADR-006` já mandou manter separados, e tornaria impossível evoluir Pricing (ex.: adicionar um novo `market_scope`) sem tocar em uma tabela que o Catálogo Editorial também depende. Mesmo raciocínio já usado para não reaproveitar `admin_action_log` em `catalog_admin_action_log` (`ADR-021`) e para não emendar `ADR-006` na criação de `ADR-027`.

## Tratar Pricing como uma extensão de `Card Variant`

Rejeitada. `Card Variant` (`ADR-028`) é dado editorial permanente (existe independentemente de qualquer fonte de preço observar ou não aquela impressão); Pricing é dado de mercado, volátil, de terceiros. Colapsar os dois violaria diretamente `ADR-006` e a correção conceitual já registrada na revisão 2 da prova técnica da JustTCG (printing ≠ condição, Pricing é domínio próprio, não sub-tabela de Catálogo).

## Persistir a classificação de valuation (`INTERNATIONAL_ITEM_VALUATION` etc.) como coluna de Pricing

Rejeitada nesta rodada. A classificação depende de um Collection Item concreto (Ownership), que ainda não existe — persistir a classificação em Pricing hoje seria antecipar uma junção que só faz sentido quando Ownership existir, além de violar o princípio de não persistir dado derivado sem justificativa concreta (`ADR-006`). Tratada como Analytics futura (`item_valuation_snapshot`, esboçado, não implementado) — ver `05f-pricing.md`.

## Não separar chamada individual (`pricing_sync_run_call`) de execução (`pricing_sync_run`)

Rejeitada. A prova técnica da JustTCG já demonstrou, em ambiente real, o valor concreto dessa granularidade (diagnosticar exatamente qual chamada recebeu `429`, confirmar a cota restante a cada instante, isolar uma falha técnica específica dentro de uma execução maior) — resumir tudo a um único registro por execução perderia informação já comprovadamente útil.

---

# Related Documents

- `ADR-006-separation-of-catalog-ownership-and-analytics.md`
- `ADR-008-external-catalog-data-sources.md`
- `ADR-013-collection-item-identity-model.md`
- `ADR-028-card-variant-governance.md`
- `../04-domain-model.md`
- `../05f-pricing.md`
- `../standards/STD-001-database-standards.md`
- `../standards/STD-002-domain-modeling.md`
- `../ROADMAP.md`

---

# Revision History

| Versão | Descrição |
|---------|-----------|
| 1.0 | Criação — formaliza Pricing como quarto domínio conceitual independente (Catálogo/Ownership/Analytics/Pricing), a separação definitiva entre printing/condição/idioma/mercado/moeda, a regra de que "Valor Brasil" é propriedade exclusiva da fonte (`market_scope`) e nunca de conversão cambial, a imutabilidade de `pricing_observation`/`pricing_fx_rate`, e a auditoria de sincronização em dois níveis já validada pela prova técnica real da JustTCG. Modelo físico completo em `05f-pricing.md`. Nenhuma tabela criada; homologação de fonte (JustTCG) segue pendente, em paralelo, sem bloquear esta decisão. |
| 1.1 | **Correção arquitetural pontual (2026-08-16, mesmo dia, ciclo seguinte)**, a pedido explícito de Fabrício — cinco pontos, sem reabrir a modelagem: (1) idioma de `pricing_product` generalizado de tri-estado binário-PT-BR para tri-estado neutro multi-idioma (`CONFIRMED`/`INFERRED`/`UNDETERMINED` + `language_id`), cobertura derivada por comparação com o idioma do Collection Item, valuation direto exigindo `CONFIRMED`; (2) "Valor Brasil" deixa de depender exclusivamente de `pricing_source.market_scope` (renomeado `default_market_scope`, agora só default declarado) e passa a exigir evidência na própria `pricing_observation` (`market_scope`/`market_label`/`market_evidence`/`market_evidence_confirmed`); (3) `pricing_set_mapping`/`pricing_card_mapping` ganham estado `NOT_FOUND` (busca concluída sem correspondência), corrigindo a contradição da v1.0 que colapsava isso sob `REJECTED`; identificadores externos tornam-se opcionais (obrigatórios só em `CONFIRMED`), `UNIQUE` simples substituída por índice único parcial, novo `last_checked_at`; (4) `pricing_condition` renomeada para `card_condition`, reclassificada como referência compartilhada e neutra, não exclusiva de Pricing; (5) diagrama Mermaid corrigido — removidas as relações `CARD_VARIANT`↔`LANGUAGE` e `PRICING_FX_RATE`↔`PRICING_OBSERVATION`, sem FK física, movidas para nota textual. Nenhuma tabela criada, nenhuma migration, nenhuma chamada à API da JustTCG; condição da homologação da JustTCG inalterada (pendente, não aprovada nem reprovada); critérios pré-registrados das Decisões A/B intocados. Decisões corretas da versão `1.0` preservadas integralmente. Detalhamento completo em `05f-pricing.md` revisão `1.1`. |
| 1.2 | **Incremento P1 — Fundação Física de Pricing (2026-08-16, mesmo dia, ciclo seguinte à correção `1.1`)**, a pedido explícito de Fabrício. `pricing_source`, `card_condition` e `pricing_condition_mapping` criadas no Supabase (Queries `3000`/`3001`/`3010`/`3011`/`3020`/`3021`/`3002`), `CONFIRMADO EXECUTADO`, com estrutura, RLS, triggers, grants e validação de 12 itens idênticos ao modelo das versões `1.0`/`1.1` — nenhuma divergência de modelagem. Milhar `3000`–`3999` formalizado como módulo Pricing em `STD-001`. Numeração de `card_condition` decidida explicitamente por Fabrício: permanece em `3000`–`3999` (`3010`–`3019`), registrando o ciclo de implementação, não pertencimento de domínio; módulo "Referências Compartilhadas" cogitado na versão `1.1` não foi criado, `4000`–`4999` permanece livre. Nenhuma fonte cadastrada, nenhuma condição semeada, nenhuma chamada à JustTCG, nenhuma das outras sete entidades implementada. Nenhum commit/push realizado. Detalhamento completo em `05f-pricing.md` revisão `1.2`. |
| 1.3 | **Incremento P2 — Correspondência Externa de Pricing (2026-08-16, mesmo dia, ciclo seguinte ao Incremento P1)**, a pedido explícito de Fabrício. `pricing_set_mapping` e `pricing_card_mapping` criadas no Supabase (Queries `3030`/`3031`/`3040`/`3041`), `CONFIRMADO EXECUTADO`, ambas vazias. Requisito de integridade físico novo: `CHECK` garantindo `NOT_FOUND` exige `last_checked_at IS NOT NULL`, sem alterar a semântica dos quatro estados já decidida na versão `1.1`. `service_role` recebeu apenas `SELECT` — capacidade de escrita deliberadamente adiada para incremento futuro de sincronização. Validação consolidada de 15 itens, transacional (`BEGIN`/`ROLLBACK`), sem dado residual. Nenhuma fonte cadastrada, nenhuma chamada à JustTCG, nenhuma das cinco entidades restantes implementada. Nenhum commit/push realizado. Detalhamento completo em `05f-pricing.md` revisão `1.3`. |
| 1.4 | **Incremento P3 — Auditoria Operacional de Sincronização (2026-08-16, mesmo dia, ciclo seguinte ao Incremento P2)**, a pedido explícito de Fabrício. `pricing_sync_run` e `pricing_sync_run_call` criadas no Supabase (Queries `3080`/`3081`/`3090`), `CONFIRMADO EXECUTADO`, ambas vazias. Dois requisitos de integridade físicos novos: `CHECK` de `finished_at >= started_at`; `CHECK` de `http_status_code` entre `100`–`599`. Índices reformulados (composto fonte+data + parcial de execuções ativas; nenhum índice isolado redundante com a `UNIQUE` de chamadas), confirmados por `EXPLAIN (ANALYZE, BUFFERS)` sobre volume sintético. `service_role` recebeu `UPDATE` restrito por coluna em `pricing_sync_run` (só campos operacionais, sem `DELETE`) e apenas `SELECT`/`INSERT` em `pricing_sync_run_call` (append-only) — mais restritivo que o padrão genérico do texto original. Validação consolidada de 16 itens funcionais + 4 planos de execução, transacional (`BEGIN`/`ROLLBACK`), sem dado residual, sem segredo. Nenhuma fonte cadastrada, nenhuma chamada à JustTCG, nenhuma das três entidades restantes implementada. Nenhum commit/push realizado. Detalhamento completo em `05f-pricing.md` revisão `1.4`. |
| 1.5 | **Incremento P4 — Produto Externo de Pricing (2026-08-16, mesmo dia, ciclo seguinte ao Incremento P3)**, a pedido explícito de Fabrício. `pricing_product` criada no Supabase (Queries `3050`/`3051`/`3052`), `CONFIRMADO EXECUTADO`, vazia. Um requisito de integridade físico novo: `CHECK` de não-branco em `external_product_id`. Índices reformulados (nenhum isolado em `pricing_card_mapping_id`, coberto pela `UNIQUE`; adicionados identidade externa, vínculo `card_variant` parcial, e composto `card_variant`+idioma confirmado parcial; um quinto padrão cogitado deliberadamente não indexado por falta de fluxo real), confirmados por `EXPLAIN (ANALYZE, BUFFERS)` sobre volume sintético — um índice adicional em `language_id` incorporado em Query `3052` após achado real do advisor de performance. `service_role` mais restritivo que P1–P3: revoga explicitamente `TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` (defaults de `pg_default_acl` até então intocados), `UPDATE` restrito por coluna, identidade imutável após insert. Validação consolidada de 19 itens funcionais + 4 planos de execução, transacional (`BEGIN`/`ROLLBACK`), sem dado residual, sem segredo. Nenhum trigger de consistência `card_variant_id`×`card_id` criado (candidato futuro). Nenhuma fonte cadastrada, nenhuma chamada à JustTCG, nenhuma das duas entidades restantes implementada. Nenhum commit/push realizado. Detalhamento completo em `05f-pricing.md` revisão `1.5`. |
| 1.6 | **Correção retroativa de segurança — sete tabelas de P1–P3 (2026-08-16, mesmo dia, mesmo ciclo do Incremento P4)**, a pedido explícito de Fabrício. Auditoria somente leitura (sem presumir os relatórios anteriores) confirmou que `pricing_source`, `card_condition`, `pricing_condition_mapping`, `pricing_set_mapping`, `pricing_card_mapping`, `pricing_sync_run` e `pricing_sync_run_call` nunca tiveram `TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` revogados de `service_role` (defaults de `pg_default_acl`, intocados desde P1–P3); `anon`/`authenticated` confirmados sem excesso. Corrigido via Query `3053` (única migration, sete tabelas), revogando apenas os quatro privilégios excedentes — `SELECT`/`INSERT`/`UPDATE` preservados integralmente, incluindo os grants de coluna restritos de `pricing_sync_run`; nenhuma alteração de RLS/policies/estrutura/índices/migrations históricas/dados. Validado por `has_table_privilege` pós-correção e por smoke test transacional real (`ROLLBACK` forçado) confirmando zero impacto funcional. Advisors sem achado novo. Nenhum commit/push realizado. Detalhamento completo em `05f-pricing.md` revisão `1.6`. |
| 1.7 | **Incremento P5 — Série Histórica de Taxas de Câmbio (2026-08-16, mesmo dia, ciclo seguinte à correção retroativa)**, a pedido explícito de Fabrício. `pricing_fx_rate` criada no Supabase (Query `3060`), `CONFIRMADO EXECUTADO`, vazia. Um requisito de integridade físico novo: `CHECK` de formato técnico em `rate_source_code` (mesmo padrão de `pricing_source.code`). `UNIQUE` de unicidade reordenada — `(from_currency, to_currency, rate_source_code, rate_date)` em vez de `(from_currency, to_currency, rate_date, rate_source_code)` — para servir como o único índice necessário aos três padrões de consulta reais, dispensando o índice adicional cogitado no texto original; confirmado por `EXPLAIN (ANALYZE, BUFFERS)` sobre volume sintético de 7.830 linhas. RLS/Grants decididos como o padrão conservador do restante do domínio (corrigindo a hipótese original de leitura pública): `anon` zero privilégios; `authenticated` só `SELECT` via `pricing_admin_select`; `service_role` apenas `SELECT`/`INSERT`, com `UPDATE`/`DELETE`/`TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` revogados explicitamente. Sem `is_active`, sem `updated_at`, sem trigger — append-only por design. Validação consolidada de 22 itens funcionais/integridade/RLS/grants, transacional (`BEGIN`/`ROLLBACK` forçado), sem dado residual. Advisors sem achado novo. Nenhuma chamada ao Banco Central, nenhuma chamada à JustTCG, nenhuma taxa real cadastrada. Única entidade restante do domínio: `pricing_observation`. Nenhum commit/push realizado. Detalhamento completo em `05f-pricing.md` revisão `1.7`. |
| 1.8 | **Incremento P6 — Observação Histórica de Preço (2026-08-16, mesmo dia, ciclo seguinte ao Incremento P5)**, a pedido explícito de Fabrício — décima e última entidade física do domínio, encerrando a fundação física completa (10 de 10). `pricing_observation` criada no Supabase (Query `3070`), `CONFIRMADO EXECUTADO`, vazia. **Correção obrigatória de identidade**, exigida pelo próprio pedido: `UNIQUE (pricing_product_id, condition_id, price_type, observed_at)` da hipótese original substituída por `UNIQUE NULLS NOT DISTINCT (pricing_product_id, condition_id, price_type, currency_code, market_label, observed_at)` (suporte a `NULLS NOT DISTINCT` confirmado em PostgreSQL 17.6 antes do DDL) — corrige a insuficiência da identidade original frente à semântica multi-mercado/multi-moeda já aprovada na versão `1.1` desta ADR; `market_scope`/`market_evidence_confirmed` permanecem fora da identidade (classificação/evidência, não o fato externo). Índices reformulados: nenhum isolado em `pricing_product_id` (já líder da identidade); `ix_pricing_observation_latest_lookup` para leitura recente sem moeda/mercado conhecidos; `ix_pricing_observation_condition_id` isolado (FK nunca em posição utilizável em outro índice); `ix_pricing_observation_sync_run_id` parcial; nenhum índice parcial dedicado a "Valor Brasil" — testado empiricamente sobre 80.000 linhas sintéticas, índice geral já suficiente. Decisão explícita de não particionar neste incremento, com checkpoint de capacidade registrado antes do DDL. RLS/Grants no mesmo padrão conservador de `pricing_product`/`pricing_fx_rate`; `RESTRICT` em `pricing_product_id`/`condition_id`, `SET NULL` em `sync_run_id`, ambos validados fisicamente. Validação consolidada de 32 itens funcionais/integridade/RLS/grants + 5 planos de execução sobre 80.000 linhas sintéticas, transacional (`BEGIN`/`ROLLBACK` forçado, fixtures completas, Catálogo Editorial intocado), sem dado residual, sem segredo. Advisors sem achado novo. Nenhuma fonte real, nenhuma chamada à JustTCG, nenhuma ingestão PTAX. **Fundação física de Pricing completa — dez de dez entidades.** Pricing ainda não é operacional (fonte homologada, condições semeadas, mappings reais, sincronização, PTAX, frontend, Analytics/Item Valuation seguem pendentes). Nenhum commit/push realizado. Detalhamento completo em `05f-pricing.md` revisão `1.8`. |
| 1.9 | **Incremento P7 — Homologação Condicionada da JustTCG (2026-08-17)**, a pedido explícito de Fabrício, decorrente da prova técnica interrompida por cota em 2026-08-16. Resultado reportado por Fabrício: 19/19 cartas e 7/7 Card Sets encontrados, preços em USD/printings/condições/histórico disponíveis, zero falha técnica, 15 requisições, nenhum segredo persistido; Decisão A aprovada; Decisão B (idioma) inconclusiva — `UNDETERMINED` nas 18 consultas novas. `JUSTTCG` cadastrada em `pricing_source` (Query `3700`, `INSERT ... ON CONFLICT DO NOTHING`, primeiro arquivo físico da faixa `3000`–`3999` no repositório), homologada só para piloto técnico de produto/printing/condição/preço USD/histórico — não para PT-BR nem "Valor Brasil"; idioma nunca inferido pelo USD. `default_market_scope = 'INTERNATIONAL'`; `requires_commercial_agreement = TRUE` (Termos vigentes da JustTCG consultados diretamente nesta rodada — `justtcg.com/terms`, tier gratuito só não comercial, plano pago exigido para uso comercial); `is_active = FALSE`, decisão deliberada (única semântica real da coluna é "não excluída fisicamente", sem consumidor operacional hoje — reavaliação para `TRUE` fica para incremento futuro, com plano pago contratado e sincronização real). Validação de 8 itens: unicidade, idempotência, RLS/grants inalterados, advisors sem achado novo, ausência de segredo, nenhuma outra tabela do domínio tocada, nenhuma migration histórica alterada, escopo confirmado (mapping/produto/observação/PTAX/scheduler/frontend fora). Nenhum commit/push realizado. Detalhamento completo em `05f-pricing.md`, seção "Incremento P7". |
| 1.10 | **Incremento P8 — Conector JustTCG e Piloto Controlado (2026-08-17)**, a pedido explícito de Fabrício. Primeiro fluxo real JustTCG → Pricing: script Deno standalone `scripts/sync-justtcg-pricing.ts` (mesmo precedente de `scripts/import-manual-assets.ts`, nunca Edge Function), com `getJustTcgSource()` como único ponto do repositório autorizado a ler `pricing_source WHERE code = 'JUSTTCG'` apesar de `is_active = FALSE` (permanece `FALSE`, este incremento não reabre a homologação comercial). Divergência material identificada e resolvida via `AskUserQuestion` (aprovada por Fabrício): `card_condition` vazia e `service_role` só `SELECT` em `pricing_set_mapping`/`pricing_card_mapping`, ambos já pendência explícita desde P1/P2. Três migrations: `3091` (`GRANT INSERT`, nunca `UPDATE`/`DELETE`, a `service_role` nas duas tabelas de mapeamento), `3701` (seed de cinco condições canônicas confirmadas pela prova técnica real — não seis, como cogitado até a versão `1.8`), `3702` (de-para JustTCG → `card_condition`). Piloto hardcoded, restrito a `ME1`/`BASE1` e três cartas por Set (as mesmas nove já validadas na prova técnica) — nunca amplia o catálogo; idempotência via `INSERT ... ON CONFLICT DO NOTHING` em toda a cadeia; execução registrada em `pricing_sync_run`/`pricing_sync_run_call`; 401 aborta, 429 aplica backoff+retentativa única; sanitização defensiva de segredo em todo texto persistido; preço preservado em USD; idioma sempre `UNDETERMINED`/`NULL`, nunca inferido pelo USD. **Piloto real não executado** — `JUSTTCG_API_KEY` ausente no ambiente de execução do agente, comportamento esperado (`--fixture-check` automático); lógica validada por harness `node` equivalente (16/16 asserções), já que o runtime Deno não está disponível neste sandbox (mesma classe de limitação já registrada para `npm run build`/`lint`). Validação pós-migration: `card_condition`/`pricing_condition_mapping` com 5 linhas cada, grants confirmados via `information_schema.column_privileges`, RLS/policy das oito tabelas tocadas confirmada, advisors sem achado novo, todas as tabelas de dado real em zero linhas, zero resíduo (`git status`). Fora de escopo: PTAX, BRL, "Valor Brasil", scheduler, frontend, ativação comercial da fonte, ingestão em massa. Nenhum commit/push realizado. Detalhamento completo em `05f-pricing.md`, seção "Incremento P8". |
| 1.11 | **Correção pós-P8 — validação de `pricing_sync_run.confirmed_by` sem RPC de UUID livre (2026-08-17)**, a pedido explícito de Fabrício, em resposta a um erro real (`permission denied for table admin_user`, `service_role` sem `SELECT` de propósito). Proposta inicial (`is_admin_user(uuid)` `SECURITY DEFINER`, `EXECUTE` só `service_role`) identificada como conflitante com este próprio ADR — a Seção "Alternatives Considered" já registra `is_admin(p_user_id UUID)` aceitando qualquer UUID como avaliada e rejeitada por Fabrício — divergência sinalizada explicitamente antes de qualquer DDL, não aplicada silenciosamente. Fabrício decidiu preservar `ADR-021` integralmente: `pricing_sync_run.confirmed_by` (Query `3082`, sem `FK`) validado por trigger `BEFORE INSERT` `SECURITY DEFINER`/`search_path=''` (`validate_pricing_sync_run_confirmed_by()`, Query `3083`), nunca por função RPC isolada — `RETURNS TRIGGER` impede chamada direta, `EXECUTE` revogado de todos os papéis (inclusive `service_role`) como defesa em profundidade, erro genérico sem interpolar o UUID. `requireAdmin()` (causa raiz, `SELECT` direto em `admin_user`) removida de `scripts/sync-justtcg-pricing.ts`; `confirmed_by` passa a integrar o `INSERT` de `pricing_sync_run`, já o primeiro write do piloto. Testado via `SET ROLE service_role`: UUID admin real insere, UUID aleatório falha com `PRICING_SYNC_RUN_CONFIRMED_BY_INVALID`, zero resíduo (`ROLLBACK`). Grants de `admin_user` inalterados; `get_advisors` sem achado novo. **`ADR-021` não foi alterado** — nenhuma revisão necessária, decisão original preservada integralmente. Nenhum commit/push realizado; runner PowerShell intocado. Detalhamento completo em `05f-pricing.md` revisão `1.11`. |
| 1.12 | **Fechamento do Incremento P8 — evidência real final (2026-08-17)**, a pedido explícito de Fabrício, ciclo posterior às correções pós-P8 (`findCard()`, finalização garantida de `pricing_sync_run`, telemetria de escrita — implementação pura, sem impacto arquitetural, documentadas só em `05f-pricing.md`/`docs/log.md` revisões `1.12`–`1.14`). Piloto real executado por Fabrício com sucesso; confirmado diretamente no Supabase pelo agente: quatro execuções `pricing_sync_run` `COMPLETED` (não duas, como a premissa inicial do pedido registrava — divergência sinalizada explicitamente, não aplicada em silêncio), cada uma com `requests_made = 7`, `rate_limit_hits = 0`, `2` Sets e `6` cartas resolvidos; `pricing_product`/`pricing_observation` estáveis em `32`/`32` nas quatro execuções; no par final da prova de idempotência, a segunda execução fechou com `productsWritten = 0`/`observationsWritten = 0`. 28 chamadas HTTP registradas, todas `SUCCESS`. Varredura SQL direta sobre campos de texto do domínio sem nenhum padrão de segredo. `get_advisors` (segurança e performance) sem achado novo referenciando Pricing. `pricing_source.is_active` confirmado `FALSE` — fechamento estritamente técnico, **não autoriza** produção comercial, PT-BR nem "Valor Brasil"; semântica de `is_active` inalterada desde P7. Sem mudança de dado, schema, migration ou código nesta rodada — fechamento documental sobre evidência já produzida. Nenhum commit/push realizado. Detalhamento completo em `05f-pricing.md` revisão `1.15`. |
| 1.13 | **Incremento P9 — Ingestão PTAX (2026-08-17)**, a pedido explícito de Fabrício. `pricing_fx_rate` (Query `3060`, Incremento P5) já modelava integralmente o que este incremento precisa — nenhuma migration/alteração de schema. Única divergência genuína encontrada na introspecção prévia (qual cotação PTAX gravar — compra ou venda — nunca definida em nenhum documento) apresentada a Fabrício via `AskUserQuestion` antes de qualquer código, seguindo instrução explícita de registrar a decisão na documentação antes da implementação — nunca escolhida em silêncio. **Decisão aprovada**: `rate` grava exclusivamente `cotacaoVenda` (nunca `cotacaoCompra`, nunca as duas — API Olinda PTAX v1 do Banco Central, pública e sem autenticação, sempre retorna as duas cotações por dia útil, confirmado na documentação oficial CKAN consultada nesta rodada); `rate_date` é sempre a data efetiva publicada pelo BCB (extraída de `dataHoraCotacao`, nunca a data de execução do script); fins de semana/feriados sem cotação publicada não geram taxa artificial nem linha duplicada; toda exibição futura desta conversão deve ser rotulada "equivalente em BRL pela PTAX Venda", nunca "Valor Brasil" (conceito distinto, já formalizado nesta ADR desde a versão `1.1`). Implementado `scripts/sync-ptax-fx-rate.ts` (mesmo precedente arquitetural de `scripts/sync-justtcg-pricing.ts` — Deno standalone, server-side, acionado manualmente, nunca Edge Function/scheduler/frontend), com validação defensiva explícita da forma da resposta (falha alto se `cotacaoVenda`/`dataHoraCotacao` não vierem no formato documentado, nunca parsing best-effort silencioso), persistência linha a linha classificando `NEW`/`CONFLICT_IGNORED`/`OTHER_ERROR` (mesma disciplina de telemetria de escrita do Incremento P8, equivalente client-side de `ON CONFLICT DO NOTHING` contra a `UNIQUE` já `CONFIRMADO EXECUTADO`), timeout de requisição, sanitização defensiva e modo `--fixture-check` 100% offline. Não há API key do Banco Central (endpoint público); única credencial em jogo é `SUPABASE_SERVICE_ROLE_KEY`, só variável de ambiente, nunca persistida. **Piloto real não executado** — mesma classe de limitação do Incremento P8: sandbox do agente sem runtime Deno, sem credenciais Supabase e sem rota de rede para `bcb.gov.br` (proxy retorna `403 blocked-by-allowlist`); forma exata da resposta viva do BCB também não confirmada por chamada de rede bem-sucedida nesta sessão — tratada como conhecimento geral estável de API pública, nunca presumida silenciosamente, com o script preparado para falhar alto em caso de divergência real. Lógica validada por harness `node` equivalente (14/14 asserções) e sintaxe confirmada por `ts.transpileModule` (zero diagnóstico); varredura de segredo no script novo negativa. Fora de escopo confirmado: conversão de `pricing_observation`, "Valor Brasil", scheduler, frontend, qualquer dado da JustTCG, armazenamento da cotação de compra. Nenhuma migration nova; nenhum commit/push realizado. Detalhamento completo em `05f-pricing.md`, seção "Incremento P9". |
| 1.14 | **Fechamento do Incremento P9 — Evidência Real Final (2026-08-17)**, a pedido explícito de Fabrício, ciclo posterior a três rodadas de correção pós-piloto real (implementação pura, sem impacto arquitetural, documentadas só em `05f-pricing.md` revisão `1.17`). Piloto real executado por Fabrício via `scripts/Executar-P9-PTAX-Local.ps1` expôs três problemas reais, todos corrigidos só em código, sem migration: parâmetro OData errado (`dataFinal` em vez de `dataFinalCotacao`, `HTTP 400` real); telemetria `NEW`/conflito por texto de erro substituída por `.upsert(..., ignoreDuplicates: true).select()` classificando pela forma da resposta; `401` do Supabase no runner causado pelo `User-Agent` padrão do Windows PowerShell 5.1, corrigido com `-UserAgent` explícito não-browser. **Evidência confirmada diretamente no Supabase pelo agente**: `pricing_fx_rate` com exatamente 6 linhas (`rate_date` 10, 11, 12, 13, 14 e 17/08/2026, cotação de venda PTAX real, `rate_source_code = 'BCB_PTAX'`), zero linha para 15–16/08 (fim de semana), zero duplicidade, segunda execução do runner com zero escrita nova — idempotência real. `get_advisors` (segurança e performance): zero achado novo referenciando `pricing_fx_rate`. Varredura de segredo negativa nos dois arquivos alterados (`scripts/sync-ptax-fx-rate.ts`, `scripts/Executar-P9-PTAX-Local.ps1`). Sem mudança de dado, schema ou migration nesta rodada de fechamento — as 6 linhas gravadas pelo piloto real permanecem intocadas. **Incremento P9 formalmente encerrado.** Nenhum commit/push realizado. Detalhamento completo em `05f-pricing.md`, revisão `1.17`. |
| 1.15 | **Incremento P10 — Projeção Cambial PTAX (2026-08-17)**, a pedido explícito de Fabrício. Primeira implementação física do Cenário Obrigatório 6 desta ADR ("preço original em USD, exibido informativamente em BRL", já descrito desde a versão `1.1`): view de leitura `public.pricing_observation_brl_equivalent` (Query `3900`, `CONFIRMADO EXECUTADO`, primeiro uso real da faixa `3900`–`3999`, antes reserva — ver `STD-001` revisão `1.34`). Projeta observações `currency_code = 'USD'` de `pricing_observation` para equivalente em BRL via `LEFT JOIN LATERAL` contra a última `pricing_fx_rate` (`BCB_PTAX`) com `rate_date <=` data UTC de `observed_at` — nunca taxa futura; `round(price * rate, 2)`; sem taxa aplicável, campos de conversão `NULL` e `fx_status = 'FX_RATE_UNAVAILABLE'`; rótulo fixo `'Equivalente em BRL pela PTAX Venda'` embutido na própria projeção — nunca "Valor Brasil", conceito distinto reservado a `BRAZIL_ITEM_VALUATION`/`market_evidence_confirmed`. **Nenhuma alteração em `pricing_observation`/`pricing_fx_rate`**, sem migration nas duas tabelas base, sem novo schema. `security_invoker = true` (mesmo padrão já em uso nas views do Catálogo Editorial): a RLS `pricing_admin_select` de ambas as tabelas base é aplicada ao papel que efetivamente consulta a view, não ao dono — condição estrutural para "não amplia acesso às tabelas brutas", validada com `SET LOCAL ROLE` real (`anon` bloqueado por `insufficient_privilege`; `authenticated` não-admin zero linhas; `authenticated` admin e `service_role` com as 32 linhas reais). `GRANT SELECT` restrito a `authenticated`/`service_role`, idêntico ao já concedido nas tabelas base. Impossibilidade de dupla conversão garantida estruturalmente pelo filtro `WHERE currency_code = 'USD'` (não por checagem em tempo de execução), comprovada com observação `BRL` sintética que nunca aparece na projeção. Validação funcional transacional (`BEGIN`/`ROLLBACK`, sem resíduo): data exata (dado real de produção, 32 observações reais de 2026-08-17), sábado/domingo resolvendo para a sexta-feira anterior, data anterior à primeira taxa cadastrada (`FX_RATE_UNAVAILABLE`), dupla conversão. Plano de execução confirmado com volume sintético (20.000 observações/522 taxas): `Index Scan`/`Index Scan Backward` sobre `ix_pricing_observation_latest_lookup`/`uq_pricing_fx_rate_pair_source_date`, sem `Seq Scan` no caminho quente, nenhum índice novo necessário. `get_advisors` (segurança e performance): zero achado novo. Fora de escopo confirmado: frontend, scheduler, persistência do equivalente BRL (sempre calculado em tempo real, nunca armazenado). Nenhum commit/push realizado. Detalhamento completo em `05f-pricing.md`, revisão `1.18`, seção "Projeção de Leitura: Equivalente em BRL (Incremento P10)". |
| 1.16 | **Incremento P11 — Contrato Seguro de Leitura de Preços por Card (2026-08-17)**, a pedido explícito de Fabrício. Primeiro caminho de leitura de Pricing disponível a qualquer usuário autenticado, sem exigir perfil administrativo: função `public.get_card_pricing_snapshot(p_card_id uuid)` (Query `3901`, `CONFIRMADO EXECUTADO`), `SECURITY DEFINER` seguindo exatamente o padrão já validado em `ADR-030` (`search_cards()`/`search_card_filter_options()` — verificação explícita de `auth.uid() IS NOT NULL`, nunca `is_admin()`; `search_path = ''`; `REVOKE ALL FROM PUBLIC, anon` + `GRANT EXECUTE TO authenticated`). Retorna o snapshot mais recente por produto/condição/tipo de preço/mercado (`DISTINCT ON` com desempate determinístico `observed_at DESC, created_at DESC, id DESC`), com equivalente BRL reaproveitando a regra do Incremento P10 (revisão `1.15`, acima); filtros de negócio obrigatórios aplicados no corpo da função — `pricing_card_mapping.match_status = 'CONFIRMED'`, `pricing_product.is_active = TRUE`, `pricing_source.is_active = TRUE` — já que a função contorna deliberadamente `pricing_admin_select`/`card_condition_admin_select` (única forma de um não-administrador ler Pricing); nunca expõe `external_product_id`, `raw_payload` ou histórico completo. Validado com `SET LOCAL ROLE` real: `anon` bloqueado por `insufficient_privilege`; `authenticated` sem JWT bloqueado pela própria função; `authenticated` não-administrador recebe exatamente o mesmo resultado que `authenticated` administrador (prova de "não exige perfil administrativo" por igualdade de resultado, não só ausência de erro); leitura direta das tabelas base por `authenticated` permanece em zero linhas mesmo com a fonte temporariamente ativa na transação de teste; enumeração por UUID indistinguível entre carta inexistente e carta sem dado de Pricing (a função nunca consulta `card`); exclusão seletiva por mapping `PENDING`/produto inativo validada com dado real do piloto P8. Achado real de performance: `ix_pricing_observation_latest_lookup` (Incremento P6) não cobre `market_label`, coluna adicional do agrupamento desta função — corrigido com `ix_pricing_observation_snapshot_lookup` (Query `3902`, mesmo precedente da Query `3052`); mesmo com o índice, o desempate por `created_at`/`id` ainda força um `Sort` interno em volume sintético extremo (50.000 observações para uma única carta, ~166ms) — achado registrado como sinal a reavaliar no futuro, não resolvido nesta rodada; em volume realista (~1.000 observações/produto, equivalente a anos de sincronização manual diária), ~27ms. `get_advisors`: único achado novo referenciando a função é `authenticated_security_definer_function_executable` (`INFO`, esperado, mesmo padrão de `search_cards()` e das funções `admin_*`). **Correção de processo registrada, não escondida**: uma transação de teste de segurança terminou com `COMMIT` em vez de `ROLLBACK` por erro do agente, deixando `pricing_source.is_active = TRUE` por menos de um minuto até ser percebido e revertido — sem consequência de dado, já que nenhum consumidor real da fonte existe ainda. `JUSTTCG` permanece `is_active = FALSE`, confirmado por chamada real em produção como `authenticated` retornando zero linhas para uma carta com dado real. Nenhuma alteração de `pricing_observation`/`pricing_fx_rate`/`pricing_product`/`pricing_card_mapping`/`pricing_source`/`card_condition`. Nenhum frontend/scheduler/variante em lote implementados. Nenhum commit/push realizado. Detalhamento completo em `05f-pricing.md`, revisão `1.19`, seção "Contrato Seguro de Leitura de Preços por Card (Incremento P11)". |
| 1.17 | **Incremento P12 — Painel de Preços da Carta, Homologação Administrativa (2026-08-17)**, a pedido explícito de Fabrício. Primeiro consumidor de frontend de Pricing: Route Handler `GET /api/cards/[cardId]/pricing` e componente compartilhado `CardPricingPanel`, reutilizado nos modais de ampliação de carta de `/catalogo/cartas` e `/pesquisa`. Modo normal (`live`, qualquer usuário autenticado): chama exclusivamente `get_card_pricing_snapshot` (Incremento P11, revisão `1.16`, acima) pelo client autenticado por cookies, nunca `service_role`; painel oculto quando a resposta vem vazia — hoje sempre, `JUSTTCG` permanece `is_active = FALSE`. Modo prévia técnica (`preview`, `?pricingPreview=1`): só ativado depois de o Route Handler confirmar `is_admin()` no servidor (mesma sessão por cookies, nunca uma checagem só no cliente); quando confirmado, lê as tabelas base de Pricing diretamente reaproveitando as policies `pricing_admin_select`/`card_condition_admin_select` já concedidas desde P1–P6 — **nenhuma RPC nova, nenhum grant ampliado** — omitindo deliberadamente, só neste modo, o filtro `pricing_source.is_active = TRUE` que o contrato público de P11 aplica, expondo os dados reais do piloto JustTCG só a quem já passou pela checagem de admin no servidor. Não-admin/anon com `?pricingPreview=1` recebem resposta idêntica ao modo `live`, sem nenhum sinal de que o modo prévia existe — validado com `SET LOCAL ROLE` real: `anon` bloqueado antes mesmo da RLS (sem `GRANT SELECT` nas seis tabelas base), `authenticated` não-admin com `is_admin()` confirmado `FALSE` e leitura direta em zero linhas. Validado com dado real do piloto P8 (6 cartas, 32 observações): modo `live` em zero linhas para as seis cartas; modo `preview` em 5/5/5/7/5/5 linhas (32 no total, idêntico ao real, sem redução do agrupamento — cada condição já tem produto próprio, achado de P11), 100% marcadas como vindas de fonte inativa, com cotação PTAX Venda real disponível confirmando conversão `CONVERTED` de fato. Exceção pontual e explícita à regra "só a imagem, nenhuma outra informação" de `/cartas` (2026-07-31) — pedido direto de Fabrício para este incremento; a regra permanece válida para qualquer outra informação. `tsc --noEmit` sem erro; `next lint`/`next build` seguem não executáveis neste sandbox (SWC ausente, mesma limitação de incrementos anteriores) — QA visual completo fica pendente de checagem local por Fabrício, sinalizado explicitamente, não escondido. Nenhuma migration, nenhuma alteração de dado, nenhuma RPC nova, nenhum grant ampliado. Nenhum commit/push realizado. **Rejeitada visualmente por Fabrício** logo em seguida — ver revisão `1.18`, abaixo. Detalhamento completo em `05f-pricing.md`, revisão `1.20`. |
| 1.18 | **Redesenho do Incremento P12 — Ícone + Popover, sem painel no modal (2026-08-18)**, a pedido explícito de Fabrício: "a solução atual foi rejeitada visualmente". `CardPricingPanel` (revisão `1.17`, acima) removido por completo dos dois modais de ampliação de carta — `CartaZoomDialog`/`/pesquisa` voltam exatamente ao estado anterior a qualquer trabalho de Pricing (`CardImagePreview` isolado, motion senoidal e View Transitions intocados; a exceção pontual à regra "só a imagem" de `/cartas`, registrada na revisão `1.17`, deixa de existir — a regra volta a valer sem exceção). Pricing passa a viver num ícone monetário discreto (`Tag`, `lucide-react`) sobreposto à miniatura de cada carta do grid, irmão do botão de ampliar (nunca dentro dele, mesmo padrão já usado pelo selo de Desativar/Reativar), com popover próprio (`CardPriceBadge`, `useAnchoredPopover`, `pricing-client.ts` — novos, ver `05f-pricing.md`) que abre em hover/foco no desktop e toque no mobile; clicar no ícone nunca abre o modal (`stopPropagation` explícito, ainda que estruturalmente desnecessário por serem elementos irmãos). Popover via portal (`createPortal` em `document.body`) para nunca ser cortado pelo grid, posicionado por medição real (`getBoundingClientRect()`, recalculado em resize/scroll, com desvio de canto quando falta espaço), com `role="dialog"`/`aria-modal="false"`/`aria-haspopup`/`aria-expanded`/`aria-controls`, fechamento por `Escape`/clique fora, sem armadilha de foco. **Nenhuma dependência nova instalada** — `npm install @radix-ui/react-popover` testado e confirmado bloqueado neste sandbox (`403 Forbidden`, sem acesso ao registry do npm); popover implementado à mão só com `react-dom`/DOM nativo. Carregamento sempre preguiçoso (só ao interagir com o ícone de uma carta específica — nunca no carregamento do grid, sem N+1) e com cache em memória por `card_id`+modo (`Map` de módulo, cacheando a própria `Promise` em voo). **Correção de data civil PTAX**: `fx_rate_date` (coluna `DATE` pura, sem hora/fuso) passa a usar `formatarDataCivil()` (nova, `lib/format-date.ts`, parseia ano/mês/dia e constrói a data no fuso local — nunca `new Date(string)` direto), corrigindo um risco identificado e deliberadamente aceito ainda na revisão `1.17` (nunca chegou a se manifestar visivelmente, por nunca ter ido a produção); taxa PTAX também migrada de `.toFixed(4)` para `Intl.NumberFormat("pt-BR", ...)`. **Sem nenhuma alteração no Route Handler/P11** — modos `live`/`preview`, `is_admin()` no servidor, reaproveitamento de `pricing_admin_select`/`card_condition_admin_select`, omissão deliberada do filtro `pricing_source.is_active` só na prévia: tudo herdado sem modificação da revisão `1.17`, já que o redesenho é estritamente de consumo no frontend. `JUSTTCG.is_active` permanece `FALSE`; nenhuma migration; nenhum grant ampliado; nenhuma RPC nova. `tsc --noEmit` sem erro (duas correções de tipo no processo: união discriminada de estado de carregamento precisou de quatro variantes de literal único em vez de agrupar três status num mesmo membro; `rows[0]` com optional chaining). `next lint`/`next build`/QA visual real seguem não executáveis neste sandbox (mesma limitação já registrada desde incrementos anteriores) — checagem local por Fabrício recomendada antes de considerar o item plenamente fechado. **Nunca chegou a ser visto por Fabrício** — nenhum commit/push realizado nesta rodada nem na anterior, então o site implantado (Vercel, conectado ao GitHub) jamais refletiu este ícone; achado só confirmado explicitamente na revisão seguinte (`1.19`), a partir de capturas de tela reais. Detalhamento completo em `05f-pricing.md`, revisão `1.21`, seção "Painel de Preços da Carta (Incremento P12)". |
| 1.19 | **Terceiro desenho do Incremento P12 — resumo de preço inline em lote, nunca dependente de hover (2026-08-18)**, a pedido explícito de Fabrício, após reportar — com capturas reais de `/pesquisa` e `/catalogo/cartas` — que o ícone da revisão `1.18` não aparecia em nenhum grid. **Causa raiz confirmada, não assumida**: `git log` mostra o último commit real do repositório como o Incremento P11 (`1b39b8f`); nenhuma das duas primeiras versões deste incremento (revisões `1.17`/`1.18`) foi commitada nem enviada ao GitHub em nenhum momento — instrução explícita de Fabrício em todas as rodadas ("Nenhum commit/push"), respeitada à risca. O projeto tem deploy Vercel conectado ao repositório GitHub (`@vercel/speed-insights` já integrado, incidente de deploy anterior já registrado neste mesmo histórico) — qualquer ambiente que reflita o site publicado nunca poderia ter mostrado nenhuma das duas versões, porque nenhuma chegou ao branch `main` remoto; as capturas de Fabrício são evidência exatamente disso, não de um bug de renderização no código. `CardPriceBadge` (ícone, revisão `1.18`) removido por completo — nunca precisou de correção, foi descartado por decisão de design junto com o requisito novo. **Requisito estrutural novo**: o resumo de preço precisa existir antes de qualquer interação do usuário — hover/foco/toque passam a abrir só um popover de *detalhe*, nunca a decidir se o preço aparece. Isso torna a arquitetura lazy-on-interact da revisão `1.18` estruturalmente incompatível: não é possível mostrar um preço antes da interação se ele só é buscado na interação. Resolvido com busca **em lote**: `POST /api/cards/pricing/batch` (novo Route Handler, sibling estático de `[cardId]/pricing` sob `app/api/cards/` — mesmo padrão já usado pelo endpoint pré-existente `filter-options`, sem colisão de rotas), recebendo até 200 `card_id` de uma vez; modo `live` chama `get_card_pricing_snapshot` (P11) uma vez por carta **em paralelo no servidor** (`Promise.all` — a função não aceita lista de IDs e nenhuma migration foi autorizada nesta rodada), nunca uma vez por carta a partir do navegador; modo `preview` (`?pricingPreview=1`, só com `is_admin()` confirmado no servidor) faz uma única consulta `.in(card_id, [...])` nas tabelas base, reaproveitando exatamente as mesmas policies das revisões `1.17`/`1.18` — nenhuma RPC nova, nenhum grant ampliado. `CardPriceSummary` (novo, substitui `CardPriceBadge`) só renderiza o texto do preço quando `usePricingBatch` (novo hook, dispara o lote assim que a lista de `card_id` do grid existe/cresce) já resolveu aquela carta — nunca condicionado a hover; interação só alterna a abertura do popover de detalhe, que reaproveita as linhas já recebidas no lote (sem nova requisição). Cache em memória por `card_id` (`pricing-batch-client.ts`, substitui `pricing-client.ts`) evita rebuscar uma carta já resolvida em qualquer grid da mesma navegação. `useAnchoredPopover` (posicionamento do popover) reaproveitado sem alteração da revisão `1.18`. **Validado com dado real do piloto P8**: query direta no Supabase confirma as 32 observações das 6 cartas (todas USD, `price_type = MARKET`) resolvendo `fx_status = 'CONVERTED'` com a PTAX Venda de 2026-08-17 já cadastrada — modo `preview` produzirá resumo real para as 6 cartas; modo `live` confirmado retornando zero linhas para todas (`JUSTTCG.is_active = FALSE`), resumo permanece oculto para qualquer usuário não-admin. Posicionamento visual seguindo literalmente o layout pedido por Fabrício: preço à direita da linha "Card Set · número" em `/pesquisa`; preço ao lado do botão de editar (depois dele) na linha de ações de `/catalogo/cartas`. Sem nenhuma alteração no Route Handler por carta (`[cardId]/pricing`)/P11 — contrato idêntico nas três rodadas. `tsc --noEmit` sem erro; corrigido durante a implementação um id de DOM do popover que usava o nome da carta em vez do `card_id` (colisão possível entre cartas homônimas de Sets diferentes). `JUSTTCG.is_active` permanece `FALSE`; nenhuma migration; nenhum grant ampliado; nenhuma RPC nova; nenhuma dependência nova instalada. `next lint`/`next build`/QA visual real seguem não executáveis neste sandbox — recomendado a Fabrício rodar `npm run dev` localmente (o deploy Vercel nunca refletirá isto sem push) antes de considerar o item plenamente fechado. Nenhum commit/push realizado. Detalhamento completo em `05f-pricing.md`, revisão `1.22`, seção "Painel de Preços da Carta (Incremento P12)". |
| 1.20 | **Rodada 2 do Incremento P12 ("P12 v4") — Resumo sem N+1 real, três grids (2026-08-18)**, a pedido explícito de Fabrício, que apontou que o incremento ainda não estava concluído: a revisão `1.19` resolvia o N+1 cliente→servidor, mas não o N+1 servidor→Postgres (modo `live` ainda chamava `get_card_pricing_snapshot` uma vez por carta em paralelo). **Migration nova, autorizada explicitamente para este propósito específico** — `public.get_cards_pricing_summary(p_card_ids uuid[])` (Query `3903`, `CONFIRMADO EXECUTADO`), variante em lote da revisão `1.16`, mesmo padrão de segurança (`SECURITY DEFINER STABLE`, `search_path = ''`, `auth.uid() IS NOT NULL`, `REVOKE ALL FROM PUBLIC, anon` + `GRANT EXECUTE TO authenticated`), com dois guards de entrada adicionais (array vazio/nulo, mais de 100 elementos, ambos `ERRCODE 22023`). Resolve o lote inteiro numa única consulta SQL (CTE `input_ids` `LEFT JOIN` CTE `candidate` com `DISTINCT ON (card_id)` `LEFT JOIN LATERAL` PTAX) — `EXPLAIN (ANALYZE, BUFFERS)` com 100 `card_id` reais confirma 12,9ms, um único `Function Scan`, sem laço. Retorno deliberadamente mínimo (`card_id`/`has_pricing`/`brl_amount`/`fx_status`), pedido literal de Fabrício ("retorno mínimo... valor BRL padrão NM/Normal"): seleção fixa e determinística — condição NM, printing "Normal", `price_type` MARKET — não uma heurística de "melhor preço" como a revisão anterior fazia no cliente. **Consequência real desta regra nesta revisão específica** (`3903`): cartas cujo único printing catalogado é Holofoil (fato real de TCG — cartas "ex" costumam ser holo-exclusivas na origem dos dados) não satisfaziam a condição e por isso não mostravam resumo, mesmo tendo preço real sob outro printing — confirmado com os 6 cards piloto do Incremento P8 (Abra/Arcanine/Bulbasaur resolviam, incluindo `R$ 1,14` para Bulbasaur; Alakazam/Mega Gardevoir ex/Mega Venusaur ex são Holofoil-only e não resolviam). Registrado como achado a comunicar a Fabrício, não absorvido silenciosamente. **Corrigido na revisão `3904` — ver `1.22`, abaixo**: esta ausência de fallback entre printings foi tratada como bug real (não limitação deliberada) e substituída por uma hierarquia Normal → Holofoil → Reverse Holofoil; o texto acima é preservado como registro histórico do comportamento desta revisão específica, não do comportamento vigente da função. `POST /api/cards/pricing/batch` reescrito para chamar a função nova uma única vez no modo `live` (substitui o `Promise.all` por carta da revisão `1.19`); teto de cartas por chamada reduzido de 200 para 100, para coincidir com o limite duro da função. Contrato de resposta agora assimétrico por modo — `preview` continua devolvendo detalhe completo (inalterado); `live` devolve só o resumo mínimo. `CardPriceSummary` passa a exibir `R$ x,xx · NM · Normal` nos dois modos (texto fixo, porque a regra de seleção — NM+Normal+MARKET — já garante que só existe resumo quando ela é satisfeita; `preview` usa a mesma regra estrita no cliente, substituindo a heurística de prioridade por tipo de preço da revisão `1.19`); popover de detalhe do modo `live` passa a buscar sob demanda, só na abertura, via o contrato por-carta inalterado (`fetchLiveDetail`, novo). Terceiro grid — Coleções (`card-set-cartas-grid.tsx`, grade "Cartas da Coleção" do hub de Card Set) — introspeccionado diretamente antes de qualquer edição, por instrução explícita de Fabrício ("Introspecte os componentes reais de Coleções antes de editar"); recebe o mesmo resumo, completando os três grids pedidos (Coleções/Catálogo de Cartas/Pesquisa). Validação de segurança desta rodada: `anon` bloqueado (`permission denied`, sem `GRANT EXECUTE`); `authenticated` sem `sub` no JWT bloqueado pela própria função (`PRICING_SUMMARY_REQUIRES_AUTHENTICATION`); array vazio e array de 101 elementos rejeitados pelos guards de entrada; simulação transacional (`BEGIN`/`ROLLBACK`, `pricing_source.is_active` temporariamente `TRUE`) confirmou os valores reais esperados. `get_advisors`: único achado novo é `authenticated_security_definer_function_executable` (`WARN`, esperado, mesmo padrão da revisão `1.16`); nenhum achado de performance novo. `JUSTTCG.is_active` confirmado `FALSE` ao final da rodada — zero resíduo, todos os testes rodaram em transações com `ROLLBACK`. `tsc --noEmit` sem erro. **Validação visual — responsabilidade de Fabrício, não substituível por SQL/Vercel**: pedido explícito de que a validação final seja local (`npm run dev`, `?pricingPreview=1`), capturando as três telas com `R$ x,xx · NM · Normal` e o popover funcionando — o deploy Vercel não reflete nenhum trabalho deste incremento (nenhum commit/push realizado em nenhuma rodada, ver revisão `1.19`) e uma query direta no Supabase, embora confirme a correção dos dados, não confirma a renderização real. Nenhum commit/push realizado. Detalhamento completo em `05f-pricing.md`, revisão `1.23`, seção "Painel de Preços da Carta (Incremento P12)", subseção "Rodada 2". |
| 1.21 | **Regra de negócio formalizada — NM como condição padrão de comparação/apresentação (2026-08-18, Rodada 4 do Incremento P12 — refino de QA visual), a pedido explícito de Fabrício.** A restrição a Near Mint (NM), já em vigor desde a revisão `1.20` como consequência técnica da seleção fixa da RPC `get_cards_pricing_summary` (condição NM, printing Normal, `price_type` MARKET), é registrada aqui como decisão de domínio deliberada, não só efeito colateral de implementação: NM é a condição padrão de comparação e apresentação em toda a superfície de Pricing do produto (pill de preço, popover de detalhe, hierarquia de seleção `PRINTING_ORDER`) porque representa mais de 95% das negociações consideradas pelo produto — por isso nenhuma superfície de frontend lista preços em Lightly Played, Moderately Played, Heavily Played ou Damaged, mesmo quando existem observações reais nessas condições em `pricing_observation`. **A restrição à condição NM em si permanece decisão de domínio vigente e não foi revista** — só a ausência de fallback *entre printings* dentro de NM foi corrigida (ver `1.22`, abaixo). Sem alteração de schema/RPC/migration nesta rodada — as demais seis correções da mesma rodada (formatação USD `pt-BR`, ícone/contraste do pill, posicionamento lateral do popover, reestruturação do cabeçalho em pares rótulo/valor, renomeação da lista secundária para "Outras variantes em NM") são puramente de apresentação e detalhadas em `05f-pricing.md`, revisão `1.25`, seção "Rodada 4 do Incremento P12 — Refino de QA Visual", não repetidas aqui por não alterarem o modelo de domínio. `tsc --noEmit` sem erro. Nenhum commit/push realizado. |
| 1.22 | **Rodada 8 do Incremento P12 — hierarquia de printing na função de resumo, correção de bug real, não decisão de domínio nova (2026-08-18), a pedido explícito de Fabrício.** Um teste transacional dedicado ("Reverse-only", dado real do Bulbasaur ME1 #001) provou que `get_cards_pricing_summary` (revisão `3903`, ver `1.20`, acima) não tinha nenhum fallback de printing — hardcoded a `source_printing_label = 'Normal'`, sem exceção; a ausência de resumo para cartas Holofoil-only, registrada em `1.20` como "consequência real da regra", era na verdade sintoma do mesmo bug: falta de hierarquia, não limitação deliberada. `public.get_cards_pricing_summary(p_card_ids uuid[])` (Query `3904`, `CONFIRMADO EXECUTADO`, `DROP FUNCTION` + `CREATE FUNCTION` — a `3903` não foi editada) implementa a hierarquia aprovada por Fabrício — Normal → Holofoil → Reverse Holofoil, sempre dentro de NM+MARKET (a restrição a NM em si, formalizada em `1.21`, não foi tocada) — com `has_pricing=true` se qualquer uma das três tiver preço elegível (produto ativo, fonte ativa, mapping `CONFIRMED`) e nova coluna de retorno `printing_label` informando qual printing foi escolhido pela hierarquia. Bug de implementação real encontrado e corrigido no mesmo ciclo, antes de qualquer validação reportada: `ERROR: 42702: column reference "card_id" is ambiguous` (identificador não qualificado ambíguo contra o próprio parâmetro `RETURNS TABLE(card_id uuid, ...)` da função) — corrigido qualificando com o alias `cbp`. Quatro cenários obrigatórios validados com dado real (sem carta sintética), todos transacionais com `ROLLBACK`, zero resíduo: Bulbasaur ME1 #001 → `Normal`; Alakazam/Mega Gardevoir ex/Mega Venusaur ex (Holofoil-only, mesmas três cartas citadas em `1.20` como não resolvendo) → `Holofoil`; Bulbasaur com Normal/Holofoil temporariamente desativados → `Reverse Holofoil` (`US$ 0,31`); carta real sem nenhum `pricing_card_mapping` → `has_pricing=false`. Segurança (assinatura, grants idênticos a `3903`, `anon`/JWT bloqueados, guards de entrada) e performance (`EXPLAIN (ANALYZE, BUFFERS)`, 10,44ms/100 UUIDs, comparável aos 12,9ms de `3903`) revalidadas; `get_advisors` sem achado novo além do esperado. Contrato frontend atualizado — `PricingLiveSummary`/`PricingSummaryRow`/`GetCardsPricingSummaryRow` ganham `printingLabel`; pill de preço em `CardPriceSummary` passa a incluir o printing resolvido no `aria-label`. `tsc --noEmit` sem erro. Nenhum commit/push realizado. Detalhamento completo em `05f-pricing.md`, revisão `1.29`, seção "Rodada 8 do Incremento P12". |
| 1.23 | **Encerramento formal do Incremento P12 — Exibição de Preços no Frontend (2026-08-18), a pedido explícito de Fabrício.** Consolidação documental, sem alteração de código/banco/migration: pill de preço nos três grids (Coleções/Catálogo de Cartas/Pesquisa), popover de detalhe, hierarquia Normal → Holofoil → Reverse Holofoil (`3904`, revisão `1.22`, acima) e busca em lote sem N+1 (`3903`, revisão `1.20`) validados e aprovados por Fabrício em ambiente local — desktop/mobile, claro/escuro, toque, teclado/foco/`Escape`, formatação BRL/USD, `build` de produção; commits `6324b66` (Rodadas 1–7) e `0dad413` (Rodada 8) sincronizados com `origin/main`. Detalhamento completo (objetivo, escopo, catorze critérios de aceite) em `05f-pricing.md`, revisão `1.30`, seção "Encerramento Formal do Incremento P12". Reafirmado sem mudança: `JUSTTCG` permanece `is_active = FALSE` até contratação de plano comercial compatível; a visualização de preços nunca chama a JustTCG, só dados já sincronizados via as funções de leitura (`get_card_pricing_snapshot`/`get_cards_pricing_summary`) — sincronização e consulta permanecem arquiteturalmente desacopladas. **Próximo incremento recomendado: P13 — Orquestração Programada de Pricing** (agendamento da ingestão PTAX, hoje manual via `scripts/sync-ptax-fx-rate.ts`, e do conector JustTCG quando reativado) — ver `docs/ROADMAP.md`, seção "Next". Correção do erro de autenticação em `/login` (`AuthApiError: Invalid Refresh Token`, cookie local, sem código alterado) mantida fora do escopo deste ADR. |
| 1.24 | **Incremento P14.1 — Ativação Comercial e Piloto Controlado da JustTCG (2026-08-19), a pedido explícito de Fabrício.** Premissas comerciais confirmadas por ele (assinatura ativa, 10.000 requests/mês, 1.000 requests/dia, 50 requests/minuto, até 100 cards/request); segredo já configurado localmente, nunca visto/solicitado/exibido/registrado por este agente. Divergência material sinalizada antes de prosseguir: `scripts/sync-justtcg-pricing.ts` (Incremento P8) foi arquiteturado de propósito para rodar só localmente — nenhuma credencial existe no sandbox deste agente nem no Vault (só tem segredos de PTAX); Fabrício optou por executar localmente e reportar o resultado sanitizado, mesmo precedente de P8/P9. Chamada mínima sem escrita (`GET /v1/sets?game=pokemon`, `HTTP 200`, `apiRequestsRemaining=9998`) confirmou credencial/contrato antes de qualquer migration. Migration `3911` (`CONFIRMADO EXECUTADO`) — `UPDATE` único e idempotente, `pricing_source.is_active: false → true`, sem alteração de estrutura/RLS/grants. Piloto real (comando existente do conector, sem alteração de código, escopo idêntico ao já mapeado em P8 — ME1/BASE1, 6 cartas) — `productsWritten=0` (32 já existiam), `observationsWritten=32` (histórico de preço novo, nunca sobrescrita), `requestsMade=7`/`rateLimitHits=0`/`status=COMPLETED`. Validação: run terminal sem inconsistência, 7 calls `SUCCESS`, `pricing_observation` `32→64` sem duplicidade estrutural, zero run ativo residual, zero segredo em texto persistido, `get_advisors` sem achado novo. Nenhuma ampliação de catálogo/mappings/cobertura, nenhuma Edge Function/Cron/nova arquitetura, nenhuma alteração de PTAX/frontend, nenhum encerramento formal do Incremento P14. Nenhum commit/push realizado. Detalhamento completo em `05f-pricing.md`, revisão `1.40`, seção "Incremento P14.1". |
| 1.25 | **Incremento P14.2 — Cobertura Escalável e Sincronização em Lotes da JustTCG (2026-08-19, mesmo dia, ciclo seguinte ao P14.1)**, a pedido explícito de Fabrício. Eliminado o padrão de uma chamada HTTP por carta de P8, substituído por descoberta paginada em lotes de até 100 cartas/requisição, validado em um único Set ainda não mapeado (`BASE4`, sem sincronizar o catálogo completo). Dois achados materiais, nenhum exigindo pausa: `POST /v1/cards` (lote documentado) exige identificadores já conhecidos, inútil para descoberta — o mecanismo real de batching para um Set inteiro é a paginação de `GET /v1/cards` (`limit`/`offset`/`meta.hasMore`); nenhuma tabela local tem nome em inglês confiável, então a resolução de Set passou a usar `release_date` exata como sinal primário, nunca nome. Nenhuma migration necessária — `match_status='PENDING'` (já existente desde P2, revisão `1.3`) reaproveitado para ambiguidade, com motivo em `match_evidence.resolution`; migration livre confirmada `3912`, não usada. `scripts/sync-justtcg-pricing.ts` evoluído em lugar: cliente com `fetchImpl` injetável; `fetchAllCardsForSet()` substitui a Fase B por-carta; correlação por número de coleção primário + nome só como verificação secundária, nunca confirmando ambiguidade; `upsertSetMapping()`/`upsertCardMapping()` com leitura prévia (corrige lacuna real de P8 — reexecução nunca promovia `PENDING`/`NOT_FOUND` para `CONFIRMED`; `CONFIRMED` nunca é rebaixado); escrita de `pricing_observation` distingue divergência real de preço de conflito inofensivo, nunca sobrescrevendo — mesma disciplina de PTAX/P9. Validação 100% offline: `tsc --strict` sem erro; 50/50 asserções (`runFixtureCheck()` via Node, sandbox sem runtime Deno). Piloto real não executado — aguardando autorização e execução local de Fabrício; cobertura esperada de 3 requisições (1 `/sets` + 2 páginas de `/cards`) para as 130 cartas do Set `BASE4`, contra as 130 que P8 exigiria. Nenhuma Edge Function/Cron, nenhuma sincronização do catálogo completo, nenhuma alteração de PTAX/frontend, nenhum GRANT/RLS novo, nenhum encerramento formal do Incremento P14, nenhum commit/push realizado. Detalhamento completo em `05f-pricing.md`, revisão `1.42`, seção "Incremento P14.2". |
| 1.26 | **Fix P14.2.1 — correção de formato de `release_date` na resolução de Set da JustTCG (2026-08-19, mesmo dia), a pedido explícito de Fabrício, após piloto real retornar `SET_NOT_FOUND(BASE4)`.** Causa raiz: `release_date` retornado como datetime ISO completo, não data pura — comparação de string exata nunca batia com o `date` puro do Postgres. Corrigido na fronteira de entrada (`normalizeExternalSetReleaseDate()`/`normalizeJustTcgSets()`), sem conversão de timezone. 12 novas asserções (50→62). Nenhuma migration. Ver `05f-pricing.md`, revisão `1.43`. |
| 1.27 | **Encerramento do Incremento P14.2 — piloto real validado (2026-08-19, mesmo dia), a pedido explícito de Fabrício, evidência confirmada por auditoria direta no Supabase.** Run `9df36424-b8f8-4ee8-a67f-2c04c74bd3fa` (`CARD_SYNC`/`MANUAL`/`COMPLETED`), 3 chamadas `HTTP 200`. 130 mappings: 127 `CONFIRMED`, 3 `PENDING` (fail-safe correto — número único bate, nome diverge: Nidoran♀/♂ e Impostor/Imposter Professor Oak), zero outros. 635 produtos/635 observações, zero produto em mapping pendente, zero duplicidade, zero resíduo, zero segredo. Achado de performance: run ~156s, persistência sequencial de 635+635 linhas é o novo gargalo (batching já reduziu 130 chamadas HTTP para 3). Próximo incremento recomendado: P14.3 — Persistência em Lotes no Supabase. P14 permanece em andamento. Ver `05f-pricing.md`, revisão `1.44`. |
| 1.28 | **Encerramento formal do Incremento P14.3 — persistência em lotes, hardening de segurança e deduplicação por mudança real de preço (2026-08-19, mesmo dia, ciclo seguinte ao encerramento do P14.2), a pedido explícito de Fabrício, com piloto real aprovado.** Migrations `3912`–`3915` aplicadas (`CONFIRMADO EXECUTADO`), todas `SECURITY INVOKER`, restritas a `service_role`: promoção de mapeamento só `PENDING`/`NOT_FOUND`→`CONFIRMED` (`3912`), upsert em lote de `pricing_product` (`3913`), pré-busca de observação por tupla exata mantida por compatibilidade (`3914`) substituída na Fase 3 por pré-busca da última observação por grupo sem `observed_at` na chave (`3915`) — corrige duplicidade de histórico quando o preço não muda entre execuções em dias diferentes. Regra corrigida: histórico só é gravado por mudança real de preço; bug de contagem duplicada em `observationsResolved` identificado e corrigido. Robustez de credencial corrigida via `resolveEntryDecision()`: `--fixture-check` sempre offline explícito; fora dele, qualquer credencial ausente encerra com código diferente de zero, sem rede prévia, mensagem sanitizada. `tsc --strict` limpo, 151/151 asserções offline. Piloto real aprovado: run `8e4367ad-f95d-41b6-bbbe-449bd7fb635f`, `COMPLETED`, 3 chamadas HTTP, 13 operações Supabase, 14,87s (contra ~156s do P14.2, ~10x mais rápido), 152 observações escritas (todas com mudança real de preço, zero redundante), `BASE4` inalterado (127 `CONFIRMED`/3 `PENDING`), zero run residual. P14.3 formalmente encerrado; P14 como um todo permanece em andamento — próximo incremento recomendado: expandir descoberta+persistência em lotes para todos os Sets restantes. Ver `05f-pricing.md`, revisão `1.45`. |
