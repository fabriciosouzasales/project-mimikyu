# Roadmap

| Campo | Valor |
|--------|-------|
| **Documento** | Roadmap |
| **Arquivo** | `docs/ROADMAP.md` |
| **Versão** | 1.62 |
| **Status** | Aprovado |
| **Objetivo** | Consolidar, em uma única fonte de verdade, a trajetória macro do Project Mimikyu — o que já foi concluído, o que está em andamento e o que é direção futura provável, mas ainda não comprometida. |
| **Escopo** | Marcos de alto nível (Fases/Sub-Fases/Blocos). Não substitui `docs/README.md` (estado atual detalhado), `05-modelo-de-dados.md` (execução física) nem `06-pipeline-importacao.md` (estratégia de importação). |
| **Dependências** | `docs/README.md`, `05-modelo-de-dados.md` |
| **Documentos Relacionados** | `06-pipeline-importacao.md`, `adr/ADR-013-collection-item-identity-model.md`, `adr/ADR-014-collection-and-collection-entry-model.md`, `adr/ADR-018-single-function-import-pipeline.md`, `adr/ADR-019-web-application-as-primary-interface.md` |

---

# Purpose

Este documento existe porque, ao longo de dezenas de ciclos de documentação, surgiram múltiplas formas paralelas e não reconciliadas de descrever a trajetória do projeto (`B2.x`/`B3.x`, `FASE 1-6`, `FASE 1-4`, `Fase 1-7`), nenhuma delas formalizada como fonte única de verdade. Este documento resolve essa lacuna — mas **apenas para o que já é uma decisão real de Fabrício**. Onde a direção futura ainda não foi comprometida, este documento diz isso explicitamente, em vez de adotar silenciosamente qualquer uma das propostas anteriores.

Criado em 2026-07-24, junto com a reativação da manutenção de `adr/ADR-INDEX.md` e `standards/STD-INDEX.md` — Fabrício declarou nesta data que a documentação do passado do projeto está encerrada e que os artefatos de governança (índices, roadmap) devem passar a ser mantidos ativamente a partir de agora.

---

# Now — Em Andamento

**Atualização (2026-08-16) — nenhuma frente de implementação ativa no momento; sessão corrente é de auditoria e reconciliação documental.** As três frentes que estavam efetivamente em andamento desde a última atualização desta seção (Card Variant, Auth Experience, Design System/Frontend) chegaram, cada uma, a um ponto de pausa ou fechamento explícito — ver "Concluído", abaixo, para o resumo por frente. **Nesta mesma auditoria (2026-08-16), Fabrício formalizou a sequência estratégica das próximas frentes** — ver "Next", abaixo — encerrando a indefinição de prioridade que constava nesta seção até a revisão `1.48`.

**Bloco Card Variant — fundação encerrada (decisão explícita de Fabrício, 2026-08-16).** O escopo hoje implementado — governança formalizada (`ADR-028`, revisões `1.0`–`1.4`), taxonomia de `card_variant_type` com CRUD administrativo completo (Incrementos 1–3, tela `/catalogo/tipos-variacao`), pipeline "Importar Variantes" de ponta a ponta (staging, Edge Function `import-card-variants`, decisão/confirmação em lote, resolução administrativa de mapeamento externo, painel de conclusão), indicador de Card Variants na galeria de Cartas (CV-02), e dois incidentes reais corrigidos (SV10 — job preso por timeout; SV8.5 — erro genérico em duplicidade de combinação) — está **formalmente declarado como a base necessária e suficiente para as próximas frentes (Pricing e Collection)**, encerrando a indefinição registrada até a revisão `1.48` ("pausado... sem critérios de saída formalmente declarados"). Isso **não** significa que toda evolução futura de Card Variant esteja concluída: **Vintage/Promo Variant Modeling** (ver "Débitos Técnicos Registrados") continua como evolução futura/backlog, explicitamente **não bloqueante** para Pricing nem para Collection. Ver `docs/log.md`, entradas de 2026-08-14/15/16, e `ADR-028` para o detalhamento normativo.

**Auth Experience (Customer-Facing Product Experience) — Login V1 aprovado e congelado, propagado às demais telas de autenticação (2026-08-16).** Nova frente aberta e concluída na mesma janela: auditoria da autenticação existente, fundação visual própria (`AuthHeroShell`, direção "cartas reais"), Login reconstruído e aprovado por Fabrício ("Login V1 aprovado e congelado — fechamento da frente", `docs/log.md`), e a mesma fundação propagada a Cadastro/Recuperar Senha/Atualizar Senha. Sem ADR próprio — decisão majoritariamente visual/de produto, não arquitetural (ver nota em "Concluído", abaixo, sobre o limiar para abrir ADR).

**Design System / UI Interna — arquitetura cromática validada e promovida a baseline de toda a aplicação autenticada (2026-08-16).** A partir da mesma fundação visual do Login, três rodadas de prova visual isolada em Catálogo Editorial > Visão Geral ("onyx-preview") validaram uma nova arquitetura de tema (workspace off-white/grafite que inverte por tema; navegação como âncora fixa sempre escura; dourado como accent, nunca cor dominante) — promovida a baseline de todas as páginas internas, com harmonização de formulários (tokens `--control-*`, `Select` compartilhado — ver STD-004) e demais componentes. Documentado normativamente em `docs/standards/STD-004-frontend-standards.md` (Seções 2/3/5/6, versão `1.3`) — não em ADR próprio, por não representar uma decisão arquitetural de dado/backend, mesmo critério do parágrafo acima.

**Módulo Gerencial — duas evoluções pontuais sobre o módulo já encerrado (2026-08-16).** Log de Atualizações: eventos de Governança de Variantes humanizados (rótulos de Entidade/Ação, antes exibindo nomes técnicos crus). Histórico de Importações: pipeline VARIANTES (Edge Function `import-card-variants`) passa a ser exibido lado a lado com Cartas/Imagens no histórico unificado. Nenhuma das duas reabre o encerramento formal do módulo (2026-08-09) — são evoluções aditivas sobre uma base já fechada.

**Segurança/Performance — RLS Admin Performance Hardening e Performance Foundation v1 (2026-08-14).** `is_admin()` convertida de `VOLATILE` para `STABLE`, com as 24 policies do Catálogo Editorial reescritas para `(select is_admin())` (Query `2134`, CONFIRMADO EXECUTADO E VALIDADO em segurança e performance); mais quatro incrementos de eliminação de over-fetch/leitura redundante em `/catalogo`, `/catalogo/cartas` e `/catalogo/card-sets`. Consolidado como "Performance Foundation v1", com backlog explícito para v2 registrado em `docs/log.md`. Padrões permanentes derivados formalizados em `docs/standards/STD-001-database-standards.md` (Seção "Row Level Security (RLS)", versão `1.19`) nesta própria auditoria — ver "Débitos Técnicos Registrados" para o motivo do atraso entre implementação e normatização. Least privilege ampliado em 2026-08-15 (Query `2147` — `TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` revogados de `anon`/`authenticated` nas 24 tabelas do Catálogo Editorial).

**Pricing / Market Data — fundação física concluída nos Incrementos P1–P6 (`ADR-029`/`05f-pricing.md`, 2026-08-16); domínio ainda não operacional.** Depois do discovery e da modelagem conceitual/lógica, os Incrementos P1–P6 implementaram fisicamente as **dez de dez entidades do domínio** (`pricing_source` até `pricing_sync_run_call`, incluindo `pricing_observation` no Incremento P6) — quarto domínio independente de Catálogo/Ownership/Analytics, printing/condição/idioma/moeda/mercado como dimensões nunca colapsadas. **Todas as dez entidades estão criadas no Supabase e vazias** — fundação física completa, mas Pricing **ainda não é operacional**: nenhuma fonte cadastrada, nenhuma condição semeada, nenhum mapping real, nenhum produto real, nenhum preço real, nenhuma taxa real cadastrada. **Incremento P7 — Homologação Condicionada da JustTCG executado (2026-08-17)**: prova técnica confirmou 19/19 cartas e 7/7 Card Sets encontrados, preços em USD/printings/condições/histórico disponíveis, zero falha técnica, 15 requisições, nenhum segredo persistido; `JUSTTCG` cadastrada em `pricing_source` (Query `3700`, `database/migrations/3700_seed_pricing_source_justtcg.sql`), homologada para piloto técnico de produto/printing/condição/preço USD/histórico (Decisão A aprovada) — não homologada para determinar PT-BR nem como preço do mercado brasileiro (Decisão B inconclusiva, idioma das 18 consultas novas ficou `UNDETERMINED`), `is_active = FALSE` (sem consumidor operacional hoje; plano comercial compatível ainda não contratado). "Valor Brasil" depende exclusivamente de evidência na própria `pricing_observation` — exige `market_scope = 'BRAZIL' AND market_evidence_confirmed = TRUE` na observação; `pricing_source.default_market_scope` é apenas um default informativo declarado pela fonte e nunca autoriza, sozinho, Valor Brasil. Ver `docs/adr/ADR-029-pricing-domain-model.md`, seção "Incremento P7", e `docs/05f-pricing.md`, mesma seção.

**Incrementos P8–P12 executados na sequência (2026-08-17→18), completando o arco de Pricing até a exibição ao usuário final.** P8 — primeiro conector real JustTCG → Pricing (`scripts/sync-justtcg-pricing.ts`), piloto controlado restrito a `ME1`/`BASE1`. P9 — ingestão PTAX (`scripts/sync-ptax-fx-rate.ts`), regra cambial definida (venda PTAX), piloto real confirmado no Supabase (6 taxas gravadas). P10 — projeção de leitura `pricing_observation_brl_equivalent` (equivalente em BRL em tempo real, sem armazenar). P11 — contrato seguro de leitura por carta (`get_card_pricing_snapshot`, qualquer usuário autenticado, sem exigir perfil administrativo). **P12 — Exibição de Preços no Frontend, formalmente encerrado (2026-08-18)**: pill de preço em BRL nos três grids (Coleções, Catálogo de Cartas, Pesquisa), popover de detalhe sob demanda, busca em lote sem N+1 (`get_cards_pricing_summary`, Query `3903`), hierarquia de printing Normal → Holofoil → Reverse Holofoil corrigindo um bug real de ausência de fallback (Query `3904`, achado pelo teste transacional "Reverse-only"). Validação local completa (desktop/mobile, claro/escuro, toque, teclado/foco/`Escape`, formatação BRL/USD, `build` de produção) aprovada por Fabrício; commits `6324b66`/`0dad413` sincronizados com `origin/main`. **A visualização de preços nunca chama a JustTCG** — lê exclusivamente dados já sincronizados; sincronização (P8/P9) e consulta (P11/P12) permanecem arquiteturalmente desacopladas. `JUSTTCG.is_active` permanece `FALSE` — domínio Pricing ainda não operacional em produção, pendente de plano comercial contratado. Detalhamento completo em `docs/05f-pricing.md`, seção "Encerramento Formal do Incremento P12", e `docs/adr/ADR-029-pricing-domain-model.md`, revisão `1.23`. **Próximo passo real**: P13 — Orquestração Programada de Pricing (ver "Next", abaixo), ou iniciar Collection — decisão de Fabrício.

**Sequência estratégica aprovada por Fabrício (2026-08-16):** Card Variant (concluído) → Pricing com fundação física concluída (dez de dez entidades, ainda não operacional) → Collection como próxima frente comprometida → Analytics/Valuation (consumirá Pricing + Collection). O início real de Collection permanece dependente de autorização explícita de Fabrício, não automático só pela conclusão física de Pricing. Ver "Next", abaixo, para o detalhamento por frente.

**Pesquisa Global de Cartas — implementada de ponta a ponta e formalmente encerrada (2026-08-17, `ADR-030`).** Frente cross-cutting, independente da sequência estratégica acima (não faz parte de Pricing/Collection/Analytics): combobox de pesquisa disponível no header a qualquer usuário autenticado (não só administrador) + página `/pesquisa` com filtros por Card Set/Categoria/Raridade, todos via URL — **sem filtro de Jogo nesta versão**, decisão de escopo explícita (corrigida no mesmo dia, ver "Débitos Técnicos Registrados" e `ADR-030` revisão `1.1`). Projeção segura nova (`public.search_cards()`/`public.search_card_filter_options()`, módulo `4000`–`4999` em `STD-001`) — não amplia o acesso direto às tabelas do Catálogo Editorial, `ADR-022` permanece intacto. Validação de segurança real (`SET ROLE anon`/`authenticated`) e de performance (`EXPLAIN` mapeado individualmente aos 10 padrões de consulta, incluindo 22.104 linhas sintéticas, transacional com `ROLLBACK`) incluídas no próprio incremento. Quatro rodadas corretivas no mesmo dia (remoção do filtro de Jogo; busca por "número/total"; preview de carta estruturalmente compartilhado com `Cartas`; modo imersivo de pesquisa mobile reescrito, referência funcional pkmn.gg). **Validação local de UI (teclado/ARIA/tema/mobile) e `npm run lint`/`build` confirmadas e aprovadas por Fabrício (2026-08-17); commit e push já realizados** — incremento formalmente encerrado. Descobriu, como efeito colateral da introspecção obrigatória, uma divergência pré-existente de rastreabilidade em `database/migrations/`, que permanece registrada como débito não bloqueante, não reconciliada (ver "Débitos Técnicos Registrados").

**Decisão real ainda em aberto, dependente de Fabrício** (não resolvida nesta auditoria, por não ser um julgamento que a documentação possa fazer sozinha): se e quando retomar Card Variant (Vintage/Promo Variant Modeling) — item de backlog não bloqueante, sem cronograma comprometido, que pode ser intercalado a qualquer momento na sequência acima sem alterá-la.

---

# Catálogo Editorial — Frentes de Encerramento

"Catálogo Editorial" é usado neste roadmap e no restante da documentação para mais de um escopo — modelo de dados, pipeline de imagens, leitura, escrita administrativa, ingestão administrativa. Esta seção formaliza as cinco frentes que compõem o módulo e o critério para declará-lo encerrado: **as cinco precisam estar concluídas**, não apenas a mais recente em andamento.

**Encerramento formal declarado em 2026-08-08, por decisão explícita de Fabrício, após auditoria documental dedicada de cada frente** (ver `docs/log.md`, entradas `docs` de 2026-08-08). Duas correções de premissa vieram dessa auditoria, registradas aqui para rastreabilidade: (1) a Frente B não tinha pendência de dado — a pendência histórica era a *existência do canal* de importação manual de imagens (agora `ADR-026`, implementado e validado), não a falta de imagens de `MEP` em si; tratar `MEP` como bloqueador foi um engano de leitura corrigido nesta revisão; (2) a Frente C não tem nenhum critério de aceite formal em aberto para Jogos/Expansões/Card Sets/Cartas — o único item concreto encontrado (detalhe completo de `/catalogo/card-sets/[code]`) é evolução futura, não bloqueio, e foi registrado como débito não bloqueante (ver "Débitos Técnicos Registrados", abaixo).

| Frente | Escopo | Estado |
|--------|--------|--------|
| A. Modelo de Dados | Entidades estruturais do catálogo (`game` até `asset_import_failure`) | **Concluído** |
| B. Pipeline de Assets | Importação automatizada (`import-card-assets`, `ADR-018`) + canal de importação manual (`ADR-026`) | **Concluído** — canal manual implementado e validado em produção (Coleção `SVE`, 2026-08-08); nenhuma pendência de dado bloqueia a frente |
| C. Interface de Consulta | Leitura administrativa do catálogo (`ADR-022`) e telas de Jogos/Expansões/Card Sets/Cartas/Visão Geral | **Concluído** — auditoria dedicada (2026-08-08) não encontrou nenhum critério de aceite formal em aberto; hub operacional de `/catalogo/card-sets/[code]` implementado no mesmo dia (revisão `1.23`), só upload de logo segue como débito não bloqueante |
| D. Escrita Administrativa | `ADR-023` — cadastro/edição/exclusão pela interface, por ciclos verticais | **Concluído (2026-08-07)** — `Game`/`Expansion`/`Card Set`/`Card` todos com ciclo vertical completo (ver "Concluído", abaixo) |
| E. Ingestão Administrativa | `ADR-024` — captura/confirmação de novas Cards a partir de fontes externas | **Concluído (2026-08-08)** — Ciclo 1/Ciclo 2 (TCGdex) validados de ponta a ponta (2026-08-07); Ciclos 3/4 (PDF) formalmente encerrados por decisão de Fabrício, não ficam como pendência |

**As cinco frentes estão concluídas — o Catálogo Editorial está formalmente encerrado.** Este encerramento permanece válido e inalterado. A partir de 2026-08-09, ele deixou de ser suficiente sozinho para iniciar a Sub-Fase 2 — Coleções, pela exigência de Card Variant como fundação intermediária; a partir de 2026-08-16, com Card Variant encerrado e a sequência estratégica formalizada, Coleções passa a depender também da fundação de Pricing/Market Data — ver "Next", abaixo.

---

# Next — Comprometido, Ainda Não Iniciado

**Pricing / Market Data — fundação física concluída (2026-08-16).** Sequência aprovada por Fabrício em 2026-08-16 (ver "Now", acima): com Card Variant encerrado como fundação, Pricing/Market Data foi a próxima frente trabalhada, antes de Collection. **Incremento P1 — Fundação Física executado (2026-08-16)**: `pricing_source`, `card_condition`, `pricing_condition_mapping` criadas no Supabase — ver `docs/development/HANDOFF-2026-08-16.md`, seção 14. **Incremento P2 — Correspondência Externa executado (2026-08-16, mesmo dia)**: `pricing_set_mapping`/`pricing_card_mapping` criadas, ambas vazias — seção 15. **Incremento P3 — Auditoria Operacional de Sincronização executado (2026-08-16, mesmo dia)**: `pricing_sync_run`/`pricing_sync_run_call` criadas, ambas vazias, validadas por performance real — seção 16. **Incremento P4 — Produto Externo de Pricing executado (2026-08-16, mesmo dia)**: `pricing_product` criada, vazia, validada por 19 itens funcionais e 4 planos de execução reais — seção 17. **Incremento P5 — Série Histórica de Taxas de Câmbio executado (2026-08-16, mesmo dia)**: `pricing_fx_rate` criada, vazia, validada por 22 itens funcionais e 3 planos de execução reais — seção 18. **Incremento P6 — Observação Histórica de Preço executado (2026-08-16, mesmo dia)**: `pricing_observation` criada, vazia, com identidade market-aware corrigida (`UNIQUE NULLS NOT DISTINCT`) e validada por 32 itens funcionais e 5 planos de execução reais sobre 80.000 linhas sintéticas — ver `docs/development/HANDOFF-2026-08-16.md`, seção 19. **Fundação física completa — dez de dez entidades `CONFIRMADO EXECUTADO`.** **Incremento P7 — Homologação Condicionada da JustTCG executado (2026-08-17)**: `JUSTTCG` cadastrada em `pricing_source` (Query `3700`), homologada para piloto técnico (produto/printing/condição/preço USD/histórico), não homologada para PT-BR/"Valor Brasil", `is_active = FALSE` até plano comercial contratado — ver `docs/adr/ADR-029-pricing-domain-model.md`, seção "Incremento P7". Pricing ainda não é operacional: nenhuma condição canônica semeada, nenhum mapping/produto/preço/taxa real cadastrado, nenhuma integração/sincronização/ingestão PTAX/frontend implementados. Nenhum escopo técnico adicional deve ser tratado como decidido além do que já está formalizado em `ADR-029`.

**Atualização (2026-08-18) — Incrementos P8–P12 executados, ver "Now", acima, para o detalhamento completo.** Conector JustTCG (P8), ingestão PTAX (P9), projeção cambial (P10), contrato de leitura por carta (P11) e exibição de preços nos três grids do frontend (P12) já estão implementados e, no caso do P12, formalmente encerrado — Pricing já é consumível pelo usuário final quando há dado elegível, mesmo com `JUSTTCG.is_active = FALSE`. Domínio segue não operacional em produção só por falta de plano comercial contratado para a fonte.

**P13 — Orquestração Programada de Pricing (recomendado, ainda não comprometido).** Cobriria a automação do que hoje é manual: agendamento da ingestão PTAX (P9, hoje só `scripts/sync-ptax-fx-rate.ts` acionado à mão por Fabrício) e, quando `JUSTTCG.is_active` for reativada por decisão comercial, o agendamento do conector de sincronização (P8, hoje só `scripts/sync-justtcg-pricing.ts`). Sem escopo técnico, mecanismo de agendamento (cron/Edge Function/scheduler externo) ou cronograma definidos nesta rodada — recomendação registrada em `docs/05f-pricing.md`, seção "Encerramento Formal do Incremento P12", e em `docs/adr/ADR-029-pricing-domain-model.md`, revisão `1.23`, aguardando priorização explícita de Fabrício antes de iniciar.

**Próxima frente comprometida, alternativa a P13**: **Collection**, agora que a fundação de Pricing está concluída — ver abaixo.

**Sub-Fase 2 — Coleções.**

O domínio do colecionador (Collection, Collection Entry, Collection Item — ver `04-domain-model.md` e `ADR-013`/`ADR-014`) já está conceitualmente modelado e aprovado, mas ainda não tem modelo físico (`05-modelo-de-dados.md`) nem tabelas criadas no Supabase. Fabrício confirmou diretamente (revisão `1.40` de `docs/README.md`) que este é um dos próximos módulos reais do projeto, distinto do Catálogo Editorial: exemplares físicos possuídos pelo usuário, objetivos de coleção, e a relação entre ambos.

**Critério de entrada (atualizado em 2026-08-16, sequência formalizada por Fabrício) — fundação física de Pricing agora concluída (Incremento P6, mesmo dia).** As cinco frentes do Catálogo Editorial seguem concluídas (ver "Catálogo Editorial — Frentes de Encerramento", acima), o Módulo Gerencial (Trilha 4) também está concluído, o bloco Card Variant está formalmente encerrado como fundação, e a fundação física de Pricing/Market Data está completa (dez de dez entidades `CONFIRMADO EXECUTADO`, ver "Next", acima) — sequenciamento macro: **Catálogo Editorial → Card Variant → Pricing (fundação) → Coleções**. O início real de Coleções permanece uma decisão de Fabrício, não automática só pela conclusão física de Pricing — Coleções continua em "Next", comprometida mas não iniciada, até esse início ser explicitamente autorizado.

Nota de escopo já registrada em `ADR-014`: Rule-Based Membership (inclusão automática de itens em uma Collection por regra, em vez de curadoria manual) não faz parte do modelo físico inicial desta Sub-Fase — deferida para um ciclo futuro dentro do próprio escopo de Coleções, quando houver necessidade concreta.

---

# Later — Direção Futura Provável, Não Comprometida

Os itens abaixo refletem temas que já apareceram em mais de uma proposta de roadmap, documento normativo, ou ADR ao longo do projeto, mas **nenhum foi formalmente comprometido em sequência, escopo ou modelo de dados**. Estão listados aqui para dar visibilidade de direção, não como plano de execução — nenhum destes vira compromisso de execução só por aparecer nesta lista.

- **Aquisição e movimentação** — registro de compras, trocas e vendas de Cards/Card Variants pelo colecionador.
- **Analytics / Valuation** — relatórios e análises sobre a Collection do usuário, consumindo Pricing + Collection depois de ambos existirem (ver sequência aprovada em "Now"/"Next", acima). Sem escopo, modelo de dados ou cronograma definidos — só entra em "Next" quando Pricing e Collection já estiverem implementados.
- **Módulos de Decks/Trocas/Marketplace** — citados como fronteiras de domínio ainda não decididas em `06-pipeline-importacao.md` ("Em Aberto"); nenhum escopo, modelo de dados ou sequência definidos.
- **Estratégia multi-provider de importação** — decisão de escopo (adotar mais de uma fonte externa além da TCGdex) confirmada em 2026-08-07, mas todas as sub-decisões técnicas (qual(is) provider(s), critério de prioridade entre fontes, modelo de conflito de dados) seguem em aberto. Ver `06-pipeline-importacao.md`, seção "Em Aberto".
- **Modelagem física de Card Translation** — campo/mecanismo de "Extracted Data" e critérios de priorização de tradução ainda em aberto. Ver `07-catalogo-editorial.md`, seção "Em Aberto".

Qualquer um destes itens só entra em "Next" quando Fabrício o confirmar explicitamente, com escopo próprio — seguindo a mesma disciplina de não resolver unilateralmente decisões de direção que já se aplica ao restante deste projeto.

---

# Débitos Técnicos Registrados (Sem Cronograma)

Itens reais, já identificados em auditoria ou por decisão explícita de adiamento, que **não bloqueiam** nenhuma frente ou fase declarada concluída neste documento, e que **não têm cronograma comprometido**. Registrados aqui só para rastreabilidade — nenhum destes é compromisso de execução.

- **Upload de logo em `/catalogo/card-sets/[code]`** — único item restante da evolução futura registrada na auditoria de 2026-08-08. O restante do escopo então identificado (galeria de cartas, histórico de importações do Set) foi implementado no mesmo dia como hub operacional V1, aprovado por Fabrício (ver `docs/05e-catalogo-editorial.md`, seção "Hub de Card Set"); só falta conectar `admin_set_card_set_logo()` ao frontend dessa rota — a função e a infraestrutura de Storage já existem desde 2026-07-26 (`273`/`275`/`276`).
- **Achados de precisão registrados na reconciliação de `docs/05b-cartas-e-raridade.md` (2026-08-16, ver "Achados desta auditoria" no próprio documento):** (1) pequena divergência entre a validação de `code` em `admin_create_card_variant_type()` (aceita dígito inicial) e a `CHECK` física da tabela (exige letra inicial) — nunca observada na prática, nenhum dos 39 tipos reais é afetado; (2) `GRANT`s de `TRUNCATE`/`TRIGGER`/`REFERENCES` não revogados de `anon`/`authenticated` na view `catalog_card_set_variant_coverage`, ao contrário das tabelas físicas (Query `2147`) — sem risco de segurança real, mas inconsistente com o padrão de least privilege já aplicado. Nenhum dos dois bloqueia qualquer frente; nenhum tem cronograma comprometido.
- **Fase 4 do Incremento 2 de Identidade e Acesso** (correção administrativa de `username`, `ADR-020`) — deferida por decisão de Fabrício desde 2026-07-26 (ver "Concluído", abaixo), hoje só mencionada dentro da narrativa de "Concluído", sem rastreamento formal de retomada.
- **Auditoria retroativa de `REVOKE ... FROM PUBLIC/anon`** em funções `admin_*` mais antigas — gap de segurança real descoberto durante o fechamento do subciclo `Card` de `ADR-023` (2026-08-07), corrigido nas três funções novas daquele ciclo; as funções `admin_*` anteriores a essa descoberta não foram auditadas retroativamente. Rastreada informalmente como task `#368`.
- **Verificação de direitos/termos de uso de imagens** de Cards importadas — item não resolvido, registrado em `06-pipeline-importacao.md`, "Em Aberto".
- **Auditoria consolidada de `GRANT`s para `service_role`** — adiada, registrada em `06-pipeline-importacao.md`, "Em Aberto".
- **Melhoria de idempotência do pipeline de importação de imagens** — adiada, registrada em `06-pipeline-importacao.md`, "Em Aberto".
- **Buckets físicos `card-back`/`artwork`** — ainda não criados no Storage, registrados em `06-pipeline-importacao.md`, "Em Aberto".
- **Validação prévia de integração externa antes de `asset_import_run`** — proposta, não implementada, registrada em `06-pipeline-importacao.md`, "Em Aberto".
- **Edge Function orquestradora especulativa `catalog-import`** — ideia registrada, sem compromisso, em `06-pipeline-importacao.md`, "Em Aberto".
- **Critérios de priorização de campos e mecanismo de extração de "Extracted Data"** — em aberto, registrado em `07-catalogo-editorial.md`, "Em Aberto" (distinto da modelagem de Card Translation, listada em "Later", acima).
- **`.claude/settings.local.json` sem bloco de `permissions` (allow/deny list de ferramentas)** — achado de auditoria parcial via AgentShield (`npx ecc-agentshield scan`, 2026-08-09, escopo restrito a `.claude/`), severidade `medium`. Hoje o controle de escopo de ação do agente depende inteiramente da disciplina do `CLAUDE.md` e da aprovação manual na sessão interativa, não de uma trava técnica declarada em `settings.json`. Não bloqueia nada em andamento; sem cronograma.
- **Ausência de hooks `PreToolUse` de segurança** — mesma auditoria AgentShield (2026-08-09), severidade `medium`. O projeto só tem hooks em `PostToolUse`/`Stop` (detector de design do skill Impeccable); nenhum hook intercepta uma operação sensível antes de ela rodar (ex.: `git commit`/`git push`, execução de SQL destrutivo) — hoje essas regras existem só como instrução textual no `CLAUDE.md`. Candidato a uma futura ADR/Standard se e quando priorizado; sem cronograma.
- **Vintage/Promo Variant Modeling** — frente de design ainda não iniciada, identificada durante o teste real de Importar Variantes contra `BASEP` (Wizards Black Star Promos, 2026-08-15): das 74 combinações `type`/`foil`/`subtype`/`stamp[]` analisadas, 30 caíram em `NEEDS_REVIEW` (sem mapeamento) e, examinadas uma a uma, se dividem em pelo menos três eixos conceituais distintos hoje colapsados no mesmo `card_variant_type` — acabamento genuíno (ex.: `holo+cosmos`, que já tem tipo canônico `COSMOS_HOLO`, só falta a linha de mapeamento), estampa promocional/proveniência de distribuição (`1st-movie`, `pokemon-center-ny`, `pikachu-tail`, `jr-stamp-rally` etc. — cada uma usada por 1-6 Cards na era toda, sem repetição prevista em eras futuras) e erro/errata de impressão (`aoki-error`, `missing-hp`, `1st-edition-error`). Forçar essas 30 linhas para dentro do modelo atual de `card_variant_type` (um `code` opaco por combinação) geraria dezenas de tipos hiperespecíficos, não sustentáveis como taxonomia canônica em escala. O job de `BASEP` (`catalog_variant_import_job`, status `STAGED`) permanece intocado, não confirmado, como primeiro caso real de referência para esta frente quando ela for priorizada. Vintage/promo já estava fora do escopo de `ADR-028` — este achado confirma que a exclusão era correta, não apenas um adiamento de conveniência. Sem escopo, modelo de dados ou cronograma definidos; não inicia sem confirmação explícita de Fabrício, mesma disciplina do restante desta seção.
- **Divergência de rastreabilidade entre `database/migrations/` e o histórico real do Supabase — parcialmente reconciliada.** Descoberta durante a introspecção obrigatória do Incremento "Pesquisa Global de Cartas" (2026-08-17, ver `ADR-030`), não causada por ele: a faixa `3000`–`3090` (Pricing P1–P6) e a migration `2050` estavam confirmadamente aplicadas no Supabase (`list_migrations`) sem arquivo `.sql` correspondente no repositório; na direção oposta, `2098`–`2153` (~18 arquivos) existem no repositório sem entrada no histórico interno do Supabase (`supabase_migrations.schema_migrations`) — o schema real bate com o que esses arquivos criam, sugerindo aplicação por execução direta fora do mecanismo que grava histórico. **Reconciliação retroativa da faixa `3000`–`3090` concluída em 2026-08-17 (ciclo posterior, pedido explícito de Fabrício)**: os 22 arquivos ausentes foram extraídos verbatim de `supabase_migrations.schema_migrations` (somente leitura, nenhuma migration reexecutada) e versionados em `database/migrations/`, cada um verificado por `md5()` contra o statement armazenado (22/22 correspondências exatas). **Ainda não reconciliadas**: a migration `2050` e a faixa `2098`–`2153`. Sem escopo, responsável ou cronograma de reconciliação definidos para o restante.
- **Watchdog automático de jobs travados em `catalog_variant_import_job`** — registrado durante a correção do incidente SV10 (2026-08-15). A causa raiz real (`fetch()` sem timeout em `resolveSetSerieName()`/`listSetCardFiles()`/`fetchCardFileSource()` da Edge Function `import-card-variants`, com a plataforma matando a invocação por estouro do teto de execução antes de o `catch()`/`failVariantJob()` rodar) foi corrigida (`AbortController`, 15s por chamada externa). Mas nenhum mecanismo independente detecta, de fora da própria invocação, um job que permaneça preso em status não-terminal por tempo anormal — hoje a única forma de destravar um job preso é uma correção manual pontual (ver Query `2149`, caso real do job `bff18ea3-1b1f-4794-ab87-f67eff2ade1a`). Por decisão explícita de Fabrício, não implementado nesta rodada — apenas registrado como débito. Sem escopo, modelo ou cronograma definidos.

---

# Concluído

- **Fase 1 — Arquitetura Conceitual.** Princípios arquiteturais, delimitação do domínio, estrutura do catálogo editorial, modelo do universo do colecionador, separação Set/Collection, estratégia de evolução incremental.
- **Sub-Fase 1 — Catálogo Editorial, Bloco A (Modelo de Dados).** Todas as entidades estruturais criadas e homologadas para as 7 Card Sets: `game`, `expansion`, `card_set`, `card` (`927`), `card_category`, `rarity` (10), `language`, `card_variant_type`, `card_variant` (`1.653`), `card_asset_type`/`card_asset`, `storage_bucket`, `asset_source`, `card_external_reference`, `card_set_external_reference`, `asset_import_run`, `asset_import_failure`.
- **Sub-Fase 1 — Catálogo Editorial, Bloco B (Pipeline de Importação), para as 5 coleções originais.** `859` Cards processadas, `859` referências externas, `1.718` Card Assets, `en`+`pt-BR`, `0` falhas.
- **Início do front-end (aplicação web).** Fabrício decidiu (2026-07-25, `ADR-019-web-application-as-primary-interface.md`) começar a construção da interface do produto sem esperar a modelagem de Coleções — React/Next.js adotado como interface principal, Power Apps/SharePoint/Power BI descartados da arquitetura-alvo. App shell e autenticação básica (Supabase Auth) no repositório (`web/`).
- **Identidade e Acesso — Incremento 1 ("Meu Perfil").** `user_profile`, separado de `auth.users` (`ADR-020`), inaugurando o Modelo Modular de Numeração do STD-001 (milhar `1000`–`1999`). Tabela, triggers, RLS, `handle_new_user()`, `username_available()`, bucket de avatares e frontend (`/perfil`) confirmados e validados ponta a ponta em produção.
- **Identidade e Acesso — Incremento 2 ("Administração de Usuários"), Fases 1–3.** Papel administrativo (`admin_user`, sem RLS direta) e auditoria (`admin_action_log`) — `ADR-021`. `is_admin()`, `admin_list_users()`, `admin_grant_admin()`/`admin_revoke_admin()` e interface (`/usuarios`) confirmados e validados em produção. Fase 4 (correção administrativa de `username`, `ADR-020`) deliberadamente fora deste incremento, por decisão de Fabrício.
- **Catálogo Editorial — Frente B (Pipeline de Assets), canal de importação manual (`ADR-026`).** Mecanismo de importação manual de imagens (`source_code = 'MANUAL'`, rastreável) formalizado — existia em produção desde 2026-07-24 sem ADR correspondente. `MEE` completa nos dois idiomas via este canal desde 2026-07-25. Emenda "segundo ponto de entrada via UI" (2026-08-07/08) adiciona `Fonte = Manual` em `/catalogo/importar-imagens` — backend (Queries `2119`–`2122`), frontend e validação funcional em produção real (Coleção `SVE`, 6 arquivos) todos CONFIRMADOS. Canal genérico e reutilizável para qualquer Coleção — não é mais um item de dado específico de `MEP`. Ver `adr/ADR-026-manual-local-file-asset-import-channel.md`.
- **Catálogo Editorial — Frente C (Interface de Consulta), fundação de autorização/logo e telas de leitura administrativa.** Descoberto que as 17 tabelas do módulo tinham RLS habilitado sem nenhuma política — formalizado como admin-only em `ADR-022-catalog-editorial-admin-only-access.md`. Leitura liberada via `catalog_admin_select` nas tabelas usadas pelas telas; escrita da logo restrita a `admin_set_card_set_logo()`; bucket privado `card-set-logo`. Queries `273`–`277` CONFIRMADAS EXECUTADAS. Guarda de servidor compartilhada (`requireCatalogoAdmin()`) em todas as rotas do módulo; telas de Jogos, Expansões, Card Sets, Cartas e Visão Geral implementadas e validadas visualmente em produção real ao longo da sessão de 2026-08-08. Auditoria documental dedicada (2026-08-08) não encontrou nenhum critério de aceite formal em aberto para nenhuma das quatro entidades; o único achado (detalhe completo de `/catalogo/card-sets/[code]`) foi confirmado como evolução futura por design original, registrada como débito não bloqueante.
- **Catálogo Editorial — Frente D (Escrita Administrativa), completa (2026-08-07).** Infraestrutura comum (schema `internal`, `catalog_admin_action_log`, `card.is_active`, `internal.write_card()`) e ciclo vertical completo (create+update+delete) para as quatro entidades do escopo: `Game`, `Expansion`, `Card Set` (com logo e código editável condicional) e `Card` (update+create+deactivate+reactivate) — este último fechado no mesmo dia da emenda "Raridade: mapeamento self-service e revalidação" de `ADR-024`. Ver `05-modelo-de-dados.md` e `05e-catalogo-editorial.md` para o detalhamento completo por entidade.
- **Catálogo Editorial — Frente E (Ingestão Administrativa), completa (2026-08-08).** Ciclo 1 (infraestrutura comum de staging/confirmação, `catalog_import_job`/`catalog_import_row`) CONFIRMADO EXECUTADO E VALIDADO (2026-08-01). Ciclo 2 (fluxo vertical completo via TCGdex — Edge Function `import-catalog-cards`, telas `/catalogo/importar-cartas`/`/catalogo/importar-imagens`, integração automática cartas→imagens) implementado, em uso ativo em produção e CONFIRMADO EXECUTADO E VALIDADO de ponta a ponta (2026-08-07). Ciclos 3/4 (canal PDF) encerrados definitivamente por decisão explícita de Fabrício (2026-08-08) — não implementados: taxa de sucesso já suficiente do canal TCGdex, direção futura de multi-provider como caminho de maior confiabilidade, e canais manuais já existentes (`ADR-023`/`ADR-026`) cobrindo o caso residual. Ver `05e-catalogo-editorial.md` e `adr/ADR-024-catalog-card-ingestion-strategy.md`.
- **Módulo Gerencial (Trilha 4) — concluído, as quatro frentes (2026-08-09).** Camada de gestão e observabilidade administrativa construída sobre o Catálogo Editorial já concluído; plano recuperado em 2026-08-08 (até então disperso em `docs/log.md` e no cabeçalho de `ADR-027`). **Visão Geral** (`/catalogo`) — dashboard executivo sobre métricas canônicas (`catalog_card_set_metrics`/`catalog_card_set_image_coverage`, Queries `2123`/`2124`, `ADR-027`): quatro StatCards principais, painel de Cobertura por idioma, "Saúde do catálogo" e drill-down completo em todos os indicadores, com a semântica de "Pendências" corrigida (só o `catalog_import_job` mais recente por Coleção). **Histórico de Importações** (`/catalogo/importacoes`) — unifica `catalog_import_job` (Cartas) e `asset_import_run` (Imagens) num histórico único, com busca, filtro de chips por Pipeline e `ImportacoesTendencia` (gráficos de tendência semanal por pipeline). **Log de Atualizações** (`/catalogo/log-atualizacoes`) — primeira leitura de `catalog_admin_action_log` (Query `2010`), paginada/filtrada inteiramente server-side (`admin_list_catalog_action_log()`, Query `2127`), com 3 gráficos semanais por categoria (`admin_get_catalog_action_log_weekly_summary()`, Query `2128`). **Central de Relatórios** (`/catalogo/relatorios`) — 6 relatórios imprimíveis via `@media print`/`window.print()` nativo (primeiro uso real de impressão no projeto), 4 reaproveitando integralmente as views canônicas e 2 (Checklist por Coleção, Resumo da Coleção) mostrando uma única Coleção por vez; o Checklist virou a baseline visual dos outros 5 depois de várias rodadas de refinamento. Todas as quatro frentes aprovadas por Fabrício em 2026-08-09 — a última, Central de Relatórios, com "Considero pela UI o bloco concluído." Ver `05e-catalogo-editorial.md`, seções correspondentes, e as revisões `1.26`–`1.44` deste documento para o detalhamento rodada a rodada.
- **Pipeline de importação de imagens (`import-card-assets`) — suporte simultâneo a EN+PT-BR, resiliência a timeout/retry, e integração automática cartas→imagens.** Evolução de v2.6.0 (correção da máquina de estados) a v2.9.3 (retry por download individual), incluindo `language_id` na identidade de `card_external_reference`. Ver `05-modelo-de-dados.md`, revisões `1.30`–`1.44`, e `06-pipeline-importacao.md`.
- **Correções de mapeamento de raridade** (`ACE_SPEC_RARE`/`SHINY_RARE`/`SHINY_ULTRA_RARE` cadastradas; símbolo de `HYPER_RARE` corrigido de uma para três estrelas, `GOLD_TRIPLE_STAR`) — ver `05-modelo-de-dados.md`, revisões `1.45`–`1.50`.
- **Galerias de Expansões e Coleções reagrupadas** (Expansões por Jogo; Coleções por Expansão) — puramente de apresentação, sem mudança de modelo de dados.
- **Bloco Card Variant — fundação encerrada por decisão explícita de Fabrício (2026-08-16), como base necessária para Pricing e Collection.** Governança (`ADR-028`, revisões `1.0`–`1.4`); taxonomia de `card_variant_type` com CRUD administrativo (`/catalogo/tipos-variacao`, Incrementos 1–3); pipeline "Importar Variantes" completo (staging, Edge Function `import-card-variants`, decisão/confirmação em lote, resolução de mapeamento externo); indicador de Card Variants na galeria de Cartas (CV-02); incidentes SV10/SV8.5 corrigidos. Estado real (2026-08-16): 4.718 Card Variants, 39 tipos canônicos, 2.433 de 7.104 Cards cobertas. Evoluções futuras (Vintage/Promo Variant Modeling) permanecem como backlog não bloqueante. Ver "Now", acima, e `docs/05b-cartas-e-raridade.md` para o modelo físico consolidado.
- **Pesquisa Global de Cartas — implementada de ponta a ponta e formalmente encerrada (2026-08-17, `ADR-030`).** Combobox no header (qualquer usuário autenticado) + página `/pesquisa` (filtros de Card Set/Categoria/Raridade via URL, sem Jogo — decisão de escopo), sobre projeção segura nova `public.search_cards()`/`public.search_card_filter_options()` (`SECURITY DEFINER STABLE`, módulo `4000`–`4999`), sem ampliar o acesso direto ao Catálogo Editorial (`ADR-022` intacto). Quatro rodadas corretivas no mesmo dia: remoção do filtro de Jogo do contrato público; busca por número aceitando o formato "número/total" (ex. "125/094"); preview de carta em `/pesquisa` estruturalmente compartilhado com `/catalogo/cartas` (`CardImagePreview`/`CardPreviewOverlay`, `HoloCard`); modo imersivo de pesquisa mobile reescrito como painel de tela cheia substituindo o header (referência funcional pkmn.gg, não visual). Validação de segurança/performance real no Supabase e validação local de UI/lint/build confirmadas e aprovadas por Fabrício — commit e push realizados. Débito não bloqueante registrado: divergência pré-existente de rastreabilidade em `database/migrations/`, descoberta (não causada) por este incremento. Ver `docs/adr/ADR-030-card-search-projection.md` e `docs/development/HANDOFF-2026-08-17.md`.
- **Auth Experience (Customer-Facing Product Experience) — Login V1 aprovado e congelado, propagado a Cadastro/Recuperar Senha/Atualizar Senha (2026-08-16).** Nova fundação visual própria (`AuthHeroShell`), sem ADR dedicado por ser decisão majoritariamente visual/de produto.
- **Design System / UI Interna — arquitetura cromática consolidada como baseline (2026-08-16).** Workspace off-white/grafite invertendo por tema, navegação como âncora fixa sempre escura, dourado como accent; harmonização de formulários (`--control-*`, `Select` compartilhado). Documentado em `docs/standards/STD-004-frontend-standards.md` (versão `1.3`).
- **Segurança/Performance — RLS Admin Performance Hardening + Performance Foundation v1 (2026-08-14/15).** `is_admin()` `STABLE` + `(select is_admin())` em 24 policies (Query `2134`); eliminação de over-fetch/leitura redundante em 3 rotas; least privilege ampliado (`TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` revogados, Query `2147`). Padrões formalizados em `docs/standards/STD-001-database-standards.md` versão `1.19`.
- **Pesquisa Global de Cartas — implementada de ponta a ponta (2026-08-17, `ADR-030`).** Projeção segura (`public.search_cards()`/`public.search_card_filter_options()`, `SECURITY DEFINER STABLE`, módulo `4000`–`4999` em `STD-001`) — nenhuma política nova em `card`/`card_set`, `ADR-022` intacto; combobox no header (qualquer usuário autenticado, `aria-activedescendant`, desktop inline/mobile overlay) e página `/pesquisa` (filtros por Card Set/Categoria/Raridade, estado 100% via URL — **sem filtro de Jogo nesta versão**, decisão de escopo). Relevância de busca fixa e determinística (carta fixada → código de Card Set exato → número exato → nome exato → prefixo → ocorrência parcial), sem seletor de ordenação. Validado com `SET ROLE anon`/`authenticated` reais (não só superusuário) e `EXPLAIN (ANALYZE, BUFFERS)` mapeado individualmente aos 10 padrões de consulta (nome exato/prefixo/parcial, número, código de Card Set, Categoria, Raridade, Card Set, filtros combinados, paginação), incluindo 22.104 linhas sintéticas transacionais. Correção de escopo no mesmo dia — assinaturas originais (`4010`/`4020`) incluíam indevidamente `p_game_code`, corrigidas via `4030`/`4031` sem alterar retroativamente as originais. `npm run typecheck` limpo; lint/build e testes de UI (teclado/ARIA/tema/mobile) pendentes de execução local de Fabrício — sandbox do agente sem navegador. Ver `docs/adr/ADR-030-card-search-projection.md` (revisão `1.1`) e `docs/standards/STD-004-frontend-standards.md` (Seção 11).
- **Pricing — Incremento P12 (Exibição de Preços no Frontend), formalmente encerrado (2026-08-18).** Pill de preço em BRL nos três grids do catálogo (`/catalogo/cartas`, `/pesquisa`, galeria de variantes), sobre o contrato de leitura em lote sem N+1 (`get_cards_pricing_summary`, migration `3903`) e a hierarquia de printing Normal → Holofoil → Reverse Holofoil corrigida na migration `3904` (achado real do teste "Reverse-only"). Popover de detalhe com variantes em NM, formatação BRL/USD, fallback `has_pricing=false` quando não há preço elegível. Validação local completa aprovada por Fabrício: desktop/mobile, temas claro/escuro, toque, teclado/foco/Escape, `npm run typecheck` e `build` de produção. Segurança/grants/advisors revalidados e testes transacionais com `ROLLBACK` sem resíduo. Commits `6324b66`/`0dad413` sincronizados com `origin/main`. **A visualização de preços nunca chama a JustTCG** — lê exclusivamente `pricing_observation` já sincronizada; sincronização e consulta permanecem arquiteturalmente desacopladas. `JUSTTCG.is_active` permanece `FALSE` (sem plano comercial contratado) — Pricing ainda não é operacional em produção, só consumível localmente quando há dado elegível. Próximo incremento recomendado: **P13 — Orquestração Programada de Pricing** (agendamento de PTAX e, quando reativada, do conector JustTCG). Ver `docs/05f-pricing.md`, seção "Encerramento Formal do Incremento P12", e `docs/adr/ADR-029-pricing-domain-model.md`, revisão `1.23`.

---

# Revision History

| Versão | Descrição |
|---------|-----------|
| 1.0 | Criação do documento (2026-07-24), a pedido explícito de Fabrício, junto com a reativação de `ADR-INDEX.md`/`STD-INDEX.md`. Consolida, pela primeira vez, uma única fonte de verdade para a trajetória do projeto — sem adotar nenhuma das múltiplas propostas de roadmap não reconciliadas surgidas ao longo do projeto (`B2.x`/`B3.x`, `FASE 1-6`, `FASE 1-4`, `Fase 1-7`); itens ainda não comprometidos por Fabrício ficam explicitamente em "Later", não em "Next". |
| 1.1 | Registrado o progresso real de "Now" (2026-07-24): pipeline `import-card-assets` executado pela primeira vez para `MEE` — referências externas confirmadas, imagens bloqueadas por gap de dados na TCGdex (ver `operations/import-card-assets.md`). Próximo passo do item "Now" passa a ser `MEP`. |
| 1.2 | `MEP` executada no mesmo dia, mesmo resultado da `MEE`: referências externas 100%, imagens bloqueadas pelo mesmo gap real de dados na TCGdex. "Now" reescrito — não há mais nenhuma coleção com execução pendente do lado do Project Mimikyu; o item permanece aberto apenas aguardando a TCGdex publicar os assets de `MEE`/`MEP`. |
| 1.3 | Decisão de Fabrício: em vez de esperar a TCGdex, importar as imagens de `MEE`/`MEP` manualmente — confirmado antes que o asset genuinamente não existe no CDN da TCGdex (404 direto, não só ausência no campo `image` da API). Novo script `scripts/import-manual-assets.ts` criado e CONFIRMADO EXECUTADO para `MEE`/`en` (8/8, 0 falhas). "Now" reescrito para refletir trabalho ativo novamente. |
| 1.4 | `MEE`/`pt-BR` executada no mesmo dia (8/8, 0 falhas) — `MEE` agora 100% completa nos dois idiomas. Falta só `MEP`/`en`+`pt-BR` (`60` Cards cada) para o Catálogo Editorial estar genuinamente fechado. |
| 1.5 | Bug real de `asset_import_run` nunca transicionar de `PENDING` corrigido e testado em produção (v2.6.0) — ver `05-modelo-de-dados.md`. `MEP`/`en` parcialmente salva localmente (`13`/`60`); execução real do script manual adiada até as duas pastas (`en`+`pt-BR`) estarem completas. |
| 1.6 | **Decisão real de Fabrício: iniciar o front-end (aplicação web) agora, em paralelo, sem esperar a modelagem de Coleções — formalizada em `ADR-019-web-application-as-primary-interface.md`.** React/Next.js adotado como interface principal; Power Apps/SharePoint/Power BI removidos da "Later" (não são mais direção provável — foram descartados). Novo parágrafo em "Now" descrevendo os dois módulos iniciais (Gestão de Usuários, Catálogo Editorial CRUD). Nenhum código de front-end ainda existe. |
| 1.7 | App shell, autenticação básica e telas de Catálogo Editorial confirmadas no repositório. **Base de dados do Incremento 1 de Identidade e Acesso ("Meu Perfil") concluída e confirmada no Supabase** (`user_profile`/`reserved_username`, `1000`–`1040`/`1710`/`1800`–`1840`, ADR-020, novo Modelo Modular de Numeração do STD-001) — falta só o frontend (formulário de cadastro + tela `/perfil`). Explicitado que Incremento 2 (lista administrativa) e papéis/permissões seguem fora de escopo até aprovação própria. |
| 1.8 | **Fechamento de governança do Incremento 1, a pedido de Fabrício (2026-07-26): banco de dados e documentação declarados formalmente CONCLUÍDOS** — nenhuma alteração adicional de modelo prevista, salvo bloqueio real durante a integração com o frontend. Deixado explícito que a implementação de interface é o único item pendente do incremento, sob Task 356 (em andamento). |
| 1.9 | **Incremento 1 fechado por completo (frontend validado em produção) e Incremento 2 ("Administração de Usuários") concluído nas Fases 1–3 (2026-07-26).** Papel administrativo (`admin_user`), auditoria (`admin_action_log`) e interface (`/usuarios`) formalizados em `ADR-021-administrative-role-model.md`, executados e validados ponta a ponta. Fase 4 (correção administrativa de `username`) explicitamente adiada para um incremento futuro, por decisão de Fabrício. |
| 1.10 | **Catálogo Editorial (frontend) — fundação de autorização e logo concluída (2026-07-26), formalizada em `ADR-022-catalog-editorial-admin-only-access.md`.** Descoberto que as 17 tabelas do módulo estavam de fato fechadas (RLS sem política); decisão de tornar isso permanente e admin-only. Queries `273`–`277` CONFIRMADAS EXECUTADAS: coluna `card_set.logo_storage_path`, política admin-only de leitura em 10 tabelas, função `admin_set_card_set_logo()`, bucket privado `card-set-logo` com quatro políticas de Storage. Novo parágrafo em "Now". Implementação da tela `/catalogo` em si permanece pendente. |
| 1.11 | **Tela Visão Geral (`/catalogo`) implementada no mesmo dia (2026-07-26), sobre a fundação da revisão `1.10`.** Guarda de servidor compartilhada (`requireCatalogoAdmin()`) nas seis rotas do módulo; quatro blocos (Estado do Catálogo, Card Sets navegável, Cartas por Raridade, Atividade Recente); nova rota de detalhe `/catalogo/card-sets/[code]`. `tsc --noEmit` limpo; validação visual em `npm run dev` pendente. Ressalva registrada em `05-modelo-de-dados.md`: indicadores exatos reconstruídos a partir do resumo pós-compactação, não do wireframe verbatim aprovado originalmente — valem conferência de Fabrício. |
| 1.12 | **Reconciliação documental (2026-07-26), motivada por auditoria externa conduzida por Fabrício após a sessão que produziu `development/HANDOFF-2026-07-26.md`.** "Now" reescrito em três trilhas explícitas: Trilha 1 (imagens `MEP`, inalterada em conteúdo), Trilha 2 (`ADR-023`, nova — registra infraestrutura comum/`Game`/`Expansion` concluídos e `Card Set` como próximo ciclo, referenciando o handoff vigente) e Trilha 3 (`ADR-024`, nova — explicitamente não iniciada). Itens já concluídos que estavam misturados em "Now" (início do front-end, Identidade e Acesso Incrementos 1/2, fundação de leitura/Visão Geral do Catálogo Editorial via `ADR-022`) movidos para "Concluído", eliminando a ambiguidade entre "em andamento" e "já fechado". Nova seção "Catálogo Editorial — Frentes de Encerramento" formaliza as cinco frentes do módulo (A. Modelo de Dados, B. Pipeline de Assets, C. Interface de Consulta, D. Escrita Administrativa, E. Ingestão Administrativa) e o critério de que as cinco precisam estar concluídas para o módulo ser declarado encerrado — "Next" (Sub-Fase 2 — Coleções) agora referencia esse critério explicitamente em vez de um "fechamento do Catálogo Editorial" genérico. |
| 1.13 | **Correção direcionada (2026-07-30), a pedido de Fabrício.** Parágrafo de abertura de "Now" corrigido: Trilha 3 (`ADR-024`) depende da conclusão da Trilha 2 (`ADR-023`) — `ADR-024` só começa após o fechamento de `ADR-023` — não são três trilhas totalmente independentes como a revisão `1.12` descrevia. Trilha 1 e Trilha 2 permanecem sem dependência entre si e podem avançar em paralelo. Sequência já comprometida das trilhas não foi alterada. |
| 1.14 | **Auditoria de reconciliação documental (2026-08-02), a pedido explícito de Fabrício ("vamos parar para organizar a casa").** Correção real registrada: a dependência sequencial planejada na revisão `1.13` (Trilha 3 só começa após o fechamento de Trilha 2) não se confirmou na prática — Fabrício redirecionou o foco para `ADR-024` com o subciclo `Card` de `ADR-023` ainda pausado, não fechado; registrado como decisão real, não como desvio não intencional. Trilha 2 atualizada: `Card Set` concluído (create+update+delete+logo+código editável), restando só o subciclo `Card`, pausado. Trilha 3 reescrita de "não iniciada" para "em andamento": Ciclo 1 confirmado executado e validado; Ciclo 2 (fluxo TCGdex completo) implementado e em uso ativo em produção, sem fechamento formal equivalente ao do Ciclo 1 — pendência real, preservada explicitamente, não declarada como resolvida. "Catálogo Editorial — Frentes de Encerramento": Frente D atualizada para ~75%; Frente E atualizada de "Não iniciada" para "Em andamento". "Concluído" ganhou cinco itens novos: ciclo `Card Set` de `ADR-023`; Ciclo 1 de `ADR-024`; evolução do pipeline de imagens (EN+PT-BR, retry, v2.6.0–v2.9.3); correções de mapeamento de raridade; reagrupamento das galerias de Expansões/Coleções. Nenhuma alteração em código, SQL ou decisão arquitetural nova — apenas reconciliação com o estado real do repositório. Multi-provider permanece explicitamente fora de "Next"/"Later" como compromisso — ver `06-pipeline-importacao.md`, seção "Em Aberto", para o registro dessa pendência arquitetural. |
| 1.15 | **Fechamento documental do Ciclo 2 de `ADR-024` (2026-08-07).** Trilha 3 e "Frentes de Encerramento" (Frente E) atualizadas: Ciclo 2 (TCGdex) tem sua documentação consolidada numa seção própria em `05e-catalogo-editorial.md`, no mesmo padrão do Ciclo 1 — falta só a confirmação da validação `2818` (somente leitura, sobre dado real de produção), apresentada a Fabrício e aguardando execução. Ver `docs/README.md` revisão `1.72` e `adr/ADR-024-catalog-card-ingestion-strategy.md` revisão `1.3` para o detalhamento completo. |
| 1.16 | **Fechamento real, mesmo dia (2026-08-07).** Validação `2818` CONFIRMADO EXECUTADO E VALIDADO, 8 de 8 itens — Ciclo 2 de `ADR-024` (Frente E) formalmente validado de ponta a ponta, mesmo padrão de rigor do Ciclo 1. Único achado real: bug em `admin_confirm_catalog_import()` v1.0 (job podia chegar a `COMPLETED` com linha de decisão nunca revisada), corrigido pela Query `2082` v1.1 + Migration `2118` de reparo retroativo, ambas confirmadas executadas. Também corrigida, de passagem, uma stale real encontrada em Trilha 2/Frente D: o subciclo `Card` de `ADR-023` já havia fechado em 2026-08-07 (revisão anterior deste mesmo dia), mas o texto ainda dizia "pausado, não iniciado"/"~75%" — ambos corrigidos para refletir a conclusão das quatro entidades (`Game`/`Expansion`/`Card Set`/`Card`). Ver `adr/ADR-024-catalog-card-ingestion-strategy.md` revisão `1.6` e `docs/05e-catalogo-editorial.md`, seção "Validação — Query 2818", para o detalhamento completo. |
| 1.17 | **Novo `ADR-026` (Manual Local-File Asset Import Channel, 2026-08-07)** e decisão de prioridade registrada: concluir a Trilha 1 (importação manual de `MEP`) é mais prioritário do que iniciar o Ciclo 3 de `ADR-024` (Trilha 3) — confirmado explicitamente por Fabrício ao perceber que o mecanismo de importação manual (em produção desde 2026-07-24) nunca tinha sido formalizado em nenhum ADR. Trilha 1 e Trilha 3 atualizadas com referências cruzadas ao novo ADR e à ordem de prioridade. Nenhuma mudança de código. |
| 1.18 | **Emenda de `ADR-026` — segundo ponto de entrada via UI (2026-08-07/08).** Trilha 1 atualizada: `/catalogo/importar-imagens` passa a suportar `Fonte = Manual` (upload direto do navegador para `card-front`), ao lado do canal `API` (TCGdex) já existente. Backend SQL CONFIRMADO EXECUTADO E VALIDADO (Queries `2119`–`2122`: políticas de Storage admin-only, `admin_persist_manual_card_asset()`, `catalog_admin_action_log` ampliada, `admin_log_manual_card_asset_import_batch()`; validação consolidada `2819`). Núcleo compartilhado, refatoração do script e Server Actions/UI ainda em implementação — próximo passo. Ver `adr/ADR-026-manual-local-file-asset-import-channel.md` revisão `1.1` e `adr/ADR-INDEX.md` revisão `2.13`. |
| 1.19 | **Frontend da emenda concluído, 2026-08-08, mesmo dia, rodada seguinte.** Núcleo compartilhado, script refatorado, Server Actions e `ImportarImagensManualPicker` implementados (`tsc --noEmit` limpo) — falta só a validação funcional em produção real (upload de um arquivo de verdade) antes de usar o canal para concluir `MEP`. Ver `adr/ADR-026-manual-local-file-asset-import-channel.md` revisão `1.1`. |
| 1.20 | **Validação funcional em produção real confirmada, 2026-08-08, mesmo dia, rodada seguinte.** Fabrício testou com a Coleção `SVE`: 6 arquivos de energia básica importados com sucesso pela UI. Emenda "Segundo ponto de entrada via UI" de `ADR-026` encerrada — backend, frontend e validação real todos confirmados. Falta usar o canal para concluir `MEP` de fato (item de dado, não de implementação). Ver `adr/ADR-026-manual-local-file-asset-import-channel.md` revisão `1.1`. |
| 1.21 | **Encerramento do canal PDF (Ciclos 3/4 de `ADR-024`), decisão explícita de Fabrício, 2026-08-08.** Motivada por três fatores: taxa de sucesso já suficiente do canal TCGdex (Ciclo 2); direção futura de multi-provider como caminho de maior confiabilidade para lacunas remanescentes do que extração de layout gráfico; e os canais manuais já existentes (`ADR-023`/`ADR-026`) cobrindo o caso residual sem automação — embasada por prova técnica exploratória real contra dois checklists (`ME04`, `ME02pt5`) que confirmou viabilidade técnica mas quantificou custo real de engenharia para robustez entre produtos. Trilha 3 renomeada de "em andamento" para "concluída" — seu escopo real (Ciclos 1/2) já estava fechado, e o canal PDF deixa de ser pendência para ser decisão encerrada. "Catálogo Editorial — Frentes de Encerramento": Frente E atualizada para **Concluído**. Corrigido de passagem (stale pré-existente): cabeçalho da Trilha 2 dizia "em andamento" enquanto o corpo já a descrevia como concluída desde 2026-08-07. Seção "Next" atualizada — só a Frente B (`MEP`) e a validação visual da Frente C seguem bloqueando o início de Coleções. Ver `adr/ADR-024-catalog-card-ingestion-strategy.md` revisão `1.7`. |
| 1.22 | **Reconciliação documental de fechamento (2026-08-08), a pedido explícito de Fabrício, ao final de uma sequência de auditorias dedicadas (Sprint Gerencial 1, matriz do módulo Gerencial, auditoria da Frente C, auditoria de planejamento).** Duas correções de premissa: (1) a Frente B nunca teve pendência de dado — a pendência histórica era a *existência* do canal de importação manual, já resolvida por `ADR-026`; `MEP` deixa de ser tratado como bloqueador. (2) a Frente C não tem nenhum critério de aceite formal em aberto — auditoria dedicada não encontrou pendência real em Jogos/Expansões/Card Sets/Cartas; o único achado (`/catalogo/card-sets/[code]`) é evolução futura por design original. **As cinco frentes do Catálogo Editorial (A–E) declaradas Concluídas — o módulo está formalmente encerrado.** "Now" reescrito: as três trilhas do Catálogo Editorial saem de "em andamento" e viram narrativa condensada em "Concluído"; nova **Trilha 4 — Módulo Gerencial** introduzida como frente ativa (plano de 4 frentes recuperado nesta sessão, até então disperso apenas em `docs/log.md` e no cabeçalho de `ADR-027`) — Visão Geral e Histórico de Importações concluídos, Log de Atualizações e Central de Relatórios (6 relatórios definidos, V1 em HTML imprimível com `@media print`) pendentes. "Next" atualizado: critério de entrada de Sub-Fase 2 — Coleções satisfeito; nenhuma ordem de prioridade entre Coleções e Módulo Gerencial proposta nesta revisão; nota de escopo de `ADR-014` (Rule-Based Membership) adicionada. "Later" expandido com itens já registrados em ADRs/documentos normativos mas nunca refletidos aqui (Decks/Trocas/Marketplace, multi-provider, Card Translation). Nova seção **"Débitos Técnicos Registrados (Sem Cronograma)"** consolida, pela primeira vez num único lugar, débitos não bloqueantes já conhecidos mas dispersos — incluindo os dois explicitamente instruídos por Fabrício (evolução de `/catalogo/card-sets/[code]`, atualização de `STD-004`) e os encontrados na auditoria de planejamento (Fase 4 de `username`, task `#368`, e os itens de `06-pipeline-importacao.md`/`07-catalogo-editorial.md` "Em Aberto"). Nenhum item de "Later" ou "Débitos" foi promovido a compromisso de execução — só ganhou rastreabilidade. |
| 1.23 | **Hub operacional de Card Set implementado (`/catalogo/card-sets/[code]`, 2026-08-08, mesmo dia, rodada seguinte).** Fabrício aprovou escopo V1 depois de uma avaliação de viabilidade dedicada, que confirmou não haver necessidade de tabela/view nova. Substitui o detalhe mínimo registrado como débito na revisão `1.22` — item "evolução futura de `/catalogo/card-sets/[code]`" atualizado em "Débitos Técnicos Registrados", agora restrito só ao upload de logo (não implementado nesta rodada). Quatro seções entregues: Cabeçalho/Estado enriquecido (Cards totais base+secretas, cartas catalogadas/esperadas, cobertura de imagens); Cartas da Coleção (`CardSetCartasGrid`, grade somente-leitura nova reaproveitando os efeitos visuais de `CartaGridCard`); Cobertura e pendências (cartas pendentes de cadastro + cobertura de imagem por idioma, recorte por Card Set de `catalog_card_set_image_coverage`); Ações contextuais (Importar Cards/Imagens, Histórico de Importações filtrado). `/catalogo/importacoes` ganhou filtro `?cardSet=<code>` para viabilizar a última ação. Ver `docs/05e-catalogo-editorial.md`, seção "Hub de Card Set". `tsc --noEmit` confirmado limpo. |
| 1.24 | **Hub de Card Set — V1 refinada e encerrada por Fabrício (2026-08-08, mesmo dia, rodadas finais).** Várias rodadas de ajuste visual sobre a `1.23`: logo do cabeçalho ampliada e identidade reorganizada em três linhas (Código-Nome+Tipo / Código-Expansão / Jogo); painel "Cobertura e pendências" deixou de ser seção própria e migrou para dentro de "Estado do Set", numa única linha (sem segunda linha de gráficos); zoom de carta com a mesma View Transitions API de `/catalogo/cartas`; ações contextuais reposicionadas para uma linha acima do bloco "Cartas da Coleção" (padrão já usado em `cartas-gallery.tsx`), sem `Panel`/card ao redor; terminologia "Card(s)" trocada por "Carta(s)" em todos os textos visíveis da tela; bandeira por idioma (emoji Unicode) nas barras de cobertura. Um relato de "bug" (Coleções pendentes/Sem imagens divergentes em `/catalogo/importar-imagens`) investigado e esclarecido como comportamento correto — os indicadores refletem o idioma selecionado (`?idioma=`), não o total global. Detalhe completo em `docs/05e-catalogo-editorial.md`, seção "Hub de Card Set". Fabrício aprovou o resultado final e encerrou o incremento — sem refinamento pendente; único débito restante continua sendo o upload de logo, já registrado em "Débitos Técnicos Registrados". `tsc --noEmit` confirmado limpo em cada rodada. |
| 1.25 | **Auditoria parcial via AgentShield (ECC), 2026-08-09 — piloto mínimo, sem instalação.** Antes de retomar o desenvolvimento funcional, Fabrício pediu avaliação de compatibilidade do projeto ECC (`affaan-m/ECC`) com o ambiente do Project Mimikyu, priorizando AgentShield e reviewers especializados, explicitamente sem Memory Vault/continuous learning/instalação full. Avaliação concluiu: nenhum conflito bloqueante, mas hooks/rules/MCP configs completos do ECC colidiriam com o hook local já ativo (`Impeccable`, `PostToolUse`/`Stop`) e com o MCP Supabase já em uso — plugin completo não recomendado nesta etapa. Piloto mínimo aprovado em duas etapas: (1) `npx ecc-agentshield scan` (só leitura, sem `--fix`) rodado por Fabrício contra `.claude/` — nota A (99/100), 2 findings `medium` (sem bloco de `permissions`; sem hooks `PreToolUse` de segurança), nenhum crítico/alto; ambos registrados como débito de governança não urgente em "Débitos Técnicos Registrados", sem alteração de `settings.local.json`. (2) Próximo passo do piloto: usar um reviewer especializado do ECC (TypeScript/Database/Code Review/Security, conforme o incremento) como segunda revisão independente na próxima implementação real — sem instalar plugin/hooks/rules adicionais e sem criar trabalho artificial só para testar o ECC. Nenhum arquivo de configuração do projeto foi alterado neste ciclo. |
| 1.26 | **Ritual de retomada (2026-08-09) — divergência real encontrada e corrigida; Histórico de Importações (UX/layout) implementado.** Antes de retomar o desenvolvimento funcional, confronto do ponto de retomada trazido por Fabrício contra o estado real do repositório (código, não só documentação — `GitHub como fonte de verdade`): confirmado que Catálogo Editorial, Visão Geral e V1 do hub de Card Set seguem concluídos como já registrado; encontrada divergência real na linha "Histórico de Importações — concluído" desta mesma seção (revisão `1.22`) — a tela ainda usava uma `<table>` simples sem busca, com colunas Pipeline/Tipo/Card Set/Fonte, nunca tendo recebido a revisão de UX/layout (mesmo modelo do bloco "Atividade Recente" da Visão Geral) que Fabrício lembrava ter definido como direção. Sinalizada explicitamente, não aplicada silenciosamente. Implementada nesta rodada: `ImportacoesTable` (`web/components/catalogo/importacoes-table.tsx`) substitui a tabela antiga — busca no topo, filtro de chips por Pipeline (Cartas/Imagens), colunas Data \| Coleção \| Execução \| Operação \| Resultado \| Status, "Coleção" em vez de "Card Set", paginação (mesmo footer de `card-sets-table.tsx`). Semântica de "0 de 0" investigada antes de tocar a apresentação (pedido explícito): confirmado nos logs do Edge Function `import-card-assets` (`index.ts`, checagens `if (!cardSet)`/`if (!externalReference)`) que é um resultado real — a run falhou antes de sequer enumerar cartas a importar (Card Set ou referência externa daquele idioma inexistente), não um dado quebrado — mantido no mesmo formato "X de Y" das demais linhas. `tsc --noEmit` confirmado limpo. Linha de "Histórico de Importações" corrigida para refletir o estado real: backend concluído (2026-08-08), UX/layout implementado (2026-08-09), pendente validação visual de Fabrício antes de declarar esta frente encerrada — nenhuma outra frente avançada nesta rodada, por instrução explícita. |
| 1.27 | **Gráficos de tendência (Cartas/Imagens) em `/catalogo/importacoes`, 2026-08-09, mesmo dia, rodada seguinte.** Pedido de Fabrício, modelo de referência anexado (widget de erros do Supabase). Ambiguidade real esclarecida antes de implementar via `AskUserQuestion`: "dois gráficos" com "barras empilhadas" parecia contraditório (um gráfico por dimensão vs. duas séries empilhadas no mesmo gráfico) — Fabrício confirmou que são dois gráficos separados, um por pipeline (Cartas, Imagens), cada um com barras empilhadas verde (sucesso)/vermelho (falha) por período. Implementado `ImportacoesTendencia` (`web/components/catalogo/importacoes-tendencia.tsx`), sem biblioteca de gráficos (nenhuma no projeto — mesma escolha já feita para a barra de cobertura por idioma do hub de Card Set: `div`s com Tailwind). Classificação binária por status: `COMPLETED` = sucesso, `FAILED`/`COMPLETED_WITH_ERRORS` = falha; status não-terminais e `CANCELLED` excluídos (ainda não são "importações realizadas"). Período quinzenal calendário (1–15 e 16–fim do mês), rótulo numérico compacto ("01/08"/"16/08"). Posicionado entre o título e a tabela, respeitando os mesmos filtros de URL já aplicados à lista. `tsc --noEmit` confirmado limpo. |
| 1.28 | **Ajustes de interação e estilo dos gráficos de tendência, 2026-08-09, mesmo dia, rodada seguinte.** Quatro pedidos de Fabrício sobre a revisão `1.27`, a partir de captura de tela real: (1) popover ao clicar numa barra (não mais só `title` nativo do navegador) — mesmo modelo do widget de referência: card flutuante acima da barra com o período por extenso e uma linha por série (quadrado colorido + rótulo + contagem), fecha ao clicar fora; componente virou Client Component (`"use client"`) por causa do estado do popover. (2) Espaçamento do card corrigido — `CardContent density="compact"` não tem `padding-top` por design (espera um `CardHeader` acima, que este card não usa), deixando o título colado na borda superior; adicionado `pt-4` e aumentado o espaçamento vertical interno. (3) Títulos alterados para "Importações de Cartas" (pipeline CARTAS) e "Importação de Imagens" (pipeline IMAGENS, singular — mantido exatamente como pedido, mesmo com a assimetria). (4) Cantos arredondados removidos das barras (`rounded-t-sm`/`rounded-t-[1px]` retirados) — só o popover em si mantém `rounded-md`, como no modelo de referência. Pequeno sinalizador vermelho adicionado antes do texto "X,X% de falhas" no cabeçalho de cada card, mesma rodada. `tsc --noEmit` confirmado limpo. |
| 1.29 | **Eixo X trocado de quinzenal para semanal + cor exata da barra de sucesso, 2026-08-09, mesmo dia, rodada seguinte.** Pedido de Fabrício: agrupamento passou de quinzena calendário (1–15/16–fim do mês) para semana calendário ISO (segunda a domingo) — `inicioDaSemana()` nova, substitui `quinzenaKey`/`quinzenaLabel`/`agruparPorQuinzena` (renomeados para `semana*`/`agruparPorSemana`, tipo `Quinzena` → `Semana`). Cor da barra de sucesso trocada para o hex exato pedido (`#3FCF8E`) via `style` inline — deliberadamente não alterado o token global `--success` (`hsl(142 71% 40%)`, mais escuro), que também é usado por `StateBadge`/`AtividadeRecente`/outros, fora do escopo deste pedido. `tsc --noEmit` confirmado limpo. |
| 1.30 | **Histórico de Importações aprovado e encerrado por Fabrício (2026-08-09).** Última rodada da frente aberta pelo ritual de retomada desta mesma data (revisão `1.26`): UX/layout, gráficos de tendência por pipeline e os ajustes finos de interação/estilo (revisões `1.27`–`1.29`) todos validados visualmente e aprovados. Trilha 4 atualizada — Histórico de Importações passa de "pendente validação visual" para "concluída". Restam pendentes, sem mudança nesta rodada: Log de Atualizações e Central de Relatórios (Trilha 4) e a decisão de prioridade frente à Sub-Fase 2 — Coleções, ambas já registradas em "Now"/"Next". |
| 1.31 | **Log de Atualizações V1 implementada e aprovada por Fabrício (2026-08-09), mesmo dia, rodada seguinte.** Proposta técnica revisada (mapa completo de classificação Cadastro/Alteração/Exclusão/Outras para os 21 `action` reais, validado semanticamente contra as migrations `2098`/`2121` — o arquivo canônico `2010` estava desatualizado) e segunda revisão independente via persona ECC database/security-reviewer (piloto formalizado na revisão `1.25`, primeira aplicação real) antes da implementação. Backend: índice `ix_catalog_admin_action_log_created_at` (Query `2125`); `internal.catalog_admin_action_category()` (Query `2126`); 4 functions de escrita corrigidas para gravar `card_set_name`/`card_set_code` no momento do evento (Queries `2080`/`2082`/`2106`/`2122`); `admin_list_catalog_action_log()` paginada/filtrada server-side (Query `2127`) e `admin_get_catalog_action_log_weekly_summary()` (Query `2128`, janela fixa de 12 semanas) — todas CONFIRMADAS EXECUTADAS por Fabrício, a maioria validada funcionalmente contra dado real de produção. Frontend: 3 gráficos semanais (Cadastro/Alteração/Exclusão, cores `bg-primary`/`bg-warning`/`bg-destructive`), filtros server-side (busca/Entidade/Ação/Usuário) e tabela paginada Data\|Quem\|Entidade\|Registro\|Ação\|Detalhes com Dialog de Detalhes a partir do `metadata` já resolvido — sem diff antes/depois inventado. Trilha 4 atualizada — Log de Atualizações passa de "pendente" para "concluída"; resta pendente, sem mudança nesta rodada: Central de Relatórios. Ver `docs/05e-catalogo-editorial.md`, seção "Log de Atualizações", para o detalhamento completo. `tsc --noEmit` confirmado limpo. |
| 1.32 | **Ajuste visual pós-V1 do Log de Atualizações + Fabrício encerra as duas telas (2026-08-09, mesmo dia, rodada seguinte).** Puramente frontend, sem mudança de schema/RPC: janela de exibição dos 3 gráficos reduzida de 12 para 10 semanas e barras 25% mais finas (`JANELA_SEMANAS`/`COLUNA_LARGURA_PX`, a agregação server-side da Query `2128` continua trazendo 12 semanas — o corte é só de exibição); cor única `#3FCF8E` substituindo os tokens `bg-primary`/`bg-warning`/`bg-destructive`; correção de um bug real introduzido pela redução de largura (colunas de largura fixa faziam o eixo encolher à esquerda do card, sobrando vão vazio à direita — corrigido com colunas `flex-1`); interação trocada de popover-ao-clicar para tooltip-ao-passar-o-mouse (`onMouseEnter`/`onMouseLeave` + `onFocus`/`onBlur` para acessibilidade via teclado), aplicada tanto em `LogAtualizacoesResumo` quanto em `ImportacoesTendencia` (retroativo à `1.30`, para as duas telas ficarem consistentes). Uma proposta de prefixar o código da Coleção no "Registro" da tabela (exigiria bump de `admin_list_catalog_action_log()`, Query `2127`) foi apresentada seguindo o ritual de query mas abortada por Fabrício antes da execução — `2127` permanece na v1.0 confirmada. Fabrício validou o resultado final e declarou formalmente encerradas as duas frentes da Trilha 4 ("Considere as duas páginas encerradas. Vamos para a próxima implementação."): Histórico de Importações e Log de Atualizações. Resta pendente na Trilha 4, sem mudança nesta rodada: Central de Relatórios — a decisão de seguir por ela ou pela Sub-Fase 2 — Coleções ainda não foi tomada por Fabrício. `tsc --noEmit` confirmado limpo em cada rodada. Ver `docs/05e-catalogo-editorial.md`, seção "Log de Atualizações" (subseção "Ajuste visual pós-V1"). |
| 1.33 | **Central de Relatórios implementada (2026-08-09), mesmo dia, rodada seguinte — Fabrício escolheu esta frente em vez da Sub-Fase 2 — Coleções.** Proposta técnica apresentada e confirmada antes da implementação (via `AskUserQuestion`, não assumida): definição de "Qualidade do Catálogo" (único dos 6 relatórios sem especificação prévia) e arquitetura (hub único `/catalogo/relatorios` + seletor de Coleção nos relatórios que mostram uma Coleção por vez). Hub com 6 cards; 4 relatórios (Cartas pendentes por Coleção, Imagens pendentes por Coleção, Qualidade do Catálogo, Cobertura Geral) são tabelas cruzando todas as Coleções, sem seletor, reaproveitando `catalog_card_set_metrics`/`catalog_card_set_image_coverage` (Query 2123/2124); os outros 2 (Checklist por Coleção, Resumo da Coleção) pedem uma Coleção via `RelatorioColecaoSeletor` e reaproveitam `getCartasCompletas()`/`getCardSetByCode()` — nenhuma query nova para esses dois, nenhuma mudança de schema/RPC em toda a frente (só 3 funções novas de leitura em `queries.ts` para os relatórios agregados). Impressão via `window.print()` nativo (`RelatorioPrintButton`) e `@media print` — primeiro uso real de impressão no projeto: `AppShell`/`Sidebar`/`Header` ganharam `print:hidden`/`print:overflow-visible` para a folha impressa não incluir navegação. Ações contextuais "Checklist"/"Resumo" acrescentadas ao hub de Card Set (`/catalogo/card-sets/[code]`), já filtradas pela Coleção. `tsc --noEmit` confirmado limpo. **Implementação completa, mas ainda NÃO aprovada por Fabrício** — validação visual real (inclusive a própria impressão) só é possível no ambiente dele; Trilha 4 (Módulo Gerencial) só pode ser declarada encerrada por completo depois dessa validação. Ver `docs/05e-catalogo-editorial.md`, seção "Central de Relatórios". |
| 1.34 | **Checklist por Coleção redesenhado (2026-08-09), mesmo dia, rodada seguinte — tratamento diferenciado a pedido de Fabrício.** Sinalizado como o relatório de maior valor para colecionadores e, futuramente, o primeiro com acesso de usuários comuns (não-admin) — política de acesso explicitamente adiada, sem escopo definido nesta rodada. Modelo de referência anexado (checklist oficial impresso de um produto Pokémon real): estrutura funcional replicada (cabeçalho com duas logos, lista em colunas com checkbox, legenda de raridade no rodapé), nunca a marca/conteúdo do modelo — logo do próprio Mimikyu + logo real da Coleção no lugar das logos do produto de referência. Layout de folha A4 (`max-w-[210mm]`, `@page { size: A4; margin: 10mm }` novo em `globals.css`, fora de qualquer `@layer`), lista em 3 colunas CSS (`columns-3`, preenche verticalmente). Checkbox ANTES do número em cada linha — pedido explícito de Fabrício, ao contrário do modelo anexado. Coluna "Cadastrada" removida (pedido explícito) e, com ela, o relatório passou a listar só Cartas ativas (`getCartasCompletas()` sem `incluirInativas`) — nenhuma query nova. `tsc --noEmit` confirmado limpo; validação visual (inclusive impressão real em A4) ainda pendente, junto com o restante da Central de Relatórios. Ver `docs/05e-catalogo-editorial.md`, seção "Checklist por Coleção — tratamento diferenciado, modelo oficial anexado". |
| 1.35 | **Ajustes pós-validação visual do Checklist por Coleção (2026-08-09), mesmo dia, rodada seguinte — três pedidos de Fabrício a partir de captura de tela real da folha de `ME3` (124 Cartas).** (1) Título movido para dentro do cabeçalho, entre as duas logos (era um `h2` solto abaixo) — renomeado de "Checklist da Coleção" para "Lista de Verificação". (2) Zebra striping branco/`#F7F5ED` por linha, com `print-color-adjust: exact` explícito (senão a maioria dos navegadores omite cor de fundo ao imprimir). (3) Contagem de colunas deixou de ser fixa em 3 — achado real de Fabrício: Coleções com mais de ~132 Cartas não caberiam mais numa única folha A4, quebrando o requisito central do relatório; `colunas = Math.max(2, Math.ceil(cartas.length / 46))`, constante `ROWS_POR_COLUNA_A4 = 46` calibrada visualmente pela folha real de `ME3` (3 colunas, folga vertical sobrando) — sujeita a ajuste após impressão real de uma Coleção grande. `tsc --noEmit` confirmado limpo; validação visual desta rodada ainda pendente. Ver `docs/05e-catalogo-editorial.md`, seção "Checklist por Coleção — tratamento diferenciado, modelo oficial anexado", subseção "Ajustes pós-validação visual". |
| 1.36 | **Mais um ajuste no Checklist por Coleção (2026-08-09), mesmo dia, rodada seguinte, a partir de captura de tela real de uma Coleção grande (`ME2.5`, 295 Cartas, 7 colunas).** Subtítulo ("Use as caixas abaixo...") deixou de ser um bloco de largura cheia abaixo do cabeçalho inteiro e passou a ficar empilhado sob o título, dentro do próprio cabeçalho — ganho de espaço vertical real, já que a altura do cabeçalho já é dominada pela logo da Coleção (`h-12`), então a segunda linha de texto não soma altura nova. Espaço entre colunas reduzido de `1.5rem` para `0.5rem` (`columnGap` inline), a pedido explícito, para ganhar espaço horizontal em Coleções com muitas colunas. `tsc --noEmit` confirmado limpo; validação visual ainda pendente. Ver `docs/05e-catalogo-editorial.md`, seção "Checklist por Coleção — tratamento diferenciado, modelo oficial anexado", subseção "Mais uma rodada de ajuste". |
| 1.37 | **Mudança de direção no Checklist por Coleção — teto de colunas, mesmo dia, teste explícito de Fabrício (2026-08-09).** Mesmo com o espaço entre colunas reduzido (revisão `1.36`), Coleções grandes continuavam ruins visualmente com 7+ colunas — texto, checkbox e símbolo de raridade espremidos demais numa coluna estreita. Fabrício pediu a direção oposta: teto explícito de colunas por página (`MAX_COLUNAS_A4`, testado inicialmente em 5); Coleções pequenas continuam usando só as colunas que precisarem (`MIN_COLUNAS_A4 = 2`); Coleções grandes o bastante para exigir mais que o teto passam a imprimir em **mais de uma folha A4** — aceito explicitamente por Fabrício, abandonando o objetivo original de "sempre uma única folha" para Coleções muito grandes. Layout CSS multi-coluna nativo já fragmenta corretamente entre páginas impressas sem nenhuma mudança adicional de código. `tsc --noEmit` confirmado limpo; validação visual (inclusive impressão real de uma Coleção agora multi-página) ainda pendente. Ver `docs/05e-catalogo-editorial.md`, seção "Checklist por Coleção — tratamento diferenciado, modelo oficial anexado", subseção "Mudança de direção — teto de colunas". |
| 1.38 | **Teto de colunas do Checklist por Coleção ajustado de 5 para 4 (2026-08-09), mesmo dia, rodada seguinte.** Pedido direto de Fabrício, sem nova justificativa registrada além da rodada anterior (`1.37`) — só o valor de `MAX_COLUNAS_A4` mudou (`5` → `4`); `MIN_COLUNAS_A4` permanece `2`; toda a lógica de clamp e a aceitação de relatório multi-página para Coleções grandes seguem inalteradas. `tsc --noEmit` confirmado limpo; validação visual ainda pendente. Ver `docs/05e-catalogo-editorial.md`, seção "Checklist por Coleção — tratamento diferenciado, modelo oficial anexado", subseção "Mudança de direção — teto de colunas". |
| 1.39 | **Checklist por Coleção — fontes reduzidas e cabeçalho repetido a cada página (2026-08-09), mesmo dia, rodada seguinte.** Dois pedidos de Fabrício: fontes reduzidas um passo em toda a folha (título 18px→16px, subtítulo/legenda 10px→9px, linhas de Carta 11px→10px, rodapé 9px→8px); cabeçalho (logos + título) passa a se repetir em cada página impressa, agora que Coleções grandes podem gerar mais de uma folha. Lista trocada de CSS multi-coluna (`column-count`) para uma **tabela HTML real** (`<table>`/`<thead>`/`<tbody>`, preenchida na mesma ordem coluna-major de antes) — `<thead>` repete nativamente em toda página impressa em todos os navegadores principais, sem depender de `position: fixed` (inconsistente entre navegadores) nem de alterar a margem global do `@page` (que afetaria todos os relatórios da Central). Espaçamento entre colunas preservado via `border-spacing` (mesmo `0.5rem`). `tsc --noEmit` confirmado limpo; validação visual (inclusive impressão real multi-página, para confirmar a repetição do cabeçalho) ainda pendente. Ver `docs/05e-catalogo-editorial.md`, seção "Checklist por Coleção — tratamento diferenciado, modelo oficial anexado", subseção "Mudança estrutural — cabeçalho repetido a cada página + fontes reduzidas". |
| 1.40 | **Checklist por Coleção aprovado por Fabrício ("Visualmente excelente") e declarado baseline visual da Central de Relatórios (2026-08-09), mesmo dia, rodada seguinte.** Três ajustes finais no Checklist: subtítulo reescrito para não associar o relatório a posse registrada ("Use as caixas abaixo para sua conferência das Cartas desta Coleção." — domínio ainda é o Catálogo Editorial, não uma coleção pessoal); título de volta a 18px em negrito (`text-lg font-bold`, era 16px/semibold desde a revisão `1.39`). Escopo maior: três componentes extraídos para `components/catalogo/` — `RelatorioFolha` (folha A4), `RelatorioCabecalho` (logo Mimikyu + título/subtítulo + logo da Coleção quando aplicável) e `RelatorioRodape` ("Gerado por Mimikyu em {data}") — e reaplicados nos outros 5 relatórios da Central (Cartas pendentes, Imagens pendentes, Qualidade do Catálogo, Cobertura Geral, Resumo da Coleção). Nos 4 relatórios tabulares, o cabeçalho entrou dentro do próprio `<thead>` da `DataTable`, junto com a linha de cabeçalho de coluna já existente, repetindo ambos em toda página impressa pelo mesmo princípio do Checklist. Cores de texto trocadas de tokens de tema para `neutral-*` fixo nos 5 relatórios, para bater com o fundo branco fixo da folha. Achado real corrigido de passagem: os 5 relatórios nunca tinham `print:hidden` no `PageHeader` de tela — corrigido. `tsc --noEmit` confirmado limpo; validação visual dos 5 relatórios com o novo tratamento (só o Checklist já foi validado por Fabrício) ainda pendente. Ver `docs/05e-catalogo-editorial.md`, seção "Checklist por Coleção — tratamento diferenciado, modelo oficial anexado", subseção "Checklist por Coleção aprovado — vira baseline visual da Central de Relatórios". |
| 1.41 | **Ajustes finais na baseline visual da Central de Relatórios (2026-08-09), mesmo dia, rodada seguinte — Fabrício sinalizou que seria a última rodada.** Zebra striping (branco/`#F7F5ED`) e cores fixas `neutral-*` — que a rodada anterior já afirmava ter aplicado, mas na prática só o Resumo da Coleção tinha recebido — corrigidos de fato nos 4 relatórios tabulares (Cartas pendentes, Imagens pendentes, Qualidade, Cobertura Geral). Linha de totais adicionada em "Qualidade do Catálogo" (soma de cada coluna numérica) e em "Cobertura Geral" (soma de Cadastradas/Com imagem + percentual geral recalculado das somas, não a média dos percentuais de linha — nota de definição registrada em comentário de código sobre a duplicação de Cadastradas por linha de idioma). Rodapé renomeado de "Gerado por Mimikyu" para "Gerado por MMKYU Collector" (nome de marca ao usuário final) e fixado no rodapé físico de cada folha impressa (`print:fixed print:bottom-0`) — para nunca aparecer "no meio da página" em relatórios curtos; único mecanismo nativo disponível, já que `<tfoot>` não empurra conteúdo até o fim físico da página. Contador "Página X de Y" — investigado e **adiado por decisão explícita de Fabrício**, depois de confirmado que navegadores não expõem nativamente a paginação real durante a impressão (exigiria biblioteca de pré-paginação, ex. Paged.js); três opções apresentadas via `AskUserQuestion`, escolhida "não incluir por enquanto". `tsc --noEmit` confirmado limpo. Ver `docs/05e-catalogo-editorial.md`, seção "Checklist por Coleção", subseção "Ajustes finais na baseline". |
| 1.42 | **Cabeçalho em 3 linhas no Checklist e no Resumo da Coleção + linha de totais em "Imagens pendentes por Coleção" (2026-08-09), mesmo dia, rodada seguinte — últimos ajustes capturados por Fabrício analisando várias folhas impressas lado a lado.** `RelatorioCabecalho` ganhou a prop opcional `identificacaoColecao`: quando presente, a identificação da Coleção (`{código} · {nome}`) aparece em destaque (18px, negrito) na linha 1, o nome fixo do relatório ("Lista de Verificação de Cartas" / "Resumo da Coleção") vira a linha 2 (menor), e o subtítulo permanece na linha 3 — antes o código/nome da Coleção vinha concatenado dentro do próprio título, truncando cedo em Coleções com nome longo. Nos 4 relatórios que cruzam todas as Coleções (sem `identificacaoColecao`), o cabeçalho permanece com as 2 linhas originais, sem mudança. Linha de totais adicionada em "Imagens pendentes por Coleção" (soma de Cadastradas/Com imagem/Sem imagem), mesmo padrão visual de "Qualidade do Catálogo"/"Cobertura Geral". `tsc --noEmit` confirmado limpo. Ver `docs/05e-catalogo-editorial.md`, seção "Checklist por Coleção", subseção "Últimos ajustes". |
| 1.43 | **Ícone ausente em "Log de Atualizações" + rolagem infinita em Cartas/Coleções (2026-08-09), mesmo dia, rodada seguinte — dois achados de Fabrício numa inspeção geral das páginas do Catálogo Editorial.** (1) `PageTitle` de `/catalogo/log-atualizacoes` nunca tinha recebido o wrapper de ícone (`ScrollText`) que toda outra página do módulo usa ao lado do título — omissão da V1 desta tela, corrigida. (2) Botão "Ver todas as N cartas" removido de `CartasGallery` (`/catalogo/cartas`) e de `CardSetCartasGrid` (hub de Card Set); novo hook compartilhado `useInfiniteReveal` (`web/hooks/use-infinite-reveal.ts`) revela lotes de cartas via `IntersectionObserver` sobre um elemento sentinela, conforme o usuário rola a tela — mesmo `PAGE_SIZE` de antes em cada tela, só o mecanismo de revelação mudou. `tsc --noEmit` confirmado limpo. Ver `docs/05e-catalogo-editorial.md`, subseções "Ícone do título ausente, corrigido" e "Paginação por rolagem infinita, em vez do botão 'Ver todas'". |
| 1.44 | **Barras de "Cobertura por idioma" ampliadas no hub de Card Set (2026-08-09), mesmo dia, rodada seguinte** — Fabrício, ainda na mesma inspeção geral: "aumentar um pouco os gráficos de Cobertura por idioma para ocuparmos melhor o espaço do card". Coluna de cada idioma alargada de `w-40` para `w-56` em `app/catalogo/card-sets/[code]/page.tsx` — o painel "Estado do Set" já tinha vão sobrando à direita das duas barras em Coleções com só 2 idiomas (o caso comum); só a largura mudou, altura da barra e tamanho dos textos seguem da revisão anterior. `tsc --noEmit` confirmado limpo. Ver `docs/05e-catalogo-editorial.md`, seção "Hub de Card Set". |
| 1.45 | **Módulo Gerencial (Trilha 4) formalmente encerrado (2026-08-09) — Fabrício: "Considero pela UI o bloco concluído."** Última frente pendente (Central de Relatórios) aprovada visualmente, fechando as quatro frentes da Trilha 4 (Visão Geral, Histórico de Importações, Log de Atualizações, Central de Relatórios — todas já concluídas individualmente nas revisões `1.26`–`1.44`). "Now" reescrito: a seção detalhada da Trilha 4 sai de "em andamento" e vira narrativa condensada em "Concluído" (bullet único, substituindo o antigo bullet parcial de Visão Geral/Histórico de Importações); nenhuma frente comprometida ativa permanece. "Next" atualizado: a decisão de priorização entre Sub-Fase 2 — Coleções e Módulo Gerencial deixa de existir (o segundo está concluído) — Sub-Fase 2 passa a ser a única frente comprometida ainda não iniciada, aguardando só a decisão de Fabrício sobre quando começar. Campo "Versão" deste documento corrigido de `1.33` (desatualizado há várias rodadas) para acompanhar a Revision History. Ver `docs/05e-catalogo-editorial.md`, seção "Central de Relatórios", subseção "Encerramento formal". |
| 1.46 | **Decisão de sequenciamento macro: Card Variant como bloco intermediário entre Catálogo Editorial e Coleções (2026-08-09).** Fabrício determinou que a Sub-Fase 2 — Coleções não deve iniciar imediatamente após o Catálogo Editorial (que permanece encerrado, escopo `Game → Expansion → Card Set → Card` inalterado). Novo sequenciamento: **Catálogo Editorial → Card Variant → Coleções** — motivado por `Card` representar a carta editorial genérica enquanto `Card Variant` representa as formas colecionáveis específicas dela, e Collections precisar referenciar uma variante inequívoca (não a Card genérica) para registrar posse. Não é domínio novo: `card_variant_type`/`card_variant`/triggers/validações/seeds já existem no repositório — o bloco deve começar por auditoria do existente, não modelagem do zero. Nenhuma implementação/alteração de Card Variant nesta rodada, só o planejamento: "Now" registra a decisão e sua motivação; "Next" ganha um novo bloco "Card Variant" (ainda sem escopo/critérios de saída) posicionado antes de "Sub-Fase 2 — Coleções", cujo critério de entrada foi atualizado para depender também do encerramento desse novo bloco. Definição detalhada do escopo de Card Variant a partir de 2026-08-13, após auditoria técnica final do Catálogo Editorial. |
| 1.47 | **Correção do incidente SV10 (2026-08-15) e registro do débito de watchdog.** Investigação read-only confirmou causa raiz: `fetch()` sem timeout em `resolveSetSerieName()`/`listSetCardFiles()`/`fetchCardFileSource()` da Edge Function `import-card-variants`, com a plataforma matando a invocação por estouro do teto de execução (~150s, HTTP 546) antes de `catch()`/`failVariantJob()` rodar — job ficava preso em `PROCESSING` indefinidamente, e o frontend (`ImportProgressVariantes`) tratava `Boolean(job)` como conclusão, exibindo "Concluído" mesmo para um job travado. Correção mínima aprovada e implementada no mesmo ciclo: `AbortController` (15s) nas três chamadas externas; job preso `bff18ea3-1b1f-4794-ab87-f67eff2ade1a` marcado `FAILED` via Query `2149` (CONFIRMADO EXECUTADO), liberando o fingerprint de SV10; `ImportProgressVariantes` reescrito para derivar o estado exibido de `job.status` real via `resolveJobPhase()`, com estado de erro genuíno para `FAILED`/`CANCELLED`. Edge Function reimplantada em produção (versão 2). Novo débito registrado em "Débitos Técnicos Registrados": watchdog automático de jobs travados, explicitamente não implementado nesta rodada, por decisão de Fabrício. |
| 1.48 | **Auditoria documental completa (2026-08-16) — "Now"/"Next"/"Débitos Técnicos"/"Concluído" reconciliados contra o estado real do repositório, `docs/log.md` e `ADR-028`.** Este documento estava desatualizado desde a revisão `1.47`/2026-08-09: descrevia o bloco Card Variant como "ainda sem escopo detalhado... nenhuma implementação até lá", quando na realidade ele foi extensamente auditado, projetado e implementado entre 2026-08-14 e 2026-08-15 (governança `ADR-028` revisões `1.0`–`1.4`, taxonomia com CRUD administrativo, pipeline "Importar Variantes" completo, dois incidentes reais corrigidos) e só então pausado por instrução explícita de Fabrício ("não altere mais essa frente") — não formalmente encerrado com o mesmo rigor do Catálogo Editorial. Removido de "Next" (já não é "ainda não iniciado"); "Now" reescrito para descrever o estado real e as três decisões de fato pendentes com Fabrício (se a pausa satisfaz o critério de entrada de Coleções; se/quando retomar Card Variant; prioridade entre as frentes). Também ausentes deste documento até esta revisão: a frente inteira de Auth Experience (Login V1 aprovado e congelado, propagado às demais telas) e a consolidação do Design System (arquitetura cromática promovida a baseline), ambas concluídas em 2026-08-16 sem nunca terem sido registradas aqui — adicionadas a "Now"/"Concluído". Duas evoluções aditivas sobre o Módulo Gerencial já encerrado (humanização do Log de Atualizações; pipeline VARIANTES no Histórico de Importações) e a consolidação de Segurança/Performance (RLS Admin Performance Hardening, Performance Foundation v1, least privilege) também registradas pela primeira vez. Frase mínima de status adicionada a "Later" para Pricing/Market Data: discovery em andamento, nenhuma decisão tomada — conforme escopo explícito desta auditoria. Item de "Débitos Técnicos Registrados" sobre `STD-004` removido por estar resolvido (o documento já reflete `Card`/`CardContent` em Jogos e `CreateGameDialog`/`EditGameDialog` em Game, confirmado por leitura direta); novo débito real registrado no mesmo lugar: `docs/05b-cartas-e-raridade.md` (modelo físico de Card Variant) não acompanhou nenhum dos incrementos de 2026-08-14/15 — reconstituição fica para um ciclo dedicado, por exigir conferência migration a migration. Nenhuma implementação, migration ou mudança de código nesta rodada — escopo exclusivamente documental, conforme instrução explícita de Fabrício. Ver `docs/README.md` revisão equivalente e `docs/log.md`, entrada `lint` de 2026-08-16. |
| 1.49 | **Reconciliação com decisões formais de Fabrício (2026-08-16), segunda rodada de auditoria documental, motivada por rejeição explícita do encerramento anterior devido à inconsistência crítica de `docs/05b-cartas-e-raridade.md`.** Card Variant declarado fundação encerrada (necessária e suficiente para Pricing e Collection; evoluções futuras como Vintage/Promo seguem backlog não bloqueante) — `docs/05b-cartas-e-raridade.md` reconstruído por introspecção direta do Supabase (schema físico real, governança, pipeline de importação de variantes, achados de precisão). Sequência estratégica aprovada e formalizada — Card Variant → Pricing/Market Data (discovery concluído, fundação a definir) → Collection → Analytics/Valuation — substituindo a indefinição de prioridade registrada até `1.48`. "Next" reescrito: Pricing/Market Data entra como próxima frente comprometida (critério de entrada satisfeito, fundação técnica ainda não definida/aprovada); critério de entrada de Coleções atualizado para depender também da fundação de Pricing, não apenas do encerramento de Card Variant. "Débitos Técnicos" atualizado: item sobre `05b-cartas-e-raridade.md` desatualizado (registrado na `1.48`) substituído pelos dois achados de precisão reais da reconciliação (divergência de regex de validação de `code`; `GRANT`s não revogados na view `catalog_card_set_variant_coverage`) — nenhum bloqueante. Achado novo desta rodada: job `BASE1` (Coleção Básica, `STAGED`, 410 linhas 100% sem mapeamento) nunca antes documentado em nenhum artefato canônico. Ver `docs/README.md` revisão equivalente e `docs/log.md`, entrada de 2026-08-16. |
| 1.50 | **Modelagem conceitual e lógica do domínio Pricing concluída (`ADR-029`/`05f-pricing.md`, 2026-08-16, mesma data, ciclo seguinte) — decoupled da homologação da JustTCG, que segue interrompida por limitação de cota, não aprovada nem reprovada.** Dez entidades (`pricing_source` até `pricing_sync_run_call`), quarto domínio independente de Catálogo/Ownership/Analytics; printing/condição/idioma/moeda/mercado como dimensões independentes; "Valor Brasil" exclusivamente de `pricing_source.market_scope`. **Nenhuma tabela criada** — decisão de modelagem, não implementação. "Now"/"Next" reescritos: Pricing passa de "definição da fundação, discovery apenas" para "modelagem concluída, implementação física ainda não iniciada, condicionada à homologação de pelo menos uma fonte". Nenhuma mudança na sequência estratégica (Card Variant → Pricing → Collection → Analytics) nem no critério de entrada de Coleções. Ver `docs/README.md` revisão equivalente, `adr/ADR-INDEX.md` revisão `2.17` e `docs/log.md`, entrada `docs` de 2026-08-16. |
| 1.51 | **Incremento P1 — Fundação Física de Pricing executada (2026-08-16, mesma data, ciclo seguinte à correção pontual de `05f-pricing.md`/`ADR-029` revisão `1.1`).** `pricing_source`, `card_condition` e `pricing_condition_mapping` criadas no Supabase — as três entidades que não dependem de homologação de fonte externa; `CONFIRMADO EXECUTADO`, validação de 12 itens confirmada. "Next" atualizado: Pricing passa de "implementação física ainda não iniciada" para "iniciada — fundação parcial executada, restante condicionado à homologação de fonte". Nenhuma fonte cadastrada, nenhuma condição semeada, nenhuma das outras sete entidades implementada. Ver `docs/development/HANDOFF-2026-08-16.md`, seção 14, e `docs/log.md`, entrada `feature` de 2026-08-16. |
| 1.52 | **Incremento P2 — Correspondência Externa de Pricing executada (2026-08-16, mesma data, ciclo seguinte ao Incremento P1).** `pricing_set_mapping` e `pricing_card_mapping` criadas no Supabase (`CONFIRMADO EXECUTADO`, ambas vazias), validação transacional de 15 itens confirmada. "Next" atualizado: cinco entidades do domínio agora implementadas, restam cinco (`pricing_product` até `pricing_sync_run_call`), todas ainda condicionadas à homologação de fonte. Nenhuma fonte cadastrada, nenhuma chamada à JustTCG. Ver `docs/development/HANDOFF-2026-08-16.md`, seção 15, e `docs/log.md`, entrada `feature` de 2026-08-16. |
| 1.53 | **Incremento P3 — Auditoria Operacional de Sincronização executada (2026-08-16, mesma data, ciclo seguinte ao Incremento P2).** `pricing_sync_run` e `pricing_sync_run_call` criadas no Supabase (`CONFIRMADO EXECUTADO`, ambas vazias), validação transacional de 16 itens funcionais confirmada, incluindo validação de performance real (`EXPLAIN ANALYZE` sobre volume sintético) e verificação de ausência de segredos. "Next" atualizado: sete entidades do domínio agora implementadas, restam três (`pricing_product`, `pricing_fx_rate`, `pricing_observation`), todas ainda condicionadas à homologação de fonte. Nenhuma fonte cadastrada, nenhuma chamada à JustTCG. Ver `docs/development/HANDOFF-2026-08-16.md`, seção 16, e `docs/log.md`, entrada `feature` de 2026-08-16. |
| 1.54 | **Incremento P4 — Produto Externo de Pricing executado (2026-08-16, mesma data, ciclo seguinte ao Incremento P3).** `pricing_product` criada no Supabase (`CONFIRMADO EXECUTADO`, vazia), validação transacional de 19 itens funcionais confirmada, incluindo validação de performance real (`EXPLAIN ANALYZE` sobre volume sintético) e verificação de ausência de segredos. "Next" atualizado: oito entidades do domínio agora implementadas, restam duas (`pricing_fx_rate`, `pricing_observation`), ambas ainda condicionadas à homologação de fonte. Nenhuma fonte cadastrada, nenhuma chamada à JustTCG. Ver `docs/development/HANDOFF-2026-08-16.md`, seção 17, e `docs/log.md`, entrada `feature` de 2026-08-16. |
| 1.55 | **Incremento P5 — Série Histórica de Taxas de Câmbio executado (2026-08-16, mesma data, ciclo seguinte ao Incremento P4).** `pricing_fx_rate` criada no Supabase (`CONFIRMADO EXECUTADO`, vazia), validação transacional de 22 itens funcionais confirmada, incluindo validação de performance real (`EXPLAIN ANALYZE` sobre volume sintético de 7.830 linhas) e verificação de ausência de segredos. "Next" atualizado: nove entidades do domínio agora implementadas, resta apenas `pricing_observation`, ainda condicionada à homologação de fonte. Nenhuma fonte cadastrada, nenhuma chamada ao Banco Central nem à JustTCG, nenhuma taxa real cadastrada. Ver `docs/development/HANDOFF-2026-08-16.md`, seção 18, e `docs/log.md`, entrada `feature` de 2026-08-16. |
| 1.56 | **Incremento P6 — Observação Histórica de Preço executado (2026-08-16, mesma data, ciclo seguinte ao Incremento P5) — fundação física de Pricing concluída.** `pricing_observation` criada no Supabase (`CONFIRMADO EXECUTADO`, vazia), com correção obrigatória de identidade market-aware (`UNIQUE NULLS NOT DISTINCT`, exigida pela própria semântica multi-mercado/multi-moeda já aprovada em `ADR-029`), validação transacional de 32 itens funcionais confirmada, incluindo validação de performance real (`EXPLAIN ANALYZE` sobre volume sintético de 80.000 linhas) e verificação de ausência de segredos. "Next" reescrito: dez de dez entidades do domínio agora implementadas — fundação física de Pricing/Market Data completa. Pricing ainda não é operacional (fonte homologada, condições semeadas, mappings reais, sincronização, ingestão PTAX, frontend e Analytics/Item Valuation seguem pendentes). Critério de entrada de Coleções atualizado: fundação física de Pricing satisfeita; início real de Coleções permanece decisão de Fabrício. Nenhuma fonte cadastrada, nenhuma chamada à JustTCG. Ver `docs/development/HANDOFF-2026-08-16.md`, seção 19, e `docs/log.md`, entrada `feature` de 2026-08-16. |
| 1.57 | **Correção pontual da seção "Now" (2026-08-16, mesma data), a pedido explícito de Fabrício — o parágrafo "Pricing / Market Data" e a frase da sequência estratégica em "Now" haviam ficado desatualizados desde antes do Incremento P1, ainda descrevendo "implementação física ainda não iniciada".** Parágrafo reescrito para registrar a fundação física concluída nos Incrementos P1–P6 (dez de dez entidades implementadas, todas vazias), Pricing ainda não operacional, JustTCG pendente (nem aprovada nem reprovada), nenhuma fonte/condição/mapping/produto/preço/taxa real cadastrada, e a regra de "Valor Brasil" corrigida — depende exclusivamente de `pricing_observation.market_scope = 'BRAZIL' AND market_evidence_confirmed = TRUE`, nunca do `default_market_scope` da fonte sozinho. Frase da sequência estratégica corrigida para "Card Variant (concluído) → Pricing com fundação física concluída → Collection como próxima frente comprometida → Analytics/Valuation", preservando a declaração de que o início real de Collection depende de autorização explícita de Fabrício. Seção "Next" não tocada — já estava correta desde a revisão `1.56`. Nenhuma migration, código ou outro documento alterado nesta rodada — escopo exclusivamente este arquivo e `docs/log.md`. |
| 1.58 | **Incremento "Pesquisa Global de Cartas" implementado de ponta a ponta (2026-08-17, ver `ADR-030`).** Frente cross-cutting nova, independente da sequência Card Variant → Pricing → Collection → Analytics — combobox de pesquisa no header (qualquer usuário autenticado) + página `/pesquisa` (filtros de Jogo/Card Set/Categoria/Raridade, estado via URL), servidos por uma projeção segura nova (`public.search_cards()`/`public.search_card_filter_options()`, módulo `4000`–`4999` em `STD-001`) que não amplia o acesso direto ao Catálogo Editorial — `ADR-022` permanece intacto. Adicionado parágrafo em "Now" e bullet em "Concluído"; nova entrada em "Débitos Técnicos Registrados" para a divergência pré-existente de rastreabilidade entre `database/migrations/` e o histórico real do Supabase, descoberta (não causada) por este incremento durante a introspecção obrigatória. Ver `docs/adr/ADR-030-card-search-projection.md`, `docs/standards/STD-001-database-standards.md` (versão `1.27`), `docs/standards/STD-004-frontend-standards.md` (versão `1.4`) e `docs/log.md`. |
| 1.59 | **Correção de escopo — remoção do filtro de Jogo (2026-08-17, mesmo dia, rodada de aceite).** A revisão `1.58` descrevia um filtro de Jogo em `/pesquisa` (URL, select, contrato de `search_cards`/`search_card_filter_options`), nunca aprovado para esta versão — identificado por Fabrício em revisão de aceite. Corrigido sem alterar retroativamente `4010`/`4020`: Queries `4030`/`4031` dropam as assinaturas antigas e recriam ambas as funções sem Jogo. Parágrafos em "Now" e "Concluído" reescritos; performance revalidada com mapeamento individual aos 10 padrões de consulta pedidos (antes descritos de forma agregada como "7 padrões"). Ver `docs/adr/ADR-030-card-search-projection.md` (revisão `1.1`), `docs/standards/STD-001-database-standards.md` (versão `1.28`), `docs/standards/STD-004-frontend-standards.md` (versão `1.5`) e `docs/log.md`. |
| 1.60 | **Pesquisa Global de Cartas formalmente encerrada (2026-08-17, mesmo dia) — validação local aprovada por Fabrício, commit e push realizados.** Depois das quatro rodadas corretivas do mesmo dia (remoção do filtro de Jogo; busca por "número/total"; preview de carta compartilhado com `Cartas`; modo imersivo de pesquisa mobile), Fabrício confirmou em chat que a validação local (UI/teclado/tema/mobile, `npm run lint`/`build`) foi executada e aprovada, e que o commit + push das cinco commits do incremento (`0a0eea2`…`12123d1`) já foram realizados — `git log`/`git status` confirmam árvore de trabalho limpa e branch sincronizado com `origin/main`. Parágrafo em "Now" e bullet em "Concluído" atualizados para remover a linguagem de pendência. Nenhuma migration/código alterado nesta rodada — escopo exclusivamente documental (`ROADMAP.md`, `docs/README.md`, handoff vigente, `docs/log.md`). |
| 1.61 | **Incremento P7 — Homologação Condicionada da JustTCG executado (2026-08-17, ciclo seguinte).** Parágrafos "Now" e "Next" sobre Pricing reescritos: `JUSTTCG` cadastrada em `pricing_source` (Query `3700`, `database/migrations/3700_seed_pricing_source_justtcg.sql`, `CONFIRMADO EXECUTADO`) — prova técnica confirmou 19/19 cartas e 7/7 Card Sets, preços USD/printings/condições/histórico, zero falha técnica, 15 requisições, nenhum segredo persistido. Homologada para piloto técnico de produto/printing/condição/preço USD/histórico (Decisão A aprovada); não homologada para determinar PT-BR nem como preço do mercado brasileiro (Decisão B inconclusiva, idioma das 18 consultas novas `UNDETERMINED`). `is_active = FALSE` — sem consumidor operacional hoje, plano comercial compatível ainda não contratado (Termos de Uso da JustTCG, `justtcg.com/terms`, Seção 6/7.1, restringem uso comercial ao tier pago). Sem alteração de estrutura/RLS/grants/migrations históricas. "Próximo passo" real: contratar plano comercial ou iniciar Collection — decisão de Fabrício. Ver `docs/adr/ADR-029-pricing-domain-model.md`, seção "Incremento P7" (revisão `1.9`), `docs/05f-pricing.md`, mesma seção (versão `1.9`), `docs/standards/STD-001-database-standards.md` versão `1.30` e `docs/log.md`, entrada `feature` de 2026-08-17. |
| 1.62 | **Incremento P12 — Exibição de Preços no Frontend formalmente encerrado (2026-08-18), a pedido explícito de Fabrício.** Consolidação documental — nenhuma alteração de código, banco ou migration nesta rodada (migrations `3903`/`3904` já `CONFIRMADO EXECUTADO` em ciclos anteriores). "Now" ganha parágrafo resumindo os incrementos P8–P12 executados em sequência (2026-08-17→18) e aponta P13 como próximo passo real; "Next" registra P13 — Orquestração Programada de Pricing (agendamento de PTAX/conector JustTCG, ainda não comprometido) como alternativa a iniciar Collection; novo bullet em "Concluído" consolidando objetivo, escopo, migrations, contratos frontend e as 14 evidências aprovadas por Fabrício. Fonte normativa completa: `docs/05f-pricing.md`, seção "Encerramento Formal do Incremento P12" (versão `1.30`), e `docs/adr/ADR-029-pricing-domain-model.md` (revisão `1.23`) — registram explicitamente que PTAX pode ser automatizada, JustTCG permanece inativa até contratação comercial, a visualização de preços nunca chama a JustTCG, e sincronização/consulta seguem desacopladas. Correção de autenticação de `/login` (`AuthApiError: Invalid Refresh Token`, cookie stale) mantida explicitamente fora do escopo deste encerramento. Nenhum commit/push realizado nesta rodada. |
