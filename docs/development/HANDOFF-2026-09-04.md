# Handoff — 2026-09-04

| Campo | Valor |
|--------|-------|
| **Documento** | Handoff de Desenvolvimento |
| **Arquivo** | `docs/development/HANDOFF-2026-09-04.md` |
| **Handoff anterior** | [`HANDOFF-2026-09-02.md`](../history/development/HANDOFF-2026-09-02.md) — superado por este documento; preservado como histórico em `docs/history/development/`. |
| **Sessão encerrada em** | 2026-09-04. `COLLECTIONS-PHYSICAL-INCREMENT-02G-IMPLEMENTATION-01` — **CONFIRMADO EXECUTADO**. Pokémon Catalog Foundation (`pokemon_generation`/`pokemon_species`/`pokemon_species_external_reference`) implementada, aplicada ao Supabase real e validada (18 itens estruturais + 13 testes comportamentais, zero falhas). Em seguida, `COLLECTIONS-POKEDEX-POSITION-PHYSICAL-IMPLEMENTATION-01`/`-CANONICAL-PROMOTION-01` — **CONFIRMADO EXECUTADO E PROMOVIDO**. Fatia A — Canonical Pokédex Foundation (`pokedex`/`pokedex_position`/`pokedex_external_reference`) aplicada ao Supabase real, validada (estrutural+comportamental+privilégios de função, PASS) e promovida para `database/schema/`. |
| **Módulo em desenvolvimento** | Collections — nona fundação física consecutiva concluída. Nenhuma rota/componente de frontend criado. |

**Natureza deste handoff**: documento **operacional**, não normativo — checkpoint de retomada. A narrativa normativa completa mora em `docs/05d-colecoes-e-usuarios.md` (modelo físico) e `docs/domain-modeling/collections/` (modelo conceitual/lógico).

---

## 1. Por que este handoff existe

O handoff anterior (`HANDOFF-2026-09-02.md`) encerrou com `COLLECTIONS-PHYSICAL-INCREMENT-02F` `CLOSED` e o Pokédex/`REFERENCE_POSITION` `CONCEPTUALLY CLOSED` (2026-09-03), mas fisicamente não iniciado. Esta sessão (2026-09-04) executou a primeira fatia física do Pokédex: a fundação de catálogo (Generation/Species/External Reference), sob o mandato `COLLECTIONS-PHYSICAL-INCREMENT-02G-IMPLEMENTATION-01`, precedido de uma auditoria física read-only (`-MODELING-AUDIT-01`, mesma data) que propôs o desenho antes de qualquer execução.

---

## 2. O que foi feito nesta sessão

**`COLLECTIONS-PHYSICAL-INCREMENT-02G-IMPLEMENTATION-01` (2026-09-04)** — Pokémon Catalog Foundation, oitava fundação física de Collections, `CONFIRMADO EXECUTADO`:

- `pokemon_generation` — `id UUID PK`, `code VARCHAR(50) UNIQUE` (`GENERATION_I`, `GENERATION_II`...), `canonical_name`, `ordinal_number INTEGER UNIQUE CHECK > 0`, `is_active`, timestamps. Sem `game_id` — entidade global do universo Pokémon, não do TCG (decisão congelada do mandato). Governança protege `id`/`code`/`ordinal_number`/`created_at` (Queries `6000`/`6001`).
- `pokemon_species` — `id UUID PK`, `generation_id UUID FK → pokemon_generation(id) ON UPDATE RESTRICT ON DELETE RESTRICT`, `national_dex_number INTEGER NOT NULL UNIQUE CHECK > 0` (corrigível administrativamente — **não** protegido por trigger de governança, por decisão explícita do mandato), `canonical_name`, `is_active`, timestamps, índice explícito em `generation_id`. Governança protege só `id`/`created_at` (Queries `6010`/`6011`).
- `pokemon_species_external_reference` — modelada sobre o padrão enxuto de `card_set_external_reference` (sem `language_id`): `pokemon_species_id FK CASCADE`, `asset_source_id FK RESTRICT`, `external_species_id`, `source_url` (check https), `metadata JSONB`, `UNIQUE(pokemon_species_id, asset_source_id)`, `UNIQUE(asset_source_id, external_species_id)`. **RLS completamente fechado** — zero policy, zero grant a `authenticated`/`anon` (decisão congelada do mandato) (Queries `6020`/`6021`).
- Seed `POKEAPI` em `asset_source` (Query `6700`) — PokéAPI representada como fonte de asset, nunca como coluna nova ou string livre.

Todas as 7 migrations aplicadas ao Supabase real (`qjfutqujxrbzgrtkpgkg`) via MCP, uma a uma, todas `{"success":true}`. Validação estrutural de 18 itens (tabelas, colunas, constraints, FKs, índices, triggers, RLS, grants) e comportamental de 13 testes (dentro de `BEGIN/ROLLBACK`, zero resíduo) — ambas ao vivo, zero falhas. `get_advisors(security)` confirmou que os 9 avisos pré-existentes de `function_search_path_mutable` não pertencem às novas funções (todas as 9 trigger functions novas têm `search_path` explícito).

**Divergência de premissa sinalizada, não investigada a fundo (por instrução explícita do mandato)**: `pricing_source` (Queries `3000`–`3002`) está `CONFIRMADO EXECUTADO` — presente no banco real e descrito como tal no texto de `STD-001` — mas **não existe arquivo correspondente em `database/schema/`** (confirmado via Glob, zero match para `300*.sql`). Registrado como débito de documentação do domínio Pricing, separado e não bloqueante para `02G`.

Nenhuma rota, componente ou linha de código de frontend foi criada. Nenhum commit/push foi realizado.

Detalhamento completo (DDL, decisões de modelagem, validação item a item, evidência real) está em `docs/05d-colecoes-e-usuarios.md`, seção "Pokémon Catalog Foundation (02G)" — não duplicado aqui. Resumo cronológico em `docs/log.md` (2026-09-04, entradas `docs` da auditoria e `feature` da implementação).

**`COLLECTIONS-POKEDEX-POSITION-PHYSICAL-IMPLEMENTATION-01` / `-CANONICAL-PROMOTION-01` (2026-09-04)** — Fatia A ("Canonical Pokédex Foundation"), nona fundação física de Collections, `CONFIRMADO EXECUTADO E PROMOVIDO`:

- `pokedex` — tabela raiz (`code`, `canonical_name`, `is_active`, timestamps), triggers normalize/govern/touch_updated_at (Queries `6030`/`6031`).
- `pokedex_position` — `pokedex_id FK CASCADE`, `species_id FK RESTRICT`, `position_number`, triggers govern/touch_updated_at (sem normalize — decisão explícita, sem campo de texto livre) (Queries `6040`/`6041`).
- `pokedex_external_reference` — `pokedex_id FK CASCADE`, `asset_source_id FK RESTRICT`, `external_pokedex_id`, `source_url`, `metadata`, triggers normalize/govern/touch_updated_at (Queries `6050`/`6051`).

As 6 Queries de estrutura aplicadas ao Supabase real na ordem exata `6030`→`6031`→`6040`→`6041`→`6050`→`6051`, sem alteração em relação ao staging v1.1 auditado. Validação estrutural+comportamental+privilégios de função (Query `6800`, executada dentro de `BEGIN...ROLLBACK`) resultou PASS em todas as seções, zero resíduo confirmado (0 linhas nas três tabelas após execução). Advisors sem achado inesperado: INFO `rls_enabled_no_policy` (design pretendido — RLS fechado, zero policy, zero privilégio de cliente) e INFO `unindexed_foreign_keys` em `pokedex_position.species_id` (decisão explícita de não antecipar índice especulativo sem volume real).

Após auditoria pós-implementação externa com resultado PASS, as 6 Queries de estrutura foram promovidas para `database/schema/` — mudança restrita a cabeçalho (Status/Data) e rodapé de cada arquivo; corpo SQL byte-idêntico ao staging aprovado. `6800` **não foi promovida** — permanece em `database/proposals/2026-09-04-pokedex-foundation/` como evidência histórica de validação executada.

Detalhamento completo está em `docs/05d-colecoes-e-usuarios.md`, seção "Collection Pokédex Reference / REFERENCE_POSITION" (Status/Pendências atualizados) — não duplicado aqui. Resumo cronológico em `docs/log.md` (2026-09-04).

---

## 3. Estado físico atual de Collections

Herdado do handoff anterior (sem alteração nesta rodada): `inventory`/`physical_card`, `storage_container`, `collection` (`OPEN_CURATION`/`REFERENCE_BASED`, `completion_policy` `NONE`/`STANDARD_SET`/`MASTER_SET`), `collection_allocation`, `collection_reference`/`collection_card_set_reference`, `collection_master_set_scope` — todos `CONFIRMADO EXECUTADO`, `02F` `CLOSED`. Ver `HANDOFF-2026-09-02.md` (histórico) para o detalhamento completo dessas sete fundações.

Novo nesta rodada:

- `pokemon_generation` / `pokemon_species` / `pokemon_species_external_reference` — **CONFIRMADO EXECUTADO (2026-09-04)**. Fundação de catálogo do universo Pokémon (sem `game_id`), pré-requisito para Pokédex Position/Assignment/Primary Representative. RLS habilitado em todas com zero policy/grant — leitura/escrita administrativa (`internal.write_pokemon_species()`) explicitamente fora deste incremento.
- `pokedex` / `pokedex_position` / `pokedex_external_reference` — **CONFIRMADO EXECUTADO E PROMOVIDO (2026-09-04)**. Fatia A ("Canonical Pokédex Foundation") — as três tabelas existem fisicamente em `database/schema/`, RLS fechado (zero policy, zero privilégio de cliente), 0 linhas cada (nenhum seed, nenhum sourcing PokéAPI iniciado).
- `collection_pokedex_reference` (Fatia B), Card → Primary Species/sourcing (Fatia C), Position Assignment/Primary Representative (Fatia D), REFERENCE_POSITION Completion (Fatia E), pipeline de sourcing (PokéAPI/TCGdex/reconciliação editorial) — modelagem conceitual/lógica `CONCEPTUALLY CLOSED` (2026-09-03), **ainda `PHYSICALLY NOT STARTED`**. `02G` cobriu apenas a fundação de catálogo (Generation/Species/External Reference); Fatia A cobriu apenas o esqueleto do Pokédex (`pokedex`/`pokedex_position`/`pokedex_external_reference`) — nenhuma das duas cobre Position Assignment.

---

## 4. Próximo passo

`COLLECTIONS-PHYSICAL-INCREMENT-02G` e `COLLECTIONS-POKEDEX-POSITION-PHYSICAL-IMPLEMENTATION-01`/`-CANONICAL-PROMOTION-01` — **CONFIRMADO EXECUTADO E VALIDADO (2026-09-04)** — nenhum trabalho pendente nestas duas fatias específicas. **Próximo checkpoint planejado: Fatias B (Collection Pokédex Reference + Adopted Scope), C (Card → Primary Species / sourcing), D (Position Assignment + Primary Representative) e E (REFERENCE_POSITION Completion)** — já modeladas conceitualmente (LDM-175–185, `ADR-011` v1.2), ainda sem modelo físico. Sequência futura preservada: Fatias B/C/D/E → Binder/Layout Foundation → Bulk Collection Operations → UX Read Models/Contracts → UX Flows/Wireframes → Frontend Collections. UX-01 (North Star de produto/frontend de Collections) permanece como alternativa em aberto — nenhuma rota/componente de frontend criado ainda para nenhuma das nove fundações físicas.

Débito de documentação registrado nesta rodada, não bloqueante e fora do escopo de investigação de `02G`: arquivo ausente em `database/schema/` para `pricing_source` (Queries `3000`–`3002`), apesar de `CONFIRMADO EXECUTADO`.

Conflito pré-existente ainda não resolvido, sinalizado sem decisão unilateral: o rótulo de navegação "Coleções" já é usado hoje por `/catalogo/card-sets` (Card Set, editorial) e vai colidir com o nome do domínio Collections quando este ganhar rotas próprias.

---

## Revision History

| Versão | Descrição |
|---------|-----------|
| 1.0 | Criação deste handoff (2026-09-04), sucedendo `HANDOFF-2026-09-02.md` (movido para `docs/history/development/`). Registra `COLLECTIONS-PHYSICAL-INCREMENT-02G-IMPLEMENTATION-01` — Pokémon Catalog Foundation (`pokemon_generation`/`pokemon_species`/`pokemon_species_external_reference`, Queries `6000`–`6021`+`6700`) `CONFIRMADO EXECUTADO`, oitava fundação física de Collections. Registra o gap de documentação de `pricing_source` como débito não bloqueante. Próximo checkpoint: POKEDEX POSITION PHYSICAL MODELING. Ver `docs/log.md` (2026-09-04). |
| 1.1 | **Atualização no mesmo dia (2026-09-04) — `COLLECTIONS-POKEDEX-POSITION-PHYSICAL-IMPLEMENTATION-01`/`-CANONICAL-PROMOTION-01`.** Registra a nona fundação física de Collections, Fatia A ("Canonical Pokédex Foundation"): `pokedex`/`pokedex_position`/`pokedex_external_reference` (Queries `6030`–`6051`) aplicadas ao Supabase real na ordem exata `6030`→`6031`→`6040`→`6041`→`6050`→`6051`, validação estrutural+comportamental+privilégios de função (Query `6800`) PASS em todas as seções, zero resíduo. Após auditoria pós-implementação externa PASS, as 6 Queries de estrutura promovidas para `database/schema/` (mudança restrita a cabeçalho/rodapé; corpo SQL byte-idêntico ao staging); `6800` não promovida, permanece em `database/proposals/2026-09-04-pokedex-foundation/` como evidência histórica. Seções "2. O que foi feito nesta sessão", "3. Estado físico atual de Collections" e "4. Próximo passo" atualizadas — próximo checkpoint passa de "POKEDEX POSITION PHYSICAL MODELING" para as Fatias B/C/D/E. Nenhuma rota/componente de frontend criado; nenhum commit/push realizado. Ver `docs/05d-colecoes-e-usuarios.md` revisão `1.10` e `docs/log.md` (2026-09-04). |
