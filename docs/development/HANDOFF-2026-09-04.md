# Handoff — 2026-09-04

| Campo | Valor |
|--------|-------|
| **Documento** | Handoff de Desenvolvimento |
| **Arquivo** | `docs/development/HANDOFF-2026-09-04.md` |
| **Handoff anterior** | [`HANDOFF-2026-09-02.md`](../history/development/HANDOFF-2026-09-02.md) — superado por este documento; preservado como histórico em `docs/history/development/`. |
| **Sessão encerrada em** | 2026-09-04. `COLLECTIONS-PHYSICAL-INCREMENT-02G-IMPLEMENTATION-01` — **CONFIRMADO EXECUTADO**. Pokémon Catalog Foundation (`pokemon_generation`/`pokemon_species`/`pokemon_species_external_reference`) implementada, aplicada ao Supabase real e validada (18 itens estruturais + 13 testes comportamentais, zero falhas). Em seguida, `COLLECTIONS-POKEDEX-POSITION-PHYSICAL-IMPLEMENTATION-01`/`-CANONICAL-PROMOTION-01` — **CONFIRMADO EXECUTADO E PROMOVIDO**. Fatia A — Canonical Pokédex Foundation (`pokedex`/`pokedex_position`/`pokedex_external_reference`) aplicada ao Supabase real, validada (estrutural+comportamental+privilégios de função, PASS) e promovida para `database/schema/`. Por fim, `POKEMON-REGION-FOUNDATION-PHYSICAL-IMPLEMENTATION-01`/`-CANONICAL-PROMOTION-01` — **CONFIRMADO EXECUTADO E PROMOVIDO. POKÉMON REGION FOUNDATION — CLOSED / IMPLEMENTED / VALIDATED.** `pokemon_region`/`pokemon_region_external_reference`/`pokemon_generation.main_region_id` aplicadas ao Supabase real, validadas (postcheck independente, PASS) e promovidas para `database/schema/`. |
| **Módulo em desenvolvimento** | Collections — **domínio físico Pokédex A–E fechado** (Fatia D `IMPLEMENTED / VALIDATED / CLOSED` e Fatia E `IMPLEMENTED / LIVE / VALIDATED / PERFORMANCE-MEASURED / CLOSED`, ambas 2026-09-06; ver Seções 3 e 4). **Próxima frente: Binder/Layout Foundation — NOT STARTED.** Nenhuma rota/componente de frontend de Collections criado. |

**Natureza deste handoff**: documento **operacional**, não normativo — checkpoint de retomada. A narrativa normativa completa mora em `docs/05d-colecoes-e-usuarios.md` (modelo físico) e `docs/domain-modeling/collections/` (modelo conceitual/lógico).

---

## 1. Por que este handoff existe

O handoff anterior (`HANDOFF-2026-09-02.md`) encerrou com `COLLECTIONS-PHYSICAL-INCREMENT-02F` `CLOSED` e o Pokédex/`REFERENCE_POSITION` `CONCEPTUALLY CLOSED` (2026-09-03), mas fisicamente não iniciado. Esta sessão (2026-09-04) executou a primeira fatia física do Pokédex: a fundação de catálogo (Generation/Species/External Reference), sob o mandato `COLLECTIONS-PHYSICAL-INCREMENT-02G-IMPLEMENTATION-01`, precedido de uma auditoria física read-only (`-MODELING-AUDIT-01`, mesma data) que propôs o desenho antes de qualquer execução.

---

## 2. O que foi feito nesta sessão

**`COLLECTIONS-PHYSICAL-INCREMENT-02G-IMPLEMENTATION-01` (2026-09-04)** — Pokémon Catalog Foundation, oitava fundação física de Collections, `CONFIRMADO EXECUTADO`:

- `pokemon_generation` — `id UUID PK`, `code VARCHAR(50) UNIQUE` (`GENERATION_I`, `GENERATION_II`...), `canonical_name`, `ordinal_number INTEGER UNIQUE CHECK > 0`, `is_active`, timestamps. Sem `game_id` — entidade global do universo Pokémon, não do TCG (decisão congelada do mandato). Governança protege `id`/`code`/`ordinal_number`/`created_at` (Queries `6000`/`6001`).
- `pokemon_species` — `id UUID PK`, `generation_id UUID FK → pokemon_generation(id) ON UPDATE RESTRICT ON DELETE RESTRICT`, `national_dex_number INTEGER NOT NULL UNIQUE CHECK > 0` (corrigível administrativamente — **não** protegido por trigger de governança, por decisão explícita do mandato), `canonical_name`, `is_active`, timestamps, índice explícito em `generation_id`. Governança protege só `id`/`created_at` (Queries `6010`/`6011`).
- `pokemon_species_external_reference` — modelada sobre o padrão enxuto de `card_set_external_reference` (sem `language_id`): `pokemon_species_id FK CASCADE`, `asset_source_id FK RESTRICT`, `external_species_id`, `source_url` (check https), `metadata JSONB`, `UNIQUE(pokemon_species_id, asset_source_id)`, `UNIQUE(asset_source_id, external_species_id)`. **RLS completamente fechado** — zero policy, zero grant a `authenticated`/`anon` (decisão congelada do mandato) (Queries `6020`/`6021`).
- Seed `POKEAPI` em `asset_source` (Query `6700`) — PokéAPI representada como fonte de asset, nunca como coluna nova ou string livre.

Todas as 7 migrations aplicadas ao Supabase real (`qjfutqujxrbzgrtkpgkg`) via MCP, uma a uma, todas `{"success":true}`. Validação estrutural de 18 itens (tabelas, colunas, constraints, FKs, índices, triggers, RLS, grants) e comportamental de 13 testes (dentro de `BEGIN/ROLLBACK`, zero resíduo) — ambas ao vivo, zero falhas. `get_advisors(security)` confirmou que os 9 avisos pré-existentes de `function_search_path_mutable` não pertencem às novas funções (todas as 9 trigger functions novas têm `search_path` explícito).

**Divergência de premissa sinalizada, não investigada a fundo (por instrução explícita do mandato)**: `pricing_source` (Queries `3000`–`3002`) está `CONFIRMADO EXECUTADO` — presente no banco real e descrito como tal no texto de `STD-001` — mas **não existe arquivo correspondente em `database/schema/`** (confirmado via Glob, zero match para `300*.sql`). Registrado como débito de documentação do domínio Pricing, separado e não bloqueante para `02G`.

Nenhuma rota, componente ou linha de código de frontend foi criada. Nenhum commit/push foi realizado.

Detalhamento completo (DDL, decisões de modelagem, validação item a item, evidência real) está em `docs/05d-colecoes-e-usuarios.md`, seção "Pokémon Catalog Foundation (02G)" — não duplicado aqui. Resumo cronológico em `docs/log.md` (2026-09-04, entradas `docs` da auditoria e `feature` da implementação).

**`COLLECTIONS-POKEDEX-POSITION-PHYSICAL-IMPLEMENTATION-01` / `-CANONICAL-PROMOTION-01` (2026-09-04)** — Fatia A ("Canonical Pokédex Foundation"), nona fundação física de Collections, `CONFIRMADO EXECUTADO E PROMOVIDO`:

- `pokedex` — tabela raiz (`code`, `canonical_name`, `is_active`, timestamps), triggers normalize/govern/touch_updated_at (Queries `6030`/`6031`).
- `pokedex_position` — `pokedex_id FK CASCADE`, `species_id FK RESTRICT`, `position_number`, triggers govern/touch_updated_at (sem normalize — decisão explícita, sem campo de texto livre) (Queries `6040`/`6041`).
- `pokedex_external_reference` — `pokedex_id FK CASCADE`, `asset_source_id FK RESTRICT`, `external_pokedex_id`, `source_url`, `metadata`, triggers normalize/govern/touch_updated_at (Queries `6050`/`6051`).

As 6 Queries de estrutura aplicadas ao Supabase real na ordem exata `6030`→`6031`→`6040`→`6041`→`6050`→`6051`, sem alteração em relação ao staging v1.1 auditado. Validação estrutural+comportamental+privilégios de função (Query `6800`, executada dentro de `BEGIN...ROLLBACK`) resultou PASS em todas as seções, zero resíduo confirmado (0 linhas nas três tabelas após execução). Advisors sem achado inesperado: INFO `rls_enabled_no_policy` (design pretendido — RLS fechado, zero policy, zero privilégio de cliente) e INFO `unindexed_foreign_keys` em `pokedex_position.species_id` (decisão explícita de não antecipar índice especulativo sem volume real).

Após auditoria pós-implementação externa com resultado PASS, as 6 Queries de estrutura foram promovidas para `database/schema/` — mudança restrita a cabeçalho (Status/Data) e rodapé de cada arquivo; corpo SQL byte-idêntico ao staging aprovado. `6800` **não foi promovida** — permanece em `database/proposals/2026-09-04-pokedex-foundation/` como evidência histórica de validação executada.

Detalhamento completo está em `docs/05d-colecoes-e-usuarios.md`, seção "Collection Pokédex Reference / REFERENCE_POSITION" (Status/Pendências atualizados) — não duplicado aqui. Resumo cronológico em `docs/log.md` (2026-09-04).

**`POKEMON-REGION-FOUNDATION-PHYSICAL-IMPLEMENTATION-01` / `-CANONICAL-PROMOTION-01` (2026-09-04)** — Pokémon Region Foundation, décima fundação física de Collections, `CONFIRMADO EXECUTADO E PROMOVIDO`. **POKÉMON REGION FOUNDATION — CLOSED / IMPLEMENTED / VALIDATED**:

- `pokemon_region` — tabela raiz (`code`, `canonical_name`, `is_active`, timestamps), sem FK, triggers normalize/govern/touch_updated_at (Queries `6060`/`6061`).
- `pokemon_region_external_reference` — evidência de integração externa por Fonte, mesmo padrão de `pokemon_species_external_reference`/`pokedex_external_reference`, RLS completamente fechado (Queries `6070`/`6071`).
- `pokemon_generation.main_region_id` — `UUID NOT NULL REFERENCES pokemon_region(id) ON UPDATE RESTRICT ON DELETE RESTRICT` (Query `6080`, v1.1), cardinalidade N:1, sem `UNIQUE`/índice dedicado (decisão proporcional ao volume).

Precedida por `POKEMON-REGION-DOMAIN-MODELING-AUDIT-01` (auditoria read-only da PokéAPI, confirmando Region como entidade independente de Generation — Orre/Hisui sem Main Generation) e `POKEMON-REGION-FOUNDATION-PHYSICAL-MODELING-01` (modelagem física read-only), seguidas de staging (`-STAGING-01`) com revisão (`-STAGING-REVISION-01`, GATE 4 — FK explícita `ON UPDATE RESTRICT`, provas ampliadas em `6810`) em `database/proposals/2026-09-04-pokemon-region-foundation/`. As 5 Queries de estrutura aplicadas ao Supabase real na ordem exata `6060`→`6061`→`6070`→`6071`→`6080`; validação estrutural+comportamental+privilégios de função (Query `6810`, `BEGIN...ROLLBACK`) confirmada PASS em todas as seções por postcheck independente (GATE 8) — inclui prova simultânea `confupdtype='r' AND confdeltype='r'`, todas as 5 UNIQUE/CHECK, zero DML de `service_role`, prova comportamental de N:1 — zero resíduo. Advisors: 2 achados INFO novos (`rls_enabled_no_policy` em `pokemon_region`/`pokemon_region_external_reference`, esperado) e 1 achado INFO novo (`unindexed_foreign_keys` em `pokemon_generation_main_region_id_fkey`, aceito por decisão explícita).

Após auditoria pós-implementação externa PASS (GATE 8), as 5 Queries de estrutura foram promovidas para `database/schema/` — mudança restrita a cabeçalho (Status/Data) e rodapé; corpo SQL byte-idêntico ao staging aprovado. `6810` **não foi promovida** — permanece em `database/proposals/2026-09-04-pokemon-region-foundation/` como evidência histórica de validação executada.

Detalhamento completo está em `docs/05d-colecoes-e-usuarios.md`, seção "Collection Pokédex Reference / REFERENCE_POSITION" (subseção "Pokémon Region", Status/Pendências atualizados), `docs/domain-modeling/collections/logical-model.md` (LDM-186 a LDM-190, inteiramente aditivo) e `docs/adr/ADR-011-pokemon-tcg-domain-scope.md` (v1.3) — não duplicado aqui. Resumo cronológico em `docs/log.md` (2026-09-04).

---

## 3. Estado físico atual de Collections

Herdado do handoff anterior (sem alteração nesta rodada): `inventory`/`physical_card`, `storage_container`, `collection` (`OPEN_CURATION`/`REFERENCE_BASED`, `completion_policy` `NONE`/`STANDARD_SET`/`MASTER_SET`), `collection_allocation`, `collection_reference`/`collection_card_set_reference`, `collection_master_set_scope` — todos `CONFIRMADO EXECUTADO`, `02F` `CLOSED`. Ver `HANDOFF-2026-09-02.md` (histórico) para o detalhamento completo dessas sete fundações.

Novo nesta rodada:

- `pokemon_generation` / `pokemon_species` / `pokemon_species_external_reference` — **CONFIRMADO EXECUTADO (2026-09-04)**. Fundação de catálogo do universo Pokémon (sem `game_id`), pré-requisito para Pokédex Position/Assignment/Primary Representative. RLS habilitado em todas com zero policy/grant — leitura/escrita administrativa (`internal.write_pokemon_species()`) explicitamente fora deste incremento.
- `pokedex` / `pokedex_position` / `pokedex_external_reference` — **CONFIRMADO EXECUTADO E PROMOVIDO (2026-09-04)**. Fatia A ("Canonical Pokédex Foundation") — as três tabelas existem fisicamente em `database/schema/`, RLS fechado (zero policy, zero privilégio de cliente). **Populadas desde 2026-09-05** pelo Initial Load via PokéAPI (contagens canônicas em `docs/06a-pokemon-catalog-sourcing.md`).
- `pokemon_region` / `pokemon_region_external_reference` / `pokemon_generation.main_region_id` — **CONFIRMADO EXECUTADO E PROMOVIDO (2026-09-04)**. Pokémon Region Foundation — Region confirmada entidade canônica própria, independente de Generation (Orre/Hisui sem Main Generation); cardinalidade Generation→Main Region N:1. Todas fisicamente em `database/schema/`, 0 linhas cada.
- `pokemon_generation_external_reference` (`6090`/`6091`), run ledger `pokemon_catalog_sourcing_run` + triggers (`6100`/`6101`), hash helper (`6102`), RPCs `open_run`/PLAN/APPLY (`6103`-`6105`), auxiliares `reconcile`/`heartbeat`/`close_failed` (`6106`-`6108`), hotfixes `6109` (runtime `run_code` ambíguo) e `6110` (temporal `NOW()`→`CLOCK_TIMESTAMP()`) — **CONFIRMADO EXECUTADO E PROMOVIDO (2026-09-04)**. **Pokémon Catalog Sourcing foundation** (13 objetos físicos): infraestrutura completa de run lifecycle/PLAN/APPLY para o sourcing de Regions/Generations/Species/National Pokédex via PokéAPI, validada por `6820` v2.3 (16 Seções, PASS integral, zero resíduo) e promovida para `database/schema/`. `6820` não promovido, permanece em `database/proposals/2026-09-04-pokemon-catalog-sourcing/` como evidência histórica. **Correção (2026-09-05): a carga real de dados via PokéAPI já ocorreu** — ver `POKEMON CATALOG SOURCING INITIAL LOAD — IMPLEMENTED / LIVE / SECURED / IDEMPOTENT / CLOSED` no cabeçalho deste handoff e na Seção 4 abaixo; o texto original desta linha (afirmando que o DRY_RUN/APPLY real "ainda NÃO ocorreu") ficou obsoleto já em 2026-09-05 e é corrigido nesta revisão.
- `collection_pokedex_reference` / `collection_pokedex_scope_generation` (Fatia B) — **IMPLEMENTED / VALIDATED / CLOSED (2026-09-05)**, Queries `5085`–`5099`, promovidas para `database/schema/`.
- `card_primary_species` + triggers (`6112`/`6113`), `catalog_admin_action_log` alargada (`2159`), `admin_resolve_card_primary_species()` (`6114`), `resolve_card_primary_species_bulk()` (`6115`), `resolve_card_primary_species_for_catalog_import_job()` (`6116`) — Fatia C, **CONFIRMADO EXECUTADO (2026-09-05)**, promovidas para `database/schema/`; ver Seção 4 abaixo. Dois callers reais integrados (`confirmarImportacao()`/`revalidate-catalog-import-rows`); backfill `5675`/`6435` Cards POKEMON (88,19%), `760` pendentes editoriais. **Fatia C — `CLOSED`.** `02G` cobriu a fundação de catálogo (Generation/Species/External Reference); Fatia A cobriu o esqueleto do Pokédex (`pokedex`/`pokedex_position`/`pokedex_external_reference`); Pokémon Region Foundation cobriu Region/Region External Reference/Main Region; a Pokémon Catalog Sourcing foundation cobriu o mecanismo de run/PLAN/APPLY (já executado, ver acima); Fatia B cobriu `collection_pokedex_reference`/Adopted Scope; Fatia C cobriu a resolução de Card → Primary Species. **Position Assignment e Primary Representative foram materializados na Fatia D — `IMPLEMENTED / VALIDATED / CLOSED` (2026-09-06) — e a completude de `REFERENCE_POSITION` na Fatia E — `IMPLEMENTED / LIVE / VALIDATED / PERFORMANCE-MEASURED / CLOSED` (2026-09-06). Com isso o domínio físico Pokédex A–E está fechado; próxima frente: Binder/Layout Foundation, ainda não iniciada.** Detalhamento nos dois bullets abaixo.
- `collection_pokedex_position_assignment` / `collection_pokedex_position_primary_representative` + 3 triggers de ator/match/imutabilidade, trigger automático de classificação `SPECIES_MATCH` e 4 RPCs (Fatia D, Queries `6117`–`6126`) — **IMPLEMENTED / VALIDATED / CLOSED (2026-09-06)**, promovidas para `database/schema/`. Assignment explícita por Position (nunca automática), `SPECIES_MATCH` vs. `USER_OVERRIDE`, Primary Representative opcional (0..1, sem auto-criação), mover = DELETE+INSERT. 0 linhas em produção — nenhuma Collection Pokédex real criada ainda.
- Ramo `REFERENCE_POSITION` de `collection_completion_summary()` + read model `collection_pokedex_scope_positions()` (Fatia E, Queries `5100`–`5103`) — **IMPLEMENTED / LIVE / VALIDATED / PERFORMANCE-MEASURED / CLOSED tecnicamente (2026-09-06)**, promovidas para `database/schema/` com histórico incremental preservado e não foldado (`5100 → 5102`, `5101 → 5103`). **Sem tabela nova, sem coluna nova, sem índice novo, sem denormalização.** Um BLOCKER de performance real (custo Θ(|Scope| × |Allocations|) contra a Pokédex NATIONAL de 1025 Positions) foi diagnosticado como query-shape e remediado por reordenação de junção; medição final contra as funções live: **13 HEALTHY / 0 ATTENTION / 0 BLOCKER**.

---

## 4. Próximo passo

**`POKEMON REGION FOUNDATION` — `CLOSED / COMMITTED / PUSHED`** (commit `73f7ddd9ff22e9e267a9aba71692a8be273ee89a`). `COLLECTIONS-PHYSICAL-INCREMENT-02G` e `COLLECTIONS-POKEDEX-POSITION-PHYSICAL-IMPLEMENTATION-01`/`-CANONICAL-PROMOTION-01` seguem `CONFIRMADO EXECUTADO E VALIDADO` — nenhum trabalho pendente nestas fatias.

**`POKEMON CATALOG SOURCING INITIAL LOAD` — `IMPLEMENTED / LIVE / SECURED / IDEMPOTENT / CLOSED` (2026-09-05).** A sourcing foundation física do contrato canônico (`docs/06a-pokemon-catalog-sourcing.md`, agora v1.3) foi implementada (13 objetos `6090`-`6110`, `GATE 9 RECONCILED`) e, nesta rodada, a carga real via PokéAPI foi executada no banco de produção: primeiro DRY_RUN (`RUN-20260905-00000101`) e primeiro APPLY (`RUN-20260905-00000121`), ambos `COMPLETED` (`2071` linhas: Regions=11/Generations=9/Species=1025/Pokédex=1/Positions=1025 + xrefs). Achado de segurança residual pós-APPLY (`service_role` com `TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` remanescentes — herança do default ACL de `postgres`, não coberta pelo padrão histórico que visava apenas `anon`/`authenticated`) corrigido pela Query `6111` (REVOKE atômico, escopo restrito às 9 tabelas, sem alterar o default ACL global) — CONFIRMADO EXECUTADO, validado PASS A-E pela Query `6821` v1.1 (revisada antes da execução: PUBLIC provado via `aclexplode`/`grantee=0` em vez de `has_function_privilege('public', ...)`, assinaturas exatas via `regprocedure`, active-run set canônico `PENDING`/`ACQUIRING`/`PLANNING`/`APPLYING`, `search_path` exato `search_path=""`, contagem de triggers com `tgenabled<>'D'`). Idempotência confirmada por um segundo ciclo real: DRY_RUN `RUN-20260905-00000141` (mesmo `snapshot_hash`, 100% `UNCHANGED`) e APPLY `RUN-20260905-00000161` (`0 INSERT`/`0 UPDATE`/`2071 UNCHANGED`). Zero órfão, zero mismatch, zero duplicidade, `0` active runs. `6111` promovido para `database/schema/`; `6821` mantido em `database/proposals/2026-09-05-pokemon-catalog-sourcing-security-hardening/` como evidência histórica; `.pokemon-catalog-sourcing-snapshots/` adicionado ao `.gitignore`; executor Deno incorporado ao repositório sem alteração funcional.

**Fatia B ("Collection Pokédex Reference + Adopted Scope") — IMPLEMENTED / VALIDATED / CLOSED (2026-09-05, `COLLECTIONS-POKEDEX-FATIA-B-PHYSICAL-MODELING-AUDIT-01`/`-REVISION-01` → `-IMPLEMENTATION-01` → `-CANONICAL-PROMOTION-01`).** Décima segunda fundação física de Collections: `collection_pokedex_reference` (subtipo POKEDEX de `collection_reference`) e `collection_pokedex_scope_generation` (Scope `FULL_REFERENCE`/`GENERATION_FILTERED`), Queries `5085`–`5099`, aplicadas ao Supabase real na ordem exata via MCP, validadas por postcheck físico independente PASS (estrutura, triggers, RLS, privilégios, testes funcionais em `BEGIN...ROLLBACK`) e promovidas para `database/schema/` (13 arquivos novos + extensão v1.1 de `5057`, fold-in da Query `5092`); `database/proposals/2026-09-05-collections-pokedex-fatia-b-scope/` mantida integralmente como evidência histórica/auditoria. Inclui a correção REVISION-01 de `completion_policy = 'REFERENCE_POSITION'` (identidade física correta da Collection Pokédex, não `'NONE'`) — o cálculo de completion em si permanece responsabilidade integral de uma futura Fatia E. Ver `docs/05d-colecoes-e-usuarios.md` revisão `1.12`.

**Débito registrado, não corrigido nesta rodada:** privilégios estruturais de `service_role` (`TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN`) ainda presentes nas 5 tabelas legadas de Collection (anteriores ao padrão de fechamento na origem já usado desde a Fatia B) — item separado, não bloqueante.

**Fatia C ("Card → Primary Species / sourcing") — CONFIRMADO EXECUTADO (2026-09-05, `COLLECTIONS-POKEDEX-FATIA-C-PHYSICAL-MODELING-AUDIT-01`/`-REVISION-01` → `-IMPLEMENTATION-01-RESUME` → `-BACKFILL-APPLY-01` → `-INCREMENTAL-INTEGRATION-AUDIT-01`/`-REVISION-01`/`-FINAL-CHECK-01` → `-INCREMENTAL-IMPLEMENTATION-01` → `-CANONICAL-CLOSEOUT-01`).** Décima terceira fundação física de Collections: tabela `card_primary_species` + triggers de categoria/imutabilidade/`updated_at` (Queries `6112`/`6113`), alargamento das CHECKs de `catalog_admin_action_log` para o estado real reconciliado — 29 actions/11 entity_types/11 ramos (`2159`), resolução editorial individual via UPSERT (`admin_resolve_card_primary_species()`, `6114`), resolução automática em lote apenas-INSERT (`resolve_card_primary_species_bulk()`, `6115`, `SERVICE_ROLE ONLY`) e orquestração pós-confirmação de importação (`resolve_card_primary_species_for_catalog_import_job()`, `6116`, filtra `source = 'TCGDEX'`, vínculo exclusivo via `resulting_card_id` — nunca `matched_card_id`). Dois callers reais e não-bloqueantes integrados: `confirmarImportacao()` (`web/app/catalogo/importar-cartas/tcgdex/actions.ts`) e o handler de `supabase/functions/revalidate-catalog-import-rows/index.ts` (reimplantado com sucesso, versão 9, `ACTIVE`). Backfill do catálogo existente: `5675`/`6435` Cards POKEMON resolvidas automaticamente (cobertura inicial 88,19%), `760` pendentes editoriais, executado com guard de divergência e zero resíduo. Postcheck estrutural/segurança e 26 cenários funcionais (`BEGIN...ROLLBACK`) PASS. As 6 Queries promovidas para `database/schema/`; `database/proposals/2026-09-05-fatia-c-card-primary-species/` e `database/proposals/2026-09-05-fatia-c-incremental-integration/` mantidas como evidência histórica. Ver `docs/05d-colecoes-e-usuarios.md` revisão `1.13`.

**Fatias D e E — FECHADAS (2026-09-06).** Fatia D (Position Assignment + Primary Representative, `6117`–`6126`) e Fatia E (REFERENCE_POSITION Completion, `5100`–`5103`) foram implementadas, validadas e promovidas — o domínio físico Pokédex A–E está fechado. A rodada de closeout da Fatia E também registrou, em `docs/05d-colecoes-e-usuarios.md`, a seção **Product / UX Traceability — Pokédex**: memória de produto com os 10 comportamentos já congelados nas Fatias A–E (incluindo que **não existe auto-assignment** — a seleção da Physical Card e a Assignment continuam sendo atos explícitos), sem criar fase nova, sem wireframes, sem componentes e sem antecipar Binder.

**Nenhuma tela/rota de Collections criada até aqui** — todo o trabalho das Fatias A–E é de banco.

**Próximo passo: Binder/Layout Foundation.** Sequência futura preservada: Binder/Layout Foundation → Bulk Collection Operations → UX Read Models/Contracts → UX Flows/Wireframes → Frontend Collections. UX-01 (North Star de produto/frontend de Collections) permanece como alternativa em aberto — nenhuma rota/componente de frontend criado ainda para nenhuma das treze fundações físicas.

Débito de documentação registrado em rodada anterior, não bloqueante e fora do escopo de investigação de `02G`: arquivo ausente em `database/schema/` para `pricing_source` (Queries `3000`–`3002`), apesar de `CONFIRMADO EXECUTADO`.

Conflito pré-existente ainda não resolvido, sinalizado sem decisão unilateral: o rótulo de navegação "Coleções" já é usado hoje por `/catalogo/card-sets` (Card Set, editorial) e vai colidir com o nome do domínio Collections quando este ganhar rotas próprias.

---

## Revision History

| Versão | Descrição |
|---------|-----------|
| 1.0 | Criação deste handoff (2026-09-04), sucedendo `HANDOFF-2026-09-02.md` (movido para `docs/history/development/`). Registra `COLLECTIONS-PHYSICAL-INCREMENT-02G-IMPLEMENTATION-01` — Pokémon Catalog Foundation (`pokemon_generation`/`pokemon_species`/`pokemon_species_external_reference`, Queries `6000`–`6021`+`6700`) `CONFIRMADO EXECUTADO`, oitava fundação física de Collections. Registra o gap de documentação de `pricing_source` como débito não bloqueante. Próximo checkpoint: POKEDEX POSITION PHYSICAL MODELING. Ver `docs/log.md` (2026-09-04). |
| 1.1 | **Atualização no mesmo dia (2026-09-04) — `COLLECTIONS-POKEDEX-POSITION-PHYSICAL-IMPLEMENTATION-01`/`-CANONICAL-PROMOTION-01`.** Registra a nona fundação física de Collections, Fatia A ("Canonical Pokédex Foundation"): `pokedex`/`pokedex_position`/`pokedex_external_reference` (Queries `6030`–`6051`) aplicadas ao Supabase real na ordem exata `6030`→`6031`→`6040`→`6041`→`6050`→`6051`, validação estrutural+comportamental+privilégios de função (Query `6800`) PASS em todas as seções, zero resíduo. Após auditoria pós-implementação externa PASS, as 6 Queries de estrutura promovidas para `database/schema/` (mudança restrita a cabeçalho/rodapé; corpo SQL byte-idêntico ao staging); `6800` não promovida, permanece em `database/proposals/2026-09-04-pokedex-foundation/` como evidência histórica. Seções "2. O que foi feito nesta sessão", "3. Estado físico atual de Collections" e "4. Próximo passo" atualizadas — próximo checkpoint passa de "POKEDEX POSITION PHYSICAL MODELING" para as Fatias B/C/D/E. Nenhuma rota/componente de frontend criado; nenhum commit/push realizado. Ver `docs/05d-colecoes-e-usuarios.md` revisão `1.10` e `docs/log.md` (2026-09-04). |
| 1.2 | **Atualização no mesmo dia (2026-09-04) — `POKEMON-REGION-FOUNDATION-PHYSICAL-IMPLEMENTATION-01`/`-CANONICAL-PROMOTION-01`. POKÉMON REGION FOUNDATION — CLOSED / IMPLEMENTED / VALIDATED.** Registra a décima fundação física de Collections, Pokémon Region Foundation: `pokemon_region`/`pokemon_region_external_reference` (Queries `6060`/`6061`/`6070`/`6071`) e `pokemon_generation.main_region_id` (Query `6080`, N:1, FK `ON UPDATE RESTRICT ON DELETE RESTRICT`) aplicadas ao Supabase real na ordem exata `6060`→`6061`→`6070`→`6071`→`6080`, validação estrutural+comportamental+privilégios de função (Query `6810`) confirmada PASS em todas as seções por postcheck independente, zero resíduo. Region confirmada entidade canônica própria, independente de Generation (Orre/Hisui sem Main Generation). Após auditoria pós-implementação externa PASS (GATE 8), as 5 Queries de estrutura promovidas para `database/schema/` (mudança restrita a cabeçalho/rodapé; corpo SQL byte-idêntico ao staging); `6810` não promovida, permanece em `database/proposals/2026-09-04-pokemon-region-foundation/` como evidência histórica. Seções "2. O que foi feito nesta sessão", "3. Estado físico atual de Collections" e "4. Próximo passo" atualizadas — próximo checkpoint passa a ser retomar `POKEMON-CATALOG-SOURCING-INITIAL-LOAD` incorporando `regions[]` antes de Generations. Nenhuma decisão conceitual/lógica pré-existente reaberta (LDM-186–LDM-190 é bloco inteiramente aditivo); nenhuma rota/componente de frontend criado; nenhum commit/push realizado. Ver `docs/05d-colecoes-e-usuarios.md` revisão `1.11`, `docs/domain-modeling/collections/logical-model.md` (LDM-186–LDM-190), `docs/adr/ADR-011-pokemon-tcg-domain-scope.md` (v1.3) e `docs/log.md` (2026-09-04). |
| 1.3 | **Atualização no mesmo dia (2026-09-04) — `POKEMON-CATALOG-SOURCING-CONTRACT-CANONICALIZATION-01`. POKÉMON REGION FOUNDATION — CLOSED / COMMITTED / PUSHED (commit `73f7ddd9ff22e9e267a9aba71692a8be273ee89a`). POKEMON CATALOG SOURCING CONTRACT — CANONICALIZED / AWAITING COMMIT.** O contrato completo de Pokémon Catalog Sourcing (Regions → Generations + Main Region → Species → National Pokédex → Positions) — fechado em rodadas de chat anteriores (`POKEMON-CATALOG-SOURCING-INITIAL-LOAD-MODELING-AUDIT-01`), nunca antes persistido — foi reconstruído (`-CONTRACT-RECONSTRUCTION-01`, zero contradição contra o HEAD físico real) e canonicalizado em novo documento `docs/06a-pokemon-catalog-sourcing.md`. Seção "4. Próximo passo" reescrita: próximo gate passa a ser `POKEMON-CATALOG-SOURCING-INITIAL-LOAD — GATE 3 STAGING`, autorizável após commit/push documental. Rodada estritamente documental — nenhum SQL, nenhuma alteração de banco, nenhum sourcing iniciado, nenhuma decisão conceitual/lógica pré-existente reaberta. Ver `docs/06a-pokemon-catalog-sourcing.md`, `docs/INDEX.md` (revisão `1.56`), `docs/README.md` e `docs/log.md` (2026-09-04). |
| 1.4 | **Reconciliação pós-commit (2026-09-04), `POKEMON-CATALOG-SOURCING-CONTRACT-POST-COMMIT-CLOSEOUT-01`.** Commit/push documental confirmado no remote (`0e032cbcc2b903a4859838acc98e069f9543588d`, `docs(collections): canonicalize pokemon catalog sourcing contract`) — inclui também as três rodadas de auditoria/revisão do contrato (`REVISION-01`/`02`/`03`) aplicadas antes do commit. Campo "Módulo em desenvolvimento" e Seção "4. Próximo passo" reescritos: **`POKEMON CATALOG SOURCING CONTRACT — CLOSED / COMMITTED / PUSHED`**; próximo checkpoint `GATE 3 — POKEMON CATALOG SOURCING STAGING`, liberável mas dependente de autorização explícita de Fabrício para início. Reconciliação estritamente documental — nenhum SQL, nenhuma alteração de banco, nenhum sourcing iniciado, nenhuma alteração ao contrato técnico. Ver `docs/06a-pokemon-catalog-sourcing.md` (v1.1), `docs/README.md`, `docs/ROADMAP.md`, `docs/INDEX.md` e `docs/log.md` (2026-09-04). |
| 1.5 | **Décima primeira fundação física de Collections CONFIRMADO EXECUTADO E PROMOVIDO (2026-09-04) — `POKEMON-CATALOG-SOURCING-GATE-5-IMPLEMENTATION-01` + hotfixes `6109`/`6110` + `POKEMON-CATALOG-SOURCING-GATE-9-PROMOTION-RECONCILIATION-01`. POKEMON CATALOG SOURCING — GATE 9 RECONCILED.** 13 objetos físicos (`6090`-`6110`: `pokemon_generation_external_reference`+triggers, run ledger `pokemon_catalog_sourcing_run`+triggers, hash helper, RPCs `open`/PLAN/APPLY, auxiliares `reconcile`/`heartbeat`/`close_failed`, hotfix runtime `6109` e hotfix temporal `6110`) aplicados ao Supabase real, validados por `6820` v2.3 (16 Seções, PASS integral, zero resíduo) e por auditorias independentes (GATE 7/GATE 8), e promovidos para `database/schema/` (`6109`/`6110` como migrations próprias). `6820` não promovido, permanece em `database/proposals/2026-09-04-pokemon-catalog-sourcing/` como evidência histórica, com cabeçalho atualizado para `CONFIRMADO EXECUTADO — resultado PASS`. Residual conhecido, não corrigido: `open_pokemon_catalog_sourcing_run` mantém `NOW()` no stale recovery — `KNOWN / ACCEPTED / NON-BLOCKING`. Campo "Módulo em desenvolvimento" e Seção "4. Próximo passo" reescritos — próximo checkpoint passa a ser `POKEMON-CATALOG-SOURCING-INITIAL-LOAD` (carga real via PokéAPI, ainda não executada). Nenhuma decisão conceitual/lógica reaberta; nenhuma rota/componente de frontend criado; nenhum commit/push realizado. Ver `docs/06a-pokemon-catalog-sourcing.md` (v1.2), `docs/README.md`, `docs/ROADMAP.md` (revisão `1.81`), `docs/INDEX.md` (revisão `1.58`) e `docs/log.md` (2026-09-04). |
| 1.6 | **`POKEMON CATALOG SOURCING INITIAL LOAD — IMPLEMENTED / LIVE / SECURED / IDEMPOTENT / CLOSED` (2026-09-05), `POKEMON-CATALOG-SOURCING-INITIAL-LOAD-FINAL-REPOSITORY-RECONCILIATION-01`.** Sourcing real via PokéAPI executado no banco de produção: primeiro DRY_RUN (`RUN-20260905-00000101`)/APPLY (`RUN-20260905-00000121`) reais, ambos `COMPLETED` (`2071` linhas + xrefs). Hardening `6111` (REVOKE de privilégios estruturais residuais de `service_role`, achado pós-APPLY) CONFIRMADO EXECUTADO e validado PASS A-E pela Query `6821` v1.1 (corrigida antes da execução por auditoria externa — GATE 4 físico: PUBLIC via `aclexplode`, assinaturas exatas, active-run set canônico, `search_path` exato, `tgenabled<>'D'`). Idempotência confirmada por segundo ciclo real (`RUN-20260905-00000141`/`RUN-20260905-00000161`, 100% `UNCHANGED`). `6111` promovido para `database/schema/`; `6821` mantido em `database/proposals/2026-09-05-pokemon-catalog-sourcing-security-hardening/` como evidência histórica; `.pokemon-catalog-sourcing-snapshots/` adicionado ao `.gitignore`; executor Deno incorporado sem alteração funcional. Campo "Módulo em desenvolvimento" e Seção "4. Próximo passo" reescritos — **próxima frente do projeto passa a ser Collections, não mais este contrato**. Nenhuma decisão conceitual/lógica reaberta; nenhuma rota/componente de frontend criado; nenhum commit/push realizado. Ver `docs/06a-pokemon-catalog-sourcing.md` (v1.3), `docs/README.md`, `docs/ROADMAP.md` (revisão `1.82`), `docs/INDEX.md` (revisão `1.59`) e `docs/log.md` (2026-09-05). |
| 1.7 | **Fatia B ("Collection Pokédex Reference + Adopted Scope") — IMPLEMENTED / VALIDATED / CLOSED (2026-09-05, `COLLECTIONS-POKEDEX-FATIA-B-PHYSICAL-MODELING-AUDIT-01`/`-REVISION-01` → `-IMPLEMENTATION-01` → `-CANONICAL-PROMOTION-01`).** Registra a décima segunda fundação física de Collections: `collection_pokedex_reference`/`collection_pokedex_scope_generation` (Queries `5085`–`5099`) aplicadas ao Supabase real, validadas por postcheck físico independente PASS e promovidas para `database/schema/` (13 arquivos novos + extensão v1.1 de `5057`, fold-in da Query `5092`); staging mantida integralmente em `database/proposals/2026-09-05-collections-pokedex-fatia-b-scope/` como evidência histórica. Débito de privilégios estruturais de `service_role` nas 5 tabelas legadas de Collection registrado como item separado, não bloqueante, não corrigido nesta rodada. **Corrigida, no mesmo ciclo, a passagem obsoleta da Seção 3 (linha sobre `pokemon_generation_external_reference`/run ledger) que ainda afirmava que a carga real via PokéAPI "ainda NÃO ocorreu"** — desatualizada desde a revisão `1.6` (2026-09-05), quando o Initial Load já havia sido confirmado executado; texto corrigido para refletir o Initial Load como já concluído. Campo "Módulo em desenvolvimento", Seção "3. Estado físico atual de Collections" e Seção "4. Próximo passo" reescritos — **próxima frente do projeto passa a ser Fatia C — Card → Primary Species / sourcing**. Nenhuma decisão conceitual/lógica reaberta; nenhuma rota/componente de frontend criado; nenhum commit/push realizado. Ver `docs/05d-colecoes-e-usuarios.md` revisão `1.12`, `docs/ROADMAP.md` (revisão `1.83`) e `docs/log.md` (2026-09-05). |
| 1.8 | **Fatia C ("Card → Primary Species / sourcing") — CONFIRMADO EXECUTADO (2026-09-05, `COLLECTIONS-POKEDEX-FATIA-C-PHYSICAL-MODELING-AUDIT-01`/`-REVISION-01` → `-IMPLEMENTATION-01-RESUME` → `-BACKFILL-APPLY-01` → `-INCREMENTAL-INTEGRATION-AUDIT-01`/`-REVISION-01`/`-FINAL-CHECK-01` → `-INCREMENTAL-IMPLEMENTATION-01` → `-CANONICAL-CLOSEOUT-01`).** Registra a décima terceira fundação física de Collections: `card_primary_species` + triggers (Queries `6112`/`6113`), alargamento das CHECKs de `catalog_admin_action_log` (`2159`, 29 actions/11 entity_types/11 ramos), `admin_resolve_card_primary_species()` (`6114`), `resolve_card_primary_species_bulk()` (`6115`, `SERVICE_ROLE ONLY`) e `resolve_card_primary_species_for_catalog_import_job()` (`6116`, vínculo exclusivo via `resulting_card_id`, nunca `matched_card_id`) aplicadas ao Supabase real, validadas por postcheck estrutural/segurança e 26 cenários funcionais (`BEGIN...ROLLBACK`) PASS, e promovidas para `database/schema/`; staging mantida integralmente em `database/proposals/2026-09-05-fatia-c-card-primary-species/` e `database/proposals/2026-09-05-fatia-c-incremental-integration/` como evidência histórica. Dois callers reais e não-bloqueantes integrados (`confirmarImportacao()`/`revalidate-catalog-import-rows`, Edge Function reimplantada v9 `ACTIVE`). Backfill do catálogo existente: `5675`/`6435` Cards POKEMON resolvidas automaticamente (cobertura inicial 88,19%), `760` pendentes editoriais. `web/tsconfig.tsbuildinfo` revertido (artefato de build, não pertence ao commit). Campo "Módulo em desenvolvimento", Seção "3. Estado físico atual de Collections" e Seção "4. Próximo passo" reescritos — **próxima frente do projeto passa a ser Fatia D**. Nenhuma decisão conceitual/lógica reaberta; nenhuma rota/componente de frontend criado; nenhum commit/push realizado (Fabrício faz o commit final). Ver `docs/05d-colecoes-e-usuarios.md` revisão `1.13`, `docs/06a-pokemon-catalog-sourcing.md` (v1.4), `docs/ROADMAP.md` (revisão `1.84`) e `docs/log.md` (2026-09-05). |
| 1.9 | **Atualização (2026-09-06) — `COLLECTIONS-POKEDEX-FATIA-D-PROMOTION-CLOSEOUT-01` e a cadeia da Fatia E até `COLLECTIONS-POKEDEX-FATIA-E-FINAL-DOC-CORRECTION-01`.** Seções 3 e 4 atualizadas: **Fatia D — IMPLEMENTED / VALIDATED / CLOSED** (`6117`–`6126`) e **Fatia E — IMPLEMENTED / LIVE / VALIDATED / PERFORMANCE-MEASURED / CLOSED tecnicamente** (`5100`–`5103`, sem tabela/coluna/índice novos; BLOCKER de performance real diagnosticado como query-shape e remediado, medição final 13 HEALTHY / 0 ATTENTION / 0 BLOCKER contra as funções live). Registrada também a seção **Product / UX Traceability — Pokédex** em `05d` e a nota de que **nenhuma tela de Collections foi criada ainda**. "Próximo passo" deixou de apontar Fatia D e passa a ser **Binder/Layout Foundation**, com a sequência macro preservada (Binder/Layout Foundation → Bulk Collection Operations → UX Read Models/Contracts → UX Flows/Wireframes → Frontend Collections). Corrigida ainda a afirmação stale de que `pokedex`/`pokedex_position` estariam com 0 linhas e sem sourcing — o Initial Load via PokéAPI foi executado e fechado em 2026-09-05. Documento operacional; nenhuma decisão normativa nova. Ver `docs/05d-colecoes-e-usuarios.md` revisão `1.16`, `docs/ROADMAP.md` v1.88 e `docs/log.md`. |
| 1.10 | **Correção de stale corrente (2026-09-06, `COLLECTIONS-POKEDEX-FATIA-E-FINAL-DOC-CORRECTION-02`), detectado na auditoria direta final. Nenhuma decisão nova.** A Seção 3 ainda afirmava, em texto corrente, que Position Assignment/Primary Representative (Fatia D) e REFERENCE_POSITION Completion (Fatia E) permaneciam `PHYSICALLY NOT STARTED` e que a Fatia D era o próximo passo — contradizendo os bullets imediatamente posteriores. Trecho substituído pelo estado corrente: Fatia C `CLOSED`; Fatia D `IMPLEMENTED / VALIDATED / CLOSED`; Fatia E `IMPLEMENTED / LIVE / VALIDATED / PERFORMANCE-MEASURED / CLOSED`; domínio físico Pokédex A–E fechado; próxima frente **Binder/Layout Foundation**. Campo "Módulo em desenvolvimento" simplificado para abrir direto no estado corrente, em vez de "Fatia C ...". Nenhuma entrada desta Revision History foi apagada ou reescrita; nenhum SQL executado, nenhum banco alterado. |
