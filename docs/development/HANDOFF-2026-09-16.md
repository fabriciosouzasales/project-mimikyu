# Handoff — 2026-09-16

| Campo | Valor |
|---|---|
| **Documento** | Handoff de desenvolvimento — ponto de retomada |
| **Arquivo** | `docs/development/HANDOFF-2026-09-16.md` |
| **Versão** | 1.2 |
| **Status** | **VIGENTE** |
| **Criado em** | 2026-09-16, em `CARD-VARIANTS — JUMBO INCIDENT / DOCUMENTATION-CLOSEOUT-01` |
| **Sucede** | `HANDOFF-2026-09-09.md`, movido para `docs/history/development/` |
| **NEXT** | **`CARD VARIANTS — HISTORICAL BULK IMPORT`** — etapa restante de `CATALOG-HISTORICAL-BOOTSTRAP-03`. A **`CARD-VARIANTS — EDITORIAL CONVERGENCE` está `CLOSED` (2026-09-18)**: SVE, SV5, BASEP e BASE3 fechados; BASE1 sem pendência editorial. Universo elegível **169 Card Sets / 16.705 Cards** (170 com Cards e zero Variants, menos `ME5.5`, fora do denominador operacional). Baseline **datado, não contrato rígido** — remedir antes de executar. Nada inicia sem mandato explícito de Fabrício. |
| **Pendência operacional imediata** | **Commit + Push** do closeout documental de BASEP + BASE3 pela interface do GitHub. Os estados **não se confundem**: **BASEP e BASE3 técnicos = `EXECUTADO / LIVE VALIDATED`** (migration `2201` `20260918011647`; `2202` executada direto no SQL Editor; runners e harness `2829` PRE/POST 11/11 PASS) · **closeout documental = `PENDING COMMIT + PUSH` nesta rodada.** |

---

## 1. O que fechou nesta rodada

### `CATALOG-HISTORICAL-BOOTSTRAP-03` → **`JUMBO SIZE-SCOPE INCIDENT` — CLOSED tecnicamente**

O incidente JUMBO está **tecnicamente fechado**. Isso **não** fecha
`CATALOG-HISTORICAL-BOOTSTRAP-03` como frente, nem a Editorial Convergence — fecha o
eixo de **escopo por tamanho** que os bloqueava com dado sujo.

**Causa raiz.** A fonte TCGdex modela `size` no objeto de variante (`size: "jumbo"`).
O extractor histórico da Edge `import-card-variants` lia apenas
`type`/`foil`/`subtype`/`stamp` e **descartava `size`**. Uma variante JUMBO podia ser
absorvida pelo fluxo normal como se fosse sua gêmea STANDARD.

**Semântica final, fechada nos três pontos (SQL, Edge, UI):**

| `size` normalizado | Classificação | Efeito |
|---|---|---|
| ausente / `null` / branco / `STANDARD` | `IN_SCOPE` | fluxo legado, **bit a bit** |
| `JUMBO` | `OUT_OF_SCOPE` | decisão automática do sistema |
| outro valor não vazio | `UNSUPPORTED` | **fail-closed**, revisão humana |

`size` **permanece fora da identidade residual**; o guard precede `routePrinting` e o
dedupe, e a linha fora de escopo dedupa em espaço próprio.

**Estado de uma linha JUMBO** — staging `INVALID / NEW / SKIPPED / PENDING` com
`normalized_data.skip_reason = 'SIZE_OUT_OF_SCOPE'`; após confirmação
`INVALID / NEW / SKIPPED / UNCHANGED`. **Nunca materializa `card_variant`.**

### `CARD-VARIANTS — EDITORIAL CONVERGENCE` → **`SV5` CLOSED** (2026-09-16)

**Segundo Card Set a fechar** — e o **primeiro a fechar por decisão consciente sobre os
resíduos, não por resolução de todos eles.**

| Job | Papel | Estado |
|---|---|---|
| `601f7c96-8118-4b97-a0f3-cbbf418e8c21` | **canônico** — carrega as decisões editoriais | `COMPLETED` · 428 total · 417 `VALID` · **11 `NEEDS_REVIEW` preservadas** · 416 `APPROVED/INSERTED` · 12 `SKIPPED/UNCHANGED` · 0 `PENDING` · 0 `FAILED` |
| `e575fed9-055c-4a35-b661-dea1f627033c` | **validação de UI** — evidência de teste, **sem impacto no catálogo** | `CANCELLED` · 432 rows preservadas |

**Não há job `STAGED` de SV5.** Os dois jobs têm papéis distintos e nenhum substitui o
outro.

**As decisões comprovadas foram materializadas; as não comprovadas foram DEFERIDAS.** As
12 deferidas são 11 variantes sem evidência externa suficiente (Championships
Europe/North America/Latin America nas faixas Staff / Top 8 / Champion, Master Ball League
base e Judge) mais **`POKEMON_DAY_COSMOS_HOLO`** — `VALID`, porém deferida por falta de
prova específica do acabamento COSMOS.

> **`DEFERRED` não é rejeição e não é conclusão semântica futura.** É o registro honesto
> de que a variante foi analisada, a evidência não bastou, e a decisão foi não catalogar
> agora. Também **não é pendência** — nenhum contador de pendência a inclui — e **não é
> erro**. A variante volta ao fluxo canônico assim que houver evidência nova.

**Correção Master Ball / COSMOS.** O mapping SCOPED `sv05 · NORMAL + {MASTER-BALL-LEAGUE}
→ MASTER_BALL_LEAGUE_COSMOS_HOLO` afirmava acabamento sem lastro: a pesquisa externa foi
encerrada como **`EVIDENCE EXHAUSTED`**, a única atestação de "Cosmos Holo" pertencia a
**outro produto** (Play! Prize Pack Series), e o corpus já tinha interpretação anterior e
**diferente** do mesmo selo (GLOBAL `holo + {master-ball-league} → MASTER_BALL_HOLO`).
Migration **`2200` v1.1** (LIVE `20260916212542`) removeu o mapping, devolveu a linha a
`NEEDS_REVIEW` e recalculou `valid_rows`; o VT foi **desativado** pela RPC canônica
`admin_deactivate_card_variant_type` — **não removido**, porque ausência de prova não é
prova de ausência — e **nenhum `card_variant` chegou a ser materializado por ele**.
Registro de honestidade: a v1.0 **abortou** por defeito do próprio guard `G22`; o rollback
foi **TOTAL**, provado por fingerprint e pelo `updated_at` original da linha.

**UI.** A semântica de `DEFERRED` foi fechada no contrato único
(`web/lib/catalogo/variant-size-scope.ts`), distinguindo deferimento editorial
(`NEW + SKIPPED + IN_SCOPE`) de variante **já existente** (`MATCHED + SKIPPED`) e de
**JUMBO** (`OUT_OF_SCOPE + SKIPPED`). As três usam `SKIPPED` — confundi-las produziu duas
regressões reais, ambas corrigidas com testes dedicados (`variant-size-scope.test.ts`,
15/15). **Validação visual em produção = PASS.** Commits `41fbe40` e `ee5b17a`.

---

### `CARD-VARIANTS — EDITORIAL CONVERGENCE` → **`BASEP` e `BASE3` CLOSED; a frente CLOSED** (2026-09-18)

**Terceiro e quarto Card Sets — e o encerramento da frente.**

| | BASEP | BASE3 |
|---|---|---|
| job | `cf829d56-921c-4e97-983d-0aec56690464` | `d5b7a148-0459-40fd-a32f-13fdcb845026` |
| status | `COMPLETED` | `COMPLETED` |
| linhas | 74 · 72 `VALID` · **2 `NEEDS_REVIEW` deferidas** | 177 · **177 `VALID`** · **0 `NEEDS_REVIEW`** |
| decisão / persistência | 72 `APPROVED`+`INSERTED` · 2 `SKIPPED`+`UNCHANGED` | **177 `APPROVED`+`INSERTED`** · 0 `SKIPPED` · 0 `REJECTED` · 0 `UNCHANGED` · 0 `FAILED` |
| cobertura | **53/53** Cards (72 variantes) | **62/62** Cards (177 variantes) |

`card_variant` **7.469 → 7.671**. Taxonomia terminal: 97 `card_variant_type` · **94**
`card_variant_type_external_mapping` (**75 GLOBAL** · 8 `sv05` · 6 `basep` · 5 `base3`) ·
9 `card_printing_trait` · 11 `card_printing_profile` · 10 `card_printing_external_mapping`.

**BASE3 é o único Set da frente a fechar com zero deferimento.** Objetos novos:
`PRERELEASE_HOLO` (95), `PRERELEASE_COSMOS_HOLO` (96), `W_PROMO_STAMPED` (97), trait
`EVOLUTION_BOX_ERROR` (9) e profile `EVOLUTION_BOX_ERROR` (11); mappings: 3 SCOPED `base3`,
1 GLOBAL (`{WOTC}`) e 1 Printing (`subtype:evolution-box-error`).

**Artefatos (`database/proposals/2026-09-18-base3-editorial-convergence/`):** `2202`
(executada — ver §6 para a divergência de ledger), `2829_base3_collision_gate.sql`
(**PRE 11/11 PASS · POST 11/11 PASS**), `base3-authenticated-runner.js` (DRY_RUN PASS +
LIVE PASS) e `README.md`. Para BASEP: `2201` (migration `20260918011647`),
`basep-authenticated-runner.js` e `README.md`.

**BASEP e BASE3 foram laboratório editorial** para a campanha histórica de Variants em
escala — não fins em si. Narrativa completa em `05b-cartas-e-raridade.md`, revisão `1.4`.

### Migrations LIVE

| Query | Objeto | Migration | md5 LIVE |
|---|---|---|---|
| `2198` | `internal.compute_variant_residual_signature` — size guard no ponto único de routing | `20260916012057` | `1c5b8352ab53b1e6d4f1c08537824a4b` |
| `2199` | `public.admin_decide_catalog_variant_import_row` — GUARD 7, imutabilidade da decisão `SIZE_OUT_OF_SCOPE` | `20260916170733` | `bc2e5f378a95e02fbcc8b10819451f52` |

`2199` aplicada com **delta de dados = ZERO** (14 fingerprints idênticos).

### Gates

| Gate | Resultado |
|---|---|
| `2827` v1.1.1 | **29 PASS / 0 FAIL**, zero resíduo |
| `2828` v2.0 | **`gate_state = COMPLETE`** — estrutural 18/0; E2E autenticado 5 PASS / 0 PENDING / 0 FAIL |
| E2E B1–B5 (sessão admin real, navegador) | **PASS integral**; fixture removida com **zero resíduo** |
| Suíte lógica Deno canônica | **Deno 2.9.3 — 81 casos / 81 PASS / 0 FAIL** |
| `typecheck` web · `git diff --check` | PASS · PASS |

`S1.15` teve prova **não-vácua** (`linhas_marcadas >= 1`, `divergentes = 0`) primeiro
com a fixture E2E e depois com a população produtiva real do rerun de BASE1.

### Edge

| Item | Valor |
|---|---|
| `import-card-variants` | **v11** · `ACTIVE` · `verify_jwt = true` |
| `ezbr_sha256` | `751ac62dd99baea63d84ec755726e82e1d6829d178ce0f51f6ca02ca1cf0aa4a` |

### BASE1 — job histórico preservado, rerun real executado

**Histórico `af230406-9055-4df3-ac17-b837a4865e76`**: **415 rows intactas**;
`STAGED → CANCELLED` **apenas** para liberar o escopo. Fingerprint integral idêntico
antes/depois (`c41957c0ca10a6666aee76918ab8ee12`) — nenhuma row removida ou alterada.

**Rerun `c4d185ad-4971-4be6-af10-0b3e66466536`**: staging 415 total · 411 `VALID` ·
4 `INVALID` · **0 `NEEDS_REVIEW`** · 4 `SIZE_OUT_OF_SCOPE`; após confirmação
`COMPLETED` · inserted 0 · **unchanged 415** · failed 0 · skipped 4.
`card_variant` permaneceu **7461**.

**Descoberta final: existem QUATRO JUMBO em BASE1, não três.**

| Card | Diagnóstico histórico |
|---|---|
| Bulbasaur **#044** | aparecia como `NEEDS_REVIEW` por `NORMAL` + `PIKACHU` |
| Charmander **#046** | idem |
| Squirtle **#063** | idem |
| **Pikachu #058** | `raw` real = `size=jumbo`, `normal`, **sem stamp/subtype** — como `size` era descartado, foi **silenciosamente absorvido como variante válida** |

Pikachu #058 prova a segunda face do incidente: ele não causava apenas **falsa
pendência editorial**, causava também **falsa classificação válida**.

---

## 2. Frontend — READY, **ainda não publicado**

O frontend em produção (commit anterior) exibiu, após a confirmação do rerun:
*"4 sem mapeamento"*, *"4 a revisar"*, *"Concluído com pendências"* e o botão
*"Resolver mapeamentos pendentes"*. **Causa**: `totalRows - validRows` usado como
proxy de pendência editorial — verdadeiro antes do incidente, falso depois que
`INVALID` ganhou um segundo significado.

O working tree corrige integralmente: `mappingPendingRows` / `unsupportedSizeRows` /
`outOfScopeRows` separados e derivados dos **marcadores** (`skip_reason` /
`review_reason`), não de subtração; JUMBO visível como **"Fora de escopo"** (tom
`muted`, nunca `danger`); nenhuma ação editorial ou de mapeamento; checkbox
bloqueado; job apenas com OUT_OF_SCOPE classificado como **sucesso**; contador
explícito "Fora de escopo"; zeros de mapping não renderizados.

> **Código `READY / INCLUDED IN CLOSEOUT` — NÃO publicado.** A Vercel segue servindo o
> commit anterior; a publicação ocorre no Commit + Push. **A validação visual
> pós-deploy permanece como último postcheck operacional**, ainda não realizada.
> Nada aqui afirma que a UI corrigida já foi validada em produção.

---

## 3. Próximos passos

> **Documentar a sequência NÃO autoriza a sua execução.** Cada frente abaixo depende
> de mandato explícito de Fabrício.

### 3.1 Imediato  *(atualizado em 2026-09-18)*

1. **Commit + Push** do **closeout consolidado BASEP + BASE3 / Editorial Convergence**
   pela interface do GitHub — 7 `.md` tracked alterados + as duas pastas de proposta
   untracked (`database/proposals/2026-09-18-basep-editorial-convergence/` e
   `.../2026-09-18-base3-editorial-convergence/`).

> **O closeout documental de SV5 deixou de ser a pendência imediata** — foi absorvido
> pelas rodadas seguintes e não é mais o item corrente. As duas linhas de SV5 abaixo ficam
> como registro do estado de versionamento daquela rodada.

**Estado de versionamento — estados distintos, não confundir:**

| O quê | Estado |
|---|---|
| **BASEP e BASE3 técnicos** — `2201` (`20260918011647`), `2202` (SQL Editor), runners e harness `2829`, jobs `COMPLETED` | **`EXECUTADO / LIVE VALIDATED`** |
| **Closeout documental BASEP + BASE3** — os `.md` desta rodada, incluindo este handoff | **`PENDING COMMIT + PUSH`** — é exatamente o item 1 acima |
| *(registro anterior)* **SV5 técnico** — migration `2200` v1.1, correção da UI, suíte de testes | **`COMMITTED / PUSHED`** (`b11fb0b`, `41fbe40`, `ee5b17a`) |
| *(registro anterior)* **Closeout documental de SV5** | superado — não é mais pendência corrente |

**Encerrado nesta data:** o Commit + Push do **incidente JUMBO e da parte técnica** do
fechamento de SV5 (`b11fb0b`, `41fbe40`, `ee5b17a`) e a **validação visual pós-deploy**,
que fechou **PASS** — era o último postcheck operacional em aberto da frente JUMBO.
**O closeout documental não está incluído nesse "encerrado"** — ele segue pendente.

### 3.2 Retomada — `CARD VARIANTS — HISTORICAL BULK IMPORT`  *(atualizado em 2026-09-18)*

> **Superado em 2026-09-18.** O conteúdo original desta seção apontava `BASEP` e depois
> `BASE3` como retomada. **As duas fecharam**, e com elas a Editorial Convergence. A
> retomada passa a ser **`CARD VARIANTS — HISTORICAL BULK IMPORT`**: importar Variants em
> lote para os **169 Card Sets elegíveis** (**16.705** Cards), reutilizando a taxonomia e
> os mappings estabilizados, materializando automaticamente o conhecido e deixando em
> `NEEDS_REVIEW` **apenas resíduo semântico real** — sem tratamento artesanal Set a Set.
> `ME5.5` está **fora do denominador operacional**. Sequência macro: esta campanha →
> `VARIANT-DISPLAY-SEMANTICS-01` → `BOOTSTRAP-03` closeout →
> `CATALOG-VARIANT-DEFAULT-BACKFILL-01` → gate final de Card Variants → Collections.
> O texto original segue preservado abaixo como registro histórico.

| Card Set | Estado | Observação |
|---|---|---|
| **SVE** | **CLOSED** | primeiro Set a zerar resíduos |
| **BASE1** | **0 `NEEDS_REVIEW`** | fechado pela correção JUMBO — os antigos "3 resíduos" eram 3 dos 4 JUMBO, nunca pendência editorial |
| **SV5** | **CLOSED** (2026-09-16) | fechou **com 12 variantes deliberadamente `DEFERRED`**, não por resolução de todas — ver §1 |
| **BASEP** | **27 `NEEDS_REVIEW`** | permanece na frente, não é pulada |
| **BASE3** | **5 resíduos ambíguos** | **manter para etapa posterior** — os mais ambíguos do corpus; dois dependem de decisões transversais que ainda não existem |

**Próximo foco da frente: `BASEP` (27).** **Depois de `BASEP`, e somente depois: `BASE3` (5)** — as duas
frentes são **sequenciais**, não paralelas nem equivalentes. **Nenhuma dessas frentes começa agora.**

### 3.3 Depois da campanha histórica de Variants  *(atualizado em 2026-09-18)*

> **Corrigido em 2026-09-18.** O texto anterior mandava "retomar/completar
> `BOOTSTRAP-03` (Cards + Assets)". **Isso está superado**: Cards e Assets históricos já
> foram majoritariamente carregados, e os resíduos são
> `KNOWN SOURCE-COVERAGE RESIDUALS / NON-BLOCKING` — **não são frente, não são retomada**.

Sequência macro congelada:

```
CARD VARIANTS — HISTORICAL BULK IMPORT
  → VARIANT-DISPLAY-SEMANTICS-01
  → CATALOG-HISTORICAL-BOOTSTRAP-03 closeout
  → CATALOG-VARIANT-DEFAULT-BACKFILL-01
  → gate final de Card Variants
  → Collections
      BULK-04 → BULK-05 → BULK-06
      → UX Read Models/Contracts
      → UX Flows/Wireframes
      → Frontend Collections
```

---

## 4. Estado limpo esperado (working tree)

### 4.0 Estado corrente — rodada de 2026-09-18 *(BASEP + BASE3 / Editorial Convergence)*

**Modificados — 7, todos documentação:**

```
docs/05b-cartas-e-raridade.md
docs/05d-colecoes-e-usuarios.md
docs/INDEX.md
docs/README.md
docs/ROADMAP.md
docs/development/HANDOFF-2026-09-16.md
docs/log.md
```

**Novos (2 diretórios untracked):**

```
database/proposals/2026-09-18-basep-editorial-convergence/
database/proposals/2026-09-18-base3-editorial-convergence/
```

**Nenhuma alteração funcional pendente** — zero arquivo de `web/`, `supabase/functions/`
ou `database/schema/` modificado. `git diff --check` limpo.

---

### 4.1 Snapshot histórico — working tree da frente JUMBO *(2026-09-16, NÃO é estado corrente)*

> As listas abaixo descrevem o working tree **daquela** rodada e estão **fechadas**
> (`COMMITTED / PUSHED` em `b11fb0b`, `41fbe40`, `ee5b17a`). Preservadas como registro.

**Modificados (7):**

```
supabase/functions/import-card-variants/index.ts
supabase/functions/import-card-variants/services/github-source.ts
supabase/functions/import-card-variants/types.ts
web/app/catalogo/importar-variantes/actions.ts
web/components/catalogo/importar-variantes-view.tsx
web/components/catalogo/revisao-importacao-variantes-table.tsx
web/lib/catalogo/queries.ts
```

**Novos (3 + 1 diretório):**

```
supabase/functions/import-card-variants/services/size-scope.ts
supabase/functions/import-card-variants/services/size-scope.test.ts
web/lib/catalogo/variant-size-scope.ts
database/proposals/2026-09-15-card-variant-size-scope-guard/
```

Mais a documentação desta rodada (`docs/`). `web/tsconfig.tsbuildinfo` é artefato de
build e **não** entra — se aparecer modificado após um `tsc`, reverter.

**Nada foi promovido para `database/schema/` ou `database/migrations/` nesta frente.**
`2198`/`2199` são migrations incrementais sobre funções cujo canônico é `2176` e
`2163`; a promoção canônica, se desejada, é rodada própria e ainda **não autorizada**.

---

## 5. Contratos que precisam sobreviver a esta rodada

1. **`size` não é identidade.** Nunca entra na assinatura residual. É **estado de
   escopo** — por isso a propagação de mapping (Query 2193) não o filtra.
2. **`SIZE_OUT_OF_SCOPE` é decisão do sistema, não editorial.** `2199`/GUARD 7 recusa
   `APPROVED`/`REJECTED`/`PENDING` sobre a linha e aceita `SKIPPED` (no-op
   idempotente). A UI trava a linha; a garantia real é server-side.
3. **`UNSUPPORTED_SIZE_VALUE` é pendência HUMANA, não de mapeamento.** Não existe
   "mapeamento de tamanho"; oferecer resolução por mapping converteria decisão de
   escopo em tradução de vocabulário.
4. **`INVALID` deixou de ser sinônimo de erro.** Qualquer contador futuro que derive
   pendência de `totalRows - validRows` reintroduz o defeito — contar pelos
   **marcadores**.
5. **`validation_status = INVALID` é reservado** para fora de escopo por tamanho. Não
   reutilizar para outra semântica sem reabrir esta decisão.

---

## 6. Pendências e dívidas preservadas (não bloqueantes)

> **Novas nesta revisão (2026-09-18).**
>
> - **`2202` executada sem linha no ledger.** O trait `EVOLUTION_BOX_ERROR` está LIVE
>   (`created_at` 2026-09-18 02:51:27 UTC), mas **não há registro correspondente em
>   `supabase_migrations.schema_migrations`** — a Query foi executada direto no SQL Editor,
>   diferente de `2198`/`2199`/`2200`/`2201`, que passaram por `apply_migration`. É a mesma
>   classe da dívida já registrada no ROADMAP ("Divergência de rastreabilidade entre
>   `database/migrations/` e o histórico real do Supabase"). Registrado, **não mascarado**.
> - **`KNOWN SOURCE-COVERAGE RESIDUALS / NON-BLOCKING`:** os **4 Card Sets sem Cards**
>   (`CEL25CC`, `EXU`, `RC`, `SP`) e os **Assets residuais**. Já foi concluído que a TCGdex
>   **não oferece hoje** os dados faltantes. **Não são frente atual, não são próximo passo**
>   e não devem ser registrados como gap da campanha histórica.
> - **`ME5.5 — Celebração de 30 Anos` fora do denominador operacional.** Lançamento muito
>   recente, cadastrada manualmente, fonte ainda incompleta (LIVE: 2 Cards / 0 Variants).
>   Continua no catálogo total; **não** conta como pendência nem como Set elegível.
> - **As 2 variantes `DEFERRED` de BASEP** — `holo + {pikachu-tail}`
>   (`SOURCE_CONTRADICTION`) e `holo + subtype:missing-hp` (`SOURCE_OMISSION`). Somadas às
>   12 de SV5: **14 deferidas no total**, todas reabríveis pelo fluxo canônico.
> - **Limitação de domínio aceita em BASE3:** a identidade do Zapdos Cosmos ©1999 fica
>   `COSMOS_HOLO + NULL` — o modelo não distingue essa tiragem de uma Cosmos "neutra".
>   Inambíguo hoje; análogo ao caso de idioma de `STANDARD_PIKACHU_WORLD_2000`.

- ~~Validação visual pós-deploy da UI (seção 3.1)~~ — **ENCERRADA em 2026-09-16, PASS.**
- **SV5 — 12 variantes `DEFERRED`.** Não é dívida técnica nem pendência: é registro de
  pergunta em aberto, reabrível pelo fluxo canônico quando houver evidência. Fica aqui
  para que o estado não seja relido como esquecimento.
- **5 mappings SCOPED `sv05` nunca auditados contra evidência externa** (`EBGAMES_HOLO`,
  `GAMESTOP_HOLO`, `PLAYER_REWARD_REVERSE`, `POKEMON_DAY_COSMOS_HOLO`,
  `SET_LOGO_COSMOS_HOLO`). Dois afirmam COSMOS — mesma classe de afirmação que a
  `2200` revogou no Master Ball. Sem contradição concreta medida; registrado como
  lacuna, não como defeito.
- ~~`BASE3` — 5 resíduos ambíguos.~~ **SUPERADO em 2026-09-18: `BASE3` está `CLOSED` / `COMPLETED` / 177 `VALID` / 0 `NEEDS_REVIEW`.** A limitação de domínio do Zapdos Cosmos ©1999 continua válida e está registrada em separado, acima.
- Dívidas herdadas do handoff anterior seguem válidas: `D1`, `D2`, 6 erros de typing
  Pricing/JustTCG, `D-1` do consumidor 2, frontend `B-5`, privilégios estruturais de
  `service_role` nas 5 tabelas legadas de Collection.
- Promoção canônica de `2198`/`2199` para `database/schema/` — não feita, não
  autorizada.

---

## 7. Onde está a evidência

| O quê | Onde |
|---|---|
| Migrations, harnesses, README de fechamento (§9) | `database/proposals/2026-09-15-card-variant-size-scope-guard/` |
| Contrato de escopo na Edge | `supabase/functions/import-card-variants/services/size-scope.ts` |
| Contrato de escopo na UI | `web/lib/catalogo/variant-size-scope.ts` |
| Suíte lógica (81 casos A–J) | `supabase/functions/import-card-variants/services/size-scope.test.ts` |
| Detalhamento normativo | `docs/05b-cartas-e-raridade.md`, seções "Eixo de escopo por tamanho (incidente JUMBO)", "SV5 — **CLOSED** dentro da Editorial Convergence" e "Deferimento editorial (`DEFERRED`)" |
| Correção Master Ball / COSMOS (`2200` v1.1) | `database/proposals/2026-09-16-sv5-master-ball-mapping-correction/` |
| Contrato de `DEFERRED` na UI + suíte de regressão | `web/lib/catalogo/variant-size-scope.ts` · `web/lib/catalogo/variant-size-scope.test.ts` |

---

## Revision History

| Versão | Descrição |
|---------|-----------|
| 1.0 | **Criação (2026-09-16), em `CARD-VARIANTS — JUMBO INCIDENT / DOCUMENTATION-CLOSEOUT-01`.** Sucede `HANDOFF-2026-09-09.md`, movido para `docs/history/development/`. Registra o fechamento técnico do incidente JUMBO: causa raiz (`size` descartado pelo extractor), semântica final de três ramos com `size` fora da identidade residual, `2198` e `2199` LIVE, harnesses `2827` 29/29 e `2828` `COMPLETE` (18 estruturais + 5 E2E autenticados), Edge v11 `ACTIVE`, rerun real de BASE1 (`0 NEEDS_REVIEW`, 4 JUMBO, `card_variant` 7461 inalterado) e o job histórico preservado com 415 rows. Registra a descoberta de que são **quatro** JUMBO — Pikachu #058 provando que o incidente também produzia **falsa classificação válida**, não só falsa pendência. Frontend registrado como **READY / INCLUDED IN CLOSEOUT, não publicado**, com a validação visual pós-deploy como último postcheck em aberto. NEXT: Editorial Convergence com SVE `CLOSED`, BASE1 `0`, SV5 10, BASEP 27, BASE3 5 — nenhuma autorizada a iniciar. |
| 1.1 | **`SV5` `CLOSED` dentro da Editorial Convergence (2026-09-16, `SV5-EDITORIAL-CONVERGENCE-CLOSEOUT-01`).** Seção nova em §1 registrando o **segundo Card Set a fechar** e o primeiro a fechar **por decisão consciente sobre os resíduos**: job canônico `601f7c96` `COMPLETED` (428 / 417 `VALID` / 11 `NEEDS_REVIEW` preservadas / 416 `APPROVED/INSERTED` / 12 `SKIPPED/UNCHANGED` / 0 `PENDING` / 0 `FAILED`), **sem job `STAGED` de SV5**, e o job de validação `e575fed9` `CANCELLED` com 432 rows preservadas e **sem impacto no catálogo** — papéis distintos. Registra as **12 variantes `DEFERRED`** (11 sem evidência externa + `POKEMON_DAY_COSMOS_HOLO` por falta de prova específica de COSMOS) e a regra de que **deferir não é rejeitar nem concluir semanticamente**. Registra a **correção Master Ball / COSMOS**: `EVIDENCE EXHAUSTED`, migration `2200` v1.1 (`20260916212542`), VT **desativado e não removido**, zero `card_variant` materializado, e o abort da v1.0 com **rollback TOTAL provado**. Registra o fechamento da semântica de `DEFERRED` na UI (`MATCHED/SKIPPED` ≠ deferida ≠ JUMBO; suíte 15/15) e a **validação visual em produção PASS**, que encerra o último postcheck da frente JUMBO. NEXT e §3.2 atualizados para **NEXT = `BASEP` (27) e, somente depois de `BASEP`, `BASE3` (5)** — frentes **sequenciais**, não paralelas nem equivalentes (precisão editorial aplicada em `SV5 — CLOSEOUT FINAL CORRECTION-01`, mesma data). A mesma correção separou os dois estados de versionamento: **SV5 técnico = `COMMITTED / PUSHED`** (`b11fb0b`, `41fbe40`, `ee5b17a`) e **closeout documental = `PENDING COMMIT + PUSH`** nesta rodada. §6 ganhou as 12 deferidas e os **5 mappings SCOPED `sv05` nunca auditados** como lacuna registrada; §7 ganhou os ponteiros da `2200` e do contrato de UI. Rodada exclusivamente documental. |
| 1.2 | **`BASEP` e `BASE3` `CLOSED`; `CARD-VARIANTS — EDITORIAL CONVERGENCE` `CLOSED` (2026-09-18, `BASEP + BASE3 — EDITORIAL CONVERGENCE DOCUMENTATION CLOSEOUT-01`).** Seção nova em §1 com o fechamento dos dois últimos Card Sets da frente: `BASEP` `COMPLETED` (74 / 72 `VALID` / **2 `DEFERRED`** / 53-53 Cards / 72 variantes) e `BASE3` `COMPLETED` (**177 / 177 `VALID` / 0 `NEEDS_REVIEW` / 177 `APPROVED`+`INSERTED` / 0 `SKIPPED` / 0 `FAILED`** / 62-62 Cards / 177 variantes) — **BASE3 é o único Set da frente a fechar com zero deferimento**. `card_variant` **7.469 → 7.671**; taxonomia terminal com **94** `card_variant_type_external_mapping` (75 GLOBAL · 8 `sv05` · 6 `basep` · 5 `base3`). Cabeçalho: `NEXT` reapontado para **`CARD VARIANTS — HISTORICAL BULK IMPORT`** (169 Sets elegíveis / 16.705 Cards, `ME5.5` fora do denominador) e pendência operacional trocada para o Commit + Push deste closeout. §3.2 e §3.3 marcados como **superados** com nota de reconciliação no topo, texto original preservado. §6 ganhou quatro registros novos: a **`2202` executada sem linha no ledger** (execução direta no SQL Editor — divergência real, registrada e não mascarada), os **4 Sets sem Cards + Assets residuais como `KNOWN SOURCE-COVERAGE RESIDUALS / NON-BLOCKING`**, o `ME5.5` fora do denominador operacional, as **2 deferidas de BASEP** (total 14 com as de SV5) e a limitação de domínio aceita do Zapdos Cosmos ©1999. Rodada exclusivamente documental: nenhum código, nenhum SQL, nenhuma execução no banco. |
