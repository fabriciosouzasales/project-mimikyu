# Handoff — 2026-08-08

| Campo | Valor |
|--------|-------|
| **Documento** | Handoff de Desenvolvimento |
| **Arquivo** | `docs/development/HANDOFF-2026-08-08.md` |
| **Handoff anterior** | [`HANDOFF-2026-08-02.md`](../history/development/HANDOFF-2026-08-02.md) — superado por este documento; preservado como histórico em `docs/history/development/`. |
| **Sessão encerrada em** | 2026-08-08 |
| **Módulo em desenvolvimento** | Catálogo Editorial formalmente encerrado nesta janela (cinco frentes A–E concluídas); Módulo Gerencial (Trilha 4 do `ROADMAP.md`) iniciado — Visão Geral e Histórico de Importações concluídos. Hub de Card Set (`/catalogo/card-sets/[code]`) — o único débito real identificado na Frente C — implementado e encerrado na mesma janela, como incremento avulso fora do "Próximos passos" original. |

**Natureza deste handoff**: documento **operacional**, não normativo — descreve o estado real observado no repositório em 2026-08-08, para orientar a retomada da próxima sessão. Não redefine arquitetura, não substitui ADRs nem `05-modelo-de-dados.md`. Mesma disciplina do handoff anterior.

---

## 1. Resumo Executivo

Entre `HANDOFF-2026-08-02.md` e esta data, as frentes que aquele handoff registrava como pendentes ou "sem fechamento formal" foram concluídas, e uma nova camada (Módulo Gerencial) foi iniciada sobre o Catálogo Editorial já fechado:

- **`ADR-023` (Escrita Administrativa): concluído (2026-08-07).** Subciclo `Card` — o único ainda "pausado, nunca iniciado" no handoff anterior — fechado: cadastro, atualização, desativação/reativação, tudo via UI.
- **`ADR-024` Ciclo 2 (fluxo TCGdex): fechamento formal concluído (2026-08-07).** Validação `2818` (8/8 itens), com um bug real encontrado e corrigido no caminho (`admin_confirm_catalog_import()` v1.0 permitia `COMPLETED` com linhas de decisão nunca revisadas).
- **`ADR-024`, emenda "Raridade: mapeamento self-service" (2026-08-07).** `RARITY_NAME_ALIASES` (hardcoded em código) substituído por `rarity_external_mapping` + tela `/catalogo/raridades`.
- **`ADR-026` criado (2026-08-07)**, formalizando o canal Manual (`scripts/import-manual-assets.ts`, em produção desde 2026-07-24) que nenhum ADR cobria ainda.
- **`ADR-026`, emenda "segundo ponto de entrada via UI" (2026-08-07/08).** `/catalogo/importar-imagens` ganhou `Fonte = Manual` (upload direto do navegador) — backend e frontend confirmados, validado em produção real com `SVE` (6/6 sucesso).
- **`ADR-024`, Ciclos 3/4 (canal PDF): encerrados definitivamente (2026-08-08)** — decisão explícita de Fabrício, não uma implementação adiada. Ver seção 6.
- **Limpeza de UI, consequência direta do item anterior**: seletor de Fonte API/PDF removido de `/catalogo/importar-cartas`; ajustes de altura de controle nas duas telas de importação. Ver seção 7.
- **Módulo Gerencial — Sprint Gerencial 1, concluída (2026-08-08).** Visão Geral (`/catalogo`) ganhou drill-down completo (todo StatCard, linha de idioma e segmento de saúde navegável) sobre as métricas canônicas de `ADR-027`; Histórico de Importações (`/catalogo/importacoes`) unificou Cartas e Imagens num único histórico paginado corretamente. Ver seção 8.
- **Correção real de semântica — "Pendências" (2026-08-08).** O KPI contava qualquer `catalog_import_job` histórico em 4 status, inflando o número com tentativas antigas já superadas (9 jobs, todos superados). Regra corrigida para considerar só o job mais recente por Coleção, validada contra produção real (0 pendências). Ver seção 9.
- **Encerramento formal do Catálogo Editorial (2026-08-08).** Após uma sequência de auditorias dedicadas (matriz de 4 frentes do Módulo Gerencial, auditoria da Frente C, auditoria de planejamento), as cinco frentes (A–E) foram declaradas Concluídas — com duas correções de premissa relevantes (MEP não é bloqueador; Frente C sem pendência formal). Ver seção 10.
- **Hub operacional de Card Set implementado e encerrado (2026-08-08, mesmo dia, depois do encerramento formal acima).** O único achado da auditoria da Frente C (`/catalogo/card-sets/[code]` "em construção") foi resolvido no mesmo ciclo — não ficou como débito por muito tempo. Fabrício aprovou um escopo V1 depois de avaliação de viabilidade dedicada, e o incremento passou por várias rodadas de refinamento visual até aprovação final. Ver seção 11.

Com isso, o Catálogo Editorial está formalmente encerrado — ver `ROADMAP.md`, seção "Catálogo Editorial — Frentes de Encerramento". A frente ativa do projeto passa a ser o Módulo Gerencial (Log de Atualizações e Central de Relatórios, ainda pendentes) e/ou a Sub-Fase 2 — Coleções (critério de entrada satisfeito, prioridade entre as duas ainda não decidida por Fabrício) — ver seção 14, item 1, inalterado por este incremento avulso.

---

## 2. `ADR-023` — Escrita Administrativa (concluído, 2026-08-07)

Estado no handoff anterior: `~75%`, subciclo `Card` "pausado, nunca iniciado". Estado atual: as quatro operações administrativas de `Card` via UI fechadas no mesmo dia — atualização (`admin_update_card()`, Query `2114`), cadastro (`admin_create_card()`, Query `2115`), desativação/reativação (`admin_deactivate_card()`/`admin_reactivate_card()`, Queries `2116`/`2117`, soft delete real via `is_active`, sem cascata sobre `card_variant`/`card_asset`). Botões de ação rápida no grid (`/catalogo/cartas`): Editar+Desativar (Card ativa) / Editar+Reativar (Card inativa); botão "Nova Carta"; toggle "Mostrar inativas". Validação funcional `2817` (15 cenários) CONFIRMADA EXECUTADA. Pendência isolada, deliberadamente fora deste fechamento: auditoria retroativa de `REVOKE ... FROM PUBLIC/anon` nas demais funções `admin_*` do módulo (gap de segurança descoberto durante esta rodada, corrigido só nas três funções novas) — task `#368`, adiada.

Detalhe completo: `05e-catalogo-editorial.md`, `adr/ADR-023-catalog-editorial-write-authorization.md`.

---

## 3. `ADR-024` Ciclo 2 — fechamento formal (2026-08-07)

Validação `2818` (somente leitura, 8 verificações sobre dado real de produção) CONFIRMADA EXECUTADA E VALIDADA — 8 de 8 itens. Único achado real: item 4 (linhas `PENDING` dentro de job `COMPLETED`) devolveu 3 jobs divergentes — bug real em `admin_confirm_catalog_import()` v1.0 (o cálculo de status final não enxergava `decision_status = 'PENDING'` fora do filtro `APPROVED`/`SKIPPED`). Corrigido por Query `2082` v1.1 + Migration `2118` (reparo retroativo dos 2 jobs realmente afetados, SV2/SV5); o terceiro job (`REJECTED` em massa) era falso alarme, comportamento correto por desenho. Investigação adicional confirmou que reimportações posteriores completas (`COMPLETED`, 100%) já tinham resolvido SV2/SV5 na prática — decisão de Fabrício: deixar os 2 jobs antigos como estão (`STAGED`), sem efeito real no catálogo.

Detalhe completo: `05e-catalogo-editorial.md`, seção "Validação — Query 2818".

---

## 4. `ADR-024` — Raridade self-service (2026-08-07)

`RARITY_NAME_ALIASES` (hardcoded em `import-catalog-cards/services/normalize.ts`) substituído por `rarity_external_mapping` (tabela própria) + tela `/catalogo/raridades` — Queries `2094`–`2113`: extensão/normalização compartilhada (`normalize_external_catalog_value()`, `unaccent`), modelo (`rarity_external_mapping`), CRUD administrativo (`admin_create_rarity[_with_external_mapping]`/`admin_update_rarity[_external_mapping]`), backfill dos 25 aliases antigos, revalidação sem reimportação (`svc_apply_catalog_import_revalidation()`, chamada pela Edge Function `revalidate-catalog-import-rows`) e GRANTs de apoio. Corrigir ou cadastrar um mapeamento de raridade deixou de exigir deploy de código.

Detalhe completo: `05e-catalogo-editorial.md`, seção "Raridade — mapeamento self-service e revalidação".

---

## 5. `ADR-026` — canal Manual, segundo ponto de entrada via UI (2026-08-07/08)

`ADR-026` formalizou em 2026-08-07 o canal Manual (`scripts/import-manual-assets.ts`, `asset_source` `MANUAL`) já em produção desde 2026-07-24 (`MEE`/`en`, 8/8, 0 falhas), que nenhum ADR cobria ainda. Na mesma janela, ganhou um segundo ponto de entrada via `/catalogo/importar-imagens` (`Fonte = Manual`, upload direto do navegador para `card-front`, sem depender de arquivos já organizados em disco): backend (Queries `2119` políticas de Storage, `2120` `admin_persist_manual_card_asset()`, `2121` amplia `catalog_admin_action_log`, `2122` `admin_log_manual_card_asset_import_batch()`; validação consolidada `2819`, 4/4) e frontend (núcleo compartilhado `web/lib/catalogo/manual-asset-import/core.ts`, reaproveitado pelo script CLI; componente `ImportarImagensManualPicker`) ambos CONFIRMADOS EXECUTADOS E VALIDADOS. Testado em produção real com `SVE` (6 arquivos de energia básica, 6/6 sucesso) em 2026-08-08.

Detalhe completo: `adr/ADR-026-manual-local-file-asset-import-channel.md`.

---

## 6. `ADR-024` — encerramento definitivo do canal PDF (2026-08-08)

Decisão explícita de Fabrício, formalizada como emenda ao ADR, não uma reavaliação de prioridade: os Ciclos 3 (prova técnica) e 4 (fluxo PDF completo) **não serão implementados**. Motivação registrada: taxa de sucesso já suficiente do canal TCGdex (Ciclo 2, formalmente fechado — seção 3), direção futura de multi-provider (decisão de escopo de 2026-08-07, ainda sem solução técnica) tendendo a cobrir lacunas remanescentes com mais confiabilidade do que interpretar layout gráfico de checklist, e os dois canais manuais já existentes (`ADR-023` cadastro individual, `ADR-026` importação manual de imagem) já cobrindo o caso residual sem automação.

A decisão foi embasada por evidência técnica direta, não só estimativa: uma prova técnica exploratória (fora do repositório, não versionada como código de produção) testou extração de número/nome/raridade/acabamento via geometria vetorial + cor comparada contra a legenda da própria página, sem OCR nem IA visual, contra dois checklists reais (`ME04_Card_List_PTBR.pdf`: 3 colunas, 122 cartas, 100% de acerto; `ME02pt5_Card_List_PTBR.pdf`: 6 colunas, 295 cartas, 9 raridades, nomes em duas linhas — precisão inconsistente com calibração de offset fixo, exigindo detecção dinâmica de grade por produto para não degradar). O mecanismo é tecnicamente viável; o custo de engenharia para robustez entre produtos não trouxe benefício considerado suficiente frente às alternativas já cobertas.

O contrato `fonte → processador → linhas de staging` e o Princípio da Fonte Canônica (`ADR-024`) permanecem válidos e aplicáveis a qualquer fonte futura — o que se encerra é especificamente a intenção de construir um processador de PDF, não a arquitetura de ingestão como um todo.

Detalhe completo: `adr/ADR-024-catalog-card-ingestion-strategy.md`, emenda 2026-08-08.

---

## 7. Limpeza de UI — consequência do encerramento do canal PDF (2026-08-08)

Com o canal PDF encerrado, o seletor de Fonte (API/PDF) de `/catalogo/importar-cartas` (`ImportarCartasView`) deixou de representar uma escolha real — removido por completo (`type Fonte`, `FonteToggle`, `PdfUploadPanel`, todo o branching por `fonte`, incluindo `?fonte=` na URL). A tela passou a mostrar só o combobox de Coleção e o botão "Analisar", que deixou de ser condicionalmente renderizado (só aparecia com Coleção selecionada + Set localizado) e ficou permanentemente visível — a condição antiga virou só o `disabled`.

O mesmo conceito foi replicado em `/catalogo/importar-imagens` (`ImportarImagensView`) — aqui o `FonteToggle` (API/Manual) permanece, é o canal Manual da seção 5, ainda válido, diferente do canal PDF encerrado — só o combobox ganhou `max-w-[500px]` e o botão "Importar Imagens" ficou permanentemente visível (desabilitado, não escondido, quando `Fonte = Manual`, já que a ação real desse modo é o picker de arquivos abaixo).

Rodada seguinte, mesmo dia, pedido direto de Fabrício ao ver a tela renderizada ("esses componentes precisam ter essa altura toda? Visualmente desagradável"): `h-10`/`min-h-10` nessas duas telas era uma exceção isolada — o resto do app usa `h-9` como altura padrão de controle (`Button`/`Input`, ver `button.tsx`/`input.tsx`). Combobox reduzido para `min-h-9`/`py-1` nas duas telas; botão Analisar voltou ao `h-9` padrão do próprio `Button`. Em Importar Imagens, onde combobox + toggles + botão ficam lado a lado, a linha trocou `items-center` por `items-stretch` e os controles auxiliares perderam a altura fixa — todos esticam para a altura real do combobox automaticamente, em vez de repetir o mesmo número em cada um (causa dos ajustes de altura nunca totalmente resolvidos nas rodadas originais de `ImportarCartasView`, quando ainda tinha `FonteToggle` próprio).

`tsc --noEmit` confirmado limpo em todas as rodadas. Ver `web/components/catalogo/importar-cartas-view.tsx`, `web/components/catalogo/importar-imagens-view.tsx`, `web/app/catalogo/importar-cartas/page.tsx`, `docs/log.md` (quatro entradas, 2026-08-08).

---

## 8. Módulo Gerencial — Sprint Gerencial 1 (2026-08-08)

Plano recuperado nesta sessão: o Módulo Gerencial sempre teve quatro frentes (Visão Geral, Log de Atualizações, Histórico de Importações, Central de Relatórios), mas isso nunca tinha sido registrado em nenhum documento versionado — só existia disperso em `docs/log.md` e no cabeçalho de `ADR-027`. Duas frentes concluídas nesta janela:

- **Visão Geral (`/catalogo`).** Sobre as views canônicas `catalog_card_set_metrics`/`catalog_card_set_image_coverage` (`ADR-027`, Queries `2123`/`2124`), o dashboard ganhou drill-down completo: os quatro StatCards principais (Coleções, Cartas catalogadas, Cobertura, Pendências) passaram a ter `href` (novo prop opcional em `StatCard`) apontando para `/catalogo/card-sets`, `/catalogo/cartas`, `/catalogo/importar-imagens` e `/catalogo/importacoes?atencao=1` respectivamente; cada linha de "Cobertura por idioma" leva a `/catalogo/importar-imagens?idioma={code}`; cada segmento de "Saúde do catálogo" leva à tela correspondente. `tsc --noEmit` confirmado limpo em cada incremento; números conferidos contra a UI real.
- **Histórico de Importações (`/catalogo/importacoes`).** `getImportacoes()` passou a paginar as duas fontes (`asset_import_run`, `catalog_import_job`) via `fetchAllRows()`, eliminando o limite silencioso de 1.000 linhas do PostgREST. Nova coluna "Pipeline" distingue Cartas de Imagens. Filtro `?atencao=1` implementado, reutilizando exatamente a mesma fonte lógica do KPI "Pendências" (ver seção 9) — nunca uma checagem de status duplicada.

Log de Atualizações e Central de Relatórios seguem pendentes — ver `ROADMAP.md`, Trilha 4, para o escopo detalhado de cada uma (a Central de Relatórios já tem os 6 relatórios e a decisão de V1 em HTML imprimível registrados, mas nenhuma implementação existe).

Detalhe completo: `ROADMAP.md`, Trilha 4; `web/lib/catalogo/queries.ts`; `web/app/catalogo/importacoes/page.tsx`; `web/components/catalogo/visao-geral-stats.tsx`; `web/components/catalogo/stat-card.tsx`.

---

## 9. Correção de semântica — "Pendências" (2026-08-08)

O KPI "Pendências" e o filtro `?atencao=1` originalmente contavam qualquer `catalog_import_job` em `STAGED`/`CONFIRMING`/`COMPLETED_WITH_ERRORS`/`FAILED`, sem considerar se um job mais recente e bem-sucedido já existia para a mesma Coleção. Fabrício notou, olhando a tela real, que vários `FAILED` pareciam tentativas antigas já superadas — investigação confirmou: `catalog_import_job` permite abrir um novo job para a mesma Coleção assim que o anterior chega a um estado terminal (o `UNIQUE` de fingerprint só bloqueia jobs ativos), então "superado por uma tentativa posterior" é um estado real e comum, não um edge case.

Regra corrigida, no mesmo padrão já usado por `getLatestImportJobIncompleteFlags()` (pré-existente): considerar só o job mais recente por `card_set_id` (`created_at DESC`), e dentro desse subconjunto, exigir atenção se o status estiver em `{STAGED, CONFIRMING, COMPLETED_WITH_ERRORS, FAILED}` OU (`COMPLETED` com `inserted_rows + updated_rows + unchanged_rows < total_rows`). `RECEIVED`/`PROCESSING`/`CANCELLED` nunca contam. Validada com uma Query de diagnóstico somente leitura antes de qualquer mudança de código: a regra antiga contava 9 jobs, a regra nova 0 — todos os 9 confirmados, um a um, como tentativas superadas por reimportações posteriores bem-sucedidas das mesmas Coleções (`BASEP`, `ME5` ×3, `SV2` ×2, `SV3.5`, `SV5`, `SVE`). Implementada em um helper único (`getCatalogImportJobIdsExigindoAtencao()`, `web/lib/catalogo/queries.ts`), reutilizado tanto pelo KPI quanto pelo filtro do Histórico de Importações — validado ao vivo mostrando 0 pendências nas duas telas.

Detalhe completo: `web/lib/catalogo/queries.ts` (função `catalogImportJobExigeAtencao()`/`getCatalogImportJobIdsExigindoAtencao()`), `docs/log.md` (entrada `fix`, 2026-08-08).

---

## 10. Encerramento formal do Catálogo Editorial (2026-08-08)

Ao final da Sprint Gerencial 1, Fabrício pediu o próximo incremento do roadmap — o que revelou que o próprio Módulo Gerencial nunca tinha sido registrado (seção 8) e motivou uma sequência de três auditorias antes de qualquer nova implementação:

1. **Matriz das 4 frentes do Módulo Gerencial**, cruzando o plano recuperado (ver seção 8) contra o estado real do repositório — sem alterar documentação ainda.
2. **Auditoria dedicada da Frente C (Interface de Consulta)** — verificação, entidade por entidade (Jogos, Expansões, Card Sets, Cartas), de qualquer critério de aceite formal ainda em aberto. Resultado: nenhum. O único achado concreto, o detalhe incompleto de `/catalogo/card-sets/[code]` (autodocumentado no próprio arquivo como "em construção" — galeria de cartas, edição de logo, histórico de importações do Set), foi confirmado como evolução futura por design original, não como pendência real, e registrado como débito não bloqueante.
3. **Auditoria de planejamento** — varredura de `06-pipeline-importacao.md`, `07-catalogo-editorial.md`, `04-domain-model.md`, `ADR-014`, `STD-INDEX.md` e `ADR-INDEX.md` em busca de frentes/decisões já aprovadas mas não refletidas no `ROADMAP.md`. Confirmou um inventário real de itens dispersos (task `#368`, Fase 4 de `username`, multi-provider, verificação de direitos de imagem, buckets `card-back`/`artwork`, Card Translation, Decks/Trocas/Marketplace, entre outros) — nenhum promovido a compromisso de execução, todos registrados para rastreabilidade.

Correção de premissa importante feita por Fabrício durante esse processo: a pendência histórica da Frente B nunca foi "faltam imagens de `MEP`" — era a **ausência do canal** de importação manual em si, que já foi implementado e validado (`ADR-026`, seção 5). Tratar `MEP` como bloqueador da Frente B (como as revisões `1.x` anteriores deste handoff e do `ROADMAP.md` faziam) foi um engano de leitura, corrigido nesta reconciliação.

Com as três auditorias aprovadas, Fabrício declarou as cinco frentes (A–E) do Catálogo Editorial formalmente **Concluídas** e autorizou a atualização da documentação versionada — `ROADMAP.md` (revisão `1.22`), `README.md` (revisão `1.80`) e este handoff foram atualizados no mesmo ciclo, incluindo a nova seção "Débitos Técnicos Registrados (Sem Cronograma)" em `ROADMAP.md`, que consolida pela primeira vez num único lugar todos os débitos não bloqueantes conhecidos.

Detalhe completo: `ROADMAP.md`, seções "Catálogo Editorial — Frentes de Encerramento" e "Débitos Técnicos Registrados (Sem Cronograma)".

---

## 11. Hub operacional de Card Set — implementado e encerrado (2026-08-08)

Ainda no mesmo dia do encerramento formal do Catálogo Editorial (seção 10), Fabrício decidiu resolver de imediato o único achado real daquela auditoria — o detalhe incompleto de `/catalogo/card-sets/[code]` — em vez de deixá-lo só registrado como débito. Sequência real desta sub-sessão:

1. **Avaliação de viabilidade** (sem código ainda): confirmou que todo o escopo pedido por Fabrício (Cabeçalho/Estado, Cartas da Coleção, Cobertura/Pendências, Ações contextuais) já tinha suporte de dado — `catalog_card_set_metrics`, `catalog_card_set_image_coverage` e `card_set.base_set_size` já existiam; nenhuma tabela/view nova seria necessária.
2. **Implementação do escopo V1**: `CardSetOverviewRow` ganhou `id`/`baseSetSize`/`cardsPendentes`; novo tipo `CardSetDetail` acrescenta `coberturaPorIdioma` (recorte por Card Set da mesma view da Visão Geral); nova galeria somente-leitura `CardSetCartasGrid` (`web/components/catalogo/card-set-cartas-grid.tsx`), reaproveitando os efeitos visuais de `CartaGridCard` (`cartas-gallery.tsx`) sem embutir a tela `/catalogo/cartas` inteira; `/catalogo/importacoes` ganhou filtro `?cardSet=<code>`.
3. **Múltiplas rodadas de refinamento visual**, todas no mesmo dia, sobre a V1 já funcional: logo do cabeçalho ampliada e identidade reorganizada em três linhas fixas; "Cobertura e Pendências" deixou de ser seção própria e migrou para dentro de "Estado do Set", numa única linha (sem segunda linha de gráficos, depois ainda ampliados a pedido); zoom de carta com a mesma View Transitions API de `/catalogo/cartas` (miniatura morfa até virar a imagem ampliada); ações contextuais reposicionadas para uma linha acima do bloco "Cartas da Coleção" (padrão de `cartas-gallery.tsx`), sem `Panel`/card ao redor; três valores do "Estado do Set" centralizados em relação ao próprio label; terminologia "Card(s)" trocada por "Carta(s)" em todos os textos visíveis; bandeira por idioma (emoji Unicode, sem lib nova) nas barras de cobertura, reposicionada após o nome do idioma a pedido de Fabrício.
4. **Um relato de "bug" investigado e esclarecido, não corrigido**: números de "Coleções pendentes"/"Sem imagens" em `/catalogo/importar-imagens` pareciam inconsistentes com Coleções já 100% cobertas — Fabrício mesmo identificou a causa (idioma pré-selecionado na URL, vindo de um clique num gráfico de cobertura por idioma específico) e confirmou que o comportamento estava correto, não era regressão.
5. **Fechamento formal**: Fabrício aprovou o resultado final e pediu para "considerar a V1... concluída" — sem novos refinamentos pendentes.

`tsc --noEmit` confirmado limpo em cada rodada (nenhuma exceção). Um único débito não bloqueante permanece, já conhecido desde a auditoria da Frente C e não alterado por este incremento: upload de logo via `admin_set_card_set_logo()` a partir desta tela.

Detalhe completo: `docs/05e-catalogo-editorial.md`, seção "Hub de Card Set"; `ROADMAP.md`, revisões `1.23`/`1.24`; `docs/log.md`, dez entradas de 2026-08-08 (uma por rodada).

---

## 12. Limitações conhecidas (atualizado)

- `MEP` (Expansion `ME`) segue com imagens pendentes (`en`/`pt-BR`) — item de dado, não de implementação nem bloqueio: qualquer um dos dois canais (API/TCGdex ou Manual, via `/catalogo/importar-imagens`) já resolve, falta só executar. Não bloqueia mais nenhuma frente do Catálogo Editorial (ver seção 10).
- Log de Atualizações (`/catalogo/log-atualizacoes`) e Central de Relatórios seguem como as duas frentes pendentes do Módulo Gerencial — sem cronograma comprometido, sem ordem de prioridade decidida frente à Sub-Fase 2 — Coleções.
- Débitos técnicos não bloqueantes consolidados em `ROADMAP.md`, seção "Débitos Técnicos Registrados (Sem Cronograma)": upload de logo em `/catalogo/card-sets/[code]` (único item restante daquela linha — o resto foi implementado, ver seção 11), atualização de `STD-004`, task `#368` (auditoria `REVOKE`), Fase 4 de `username`, e os itens de `06-pipeline-importacao.md`/`07-catalogo-editorial.md` "Em Aberto".
- Estratégia de múltiplas fontes externas (multi-provider) confirmada como fora de escopo desta sprint (decisão de 2026-08-07) — nenhuma sub-decisão técnica (providers candidatos, fallback, rate limit) resolvida.
- Teto de execução da plataforma (Edge Function, ~150s) continua existindo para o pipeline de imagens — sem mudança nesta janela.

---

## 13. Incidentes relevantes ainda com rastro ativo

Nenhum incidente novo aberto nesta janela. Ver `HANDOFF-2026-08-02.md` (histórico, `docs/history/development/`) para ME5/SV4.5/SV5 — já resolvidos, sem pendência.

---

## 14. Próximos passos

1. **Fabrício decide a ordem de prioridade entre concluir o Módulo Gerencial (Log de Atualizações, Central de Relatórios) e iniciar a Sub-Fase 2 — Coleções** — nenhuma das duas tem precedência definida. Este é o ponto real de retomada da próxima sessão — nenhum trabalho de código deve começar antes dessa decisão.
2. `MEP` — usar o canal já existente (API ou Manual) para concluir `en`/`pt-BR` pendentes, quando conveniente (item de dado, sem urgência bloqueante).
3. Auditoria retroativa de `REVOKE ... FROM PUBLIC/anon` (task `#368`) — sem cronograma comprometido.
4. Registrar formalmente multi-provider como decisão arquitetural em aberto (não um ADR novo) caso a instabilidade da TCGdex volte a se repetir com frequência relevante.
5. Upload de logo em `/catalogo/card-sets/[code]` (débito da seção 11/12) — sem cronograma comprometido, pode ser feito a qualquer momento se Fabrício priorizar.

---

## 15. Itens que existem apenas como proposta (não implementados)

- Edge Function orquestradora (`catalog-import`) que encadearia sozinha `asset_import_run` + sincronização + imagens por idioma — ideia registrada em `06-pipeline-importacao.md`, "Em Aberto", não implementada.
- Validação prévia de integração externa antes de criar um `asset_import_run` (recusar se não houver `card_set_external_reference` ativo) — proposta, não implementada.
- Estratégia de resolução de conflitos entre múltiplas fontes externas — não definida, não implementada.
- Log de Atualizações e Central de Relatórios (Módulo Gerencial) — escopo registrado (ver seção 8 e `ROADMAP.md`, Trilha 4), nenhuma implementação iniciada.
- Upload de logo em `/catalogo/card-sets/[code]` via `admin_set_card_set_logo()` — infraestrutura de banco pronta desde 2026-07-26, nunca conectada ao frontend desta rota (ver seção 11).

---

## 16. Checklist de retomada

1. Ler este documento por inteiro — em especial a seção 11 (trabalho novo desta mesma janela, depois do encerramento formal do Catálogo Editorial) e a seção 14 (próximos passos).
2. Ler `docs/README.md` ("Status Atual do Projeto") e `docs/ROADMAP.md` para a trajetória macro — ambos já refletem o estado desta data, incluindo o encerramento formal do Catálogo Editorial e o fechamento do hub de Card Set.
3. Não há nenhuma decisão pendente de confirmação de Fabrício sobre o que já foi entregue (Catálogo Editorial e hub de Card Set, ambos encerrados e aprovados) — a única decisão real em aberto para começar a próxima sessão é de priorização (item 1 da seção 14): Módulo Gerencial vs. Sub-Fase 2 — Coleções.
4. Rodar `tsc --noEmit` e `git status --porcelain` a cada incremento (fora do escopo desta auditoria documental, mas válido para qualquer sessão de código que siga esta).
