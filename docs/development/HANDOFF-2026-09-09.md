# Handoff — 2026-09-09

| Campo | Valor |
|---|---|
| **Documento** | Handoff de desenvolvimento — ponto de retomada |
| **Arquivo** | `docs/development/HANDOFF-2026-09-09.md` |
| **Versão** | 1.6 |
| **Status** | **VIGENTE** |
| **Criado em** | 2026-09-09, em `COLLECTIONS-BULK-02-DOCUMENTATION-CLOSEOUT-01`; **atualizado** no mesmo dia em `COLLECTIONS-BULK-03-DOCUMENTATION-CLOSEOUT-01`; **atualizado em 2026-09-10** em `CATALOG-HISTORICAL-BOOTSTRAP-02-CARD-SETS-CLOSEOUT-01`; **atualizado em 2026-09-11** em `ASSETS-FINAL-CLOSEOUT`; **atualizado em 2026-09-12** em `PRIMARY-SPECIES-ME-SOURCING-01 / DOCUMENTATION-CLOSEOUT-01` e em `BULK-CONTRACT-HARDENING-CLOSEOUT-01` |
| **NEXT** | **`CARD-VARIANTS-GLOBAL-RECONCILIATION — BLOCK-01`** (519 rows `VALID` · 289 Cards · BASEP/SV5/SVE) |
| **Sucede** | `HANDOFF-2026-09-07.md`, movido para `docs/history/development/` |
| **Baseline** | HEAD de partida `9d149add43e93226331e4f4af4eaabd69ca46213` |
| **Estado do repositório** | Trabalho de BULK-02/BULK-03 e de `CATALOG-HISTORICAL-BOOTSTRAP-01`/`-02` **não commitado** — ver "Estado limpo esperado" |

---

## Estado atual — `CARD VARIANTS` em curso; Gate A **CLOSED/VALIDATED** (2026-09-12)

> ## NEXT: `CARD-VARIANTS-GLOBAL-RECONCILIATION — BLOCK-01`
>
> O **Gate A** (hardening do contrato bulk) está **fechado e validado**. `2163` e
> `2164` v1.1 estão **LIVE**; `2822` v1.1 deu **PASS integral** (37 asserções,
> 4 Seções, zero resíduo). Baseline real preservado:
> `card_variant` **6.483** · jobs `STAGED` **4** · `VALID`/`PENDING` **519** ·
> `NEEDS_REVIEW`/`PENDING` **505**.
>
> **Bloco 1** aproveita as **519 rows `VALID`** — **289 Cards**, nos Sets
> **BASEP / SV5 / SVE**. **BASE1 fica fora** porque tem **0** rows `VALID`.
> Fluxo obrigatório: selecionar só `VALID` → `2163` `APPROVED` → `2164` `CONFIRM`.
> As 519 cabem em uma chamada de cada lado (519 ≤ 10.000 e 519 ≤ 1.000).
>
> Depois do Bloco 1, e **antes** da campanha dos ~174 Sets: **classificação
> `NEEDS_REVIEW`** em `AUTO-MAPPABLE` / `AUTO-CREATABLE` / `HUMAN-REVIEW`.
> Princípio já registrado: revisão humana é **exceção**; mapping decidido uma vez
> deve ser reutilizável globalmente.

### `PRIMARY SPECIES` — **CLOSED** (2026-09-12), não reabrir

> **Não retomar os 127 residuais de Primary Species antes de Card Variants.** Eles são
> `INTENTIONAL UNRESOLVED` / `DEFERRED TO FUTURE EDITORIAL UI` e existem de propósito:
> são a **massa de teste real** da futura funcionalidade editorial do frontend.
> **`ASSETS` permanece `CLOSED`. `PRIMARY SPECIES` permanece `CLOSED`** salvo defeito
> material novo. Não abrir campanha de resolução residual, backfill adicional de
> Primary Species, nem resolução manual desses 127.

### `PRIMARY-SPECIES-ME-SOURCING-01` — o que fechou

Cadeia: `AUDIT-01` → `BACKFILL-GATE-A-01`/`-REVISION-01`/`-FINAL-CHECK-01` →
`6129` (`BACKFILL-01` + `SYNTAX-CORRECTION-01` + `RETRY`) → `6842` Gate A →
`6130` (`FINAL-HARDENING-01` + `EDITORIAL-RESOLUTION-01`) → `6842` Gates B/C →
`DOCUMENTATION-CLOSEOUT-01`.

- **Cobertura final: 17.409 / 17.536 = 99,28 %.** Residual **879 → 127**.
- **Card Sets ME* (`ME1`/`ME2`/`ME2.5`/`ME3`/`ME4`/`MEP`): 0 % → 752/752 = 100 %.**
  751 `AUTOMATIC_DEXID` (`6129`) + 1 `EDITORIAL_RECONCILIATION` (`6130`).
- **Causa-raiz provada, não inferida:** as 752 Cards vieram de seed manual (`840`),
  nunca passaram por `catalog_import_row`, e o `card_external_reference` veio depois
  pelo fluxo de assets — cujo payload de listagem não traz `dexId`. A `6116` nunca teve
  o que ler. **Não é regressão.**
- **`me01-086` = `SOURCE_DATA_ERROR` da TCGdex** (fonte publica `Mega Absol ex` com
  `dexId 351`/Castform). O `351` observado ficou preservado; a decisão editorial
  **Absol / 359** entrou por `6114` com responsável nomeado. Payload automático da
  `6115` nunca falsificado.
- **Gates:** `B1`–`B10` = **10/10 PASS**; `C1`–`C7` = **7/7 PASS**; duplicidade global
  de Primary Species = 0; universo elegível POKEMON = 17.536 inalterado.
- **Artefatos:** `6129`/`6130` promovidas para `database/migrations/` (transformação de
  **dados**, não schema); `6842` permanece **só** em
  `database/proposals/2026-09-12-primary-species-me-sourcing/`, junto das cópias de
  staging como evidência histórica.
- **Regra de domínio consolidada:** *coverage nunca prevalece sobre correctness*;
  `UNRESOLVED`/`AMBIGUOUS` é estado válido; `SOURCE_DATA_ERROR` (fonte errada, Species
  conhecida) **nunca** se confunde com ambiguidade (Species indeterminável).

### Dívidas não bloqueantes registradas

- 752 Cards ME* históricas continuam sem `catalog_import_row` (evidência da rodada
  preservada na `6129` e no `catalog_admin_action_log`).
- `MEP` tem 29 Cards Pokémon na TCGdex sem Card local.
- `6116` não emite sinal específico para ausência de `dexId` (observabilidade).
- Dívida de proveniência da `6116` pré-`6128` — mandato próprio, ver abaixo.

---

## Histórico — `PRIMARY SPECIES — INCREMENTAL HOOK` **CLOSED / PASS** (2026-09-12)

O hook pós-confirmação está corrigido e validado. A cadeia foi
`PRIMARY-SPECIES-GLOBAL-RECONCILIATION-AUDIT-01` → `BACKFILL-01/02/03` →
`INCREMENTAL-HOOK-AUDIT-01` → `-FIX-01 GATE A` → `APPLY-01` (STOP por md5) →
`RECOVERY-01` → `VALIDATION-01` (STOP por fixture) → `VALIDATION-FIX-01` →
`PROMOTION-CLOSEOUT-01`.

- **Defeito real:** a CTE `evidence` de `6116` não filtrava por `p_job_id`; `Seq Scan`
  em `catalog_import_row` estourava o `statement_timeout = 8s` de `authenticated`.
  Três jobs (`SMP`/`SM10`/`BW10`) chegaram a `COMPLETED` com Primary Species = 0 —
  causa provada pelo CSV do próprio caller (`web/scripts/bootstrap-cards/out/`),
  exatamente 3 ocorrências em toda a série.
- **Correção:** `6128` (`AND r.job_id = p_job_id`), aplicada, validada por `6841`
  (**17 PASS / 0 FAIL / 0 NOT PROVEN**) e promovida para `database/schema/`.
  `7663 ms → 262 ms` (~29×); `Seq Scan` global eliminado. Equivalência funcional:
  0 divergências em 18.234 pares `(job, card)` sobre 199 jobs.
  **Sem índice novo. Sem alteração de caller.**
- **Cobertura:** **16.657 / 17.536 = 94,99 %** (era 92,26 %).

**`PRIMARY SPECIES GLOBAL` não estava encerrado naquele momento** (superado em
2026-09-12 por `PRIMARY-SPECIES-ME-SOURCING-01` — ver bloco "Estado atual" no topo).
Residual **879**, duas classes que não devem ser colapsadas:

| Classe | Qtde | Caminho de fechamento |
|---|---:|---|
| Sem evidência durável persistida | **760** | 752 ME* têm `external_card_id` → **sourcing**; 8 SVP manuais sem nada → editorial |
| Ambiguidade real (multi-espécie) | **119** | **Decisão editorial** via `6114` |

**Dívida de proveniência aberta (mandato próprio):** o corpo LIVE da `6116` anterior
à `6128` (`md5 9d6cd767…` / 3005 B) **não era byte-idêntico** ao arquivo canônico
(`md5 fddacfc4…` / 4209 B), apesar de o cabeçalho afirmar o contrário. Corpo histórico
indisponível; nenhuma equivalência retroativa alegada. **Verificar se outras funções
promovidas divergem do canônico ainda não foi feito.**

**Observabilidade — dívida registrada:** o hook é não bloqueante por desenho e grava a
falha no CSV, mas uma linha `IMPORTED` com `motivo` de erro não se distingue de uma
linha limpa no sumário de fim de rodada. Ergonomia, não defeito; rodada própria.

---

## Histórico — macrobloco **ASSETS FORMALMENTE ENCERRADO** (2026-09-11)

Sequência aprovada a partir daqui, nesta ordem:

```
PRIMARY SPECIES → CARD VARIANTS → VARIANT DEFAULT BACKFILL → COLLECTIONS / BULK-04
```

### Números LIVE que encerram o macrobloco (medidos, não estimados)

| Indicador | Valor em 2026-09-11 |
|---|---|
| Cards ativos | **20.939** |
| Assets primários `en` | **19.945** |
| Assets primários `pt-BR` | **10.303** |
| `asset_import_run` não terminais | **0** |
| Edge `import-card-assets` | versão **45**, `ACTIVE`, `verify_jwt = true` |

### Última frente: SWSH subset asset-path alias — recuperada

Seis subsets cujo **diretório físico de assets na CDN diverge do `external_set_id` do catálogo**. Recuperados integralmente em EN:

| Card Set | Diretório real | Recuperado |
|---|---|---|
| `SWSH4.5SV` | `swsh4.5` | **122/122** |
| `SWSH12.5GG` | `swsh12.5` | **70/70** |
| `SWSH9TG` | `swsh9` | **30/30** |
| `SWSH10TG` | `swsh10` | **30/30** |
| `SWSH11TG` | `swsh11` | **30/30** |
| `SWSH12TG` | `swsh12` | **30/30** |
| **Total** | | **312 Assets EN** |

Gates: smoke `SV001` **PASS** · harness de alias **140/140** · harness de integridade de promoção **12/12** · `deno check` PASS nos três arquivos da Edge.

### Decisões que precisam sobreviver a esta rodada

- **Duas dimensões independentes, nunca misturadas:** (A) `external_set_id` canônico/deployed da API e (B) diretório físico de assets na CDN. O Gate 0 mediu metadata EN = 200 nos 6 e 404 nos IDs candidatos da issue pública — **não há correção de `external_set_id` nesses Sets**. O problema era exclusivamente asset-path.
- **Mapa de alias centralizado, sem DDL.** `ALIAS_DIRETORIO_ASSET` em `services/tcgdex.ts` carrega **apenas 6 pares de diretório** — nenhuma URL, série, idioma ou `localId`. Decisão deliberadamente reversível: se a reconciliação global revelar dezenas de aliases, aí a tabela governada passa a se pagar.
- **Alias só para `en`.** O piloto provou `404/404` em `pt-BR` nos 6. Aplicar alias em pt trocaria um 404 por outro.
- **`localId` literal, sem normalização numérica.** `SV001`, `GG01`, `TG01` passam intactos; `chaveFonte` só normaliza numéricos puros.
- **Nunca derivar URL de `collector_number` quando houver identidade autoritativa.** A rota da CDN inclui o slug da série, que não é dedutível do código do Set — foi exatamente essa dedução que o alias provou ser falsa.
- **`DERIVED_ALLOWLIST` é hipótese, não identidade.** Quando a fonte não publica `image`, a URL derivada **não** é persistida em `image_source_url` na sincronização; só é promovida **depois** do download bem-sucedido (`promoverImageSourceUrlDerivada`, com `.single()` + conferência do valor persistido). `404/404` ou falha ⇒ a coluna permanece `null`. Gravar a hipótese antes da prova plantaria no banco um endereço não comprovado que o auditor de CDN leria como AUTORITATIVO.
- **`card_external_reference` permanece fechada.** RLS `true`, zero policy, sem `SELECT` para `authenticated`/`anon`. A única via de leitura é a RPC `admin_list_card_external_image_sources` (migration `6127`, `SECURITY DEFINER`, `search_path = ''`, guard `is_admin()`, cap por `cardinality` + `array_ndims`). **Não conceder SELECT direto, não criar policy ampla, não usar `service_role` em script local.**
- **`admin_start_asset_import_run()` exige sessão admin real.** No SQL Editor `auth.uid()` é `null` e a RPC nega corretamente. O caminho legítimo é o `signInWithPassword` já usado por `web/scripts/bootstrap-cards/run-bootstrap-assets.mjs`.

### Dívida conhecida — FORA do escopo desta fase

Registrada como **dívida declarada, não como omissão**:

- **Cobertura residual de Assets.** 20.939 Cards ativos contra 19.945 `en` e 10.303 `pt-BR`. O gap remanescente é majoritariamente **ausência na fonte** (`NOT_AVAILABLE_AT_SOURCE`) — a TCGdex simplesmente não publica a imagem — e não falha de pipeline.
- **`pt-BR` dos 6 subsets SWSH:** `404/404` comprovado na CDN. Sem fonte, sem ação.
- **Sets `metadata-404` com identidade insuficiente** (XYP, TK-XY-*, 20xxXY, DC1 e afins): sem `image_source_url` persistida no idioma alvo, permanecem **INCONCLUSIVO** por desenho fail-closed. Não são "source debt comprovado".
- **`EXU`** (0 Cards no catálogo) e os Sets classe C permanentemente em zero (`RC`, `SP`) seguem como débito histórico anterior a esta frente.
- **Reconciliação global TCGDEX SET-ID / ASSET-PATH dos 199 Card Sets:** desenhada (4 baldes, read-only), **não executada**. Fica disponível para quando houver mandato.

### Decisão explícita de escopo

**Não haverá nova busca por fonte externa neste momento.** Não abrir nova investigação de Assets — nem novas APIs, nem novos aliases, nem varredura de gaps — sem mandato explícito de Fabrício. **Cobertura perfeita não é critério de encerramento deste macrobloco.** O critério satisfeito é: pipeline correto, governado, com resíduo medido e declarado.

### Artefatos desta frente

| Caminho | Papel |
|---|---|
| `supabase/functions/import-card-assets/services/tcgdex.ts` | `ALIAS_DIRETORIO_ASSET`, `resolverBaseImageUrlComAlias`, `derivarBaseImageUrlAllowlist`, `resolverBaseImageUrlDaCarta`, `ORIGEM_RESOLUCAO` |
| `supabase/functions/import-card-assets/services/database.ts` | `promoverImageSourceUrlDerivada()` — promoção com prova positiva |
| `supabase/functions/import-card-assets/index.ts` | resolução ÚNICA por `localId`; persistência condicionada a `SOURCE`; promoção após `upsertCardAsset` |
| `supabase/functions/import-card-assets/provas-asset-path-alias.ts` | **140/140** |
| `supabase/functions/import-card-assets/provas-promocao-image-source-url.ts` | **12/12** |
| `supabase/functions/import-card-assets/provas-image-quality-fallback.ts` | 35 asserções (fallback `high`→`low`) |
| `web/scripts/bootstrap-cards/audit-assets-cdn.mjs` | auditor read-only de CDN |
| `web/scripts/bootstrap-cards/provas-audit-assets-cdn.mjs` | **146/146** |
| `database/proposals/2026-09-11-admin-card-external-image-source-read/` | migration `6127` + validação `6840` (18 casos) + README |

---

## Histórico — `CATALOG-HISTORICAL-BOOTSTRAP-01` e `-02` fechados (2026-09-10)

**Ponto de retomada à época: `CATALOG-HISTORICAL-BOOTSTRAP-03` — CARDS + ASSETS PT-BR/EN** (superado pelo encerramento acima).

A camada `Expansion → Card Set` do catálogo histórico está **fechada, validada e estável**. Nada abaixo depende de trabalho pendente desta frente.

| Item | Estado medido em 2026-09-10 |
|---|---|
| Expansions Pokémon | **17**, com `release_order` refletindo a cronologia editorial real (Query one-shot `2160`, ledger `20260909231838`) |
| Card Sets Pokémon | **199** = 46 originais (curadoria humana) + **71** Batch A + **82** Batch B |
| Índice `uq_card_set_expansion_promo` | **presente no banco físico** (Query `2161`, ledger `20260910025017`) — `card_set` com 4 índices; 10 `PROMO` em 10 Expansions; zero Expansion com mais de um |
| `card_set.release_order` | definitivo nos 199 (Query `2162`, ledger `20260910025112`); local `1..N` por Expansion; faixa global `1..28`; **zero** resíduo `>= 1000` |
| Gate de validação | `921 - Validate Card Set Finalization`: **14 verificações / 14 PASS / 0 FAIL** |
| Logos | **153 com logo / 46 sem**; 156 objetos no bucket `card-set-logo`; 153 ponteiros; **0 quebrados**; 3 órfãos |

### Decisões que precisam sobreviver a esta rodada

- **Subsets e galleries são `card_set` independentes**, não atributo de um Set "pai". É por isso que a regra de `release_order` precisa do critério "publicação principal antes do subset/gallery" em empate de data.
- **`xya` e `wp` foram deliberadamente retirados do escopo de Card Set** (`DEFER_TO_CARD_VARIANTS`): pertencem à futura etapa de Card Variants. Registrado como dívida explícita, não como omissão — não recriar como Card Set.
- **`miscp` e `jumbo` permanecem `OUT_OF_SCOPE`.**
- **Nome em inglês (`EN_PROVISIONAL`) é aceitável** quando a fonte não traz PT-BR confirmado; o enriquecimento PT-BR é etapa editorial posterior e **não bloqueia** a criação do Set. No Batch B: 81 `EN_PROVISIONAL` + 1 `PT_BR_CONFIRMADO` (`mfb`).
- **Logo ausente ou falha de asset nunca bloqueia um Card Set** — logo é enriquecimento editorial, não integridade. Formato: WEBP preferencial, PNG como fallback medido (`hgss3` e `xy3` só existem em PNG). Nunca persistir URL externa; nunca usar `symbol` como logo.
- **Os 3 órfãos do bucket (2026-08-02, 04 e 05) são anteriores a esta frente**, não foram produzidos por ela e **não** foram removidos. Ficam como dívida operacional histórica.
- **Semântica congelada de `set_type`:** `PROMO` = série Black Star Promos agregadora da Expansion; `SPECIAL` = publicação editorial fechada, **mesmo quando distribuída promocionalmente**; `REGULAR` = Set regular. **Proibido** inferir `PROMO` a partir do canal de distribuição. Ver `ADR-015` revisão `1.9`.
- **`120` não foi alterada e não deve ser.** A divergência do índice PROMO era entre uma definição canônica **correta** (v2.2) e um banco físico antigo; quem reconciliou foi a `2161`, em `database/migrations/`.
- **`2162` é one-shot de dado**, não estrutura: permanece em `database/proposals/2026-09-09-card-sets-finalization/` e **não** é promovida para `database/schema/` — mesmo tratamento dado à `2160`.
- **Divergência editorial em aberto, deliberadamente não corrigida:** `SV10.5B`/`SV10.5W` mantêm a data atual (`2025-07-18`); a diferença de 1 dia fica registrada para revisão editorial futura. Corrigir data é mudança de conteúdo e não se mistura com renormalização de ordenação.
- **Cuidado técnico obrigatório em qualquer renormalização futura de `card_set.release_order`:** `uq_card_set_expansion_release_order` **não é `DEFERRABLE`** (`condeferrable = f`), então `SET CONSTRAINTS ALL DEFERRED` não tem efeito e um `UPDATE` em massa colide. Usar duas passagens com faixa temporária disjunta.

### Artefatos desta frente

| Caminho | Papel |
|---|---|
| `database/migrations/2161_create_card_set_expansion_promo_unique_index.sql` | Reconciliação física do índice PROMO — **promovida** (classe da `122`) |
| `database/proposals/2026-09-09-card-sets-finalization/2162_normalize_card_set_release_order.sql` | One-shot de dado — **não promovida**, por desenho |
| `database/proposals/2026-09-09-card-sets-finalization/921_validate_card_set_finalization.sql` | Gate read-only 14/14 — evidência |
| `database/proposals/2026-09-09-card-sets-finalization/README.md` | Registro da rodada, incluindo o aborto honesto da `IMPLEMENTATION-01` |
| `web/scripts/bootstrap-card-sets/` | Manifests dos Batches A/B, executores (`--apply` explícito, DRY_RUN por padrão) e o backfill de logos |

---

## O que fechou em 2026-09-09 (BULK-02 / BULK-03)

**`BULK-02` — B1, `register_physical_cards_bulk` — `EXECUTED / VALIDATED / CONCURRENCY PROVEN / PROMOTED / CLOSED`.**

Primeira **operação de negócio** da frente Bulk. BULK-01 tinha entregue só a infraestrutura de idempotência; agora existe a RPC que efetivamente cria patrimônio em massa.

### Migrations aplicadas

| Query | Objeto | Ledger |
|---|---|---|
| `5147` | `bulk_canonical_json()` + `bulk_request_hash()` — fonte única de canonicalização da família Bulk | `20260908235708` |
| `5148` | `bulk_lock_operation_scope()` — fonte única da ordem `INVENTORY → COLLECTION → STORAGE` (`I9`) | `20260908235737` |
| `5149` | `preview_fingerprint_register_physical_cards()` — helper INTERNO, `SECURITY INVOKER`, **`VOLATILE`** | `20260908235815` |
| `5150` | `register_physical_cards_bulk(p_request jsonb) → jsonb` | `20260909000013` |

### Provas

| Evidência | Resultado |
|---|---|
| Postcheck estrutural | **PASS** — volatilidade, `SECURITY DEFINER`/`INVOKER`, owner, `search_path`, assinatura congelada, grants nos quatro papéis, helpers de BULK-01 intactos |
| Harness `5822` v2.1 | **`64 TOTAL / 64 PASS / 0 FAIL / 0 NOT PROVEN`** |
| Zero resíduo | Δ = 0 nas **oito** tabelas do postcheck baseline-relativo |
| `CONCURRENCY-PROOF-PREVIEW-STALE` | **PASS** — B bloqueada por A (`wait_event = transactionid`); após COMMIT de A, B falhou com `PREVIEW_STALE` e **não** com `collection is archived`; F0 ≠ F1; contraprova com F1 devolveu `CREATED`/2 cartas; resíduos e role temporária = 0 |

Dois registros de honestidade, ambos documentados em `05d` e no README da proposta:

- **Adaptação de transporte do harness.** Foi medido que o canal MCP não preserva sessão nem transação entre chamadas; o `5822` rodou em chamada única, com o relatório entregue por `RAISE EXCEPTION` para forçar o rollback por construção. Nenhum caso de teste alterado.
- **Defeito real na primeira execução: 59/64.** Cinco FAIL em `E01`–`E05`, todos por asserção errada de `search_path` — o PostgreSQL persiste `SET search_path = ''` como `search_path=""`. O defeito era **do harness, nunca do produto**; corrigido reaproveitando o helper canônico `pg_temp._empty_search_path()` do `5821`, que já existia no repositório desde BULK-01.

### Promoção canônica

`5147`–`5150` promovidas para `database/schema/`, com corpo executável provado **byte-idêntico** por SHA-256 nos dois lados. `5822`, o runbook `CONCURRENCY-PROOF-PREVIEW-STALE.sql` e o `README.md` da rodada permanecem **apenas** em `database/proposals/2026-09-08-bulk-02-register-physical-cards/`, como evidência histórica.

### Dívida documental quitada

O contrato congelado de B1 — assinatura `p_request jsonb`, ordem de locks, rótulo `I9` — foi decidido em `COLLECTIONS-BULK-OPERATIONS-MODELING-FINALIZATION-01`, rodada que **não gerou artefato no repositório**. A partir desta rodada, `docs/05d-colecoes-e-usuarios.md`, seção "Bulk Operations — B1 (BULK-02)", revisão `1.23`, é a **fonte durável** desse contrato. Por decisão explícita, sem ADR novo.

### Risco fechado

O risco **`B4`** herdado de BULK-01 (`request_hash` calculado fora do banco, com normalização precisando ser idêntica dos dois lados) foi **fechado por construção**: o hash passou a ser derivado server-side por `bulk_request_hash()`. Não há mais dois lados para divergir. Riscado nas Pendências da seção BULK-01 de `05d`.

---

## `BULK-03` — Preview — `CLOSED` na mesma data

**`preview_bulk_operation` — `EXECUTED / VALIDATED / PROMOTED / CLOSED`.**

Terceiro incremento da frente Bulk e **interface pública de Preview**: produz o `preview_fingerprint` que `5150` consome e devolve, numa chamada, o resumo do que aconteceria e a lista completa de impedimentos — sem escrever nada e sem tomar lock.

| Query | Objeto | Ledger |
|---|---|---|
| `5151` | `preview_bulk_operation(p_request jsonb) → jsonb` — `SECURITY DEFINER`, `VOLATILE`, `search_path` vazio, `EXECUTE` só para `authenticated` | `20260909024422` |

| Evidência | Resultado |
|---|---|
| Harness `5823` v2.2 | **`60 TOTAL / 60 PASS / 0 FAIL / 0 NOT PROVEN`** |
| Zero resíduo | Δ = 0 nas **nove** tabelas (as oito de BULK-02 mais `public.game`) |
| Performance (`D03`) | **338,43 ms** para 1000 `card_variant_id` reais e distintos — observacional, sem SLO |
| Promoção | `5151` em `database/schema/`, corpo executável byte-idêntico por SHA-256 nas três fontes |

Escopo: **somente `REGISTER_PHYSICAL_CARDS`**. `REGISTER_CARD_SET` pertence ao vocabulário e é recusado como *não suportado nesta versão* — nunca como "inválido".

Dois registros de honestidade, ambos em `05d` e no README da proposta:

- **Primeira execução deu 59/60.** O FAIL era em `E06`, por asserção errada sobre `pg_get_function_identity_arguments()`, que devolve `p_request jsonb` e não `jsonb`. **Defeito exclusivo do harness** — `5151` estava e permaneceu correta e intocada, e o idioma correto já existia no `E06` do `5822`, sem ter sido reaproveitado.
- **`F03` passava `NULL` como `p_default_storage_container_id`**, o que teria derrubado o harness antes do gate: o guard de `create_collection()` (`5034`) é um `NOT EXISTS` incondicional. Corrigido antes da execução.

`5823` e o `README.md` da rodada permanecem apenas em `database/proposals/2026-09-09-bulk-03-preview/`, como evidência histórica.

---

## Estado limpo esperado

Ao retomar, o repositório deve estar **commitado e pushado** com o trabalho desta rodada. Se ainda não estiver:

```
database/proposals/2026-09-08-bulk-02-register-physical-cards/   (7 arquivos, untracked)
database/proposals/2026-09-09-bulk-03-preview/                   (3 arquivos, untracked)
database/schema/5147_create_bulk_canonicalization_functions.sql
database/schema/5148_create_bulk_lock_operation_scope_function.sql
database/schema/5149_create_preview_fingerprint_register_physical_cards_function.sql
database/schema/5150_create_register_physical_cards_bulk_function.sql
database/schema/5151_create_preview_bulk_operation_function.sql
docs/05d-colecoes-e-usuarios.md            (modificado)
docs/ROADMAP.md                            (modificado)
docs/README.md                             (modificado)
docs/INDEX.md                              (modificado)
docs/log.md                                (modificado)
docs/development/HANDOFF-2026-09-09.md     (novo)
docs/history/development/HANDOFF-2026-09-07.md  (movido)
```

Nenhuma migration anterior, nenhum harness anterior e nenhum arquivo fora dessa lista foi alterado. `git diff --check` limpo.

---

## Próxima frente

**`CARD-VARIANTS-GLOBAL-RECONCILIATION — BLOCK-01`** → classificação `NEEDS_REVIEW`
→ campanha dos ~174 Sets → `VARIANT DEFAULT BACKFILL` → `COLLECTIONS / BULK-04`.

O **Gate A** da frente de Card Variants está **`CLOSED` / `VALIDATED`** desde
2026-09-12 — ver "Estado atual" no topo. Artefatos:
`database/migrations/2163_…` e `…/2164_…` (LIVE),
`database/proposals/2026-09-12-card-variants-bulk-contract-hardening/` (README +
`2822` v1.1 como prova de validação, não promovida).

*(Texto anterior, superado na mesma data: "`CARD VARIANTS — GLOBAL RECONCILIATION` →
`VARIANT DEFAULT BACKFILL` → `COLLECTIONS / BULK-04`" — a frente foi aberta e seu
Gate A já fechou.)*

`PRIMARY SPECIES` está **`CLOSED`** desde 2026-09-12 (17.409/17.536 = 99,28 %) — ver
"Estado atual" no topo. Os 127 residuais são `INTENTIONAL UNRESOLVED` e **não devem ser
retomados antes de Card Variants**.

*(Texto anterior, superado na mesma data: "fechar o residual de `PRIMARY SPECIES` (879)
— sourcing das 752 Cards ME* via `external_card_id` + reconciliação editorial das 119
ambiguidades". O sourcing das 752 foi feito; as 119 passaram a ser dívida intencional.)*

*(Registro histórico da frente anterior, superada em 2026-09-11/12:)*

**`CATALOG-HISTORICAL-BOOTSTRAP-03` — CARDS + ASSETS PT-BR/EN.**

Importação em massa das Cards do catálogo histórico do TCGdex e dos seus assets nos dois idiomas, agora sobre a camada `Expansion → Card Set` já fechada (17 Expansions, 199 Card Sets). Continua **antes** de `CATALOG-VARIANT-DEFAULT-BACKFILL-01`, para que o backfill de variantes default opere sobre o catálogo já bootstrapado. **Ainda não modelada** — nenhum artefato, numeração ou contrato foi definido para ela.

Sequência macro **congelada** restante:

```
CATALOG-HISTORICAL-BOOTSTRAP-03  →  CATALOG-VARIANT-DEFAULT-BACKFILL-01  →  BULK-04  →  BULK-05  →  BULK-06
```

Depois: UX Read Models/Contracts → UX Flows/Wireframes → Frontend Collections. UX-01 permanece como alternativa em aberto, não iniciada.

`REGISTER_CARD_SET` (B2) segue **sem Preview**: exigirá função de fingerprint própria e um segundo braço no discriminador de `5151`; envelope, semântica de erro, teto e matriz de segurança são herdados sem alteração.

---

## Pendências herdadas, não bloqueantes

- `add_physical_cards()` (`5012`) e `allocate_physical_cards_to_collection()` (`5046`) permanecem canônicas, com teto de 500 e todos os callers, **até `BULK-05`**. Auditoria/migração de callers e depreciação ficam para cleanup próprio.
- `bulk_operation` cresce **sem política de retenção** — herdado de BULK-01, irrelevante no volume atual, registrado e não resolvido.
- Nenhum benchmark de performance dedicado para `5150`. O teto de 1000 e as duas escritas set-based mantêm o custo limitado por desenho; medição sob volume fica para quando houver workload real.

---

## Revision History

| Versão | Descrição |
|---------|-----------|
| 1.0 | **Criação (2026-09-09, `COLLECTIONS-BULK-02-DOCUMENTATION-CLOSEOUT-01`).** Sucede `HANDOFF-2026-09-07.md`, movido para `history/development/`. Registra o fechamento de `BULK-02` — `5147`–`5150` aplicadas, validadas (`5822` 64/64/0/0, resíduo Δ = 0 nas oito tabelas), com prova de concorrência externa de `PREVIEW_STALE` PASS, e promovidas para `database/schema/`; a quitação da dívida documental do `FINALIZATION-01` em `docs/05d` revisão `1.23`; o fechamento por construção do risco `B4`; e o ponto de retomada em `BULK-03` — Preview. |
| 1.1 | **Atualização (2026-09-09, `COLLECTIONS-BULK-03-DOCUMENTATION-CLOSEOUT-01`).** Este handoff continua sendo o vigente — **nenhum handoff novo foi criado na mesma data**. Acrescenta o fechamento de `BULK-03` — Preview: `preview_bulk_operation` (`5151`, ledger `20260909024422`) aplicada, validada por `5823` v2.2 em `60/60/0/0`, com Δ = 0 nas nove tabelas, `D03` = 338,43 ms para 1000 Card Variants reais, e promovida para `database/schema/`. Registra os dois defeitos de harness encontrados e corrigidos (o `E06` que fez a primeira execução dar 59/60 e o `F03` com `NULL` no Storage padrão), sempre distinguindo que `5151` esteve correta e intocada. A próxima frente passa de `BULK-03` para **`CATALOG-HISTORICAL-BOOTSTRAP`** — importação em massa do catálogo histórico do TCGdex, inserida antes de `CATALOG-VARIANT-DEFAULT-BACKFILL-01` e ainda não modelada —, e o "Estado limpo esperado" incorpora os arquivos de BULK-03. |
| 1.2 | **Consolidação do ponto de retomada (2026-09-10, `CATALOG-HISTORICAL-BOOTSTRAP-02-CARD-SETS-CLOSEOUT-01`).** Nova seção de topo "Estado atual — `CATALOG-HISTORICAL-BOOTSTRAP-01` e `-02` fechados", com o estado medido (17 Expansions; 199 Card Sets = 46 + 71 + 82; índice `uq_card_set_expansion_promo` live via `2161`; `release_order` definitivo via `2162`, faixa global `1..28`, zero resíduo `>= 1000`; gate `921` 14/14; logos 153/46, 0 ponteiros quebrados, 3 órfãos históricos), as decisões que precisam sobreviver à rodada e o inventário de artefatos. Seção "Próxima frente" atualizada de `CATALOG-HISTORICAL-BOOTSTRAP` (genérico) para **`CATALOG-HISTORICAL-BOOTSTRAP-03` — CARDS + ASSETS PT-BR/EN**; a seção anterior foi renomeada para "O que fechou em 2026-09-09 (BULK-02 / BULK-03)" e preservada sem reescrita. Rodada exclusivamente documental. |
| 1.5 | **`PRIMARY SPECIES` encerrado; NEXT = `CARD VARIANTS` (2026-09-12, `PRIMARY-SPECIES-ME-SOURCING-01 / DOCUMENTATION-CLOSEOUT-01`).** Seção de topo reescrita: `PRIMARY SPECIES` = **`CLOSED`** em **17.409 / 17.536 = 99,28 %** (residual **879 → 127**); Card Sets ME* de 0 % a **752/752 = 100 %** (751 `AUTOMATIC_DEXID` pela `6129` + 1 `EDITORIAL_RECONCILIATION` pela `6130`, `me01-086` → Absol/359 por `SOURCE_DATA_ERROR` da TCGdex); gates `B1`–`B10` **10/10 PASS** e `C1`–`C7` **7/7 PASS**. Causa-raiz registrada (seed manual `840` nunca passou por `catalog_import_row`; `card_external_reference` veio do fluxo de assets, sem `dexId` — a `6116` nunca teve o que ler, não é regressão). Os **127** residuais passam a `INTENTIONAL UNRESOLVED` / `DEFERRED TO FUTURE EDITORIAL UI` / `NON-BLOCKING`, preservados de propósito como massa de teste da futura UI editorial — **não retomar antes de Card Variants**. `6129`/`6130` promovidas para `database/migrations/`; `6842` permanece só em `proposals/`. Seções "Estado atual" e "Próxima frente" atualizadas; o bloco do `INCREMENTAL HOOK` foi rebaixado a histórico sem reescrita do conteúdo. **Nota de lacuna herdada:** as revisões `1.3` e `1.4` (rodadas `ASSETS-FINAL-CLOSEOUT` e `PRIMARY-SPECIES-INCREMENTAL-HOOK / PROMOTION-CLOSEOUT-01`) alteraram o cabeçalho deste handoff mas não registraram linha própria nesta tabela — apontado, **não** fabricado retroativamente. |
| 1.6 | **Gate A de `CARD VARIANTS` fechado; NEXT = `BLOCK-01` (2026-09-12, `BULK-CONTRACT-HARDENING-CLOSEOUT-01`).** Seção de topo passa de "`PRIMARY SPECIES` CLOSED" para "`CARD VARIANTS` em curso; Gate A `CLOSED`/`VALIDATED`" — o bloco de Primary Species foi rebaixado a subseção e **preservado sem reescrita**. `2163` (teto 10.000, `array_ndims` + `cardinality`, guards antes de qualquer `ANY()`/`UPDATE`) e `2164` v1.1 (teto 1.000, semântica de `p_row_ids = NULL` preservada, conjunto congelado `v_effective_row_ids` com `ORDER BY created_at, id` e `LIMIT c_max_rows + 1`, TOCTOU eliminado, `LOOP` restrito ao conjunto congelado) aplicadas no LIVE e **promovidas** para `database/migrations/`; `2822` v1.1 **PASS integral** (37 asserções, 4 Seções, todas fail-closed, zero fixture residual) e mantida **somente** em `proposals/`, mesmo padrão de `6800`/`6810`/`6820`/`6821`/`6841`/`6842`. Baseline real preservado e medido antes e depois: `card_variant` 6.483 / jobs `STAGED` 4 / `VALID`-`PENDING` 519 / `NEEDS_REVIEW`-`PENDING` 505. `NEXT` passa a `BLOCK-01` — 519 rows `VALID`, 289 Cards, Sets BASEP/SV5/SVE, com `BASE1` fora por ter 0 `VALID` —, e depois dele, antes da campanha dos ~174 Sets, a classificação `NEEDS_REVIEW` em `AUTO-MAPPABLE`/`AUTO-CREATABLE`/`HUMAN-REVIEW`. Rodada exclusivamente de promoção e documentação: **nenhum SQL executado**. |
