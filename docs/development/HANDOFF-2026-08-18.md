# Handoff — 2026-08-18

| Campo | Valor |
|--------|-------|
| **Documento** | Handoff de Desenvolvimento |
| **Arquivo** | `docs/development/HANDOFF-2026-08-18.md` |
| **Handoff anterior** | [`HANDOFF-2026-08-17.md`](../history/development/HANDOFF-2026-08-17.md) — superado por este documento; preservado como histórico em `docs/history/development/`. |
| **Sessão encerrada em** | 2026-08-18 (sete frentes: encerramento formal do Incremento **P12 — Exibição de Preços no Frontend** — ciclo exclusivamente documental —; **Incremento P13.1 — Fundação de Orquestração Programada de Pricing** (seção 7); auditoria final do P13.1 (seção 8); **Incremento P13.2 — Módulo Compartilhado e Execução Manual Auditável da PTAX** (seção 9); finalização operacional do P13.2 com o runner corrigido (seção 10); correção de autoridade temporal e ordenação da telemetria do P13.2, motivada por auditoria pós-piloto real (seção 11); e, no mesmo dia, **encerramento formal do Incremento P13.2**, após auditoria pós-idempotência real confirmar a correção — ver seção 12) |
| **Módulo em desenvolvimento** | Pricing — **Incremento P12 formalmente encerrado (2026-08-18)**. Preço em BRL nos três grids do catálogo (`/catalogo/cartas`, `/pesquisa`, galeria de variantes de Card Set), sobre o contrato de leitura em lote sem N+1 (`get_cards_pricing_summary`, migration `3903`) e a hierarquia de printing Normal → Holofoil → Reverse Holofoil corrigida (migration `3904`, achado real do teste "Reverse-only"). Validação local completa (desktop/mobile, temas, toque, teclado/foco/Escape, `typecheck`/`build`) aprovada por Fabrício; commits `6324b66`/`0dad413` sincronizados com `origin/main`. **Incremento P13.1 (mesmo dia, seção 7): fundação de schema para orquestração programada** — `FX_REFRESH`/`fx_source_code`/`confirmed_by` opcional/índices de concorrência em `pricing_sync_run` (Migrations `3905`–`3907`, `CONFIRMADO EXECUTADO`), arquitetura-alvo registrada em `ADR-031`; auditoria final (seção 8) corrigiu formato de `fx_source_code` (migration `3908`) e versionou retroativamente `3905`–`3907`. **Incremento P13.2 (mesmo dia, seção 9): lógica extraída para `supabase/functions/_shared/pricing-ptax/`** (núcleo puro, pronto para reuso pela Edge Function de P13.3), janela padrão calculada + override validado, retry HTTP, adapter manual com `--confirmed-by=`/captura de concorrência `23505`/registro em `pricing_sync_run_call`. **Finalização operacional do P13.2 (mesmo dia, seção 10): runner `Executar-P9-PTAX-Local.ps1` corrigido** para o novo contrato do adapter — pendência da seção 9 resolvida. **Correção de autoridade temporal do P13.2 (mesmo dia, seção 11)**: auditoria pós-piloto real encontrou telemetria de execução com timestamps do relógio do cliente em vez do servidor — migration `3909` (`CONFIRMADO EXECUTADO`) torna o Postgres a autoridade de `started_at`/`finished_at` de `pricing_sync_run` via trigger dedicado, adapter PTAX ajustado para não mais enviar esses valores e para persistir a telemetria antes de finalizar o run; nenhuma linha histórica alterada; JustTCG protegido automaticamente, sem alteração de código nele. **Encerramento formal do Incremento P13.2 (mesmo dia, seção 12)**: auditoria pós-idempotência real (run `eaad7b81`, somente leitura) confirmou a correção — zero inversão temporal, idempotência real comprovada — e P13.2 foi declarado formalmente concluído. **P13 como um todo permanece em andamento, não concluído.** Nenhuma extensão de agendamento habilitada, nenhum código JustTCG. Próximo passo real: **P13.3 — Edge Function `ptax-fx-refresh`** (habilitar `pg_cron`/`pg_net`, Vault, autenticação por secret dedicado, validar invocação manual antes de agendar). |

**Natureza deste handoff**: documento **operacional**, não normativo — descreve o estado real observado no repositório em 2026-08-18, para orientar a retomada da próxima sessão. Não redefine arquitetura, não substitui ADRs nem os documentos numerados. A narrativa normativa completa do P12 mora em `docs/05f-pricing.md`, seção "Encerramento Formal do Incremento P12", e em `docs/adr/ADR-029-pricing-domain-model.md`, revisão `1.23` — este handoff resume e aponta para lá, sem duplicar.

---

## 1. Resumo Executivo

Fabrício pediu o encerramento formal do Incremento P12 — Exibição de Preços no Frontend, consolidando em um único ciclo documental o que já havia sido implementado e validado ao longo de várias rodadas anteriores (v1 a v5 + QA + correção de hierarquia de printing via migration `3904`, todas já registradas em `docs/log.md` e nas revisões anteriores de `05f-pricing.md`/`ADR-029`).

Antes de editar qualquer documento, foi feita introspecção explícita para confirmar quais artefatos realmente controlam status e encerramento de incrementos neste repositório: `docs/README.md` (Placar Rápido — "Status Atual do Projeto"), `docs/ROADMAP.md` ("Now"/"Next"/"Concluído"), `docs/INDEX.md` (catálogo + ponteiro de handoff vigente), `docs/development/HANDOFF-*.md` (operacional, um vigente por vez), `docs/log.md` (índice cronológico) e os documentos normativos do domínio (`docs/05f-pricing.md`, `docs/adr/ADR-029-pricing-domain-model.md`). A decisão de onde colocar cada pedaço da narrativa seguiu o padrão já estabelecido nos encerramentos anteriores (P7, Pesquisa Global de Cartas): o detalhamento completo (objetivo, escopo, migrations, contratos frontend, as 14 evidências aprovadas) mora em `05f-pricing.md`/`ADR-029`; `ROADMAP.md`, `README.md`, `INDEX.md` e este handoff resumem e apontam para lá, sem duplicar a lista de evidências.

**Nenhuma alteração de código, banco ou migration foi realizada nesta rodada** — as migrations `3903`/`3904` já estavam `CONFIRMADO EXECUTADO` de ciclos anteriores. O trabalho desta sessão foi inteiramente de consolidação documental.

A correção do erro de autenticação `/login` (`AuthApiError: Invalid Refresh Token: Refresh Token Not Found`, reportado por Fabrício via captura de tela do overlay de erro do Next.js dev) foi investigada em paralelo, diagnosticada como um cookie `sb-*-auth-token` obsoleto no navegador — não um bug de código (não há `middleware.ts` global; cada página protegida chama `getUser()` diretamente, sem try/catch) — e mantida **explicitamente fora do escopo** deste encerramento, por instrução direta de Fabrício, que optou apenas por limpar os cookies locais.

---

## 2. Evidências e critérios de aceite consolidados

As 14 evidências abaixo, todas aprovadas por Fabrício ao longo das rodadas anteriores do P12 (v1–v5, QA, teste "Reverse-only"), foram formalmente registradas nesta rodada em `docs/05f-pricing.md`, seção "Encerramento Formal do Incremento P12":

1. Preços exibidos nos três grids (`/catalogo/cartas`, `/pesquisa`, galeria de variantes de Card Set).
2. Desktop e mobile aprovados.
3. Temas claro e escuro aprovados.
4. Interação por toque aprovada.
5. Teclado, foco e `Escape` aprovados.
6. Formatação BRL/USD aprovada.
7. Popover e apresentação das variantes em NM aprovados.
8. Fallback de printing Normal → Holofoil → Reverse Holofoil validado (migration `3904`).
9. Cenário sem preço retornando `has_pricing=false` validado.
10. Migration `3904` aplicada (`CONFIRMADO EXECUTADO`).
11. Segurança, grants, advisors e performance revalidados.
12. Testes transacionais com `ROLLBACK` e zero resíduo.
13. `typecheck` e `build` de produção aprovados.
14. Commits `6324b66` e `0dad413` sincronizados com `origin/main`.

Ver `docs/05f-pricing.md` para o detalhamento item a item (não repetido aqui, por já estar consolidado no documento normativo).

---

## 3. Migrations e contratos frontend registrados

**Migrations `CONFIRMADO EXECUTADO`** (nenhuma nova nesta rodada):
- `3903` — `get_cards_pricing_summary(uuid[])`, contrato de leitura em lote sem N+1, consumido pelos três grids.
- `3904` — hierarquia de printing Normal → Holofoil → Reverse Holofoil em `get_cards_pricing_summary`, corrigindo o bug real encontrado pelo teste "Reverse-only" (uma carta só com printing Reverse Holofoil não aparecia com preço).

**Contratos frontend entregues**: `web/lib/pricing/pricing-batch-client.ts` (tipo `PricingLiveSummary`, incluindo `printingLabel: string | null`) e `web/components/card/card-price-summary.tsx` (`CardPriceSummary`/`CardPriceDetails`, pill de preço + popover com variantes em NM, `PRINTING_ORDER`, `aria-label` dinâmico por printing).

---

## 4. Estado do domínio Pricing após o P12 e recomendação de P13

**A visualização de preços (P12) nunca chama a JustTCG** — lê exclusivamente `pricing_observation` já sincronizada via `get_cards_pricing_summary`/`get_card_pricing_snapshot`. Sincronização (P8, `scripts/sync-justtcg-pricing.ts`) e consulta/visualização permanecem arquiteturalmente desacopladas, por design desde `ADR-029`.

`JUSTTCG.is_active` permanece `FALSE` — sem plano comercial contratado (Termos de Uso, Seção 6/7.1, restringem uso comercial ao tier pago). Pricing ainda não é operacional em produção: é consumível localmente quando há dado elegível (piloto restrito já sincronizado em P8), mas sem ingestão contínua.

PTAX (P9, `scripts/sync-ptax-fx-rate.ts`) pode ser automatizada — hoje é acionada manualmente por Fabrício.

**Próximo incremento recomendado: P13 — Orquestração Programada de Pricing.** Cobriria o agendamento automático de PTAX e, quando `JUSTTCG.is_active` for reativada por decisão comercial, o agendamento do conector JustTCG. Sem escopo técnico, mecanismo de agendamento (cron/Edge Function/scheduler externo) ou cronograma definidos nesta rodada — recomendação registrada, aguardando priorização explícita de Fabrício. Alternativa igualmente válida, já registrada em `ROADMAP.md`: iniciar Collection.

---

## 5. Fora do escopo deste encerramento

A correção do erro `AuthApiError: Invalid Refresh Token: Refresh Token Not Found` em `/login` permanece **explicitamente fora do escopo** deste encerramento e de qualquer incremento fechado. Diagnóstico: cookie `sb-*-auth-token` obsoleto no navegador, produzindo um `console.error` benigno capturado pelo overlay de erro do Next.js 15 em modo dev — não há `middleware.ts` global no projeto (removido após um incidente anterior no Edge Runtime da Vercel); cada página protegida (`LoginPage`, `HomePage`, `/perfil`) chama `supabase.auth.getUser()` diretamente. Três opções foram apresentadas a Fabrício; ele escolheu explicitamente "só limpar cookies locais" — nenhuma alteração de código foi feita.

---

## 6. Checklist de Retomada

- [x] Fabrício aprovou as 14 evidências do P12 (seção 2) — confirmado em chat, 2026-08-18.
- [x] Migrations `3903`/`3904` confirmadas `CONFIRMADO EXECUTADO`.
- [x] Commits `6324b66`/`0dad413` confirmados sincronizados com `origin/main` (auditoria de repositório prévia).
- [x] Documentação atualizada no mesmo ciclo: `05f-pricing.md`, `ADR-029`, `ROADMAP.md`, `README.md`, `INDEX.md`, este handoff, `docs/log.md`.
- [ ] **Próximo passo real, em aberto, dependente de decisão de Fabrício**: iniciar P13 — Orquestração Programada de Pricing, ou iniciar Collection.
- [ ] Corrigir (ou decidir não corrigir) o erro de `/login` — fora do escopo deste encerramento, ver seção 5.

**Arquivos criados/editados nesta rodada** (todos `.md`, nenhum código/banco/migration): `docs/05f-pricing.md` (versão `1.30`); `docs/adr/ADR-029-pricing-domain-model.md` (revisão `1.23`); `docs/ROADMAP.md` (revisão `1.62`); `docs/README.md` (revisão `2.15`); `docs/INDEX.md` (revisão `1.19`); `docs/development/HANDOFF-2026-08-18.md` (este arquivo, novo); `docs/log.md`. `docs/development/HANDOFF-2026-08-17.md` movido para `docs/history/development/`.

---

---

## 7. Amendo — Incremento P13.1 (mesmo dia, ciclo seguinte)

Ainda em 2026-08-18, a pedido explícito de Fabrício: implementação do **Incremento P13.1 — Fundação de Orquestração Programada de Pricing**, primeira rodada real do P13 recomendado na seção 4. Escopo desta rodada: schema, constraints, concorrência, segurança e ADR correspondente — nenhuma extensão (`pg_cron`/`pg_net`) habilitada, nenhum Vault secret criado, nenhuma Edge Function implantada, nenhuma chamada externa, `scripts/sync-ptax-fx-rate.ts` intocado, nenhum código JustTCG.

**Migrations `3905`–`3907` (`CONFIRMADO EXECUTADO`)** em `pricing_sync_run`: `FX_REFRESH` adicionado a `run_type` sem reaproveitar `PRICE_REFRESH`; identidade cambial explícita via `fx_source_code TEXT` (alinhada a `pricing_fx_rate.rate_source_code`, sem `FK` — BCB/PTAX nunca vira linha de `pricing_source`); `CHECK` correlacionado garantindo `pricing_source_id`/`fx_source_code` mutuamente exclusivos conforme `run_type`; `confirmed_by` passa a opcional, com `CHECK` correlacionado a `triggered_by` (`MANUAL` exige admin real, `SCHEDULED` exige `NULL` — nenhum administrador sintético criado); dois índices únicos parciais (preço e cambial) garantindo aquisição atômica de execução ativa, sem advisory lock.

Onze cenários validados transacionalmente (`ROLLBACK`), zero resíduo (5 linhas reais de produção intocadas); `get_advisors` sem achado novo. Nenhum `GRANT` novo necessário — `service_role` já cobria a coluna nova em nível de tabela.

**Divergência sinalizada e resolvida**: o pedido original citava `ADR-030` para a nova decisão — já em uso por Card Search Projection, confirmado por introspecção do `ADR-INDEX.md` antes de criar qualquer arquivo. A arquitetura foi registrada em **`ADR-031`** (número livre real), cobrindo tanto a arquitetura-alvo completa do P13 (Supabase Cron/`pg_net`/Edge Function dedicada/Vault, autenticação por secret key dedicada validada manualmente em tempo constante — decisão explícita de não depender do pacote `@supabase/server`, em Public Beta) quanto esta fundação de schema.

Documentos atualizados nesta rodada: `docs/adr/ADR-031-scheduled-pricing-orchestration.md` (novo), `docs/adr/ADR-INDEX.md` (versão `2.20`), `docs/05f-pricing.md` (versão `1.31`), `docs/standards/STD-001-database-standards.md` (versão `1.38`), `docs/INDEX.md` (versão `1.20`), `docs/log.md`, este handoff. `docs/README.md`/`docs/ROADMAP.md` deliberadamente fora do escopo desta rodada (Fabrício pediu atualização só dos documentos listados).

**Pendente para P13.2+**: habilitar `pg_cron`/`pg_net`; criar o Vault secret dedicado; extrair módulo compartilhado PTAX (script/Edge Function como adaptadores); implantar a Edge Function; desenhar o mecanismo de não sobrescrever taxa histórica divergente; agendar o Job real. JustTCG permanece inteiramente fora do P13.

---

## 8. Amendo — Auditoria final do P13.1 (mesmo dia, terceiro ciclo)

Ainda em 2026-08-18, a pedido explícito de Fabrício: auditoria final do P13.1 (`git status --short`/`diff --stat`/`diff --cached --stat`/`ls-files --others`/`diff --check`). Dois achados reais, ambos corrigidos no mesmo ciclo, nenhum aplicado em silêncio:

1. **`fx_source_code` sem formato normalizado** — `pricing_source.code`/`pricing_fx_rate.rate_source_code` já exigem maiúsculas + `^[A-Z][A-Z0-9_]*$`; a migration `3905` só garantia `NOT NULL`/não-branco. `3905` não foi editada (instrução explícita) — migration `3908` (`CONFIRMADO EXECUTADO`) adiciona `ck_pricing_sync_run_fx_source_code_format`, mesmo padrão das colunas irmãs, validada com oito cenários transacionais antes da aplicação, zero linha real afetada.
2. **Migrations `3905`–`3907` ausentes de `database/migrations/`** — `CONFIRMADO EXECUTADO` no Supabase, mas sem arquivo físico no repositório, quebrando a convenção vigente desde a Query `3700`. Corrigido versionando retroativamente os quatro arquivos (`3905`–`3908`), SQL idêntico ao efetivamente aplicado.

Confirmado: `ADR-031-scheduled-pricing-orchestration.md` presente e correto; nenhum arquivo de outra frente incorporado; nada staged inesperadamente; `git diff --check` limpo. `get_advisors` sem achado novo em nenhum dos dois pontos.

Documentos atualizados nesta rodada: `docs/adr/ADR-031-scheduled-pricing-orchestration.md` (revisão `1.1`), `docs/05f-pricing.md` (versão `1.32`), `docs/standards/STD-001-database-standards.md` (versão `1.39`), `docs/log.md`, este handoff, mais quatro arquivos novos em `database/migrations/` (`3905`–`3908`).

**Nenhum commit ou push foi realizado nesta rodada.** Por instrução explícita de Fabrício, a decisão de commitar/pushar permanece com ele.

---

## 9. Amendo — Incremento P13.2 (mesmo dia, quarto ciclo)

Ainda em 2026-08-18, a pedido explícito de Fabrício: **Incremento P13.2 — Módulo Compartilhado e Execução Manual Auditável da PTAX**. Nenhuma extensão habilitada, nenhum Vault secret, nenhuma Edge Function, nenhuma chamada real ao BCB durante a validação, nenhum código JustTCG.

**Introspecção prévia**: schema efetivo de `pricing_fx_rate`/`pricing_sync_run`/`pricing_sync_run_call` confirmado (colunas, constraints, índices, grants, RLS) antes de qualquer edição — `pricing_sync_run_call` já tinha, desde `3090`, todas as colunas que o adapter precisaria; **nenhuma migration nova foi necessária**, decisão relatada em vez de aplicada.

**Módulo compartilhado**: `supabase/functions/_shared/pricing-ptax/` — núcleo puro (`core.ts` orquestrador; `period.ts`/`url.ts`/`http.ts`/`validate.ts`/`select-closing.ts`/`persist.ts`/`sanitize.ts`/`sync-run-orchestration.ts`/`types.ts`, reexportados por `mod.ts`, mesmo padrão de `_shared/catalog-normalization/`). Nenhuma leitura de `Deno.env`/cliente Supabase/relógio dentro do núcleo — `runPtaxSync()` recebe data de referência, `fetch`, espera e repositório por parâmetro, pronto para reuso direto por uma futura Edge Function agendada (P13.3), sem duplicar lógica.

**Decisões de design registradas**: janela padrão de 10 datas corridas (referência + nove dias anteriores, America/Sao_Paulo, sem calendário de dias úteis); override explícito validado (formato/ordem/máximo de 90 dias, `MAX_OVERRIDE_WINDOW_DAYS`, novo nesta rodada); seleção de fechamento por ordenação cronológica de `dataHoraCotacao` (o endpoint já confirmado por chamada real do P9 não expõe `tipoBoletim` — ampliar `$select` exigiria nova confirmação real, fora de escopo); retry HTTP de 3 tentativas, 1s/3s, só para falha de rede/timeout/408/429/5xx.

**Adapter manual** (`scripts/sync-ptax-fx-rate.ts`, reescrito): exige `--confirmed-by=` para escrita real; abre `pricing_sync_run` (`FX_REFRESH`/`MANUAL`/`pricing_source_id=NULL`/`fx_source_code='BCB_PTAX'`) como primeiro efeito colateral, ANTES de qualquer chamada ao BCB; captura o código Postgres `23505` do índice de concorrência (`3907`) e aborta sem tocar a rede se já houver execução ativa; registra cada chamada HTTP real (inclusive retries) em `pricing_sync_run_call`; nunca cria linha artificial para divergência — `error_detail` do call log reflete só a chamada HTTP em si, divergência/inválido só em `pricing_sync_run.error_summary` (nunca vazio); run com divergência/inválido termina `COMPLETED_WITH_ERRORS`, nunca `FAILED`; estado terminal garantido via `try/catch/finally`; `--dry-run` nunca cria/finaliza `pricing_sync_run`/`pricing_sync_run_call`.

**Testes**: 56 asserções 100% offline (`pricing-ptax.test.ts`), cobrindo os 16 cenários pedidos. Sandbox sem runtime Deno disponível (mesma limitação já registrada para `npm run build`/`lint`) — executadas via Node, sintaxe validada em todos os treze arquivos (`node --check`), árvore de import real do repositório confirmada de ponta a ponta com stub local de `npm:@supabase/supabase-js`. Zero chamada de rede real ao BCB confirmada.

**Pendência sinalizada, não corrigida neste ciclo** (fora do escopo pedido): `scripts/Executar-P9-PTAX-Local.ps1` pressupõe a janela hardcoded antiga do P9 e não passa `--confirmed-by=`/override — precisa de revisão antes do próximo uso manual real.

Documentos atualizados nesta rodada: `docs/adr/ADR-031-scheduled-pricing-orchestration.md` (revisão `1.2`), `docs/05f-pricing.md` (versão `1.33`), `docs/log.md`, este handoff.

**Nenhum commit ou push foi realizado nesta rodada.** Por instrução explícita de Fabrício, a decisão de commitar/pushar permanece com ele.

---

## 10. Amendo — Finalização operacional do P13.2 (mesmo dia, quinto ciclo)

Ainda em 2026-08-18, a pedido explícito de Fabrício: resolve a pendência sinalizada na seção 9. Nenhuma chamada real ao BCB nesta rodada, nenhuma migration, nenhum código JustTCG, nenhum commit/push.

**Runner reescrito**: `scripts/Executar-P9-PTAX-Local.ps1` alinhado ao contrato do adapter de P13.2 — `--confirmed-by=<admin_user_uuid>` exigido só em execução com escrita (nunca em `-DryRun`); `-OverrideStart`/`-OverrideEnd` opcionais e sempre pareados; sem datas, usa a janela padrão calculada de 10 dias; UUID e formato de data validados no PowerShell ANTES de qualquer invocação do Deno; argumentos encaminhados via array (`& deno @DenoArgs`); código de saída propagado sem alteração; segredo nunca impresso (sanitização ampliada para também redigir o formato `sb_secret_*`/`sb_publishable_*`). O antigo fluxo fixo de dupla execução (prova de idempotência contra a janela hardcoded do P9) foi removido — sem sentido com janela dinâmica; idempotência segue garantida pelo `ON CONFLICT DO NOTHING` do conector e coberta por `--fixture-check`.

**Semântica de status reconfirmada** por leitura direta de `sync-run-orchestration.ts` (código não alterado nesta rodada): fim de semana/feriado sem cotação não é erro; execução válida sem taxa nova termina `COMPLETED`; divergência histórica termina `COMPLETED_WITH_ERRORS`; falha HTTP/contrato termina `FAILED`; conflito de execução ativa (`23505`) impede a chamada ao BCB.

**Validação**: 56 asserções offline revalidadas via Node (zero regressão); `git diff --check`, `git status --short`, `git ls-files --others --exclude-standard` e varredura de segredo revalidados, sem achado novo. Os 12 arquivos novos do núcleo compartilhado (`core.ts`, `http.ts`, `mod.ts`, `period.ts`, `persist.ts`, `pricing-ptax.test.ts`, `sanitize.ts`, `select-closing.ts`, `sync-run-orchestration.ts`, `types.ts`, `url.ts`, `validate.ts`) seguem untracked, confirmados como parte do futuro commit único. Comandos Deno nativos (versão/`check`/`--fixture-check`) fornecidos a Fabrício para validação local — ver relatório desta rodada em chat.

Documentos atualizados nesta rodada: `docs/adr/ADR-031-scheduled-pricing-orchestration.md` (revisão `1.3`), `docs/05f-pricing.md` (versão `1.34`), `docs/log.md`, este handoff.

**Nenhum commit ou push foi realizado nesta rodada.** Por instrução explícita de Fabrício, a decisão de commitar/pushar permanece com ele.

---

## 11. Amendo — Correção de autoridade temporal e ordenação da telemetria do P13.2 (mesmo dia, sexto ciclo)

Ainda em 2026-08-18, a pedido explícito de Fabrício, motivado por uma auditoria pós-piloto real que Fabrício mesmo conduziu contra os dados persistidos pelo runner corrigido na seção 10. Nenhuma execução real do sincronizador, nenhuma chamada ao BCB, nenhuma migration fora da corretiva abaixo, nenhum código JustTCG, nenhum commit/push.

**Achado da auditoria**: a execução real `pricing_sync_run.id = afeca458-7a5c-467d-80c3-49f234e608f3` mostrava `pricing_sync_run_call.called_at` aparentemente 61,98s antes do próprio `started_at` do run ao qual pertence. Uma investigação somente leitura (ciclo interrompido antes deste, sem alteração de banco/arquivo/documentação) confirmou a causa-raiz por completo antes de qualquer correção ser implementada: `pricing_sync_run.started_at`/`.finished_at` eram capturados por `new Date().toISOString()` no relógio do PROCESSO CLIENTE (máquina Windows local de Fabrício), não pelo `now()` nativo do Postgres — enquanto `created_at`/`updated_at`/`pricing_sync_run_call.called_at` sempre usaram `now()` do banco. Qualquer desvio entre os dois relógios produz uma inversão temporal aparente, nunca real. Confirmado sistêmico nos 6 `pricing_sync_run` pré-existentes (P8/JustTCG de 2026-08-17, P13.2/PTAX de 2026-08-18), desvio variando 18,7s–62,0s dentro da mesma sessão. Os valores de `pricing_fx_rate` persistidos nunca estiveram incorretos — o achado é inteiramente de telemetria de execução.

**Correção implementada**: decisão registrada — "PostgreSQL é a autoridade dos timestamps persistidos" de `pricing_sync_run`. Migration `3909` (`CONFIRMADO EXECUTADO`) cria o trigger dedicado `set_pricing_sync_run_server_timestamps()` (`BEFORE INSERT OR UPDATE`, nunca consolidado em `set_updated_at()`, função genérica compartilhada por dezenas de outras tabelas): `started_at` sempre `now()` do servidor em todo `INSERT`, independentemente do valor client-side; `finished_at` sempre `now()` do servidor na primeira transição de status não-terminal para terminal (`COMPLETED`/`COMPLETED_WITH_ERRORS`/`FAILED`/`CANCELLED` — este último incluído por consistência com a `CHECK ck_pricing_sync_run_finished_consistency` já existente, divergência do pedido original sinalizada explicitamente antes de implementar); runs já terminais nunca têm `finished_at` recalculado; nenhuma linha histórica alterada ou corrigida por backfill — os 6 `pricing_sync_run` já existentes preservam o desvio já documentado. Nenhum `GRANT` novo necessário. `scripts/sync-ptax-fx-rate.ts` (único adapter alterado) para de enviar `started_at`/`finished_at` client-side e passa a persistir `pricing_sync_run_call` ANTES de finalizar o run — se a persistência das calls falhar, o run finaliza como `FAILED`, nunca `COMPLETED`. `scripts/sync-justtcg-pricing.ts` tem o mesmo padrão pré-existente e continua intocado (fora do escopo explícito), mas fica protegido automaticamente pela regra do banco, sem nenhuma alteração de código nele.

**Validação**: 10/10 cenários transacionais (`BEGIN`/`ROLLBACK`) antes da aplicação real, revalidados no schema real após aplicação; `get_advisors` (segurança + performance) sem nenhum achado novo relacionado a `pricing_sync_run`/à função nova; grants/RLS confirmados idênticos aos de antes desta rodada; 70 asserções offline (60 pré-existentes + 10 novas, incluindo reexecução idempotente real com as 7 datas do piloto e testes de orquestração local do adapter) revalidadas via harness Node (sandbox sem runtime Deno disponível), zero chamada de rede/BCB confirmada; varredura de segredo sem achado. Comandos Deno nativos (`fmt --check`/`lint`/`check`/`--fixture-check`) fornecidos a Fabrício para confirmação real local, mesma metodologia já usada nas rodadas anteriores desta sessão.

**Recomendação objetiva**: com a causa-raiz corrigida na origem (banco) e a ordem da telemetria garantida no adapter, o teste controlado de idempotência real (recomendado no fim da auditoria pós-piloto original, seção anterior a esta) pode agora ser autorizado — nenhum impedimento técnico remanescente identificado.

Documentos atualizados nesta rodada: `docs/adr/ADR-031-scheduled-pricing-orchestration.md` (revisão `1.4`), `docs/05f-pricing.md` (versão `1.35`), `docs/log.md`, este handoff, mais um arquivo novo em `database/migrations/` (`3909`).

**Nenhum commit ou push foi realizado nesta rodada.** Por instrução explícita de Fabrício, a decisão de commitar/pushar permanece com ele.

---

## 12. Amendo — Encerramento formal do Incremento P13.2 (mesmo dia, sétimo ciclo)

Ainda em 2026-08-18, a pedido explícito de Fabrício, depois de uma auditoria pós-idempotência real (somente leitura, run `eaad7b81-0305-4981-bf15-495741ceb50d`) confirmar que a correção da seção 11 funcionou como esperado. Nenhuma alteração de código, banco, migration ou configuração nesta rodada — encerramento exclusivamente documental. Nenhuma execução do sincronizador, nenhuma chamada ao BCB, nenhum commit/push.

**P13.2 declarado formalmente concluído** — cobre os três ciclos da mesma frente (seções 9, 10 e 11): módulo compartilhado e execução manual auditável da PTAX, finalização operacional do runner, e correção de autoridade temporal/ordenação da telemetria motivada por achado do próprio piloto real. **P13 como um todo permanece em andamento, não declarado concluído** — P13.1 já estava concluído desde `ADR-031` revisão `1.0`; falta apenas P13.3.

**Evidências consolidadas do encerramento**: módulo puro `supabase/functions/_shared/pricing-ptax/` com 12 arquivos, sem leitura de ambiente/cliente Supabase/relógio; adapter `scripts/sync-ptax-fx-rate.ts` e runner `scripts/Executar-P9-PTAX-Local.ps1` reescritos; janela de 10 datas corridas e override de até 90 dias; retry HTTP de 3 tentativas (1s/3s); seleção do fechamento mais recente por data; idempotência append-only; divergência nunca sobrescreve taxa; conflito de concorrência detectado antes do BCB; telemetria completa em `pricing_sync_run`/`pricing_sync_run_call`; execução manual exige administrador real; `--dry-run` não grava; zero segredo persistido/exibido; JustTCG permaneceu inativa e fora do fluxo PTAX. Migration `3909`: causa-raiz (mistura de relógio cliente/servidor), PostgreSQL como autoridade de `started_at`/`finished_at`, quatro estados terminais cobertos (`COMPLETED`/`COMPLETED_WITH_ERRORS`/`FAILED`/`CANCELLED`), calls persistidas antes da finalização, falha de persistência nunca termina como `COMPLETED`, linhas históricas preservadas sem backfill (skew conhecido nos 6 runs anteriores), JustTCG protegido pela regra do banco sem alteração de código.

**Evidências de validação**: `deno fmt --check` — 13 arquivos aprovados; `deno lint` — 13 arquivos aprovados; `deno check scripts/sync-ptax-fx-rate.ts` — aprovado; fixture-check — 70/70; testes transacionais da migration `3909` — 10/10; `get_advisors` sem achado novo relacionado; grants/RLS inalterados. Piloto persistido (`afeca458-7a5c-467d-80c3-49f234e608f3`): 7 cotações, `inserted=1`/`unchanged=6`/`divergent=0`/`invalid=0`, inclusão de 2026-08-18 = `5.20430000` — foi a partir deste piloto que o problema temporal foi descoberto. Reexecução idempotente após a correção (`eaad7b81-0305-4981-bf15-495741ceb50d`): 7 cotações, `inserted=0`/`unchanged=7`/`divergent=0`/`invalid=0`, `pricing_fx_rate` com 7 linhas sem duplicidade, `status=COMPLETED`, uma call `SUCCESS`/HTTP `200`; ordem comprovada sem inversão — `created_at = started_at`; `called_at` 1017,233 ms depois; `finished_at` 86,470 ms depois da call; `finished_at = updated_at`. Contagens finais: `pricing_fx_rate=7`, `pricing_sync_run=7`, `pricing_sync_run_call=30`, `FX_REFRESH` ativo=`0`, exatamente dois `FX_REFRESH` `MANUAL`, `JUSTTCG.is_active=FALSE`.

**Ponto de retomada — P13.3**: criar a Edge Function `ptax-fx-refresh`, reutilizando exclusivamente o módulo compartilhado; autenticação serviço-a-serviço por secret dedicado, validação conservadora e em tempo constante; nenhuma chamada JustTCG; `service_role` nunca exposto ao chamador; primeiro validar invocação manual autenticada, depois habilitar `pg_cron`/`pg_net`/Vault e agendar o Job real (salvo se o `ROADMAP.md` vigente já definir outra decomposição explícita); frontend nunca dispara sincronização, em nenhuma etapa futura.

**Auditoria final do repositório desta rodada**: `git status --short` mostra 6 arquivos modificados (documentação) mais os 3 já pendentes de ciclos anteriores (`database/migrations/3909_*.sql`, `deno-check-p13-2.txt`, `supabase/functions/_shared/pricing-ptax/` — 12 arquivos, untracked); `git diff --check` limpo; nenhum arquivo de outra frente incorporado; varredura de segredo sem achado. `deno-check-p13-2.txt` permanece untracked, não editado, não staged — **sinalizado para remoção manual por Fabrício antes do commit**, por não pertencer ao conteúdo versionável do incremento.

Documentos atualizados nesta rodada: `docs/adr/ADR-031-scheduled-pricing-orchestration.md` (revisão `1.5`), `docs/05f-pricing.md` (versão `1.36`), `docs/ROADMAP.md` (revisão `1.63`), `docs/README.md` (revisão `2.16`), `docs/INDEX.md` (versão `1.21`), `docs/log.md`, este handoff.

**Nenhum commit ou push foi realizado nesta rodada.** Por instrução explícita de Fabrício, a decisão de commitar/pushar permanece com ele.

---

## 13. Amendo — Encerramento formal do Incremento P13.3 (mesmo dia, oitavo ciclo)

Ainda em 2026-08-18, a pedido explícito de Fabrício, em três ciclos consecutivos: implementação da Edge Function `ptax-fx-refresh`, correção estrutural da porta de persistência + registro no runner nativo do Deno, e deploy controlado com piloto real autenticado.

**Implementação**: `supabase/functions/ptax-fx-refresh` reaproveita `runPtaxSync()` sem duplicar lógica; `verify_jwt=false`; autenticação por segredo dedicado no header `apikey`, comparação manual em tempo constante (`auth.ts`, sem `@supabase/server`); handler puro (`handler.ts`) testável offline sem Deno; `index.ts` como adapter fino. Execução sempre `triggered_by='SCHEDULED'`/`confirmed_by=NULL`.

**Correção estrutural (mesmo dia, antes de qualquer commit)**: a primeira versão de `run-lifecycle.ts` usava um tipo estrutural (`SupabaseLike`/`QueryBuilderLike`/`PostgrestFilterBuilderLike`) tentando reproduzir a API fluente do `@supabase/supabase-js` real — abandonada por decisão explícita de Fabrício, motivada por uma falha real de `deno check` (o retorno de `.from()` é estruturalmente diferente do retorno após `.select()`/`.insert()`/`.upsert()`/`.update()`). Substituída por `PtaxSyncRunPort` — porta funcional estreita de cinco operações de domínio, implementada uma única vez por `_shared/pricing-ptax/supabase-adapter.ts`, construída uma única vez por cada chamador (CLI e Edge Function) e reaproveitada tanto como `PtaxRateRepository` quanto como ciclo de vida do run. Zero `any`/cast duplo/supressão/tipo recursivo; testes convertidos para fakes 100% tipados da porta, nunca de `SupabaseClient`/PostgREST. Validado por `tsc --strict` real (compilador do próprio repositório, `web/node_modules/.bin/tsc`). `ptax-fx-refresh.test.ts` registrado no runner nativo via `Deno.test()` (guardado por `typeof Deno !== "undefined"`, delega ao harness já existente, nenhum cenário novo).

**Validação local real, confirmada por Fabrício** (antes do deploy): `deno fmt --check`/`deno lint`/`deno check` — 21 arquivos aprovados; `--fixture-check` do CLI — 70/70; suíte offline via `deno eval` — 43/43; `deno test` nativo — `1 passed, 0 failed`.

**Deploy controlado**: `ptax-fx-refresh` implantada no projeto Supabase `qjfutqujxrbzgrtkpgkg` via Supabase MCP (`deploy_edge_function`), arquivos correntes exatos (runtime apenas — testes e `deno.lock` fora do pacote). Confirmado: `slug=ptax-fx-refresh`, `version=1` (primeiro deploy), `status=ACTIVE`, `verify_jwt=false`. Nenhuma invocação/chamada ao BCB/alteração de banco-secrets-Cron-Vault-código-documentação/commit-push nesse ciclo.

**Piloto real autenticado** (ciclo seguinte): `PTAX_FX_REFRESH_SECRET` já configurado por Fabrício (fora do escopo deste agente — nunca visto por ele). Dois testes negativos antes do positivo: sem `apikey`/com `apikey` incorreto → `HTTP 401`, zero efeito no banco. Com `apikey` correto → `HTTP 200`/`COMPLETED`, run `e75e8fca-1c2e-498a-a473-18afa74c152d`, `triggered_by=SCHEDULED`/`confirmed_by=NULL`, 7 cotações/`inserted=0`/`unchanged=7`/`divergent=0`/`invalid=0` (reexecução idempotente sobre a janela já coberta por P13.2), uma `pricing_sync_run_call` (`SUCCESS`/`200`) persistida antes da finalização, linha temporal sem inversão. Contagens finais: `pricing_fx_rate=7`, `pricing_sync_run=8`, `pricing_sync_run_call=31`, zero `FX_REFRESH` ativo.

**P13.3 declarado formalmente concluído. P13 como um todo permanece em andamento** — falta só P13.4.

**Ponto de retomada — P13.4**: habilitar `pg_cron`/`pg_net`, criar o Vault secret dedicado do lado Postgres, e agendar o Job real chamando `ptax-fx-refresh` (já implantada e validada) periodicamente. Nome do Job, frequência e retry/backoff da própria invocação do Cron seguem em aberto. Nenhuma chamada JustTCG; `service_role` nunca exposto ao chamador; frontend nunca dispara sincronização.

**Auditoria final do repositório desta rodada**: ver relatório de encerramento apresentado a Fabrício no mesmo ciclo — `git status`/`git diff --stat` revisados, arquivos novos conferidos, `deno.json`/`deno.lock` novos (`scripts/`) conferidos como intencionais (import map do CLI), varredura de segredo sem achado.

Documentos atualizados nesta rodada: `docs/adr/ADR-031-scheduled-pricing-orchestration.md` (revisões `1.6`/`1.7`), `docs/05f-pricing.md` (versão `1.37`), `docs/ROADMAP.md` (revisão `1.64`), `docs/log.md`, este handoff.

**Nenhum commit ou push foi realizado nesta rodada.** Por instrução explícita de Fabrício, a decisão de commitar/pushar permanece com ele.

---

## 14. Amendo — Incremento P13.4: Agendamento Automático via `pg_cron`/`pg_net`/Vault (mesmo dia, nono ciclo)

Ainda em 2026-08-18, a pedido explícito de Fabrício. Introspecção prévia confirmou `pg_cron`/`pg_net` não instalados, `supabase_vault` já instalado, nenhum job homônimo, migration livre `3910`, nomes `ptax_fx_refresh_url`/`ptax_fx_refresh_secret` existentes no Vault (só nome, nunca valor), e documentação oficial da Supabase sem divergência material.

**Migration `3910_schedule_ptax_fx_refresh_weekdays_cron_job`** (`CONFIRMADO EXECUTADO`): habilita `pg_cron` (`WITH SCHEMA pg_catalog` + os dois `GRANT` oficiais a `postgres`) e `pg_net`; remove idempotentemente só um job homônimo pré-existente (nenhum existia); cria o Job único `ptax-fx-refresh-weekdays` (`0 22 * * 1-5`, `22:00 UTC`/`19:00 America/Sao_Paulo`, timezone do banco `UTC`), cujo comando chama `net.http_post` diretamente com `url`/`apikey` resolvidos por nome via `vault.decrypted_secrets` (nunca valor literal), `body='{}'`, `timeout_milliseconds=60000`. Testado transacionalmente (`BEGIN`/`ROLLBACK`) antes da aplicação real.

**Job único confirmado em produção**: `jobid=1`, `active=true`, único job em `cron.job`; `command` contém só os dois nomes do Vault, nenhum valor.

**Chamada controlada única** pelo mesmo caminho `net.http_post` (fora do Cron): `HTTP 200`/`COMPLETED`, run `5ee0b0c5-0d08-4889-908d-9d288123e1ae`, `triggered_by=SCHEDULED`/`confirmed_by=NULL`, 7 cotações/`inserted=0`/`unchanged=7`/`divergent=0`/`invalid=0`, uma `pricing_sync_run_call` (`SUCCESS`/`200`), zero run ativo residual. Contagens finais: `pricing_fx_rate=7`, `pricing_sync_run=9`, `pricing_sync_run_call=32`.

**Segurança**: schema `cron` bloqueado a `anon`/`authenticated`/`service_role`; Vault (`vault.secrets`/`vault.decrypted_secrets`) inacessível aos mesmos papéis. Duas observações de transparência sem correção nesta rodada (comportamento padrão da instalação oficial do `pg_net`, não introduzido por este agente): schema `net` com `USAGE`/`EXECUTE` client-facing por padrão; `pg_net` registrado sob `extnamespace='public'` por metadado de catálogo. `get_advisors` (performance) sem achado novo.

**P13.4 implementado e validado manualmente — aguardando a primeira execução autônoma do Cron (prevista quarta-feira, `2026-08-19`, `22:00 UTC`) antes de qualquer encerramento formal de P13.** Nenhum código JustTCG, alteração de frontend, ou objeto além do estritamente necessário (nenhuma função auxiliar/tabela/trigger/política/`GRANT`/`SECURITY DEFINER` além das duas extensões e do Job). Nenhum segredo real visto/exibido/registrado por este agente.

Documentos atualizados nesta rodada: `docs/adr/ADR-031-scheduled-pricing-orchestration.md` (revisão `1.8`), `docs/05f-pricing.md` (versão `1.38`), `docs/log.md`, este handoff.

**Nenhum commit ou push foi realizado nesta rodada.** Por instrução explícita de Fabrício, a decisão de commitar/pushar permanece com ele.

## 15. Amendo — Antecipação da validação autônoma do P13.4 e encerramento formal do Incremento P13 (2026-08-19, décimo ciclo)

A pedido explícito de Fabrício: antecipar a validação autônoma do P13.4 para `21:25 America/Sao_Paulo` de hoje (2026-08-19), sem alterar o Job permanente `ptax-fx-refresh-weekdays` nem seu `schedule`.

**Introspecção prévia**: assinaturas reais de `cron.schedule` confirmadas via `pg_get_function_identity_arguments` — só `cron.schedule(job_name text, schedule text, command text)`/`cron.schedule(schedule text, command text)` existem em `pg_cron 1.6.4`; uma tentativa real de one-shot por timestamp absoluto (`cron.schedule(nome, '2026-08-19 00:25:00', comando)`) retornou `ERRO 22023: invalid schedule ... Use cron format ... ou interval format '[1-59] seconds'` — confirmado por evidência direta que essa via não existe nesta versão.

**Fallback do próprio pedido usado**: Job temporário `ptax-fx-refresh-oneoff-20260819-0027utc`, protegido por data fixa na própria expressão cron (`27 0 19 8 *` — só dispara em 19 de agosto, `00:27 UTC`, nunca em outra data/ano), reaproveitando exatamente o mesmo comando `net.http_post`/Vault do Job permanente. Nenhuma migration nova. Job permanente (`jobid=1`) nunca tocado, confirmado antes e depois.

**Divergência sinalizada em tempo real, não aplicada em silêncio**: o horário exato pedido (`21:25 America/Sao_Paulo`/`00:25 UTC`) já havia decorrido quando a confirmação da assinatura terminou (`now()` do Postgres em `00:25:35 UTC`) — divergência entre o relógio do sandbox local e o relógio do servidor, o único que importa para o agendamento. Alvo replanejado para `00:27 UTC`/`21:27 America/Sao_Paulo`, dois minutos depois.

**Resultado — disparado pelo próprio `pg_cron`, sem chamada manual no momento do disparo**: `cron.job_run_details` (`jobid=2`, `runid=1`, `status=succeeded`, `start_time=00:27:00.142395+00` — primeira linha de `job_run_details` já existente no projeto); `net._http_response` (`id=2`, `HTTP 200`); `pricing_sync_run` (`id=832f352a-695e-4480-81df-54fb28afcd9b`, `FX_REFRESH`/`SCHEDULED`/`confirmed_by=NULL`/`COMPLETED`, `started_at=00:27:01.717`/`finished_at=00:27:02.433`, sem inversão); uma `pricing_sync_run_call` (`SUCCESS`/`200`, `called_at=00:27:02.292`); `pricing_fx_rate` sem linha nova (reexecução idempotente correta); zero run ativo residual. Job temporário removido logo após a auditoria — `cron.job` de volta a só o Job permanente, intocado.

**Achado de transparência, não introduzido nesta rodada**: duas linhas `SCHEDULED` pré-existentes em `pricing_sync_run` (`23:08:35`/`23:50:19` de `2026-08-18`) sem execução correspondente em `cron.job_run_details` — a primeira também sem `net._http_response` correspondente. Ambas terminais (`COMPLETED`), sem run ativo residual, origem não identificável a partir do banco (provável invocação HTTP direta à Edge Function, fora de `net.http_post`/Postgres). Registrado para conhecimento de Fabrício, sem bloquear esta validação.

**P13.4 e P13 — Orquestração Programada de Pricing formalmente encerrados.** Job permanente segue ativo, intocado, com seu próprio primeiro tick natural ainda previsto para hoje, `22:00 UTC` — confirmação adicional passiva, não bloqueante. Nenhum código JustTCG, alteração de frontend, ou objeto além do estritamente necessário. Nenhum segredo real visto/exibido/registrado.

Documentos atualizados nesta rodada: `docs/adr/ADR-031-scheduled-pricing-orchestration.md` (revisão `1.9`), `docs/05f-pricing.md` (versão `1.39`), `docs/ROADMAP.md` (revisão `1.65`), `docs/README.md` (revisão `2.19`), `docs/INDEX.md` (versão `1.24`), `docs/log.md`, este handoff.

**Nenhum commit ou push foi realizado nesta rodada.** Por instrução explícita de Fabrício, a decisão de commitar/pushar permanece com ele.

## 16. Amendo — Incremento P14.1: Ativação Comercial e Piloto Controlado da JustTCG (2026-08-19, décimo primeiro ciclo)

A pedido explícito de Fabrício: ativar comercialmente a fonte JustTCG. Premissas confirmadas por ele: assinatura ativa (10.000 requests/mês, 1.000 requests/dia, 50 requests/minuto, até 100 cards/request); segredo (`JUSTTCG_API_KEY`) já configurado no ambiente local, nunca solicitado/exibido/registrado por este agente.

**Introspecção prévia**: estado real de `JUSTTCG` confirmado inalterado desde o fechamento do Incremento P8 (2 `pricing_set_mapping`/6 `pricing_card_mapping` `CONFIRMED`, 32 `pricing_product`/32 `pricing_observation`, 5 `pricing_sync_run`, zero run ativo); `scripts/sync-justtcg-pricing.ts` confirmado suficiente para reuso integral, sem alteração de código; migration livre confirmada `3911`.

**Divergência material sinalizada antes de prosseguir**: o conector (Incremento P8) foi arquiteturado de propósito para rodar só localmente — nenhuma credencial existe no sandbox deste agente nem no Supabase Vault (que só tem os segredos de PTAX). Apresentada a Fabrício via `AskUserQuestion`; decisão dele: executar localmente e reportar o resultado sanitizado — mesmo precedente de P8/P9.

**Chamada mínima autenticada, sem escrita**: `GET /v1/sets?game=pokemon`, executada por Fabrício via `curl.exe` (não `Invoke-WebRequest`, que quebrou com `NullReferenceException` — bug local do PowerShell 5.1/engine do IE, sem relação com a API). Duas causas locais identificadas e corrigidas antes do resultado válido: `JUSTTCG_API_KEY` inicialmente vazia (comprimento 0) na sessão. Resultado: `HTTP 200`, `apiRequestsRemaining=9998`, 215 Sets — credencial e contrato confirmados.

**Migration `3911_activate_pricing_source_justtcg`** (`CONFIRMADO EXECUTADO`): `UPDATE` único, idempotente por `WHERE is_active = FALSE AND requires_commercial_agreement = TRUE`, sem alteração de estrutura/RLS/grants. `pricing_source.is_active: false → true`.

**Piloto real, escopo idêntico ao já mapeado em P8 — sem ampliar catálogo/mappings/cobertura**: comando existente do conector, sem alteração de código, executado por Fabrício (`--confirmed-by` do único `admin_user` real do projeto). Bloqueio adicional encontrado e resolvido em conjunto: `SUPABASE_SERVICE_ROLE_KEY` com espaço colado na ponta e, depois de removido, ainda `Unregistered API key` — chave desatualizada, corrigida por Fabrício copiando o valor atual direto do painel do Supabase. Resultado (`=== Resumo do piloto ===`, já sanitizado): `setsResolved=2`/`cardsResolved=6`, `productsWritten=0` (32 já existiam), `observationsWritten=32` (histórico de preço novo, nunca sobrescrita), `requestsMade=7`/`rateLimitHits=0`/`status=COMPLETED`.

**Validação objetiva** (consultas diretas ao Supabase): run terminal sem inconsistência (`requests_made=7`, `requests_remaining_at_end=9996`, `error_summary=NULL`); 7 `pricing_sync_run_call` todas `SUCCESS`; `pricing_product` total inalterado em 32; `pricing_observation` `32→64` sem duplicidade estrutural possível (identidade inclui `observed_at`); zero run ativo residual; varredura defensiva de segredo negativa; `get_advisors` sem achado novo referenciando o domínio.

**Fora de escopo, nada implementado nesta rodada**: nenhuma Edge Function/Cron/nova arquitetura para JustTCG; nenhuma alteração de PTAX/frontend; nenhuma ampliação de catálogo/mappings/cobertura além do já existente em P8. **Nenhum encerramento formal do Incremento P14 nesta rodada — só o P14.1.**

Documentos atualizados nesta rodada: `docs/adr/ADR-029-pricing-domain-model.md` (revisão `1.24`), `docs/05f-pricing.md` (versão `1.40`), `docs/INDEX.md` (versão `1.25`), `docs/log.md`, este handoff.

**Nenhum commit ou push foi realizado nesta rodada.** Por instrução explícita de Fabrício, a decisão de commitar/pushar permanece com ele.

## 17. Amendo — Incremento P14.2: Cobertura Escalável e Sincronização em Lotes da JustTCG (2026-08-19, décimo segundo ciclo)

A pedido explícito de Fabrício: eliminar o padrão de uma chamada HTTP por carta do conector P8, substituindo por descoberta paginada em lotes de até 100 cartas por requisição (plano Starter contratado), implementado e validado em um único Set ainda não mapeado — sem sincronizar o catálogo completo (~7.500 cartas).

**Dois achados materiais da introspecção, nenhum exigindo pausa**: (1) o endpoint de lote documentado (`POST /v1/cards`, até 100/200 conforme plano) exige identificadores já conhecidos por item — inútil para descobrir cartas ainda não mapeadas; o mecanismo real de "até 100 cartas por requisição" para um Set inteiro é a paginação de `GET /v1/cards` (`limit`/`offset`/`meta.hasMore`), documentada oficialmente como o próprio caso de uso "fetch every card in a set, paginated"; (2) nenhuma tabela local guarda nome em inglês confiável — resolução de Set passou a usar `release_date` exata como sinal primário automatizado, nunca nome.

**Nenhuma migration necessária**: `match_status='PENDING'` (já existente desde o Incremento P2) reaproveitado para "candidato ambíguo, aguardando revisão", com o motivo em `match_evidence.resolution` — mesmo padrão de fila de revisão já usado em Card Variant. Migration livre confirmada `3912`, não usada.

**Set-piloto escolhido: `BASE4`** (Coleção Básica 2/Base Set 2, `release_date=2000-02-24`, 130 cartas locais — força paginação real de 2 páginas). `ME1`/`BASE1` (P8) deliberadamente fora do escopo desta rodada.

**`scripts/sync-justtcg-pricing.ts` evoluído em lugar** (mesmo padrão das correções P8): cliente HTTP com `fetchImpl` injetável (testabilidade offline); `fetchAllCardsForSet()` pagina o Set inteiro, substituindo a Fase B por-carta; `classifyCardMatch()` decide `SAFE`/`AMBIGUOUS`/`ABSENT` por número de coleção primário + nome só como verificação secundária, nunca confirmando ambiguidade automaticamente; `upsertSetMapping()`/`upsertCardMapping()` fazem `SELECT` antes de decidir — corrige uma lacuna real de P8 (reexecução nunca promovia `PENDING`/`NOT_FOUND` para `CONFIRMED`; uma linha `CONFIRMED` nunca é rebaixada por uma classificação pior); escrita de `pricing_observation` agora distingue divergência real de preço (mesma identidade, `price` diferente) de conflito inofensivo (mesmo `price`) — divergência nunca sobrescreve, mesma disciplina já usada em PTAX (P9).

**Validação 100% offline** (sandbox sem runtime Deno — mesma limitação de P13.2/P13.3): `tsc --strict` (compilador do próprio repositório, com shims ambiente para `Deno`/`npm:@supabase/supabase-js@2`) — zero erro. Lógica pura compilada e executada via Node contra o próprio `runFixtureCheck()` — **50/50 asserções**, cobrindo as 11 categorias pedidas (lote ≤100; paginação >100; correspondência segura; número de coleção ausente; correspondência ambígua com desempate por nome; carta da API sem equivalente local; idempotência — incluindo promoção `NOT_FOUND→CONFIRMED` e proteção de `CONFIRMED`; divergência de preço; retry/rate limit; sanitização; comprovação de que as chamadas crescem por lote/página — 3 requisições para 250 cartas hipotéticas) mais as 17 de regressão herdadas de P8.

**Piloto real: não executado — aguardando autorização e execução local de Fabrício**, mesmo precedente de P8/P9/P14.1. Cobertura esperada: 1 requisição `GET /v1/sets?game=pokemon` + 2 requisições `GET /v1/cards?game=pokemon&set=<external_set_id>&limit=100&offset=0|100` = **3 requisições no total** para as 130 cartas do Set `BASE4` — contra as 130 que o desenho de P8 exigiria se estendido ao mesmo Set. Comandos de dry-run e piloto real, e o bloco sanitizado a retornar, fornecidos a Fabrício em chat.

**Fora de escopo, nada implementado nesta rodada**: nenhuma Edge Function/Cron/nova arquitetura; nenhuma sincronização do catálogo completo; nenhuma alteração de PTAX/frontend; nenhum `GRANT`/RLS novo; nenhum segredo (`JUSTTCG_API_KEY`/`SUPABASE_SERVICE_ROLE_KEY`) visto, solicitado, exibido ou registrado por este agente. Nenhum encerramento formal do Incremento P14 nesta rodada.

Documentos atualizados nesta rodada: `docs/adr/ADR-029-pricing-domain-model.md` (revisão `1.25`), `docs/05f-pricing.md` (versão `1.42`), `docs/log.md`, este handoff.

**Nenhum commit ou push foi realizado nesta rodada.** Por instrução explícita de Fabrício, a decisão de commitar/pushar permanece com ele.

## 18. Amendo — Encerramento do Incremento P14.2: Piloto Real Validado (2026-08-19, décimo terceiro ciclo)

A pedido explícito de Fabrício: documentar o encerramento do Incremento P14.2 com a evidência do piloto real, já executado e confirmado por auditoria direta no Supabase (não deste agente — confirmação repassada por Fabrício e registrada aqui como fato consolidado).

**Run**: `9df36424-b8f8-4ee8-a67f-2c04c74bd3fa` — `run_type=CARD_SYNC`, `triggered_by=MANUAL`, `status=COMPLETED`. 3 chamadas em `pricing_sync_run_call`, todas `HTTP 200`/`SUCCESS`. Set `BASE4` confirmado contra `base-set-2-pokemon` pela `release_date` normalizada (Fix P14.2.1, ciclo anterior).

**Cobertura**: 130 `pricing_card_mapping` — 127 `CONFIRMED`, 3 `PENDING`, zero `NOT_FOUND`/`REJECTED`. 635 `pricing_product` e 635 `pricing_observation`, zero produto vinculado aos 3 mappings pendentes, zero duplicidade de business key de produto, zero duplicidade de observação dentro do run, zero run ativo residual, zero segredo detectável nos payloads. Toda observação: `USD`, `market_scope=UNDETERMINED`, `market_label=JUSTTCG_AGGREGATE`, `market_evidence_confirmed=false`; todos os 635 produtos `language_status=UNDETERMINED`.

**Os 3 `PENDING` são fail-safe correto, não falha do conector** — número de coleção único bate nos três, mas `isNameCompatible()` rejeita a divergência de nome, e `classifyCardMatch()` nunca confirma automaticamente nessas condições:
- `082` — `Nidoran♀` (local) × `Nidoran F` (externo);
- `083` — `Nidoran♂` (local) × `Nidoran M` (externo);
- `102` — `Impostor Professor Oak` (local) × `Imposter Professor Oak` (externo).

Permanecem `PENDING` até revisão manual explícita.

**Achado de performance obrigatório, não corrigido nesta rodada**: o run levou aproximadamente 156 segundos. O batching do próprio P14.2 já eliminou o padrão de uma chamada HTTP por carta (130 chamadas potenciais → 3 reais), mas a persistência de 635 produtos + 635 observações no Supabase continua sequencial (um `INSERT` por vez) e é agora o gargalo dominante do tempo de execução. Catálogo completo (~7.500 cartas) e qualquer automação/agendamento permanecem fora de escopo até essa correção.

**Próximo incremento recomendado: P14.3 — Persistência em Lotes no Supabase.** O Incremento P14 como um todo permanece em andamento — este ciclo encerra formalmente só o P14.2, não o P14.

Nenhum código, migration ou chamada externa nesta rodada — só documentação.

Documentos atualizados nesta rodada: `docs/adr/ADR-029-pricing-domain-model.md` (revisão `1.27`), `docs/05f-pricing.md` (versão `1.44`), `docs/log.md`, este handoff.

**Nenhum commit ou push foi realizado nesta rodada.** Por instrução explícita de Fabrício, a decisão de commitar/pushar permanece com ele.

## 19. Amendo — Encerramento formal do Incremento P14.3: persistência em lotes, hardening de credencial e piloto real aprovado (2026-08-19, mesmo dia, ciclo seguinte ao 18)

Fabrício aprovou o piloto real do P14.3 (run `8e4367ad-f95d-41b6-bbbe-449bd7fb635f`, `COMPLETED`, colado por captura de terminal) e pediu duas coisas nesta rodada: (1) corrigir o comportamento de credencial ausente antes de qualquer encerramento documental; (2) encerrar formalmente o P14.3, registrando migrations `3912`–`3915`, segurança, batching, histórico só por mudança real, métricas de performance e o piloto aprovado — mantendo P14 em andamento, com expansão para todos os Sets como próximo incremento.

**Parte A — correção de robustez de credencial (implementada primeiro, por pedido explícito).** Removido antipadrão em `scripts/sync-justtcg-pricing.ts`: a ausência isolada de `JUSTTCG_API_KEY` fazia `main()` cair silenciosamente em `--fixture-check`, nunca checando `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`, nunca encerrando com código diferente de zero — mascarava ambiente mal configurado como validação bem-sucedida. Corrigido via nova função pura `resolveEntryDecision()` (mesmo padrão de `planVariantProjection`/`diagnoseExternalCoverage`): `--fixture-check` sempre offline explícito, com ou sem qualquer credencial; fora desse modo, ausência de qualquer uma das três variáveis obrigatórias encerra o processo com código diferente de zero, antes de qualquer rede/Supabase, mensagem sanitizada (só nomes de variável, nunca valores). 8 novos testes offline cobrindo os dois caminhos, incluindo verificação explícita de que nenhum valor de credencial vaza na saída de `MISSING_ENV`. `tsc --strict` limpo; suíte offline **151/151 asserções** (143 anteriores + 8 novas). Validado via três harnesses distintos: ambiente vazio/sem flag → `MISSING_ENV`, sai código 1; `--fixture-check` explícito → roda a suíte offline completa; credenciais fake presentes/sem `--confirmed-by` → avança corretamente até a checagem de `--confirmed-by`, provando zero rede em qualquer caminho antes da validação de env. Nenhuma nova chamada à JustTCG.

**Parte B — encerramento documental formal do P14.3.** `docs/05f-pricing.md` (versão `1.45`): Status cell ampliada com o resumo P14.1/P14.2/P14.3; nova seção "Incremento P14.3 — Persistência em Lotes, Hardening de Segurança e Deduplicação por Mudança Real de Preço" cobrindo objetivo, as quatro migrations (`3912` promoção de mapping, `3913` upsert de produto, `3914` pré-busca por tupla exata mantida por compatibilidade, `3915` pré-busca pela última observação por grupo), a regra "histórico só por mudança real de preço" e o bug de contagem duplicada corrigido, o fix de robustez de credencial desta mesma rodada, validação offline (151/151) e o piloto real aprovado com todas as métricas; nova linha de Revision History `1.45`. `docs/adr/ADR-029-pricing-domain-model.md` (revisão `1.28`): header Decisão ampliado com o mesmo resumo P14.2/P14.3; nova seção "Incremento P14.3" inserida entre o encerramento do P14.2 e o Incremento P2 (preservando a ordem de inserção cronológica já usada no documento, não a ordem numérica); nova linha de Revision History `1.28`.

**Métricas do piloto real aprovado, registradas nos três documentos**: run `8e4367ad-f95d-41b6-bbbe-449bd7fb635f`, `COMPLETED`, 3 chamadas HTTP, 13 operações Supabase, **14,87 segundos contra ~156 segundos do fluxo sequencial do P14.2 (~10x mais rápido)**; 635 produtos resolvidos/0 escritos; 635 observações resolvidas/**152 escritas**, todas as 152 com preço genuinamente diferente da observação imediatamente anterior — zero snapshot redundante; `BASE4` inalterado (127 `CONFIRMED`/3 `PENDING`); zero run ativo residual.

**Fora de escopo desta rodada**: `ROADMAP.md`/`README.md`/`INDEX.md` — não tocados, seguindo o mesmo precedente já estabelecido nos encerramentos do P14.1/P14.2 (sub-encerramentos dentro de um incremento maior ainda em andamento só atualizam `05f-pricing.md`/`ADR-029`/`docs/log.md`/handoff). Remoção da função de tupla exata (`3914`) — diferida até o código novo estar commitado. Expansão para Sets além de `BASE4`. Qualquer automação/agendamento do conector.

**P14.3 formalmente encerrado.** O Incremento P14 como um todo **permanece em andamento** — próximo incremento recomendado: expandir o mesmo padrão de descoberta paginada + persistência em lotes para todos os Sets restantes do catálogo.

Documentos atualizados nesta rodada: `scripts/sync-justtcg-pricing.ts` (fix de robustez de credencial), `docs/05f-pricing.md` (versão `1.45`), `docs/adr/ADR-029-pricing-domain-model.md` (revisão `1.28`), `docs/log.md`, este handoff.

**Nenhum commit ou push foi realizado nesta rodada.** Por instrução explícita de Fabrício, a decisão de commitar/pushar permanece com ele.

## 20. Amendo — Encerramento formal do Incremento P14.4.1: Inventário de Cobertura e Plano de Ondas, precedido por fix crítico de truncamento (2026-08-19, mesmo dia, ciclo seguinte ao 19)

Antes de expandir a descoberta+persistência em lotes (P14.2/P14.3) para todos os Sets do catálogo, Fabrício pediu um modo `--expansion-plan` (somente leitura) que inventariasse o estado real e propusesse um plano de execução em ondas — sem confirmar nenhum mapeamento novo. A primeira execução real expôs um bug crítico de truncamento, corrigido antes de qualquer resultado válido; uma segunda execução real expôs um erro de permissão adicional (`public.game`), corrigido pela Query `3917`. Este amendo encerra formalmente o P14.4.1 com o resultado da execução real já corrigida.

**Parte A — implementação do `--expansion-plan`.** `executeExpansionPlan()`/`runExpansionPlan()`, somente leitura (qualquer tentativa de escrita no cliente Supabase do modo lança erro, nunca silenciosa): lê o inventário local completo, classifica cada Set contra a lista de Sets da JustTCG (`ALREADY_CONFIRMED`/`SAFE_CANDIDATE`/`AMBIGUOUS`/`NOT_FOUND`) e agrupa os Sets pendentes em ondas (limite de 5 Sets ou 500 cartas locais por onda; Set individual acima de 500 cartas nunca é dividido, forma onda própria sinalizada `oversized`, sem interromper as ondas seguintes).

**Parte B — fix crítico de truncamento, descoberto pela primeira execução real.** O piloto real reportou 11 Sets/1.000 cartas contra os 45 Sets/7.429 cartas confirmados por introspecção direta — causa raiz, o Data API do Supabase/PostgREST corta silenciosamente em 1.000 linhas por requisição quando nenhum `.range()` é informado, e a leitura original de cartas fazia um único `.select()` sem paginação. Corrigido substituindo a leitura linha-a-linha de `card` pela view agregada `catalog_card_set_metrics` (`cards_ativas` por Set) e adicionando paginação determinística (`fetchAllPages()`/`fetchAllRowsFromTable()`) com reconciliação obrigatória contra contagem exata e independente (`fetchExactCount()`, imune ao limite de 1.000 por não materializar linhas) para toda leitura restante — qualquer divergência aborta com erro explícito antes de emitir qualquer plano parcial e antes de qualquer chamada à JustTCG. Query `3916` (`CONFIRMADO EXECUTADO`, aprovada por Fabrício com duas condições — `WHERE crd.is_active = TRUE` e `REVOKE ALL FROM PUBLIC, anon, authenticated`): `GRANT SELECT ON catalog_card_set_metrics TO service_role` + criação da view `pricing_set_coverage` (`security_invoker=true`), que agrega cobertura de produtos/observações por Set x Fonte, substituindo a leitura linha-a-linha de `pricing_card_mapping`/`pricing_product`/`pricing_observation` (a segunda superfície de truncamento).

**Parte C — Query `3917`, motivada por um segundo erro real de permissão.** A reexecução local pós-fix falhou com `permission denied for table game`, lançado por `fetchAllPages()` dentro da leitura paginada de `catalog_card_set_metrics`. Causa raiz confirmada por introspecção direta: `catalog_card_set_metrics` é `security_invoker=true`, então o papel que consulta a view precisa de `SELECT` em todas as tabelas-base que ela referencia, não só na view — `service_role` tinha `SELECT` na view (`3916`) mas não em `public.game`. `GRANT SELECT ON public.game TO service_role` validado transacionalmente (`BEGIN`/`ROLLBACK`, confirmando a leitura da view como `service_role` antes e a reversão sem resíduo depois) e aplicado real; pós-aplicação, `service_role` tem `SELECT=true`/`INSERT`/`UPDATE`/`DELETE=false` em `game` (grant exato). Migration registrada, advisors de segurança revisados sem achado novo. Fabrício também pediu, na mesma rodada: confirmação de que `executeExpansionPlan()` já executa todas as leituras/reconciliações locais antes do único `GET /sets` à JustTCG (confirmado lendo o código — já estava correto, nenhuma reordenação necessária) e um teste novo provando que qualquer falha de leitura/reconciliação local resulta em zero chamadas HTTP — dois pontos de prova adicionados a `runFixtureCheck()` (as três falhas de reconciliação já existentes mais um cenário dedicado replicando o erro real de `game`), suíte offline **184/184 asserções** (181 anteriores + 3 novas), `tsc --strict` limpo.

**Inventário reconciliado (execução real, pós-fix, pós-`3917`)**: **45 Sets locais ativos, 7.429 cartas ativas** — **3 `ALREADY_CONFIRMED`/420 cartas** (`BASE1`/`BASE4`/`ME1`), **31 `SAFE_CANDIDATE`/5.805 cartas**, **9 `AMBIGUOUS`/1.064 cartas**, **2 `NOT_FOUND`/140 cartas** (`GYM1`/`MEE`) — soma das categorias bate exatamente com o total local. **Plano de 15 ondas, nenhuma `oversized`**, estimando **89 requisições** (74 páginas de cartas + 15 resoluções de Set) para o inventário completo — estimativa derivada só da contagem local conhecida (a JustTCG não expõe total de cartas por Set antes da paginação real), podendo divergir do consumo real; o executor de ondas (P14.4.2) deve impor orçamento de requisições local, nunca confiar cegamente nesta estimativa. **Execução real validada**: exit code 0, exatamente 1 `GET /v1/sets`, zero `pricing_sync_run` criado, zero escrita em mapping/produto/observação.

**Parte D — encerramento documental formal.** `docs/05f-pricing.md` (versão `1.46`): Status cell ampliada com o resumo do P14.4.1; nova seção "Incremento P14.4.1 — Inventário de Cobertura e Plano de Ondas" cobrindo objetivo, o fix de truncamento, as migrations `3916`/`3917`, o inventário reconciliado em tabela, o plano de ondas e a ressalva sobre a estimativa; nova linha de Revision History `1.46`. `docs/adr/ADR-029-pricing-domain-model.md` (revisão `1.29`): header Decisão ampliado com o mesmo resumo; nova seção "Incremento P14.4.1" inserida entre o encerramento do P14.3 e o Incremento P2 (mesma convenção de ordem de inserção cronológica já usada no documento); nova linha de Revision History `1.29`.

**Fora de escopo desta rodada**: `ROADMAP.md`/`README.md`/`INDEX.md` — não tocados, checados e sem contradição objetiva encontrada com o encerramento do P14.4.1, mesmo precedente dos encerramentos de P14.1/P14.2/P14.3 (sub-encerramentos dentro de um incremento maior ainda em andamento só atualizam `05f-pricing.md`/`ADR-029`/`docs/log.md`/handoff). Qualquer execução de onda (confirmação de mapeamento novo, escrita de produto/observação) — o `--expansion-plan` é somente diagnóstico. Qualquer automação/agendamento. Nenhuma nova chamada à JustTCG nesta rodada de encerramento documental.

**P14.4.1 formalmente encerrado.** O Incremento P14 como um todo **permanece em andamento** — próximo incremento recomendado: **P14.4.2 — Executor Explícito de Ondas**, que consome o plano deste incremento e executa uma onda por vez (nunca todos os Sets automaticamente), com orçamento de requisições imposto localmente antes de cada onda, telemetria via `pricing_sync_run`/`pricing_sync_run_call` (reaproveitando o padrão de P8/P14.2/P14.3) e confirmação explícita por onda.

Documentos atualizados nesta rodada: `scripts/sync-justtcg-pricing.ts` (fix de truncamento + teste novo, rodada anterior a este amendo), `database/migrations/3916_pricing_set_coverage_view.sql` e `database/migrations/3917_grant_service_role_select_game.sql` (novos, `CONFIRMADO EXECUTADO`), `docs/05f-pricing.md` (versão `1.46`), `docs/adr/ADR-029-pricing-domain-model.md` (revisão `1.29`), `docs/log.md`, este handoff.

**Nenhum commit ou push foi realizado nesta rodada.** Por instrução explícita de Fabrício, a decisão de commitar/pushar permanece com ele.

## 21. Amendo — Encerramento formal do Incremento P14.4.2: Executor de Ondas, Onda 1 Real, Correção de Hierarquia de Printing (Migration 3918) e Correção do Primeiro Hover (2026-08-19, mesmo dia, ciclo seguinte ao amendo 20)

Ainda em 2026-08-19, ao longo de vários ciclos consecutivos: implementação do executor de ondas (`executeExpansionWave()`), execução real da Onda 1, diagnóstico e correção de um bug de exibição de preço que a própria auditoria pós-execução da Onda 1 revelou, e correção de um segundo defeito real no frontend reportado por Fabrício após validar o primeiro fix. Este amendo consolida o encerramento documental de tudo isso — nenhum dos ciclos anteriores havia atualizado `05f-pricing.md`/`ADR-029`/`docs/log.md`/este handoff, por instrução explícita de Fabrício de deixar a documentação para o encerramento final.

**Parte A — executor de ondas.** `executeExpansionWave()`/`runExpansionWave()` (`--wave=N`, `scripts/sync-justtcg-pricing.ts`) consome o plano do P14.4.1 e executa exatamente uma onda por vez — nunca todos os Sets automaticamente —, com orçamento de requisições configurável (`maxApiRequests`) e telemetria via `pricing_sync_run`/`pricing_sync_run_call` (mesmo padrão de P8/P14.2/P14.3). Construído em três rodadas: implementação inicial (16 cenários de teste offline); correção do wiring de `expectedSetCodes` em `main()`, que não chegava ao executor (~12 pontos de teste existentes atualizados + 8–9 cenários novos); correção de agregação por Set (`perSet.push` com os campos de projeção corretos, cenários adicionais de agregação/skip/`dry-run` sem escrita). `tsc --strict` limpo nas três rodadas.

**Parte B — Onda 1 executada de verdade.** Run `598610e6-c23a-47aa-ad65-88113a008984` (`CARD_SYNC`/`MANUAL`/`COMPLETED`, 6 requisições, `BASE2`/`BASE3`/`BASE5`/`GYM2`): 341 `pricing_card_mapping` (338 `CONFIRMED`/3 `PENDING` — mesmo fail-safe de nome divergente já observado em P14.2), **3.375 `pricing_product`/3.375 `pricing_observation`**, confirmados por auditoria somente-leitura direta no Supabase (contagem exata por Set: `BASE2`=630, `BASE3`=620, `BASE5`=830, `GYM2`=1.295).

**Parte C — bug real de exibição de preço, encontrado pela própria auditoria pós-execução, não pelo executor.** Fabrício reportou que o preço não aparecia no frontend em nenhuma das 3.375 linhas persistidas. Diagnóstico somente-leitura confirmou: `get_cards_pricing_summary` (migration `3904`, P12) reconhecia só os três printings da era moderna (`Normal`/`Holofoil`/`Reverse Holofoil`); a JustTCG rotula a era clássica (WOTC — exatamente `BASE2`/`BASE3`/`BASE5`/`GYM2`) como `Unlimited`/`1st Edition`/variantes holográficas — 100% das 3.375 linhas caíam fora do filtro.

**Migration `3918`** (`CONFIRMADO EXECUTADO`) estende a hierarquia para sete valores — `Normal → Holofoil → Reverse Holofoil → Unlimited → Unlimited Holofoil → 1st Edition → 1st Edition Holofoil` —, Ilimitada antes de 1ª Edição por decisão de produto de Fabrício (edição padrão mais comum, prioridade sobre a premium), sem fallback genérico. **Divergência sinalizada e resolvida antes de implementar** (mesmo padrão de rigor já usado nos amendos anteriores): o pedido original citava manter `SECURITY INVOKER`, mas a função real (e a irmã `get_card_pricing_snapshot`) sempre foi `SECURITY DEFINER`, por design deliberado desde P11 — apresentado via `AskUserQuestion`, Fabrício escolheu explicitamente **manter `SECURITY DEFINER`**; `search_path`/ACL inalterados, nenhum `GRANT` novo a tabela interna. Testada transacionalmente (`BEGIN`/`SET LOCAL ROLE authenticated`/`ROLLBACK`) antes da aplicação real.

**Validação pós-aplicação**: `BASE4` sem regressão; `BASE2`/`BASE3`/`BASE5`/`GYM2` agora com `has_pricing=true` e `printing_label` corretamente resolvido para `Unlimited Holofoil` nas amostras (prova da prioridade Ilimitada-antes-de-1ª-Edição); mapping `PENDING` corretamente sem preço; `get_advisors` sem achado novo além do `authenticated_security_definer_function_executable` já pré-existente e compartilhado por dezenas de outras funções; custo de performance real e limitado (24 cartas: 24,5ms/1.704 buffers → 57,0ms/2.609 buffers). Frontend (`card-price-summary.tsx`): `PRINTING_LABEL_PT`/`PRINTING_ORDER` estendidos às quatro traduções PT-BR novas.

**Parte D — segundo defeito real, no frontend, reportado por Fabrício após validar visualmente o fix da Parte C** (duas capturas de uma carta Nidoqueen): popover de detalhe travava em "Carregando detalhes…" no primeiro hover de cada carta, só resolvendo ao sair e passar o mouse de novo (o pill de resumo no grid já estava correto). Diagnóstico confirmou a causa entre as hipóteses possíveis (estado mutado fora do React, `setState` ausente, efeito com dependência incorreta, aborto prematuro): **efeito com dependência incorreta** — o `useEffect` de `CardPriceDetails` tinha `liveStatus` na própria lista de dependências, que ele mesmo definia via `setLiveStatus("loading")`, disparando um re-run/cleanup que marcava `cancelled = true` antes da resposta real de `fetchLiveDetail` chegar; a resposta era descartada, mas ainda ia parar em `liveDetailCache` — por isso reabrir mostrava o dado na hora.

**Correção mínima**: `liveStatus` removido das dependências do efeito (guard interno `liveStatus !== "idle"` preservado); `fetchLiveDetail` (`pricing-batch-client.ts`) ganhou deduplicação de chamadas em voo (`liveDetailInFlight`), exigida pelo contrato de "chamadas simultâneas deduplicadas". 6 testes novos, 100% Node nativo (`node:test`/`node:assert`, sem novo framework), cobrindo o contrato de cache/dedupe. Validação visual (hover/foco/toque, troca rápida entre cartas, resposta após fechamento) **aprovada por Fabrício**.

**Parte E — limpeza técnica de encerramento (mesma rodada, pós-aprovação visual).** Guard `liveStatus !== "idle"` revisado com o efeito já sem `liveStatus` nas deps — confirmado ainda necessário (protege o cenário de reabertura com cache já quente, onde o estado inicial nasce `"loaded"` direto do `useState`; sem o guard, o efeito chamaria `setLiveStatus("loading")` incondicionalmente e criaria um flash visível) e mantido sem alteração. `allowImportingTsExtensions` (`web/tsconfig.json`) testado empiricamente contra a alternativa de escrever o import do teste como `./pricing-batch-client.js` (convenção de mapeamento de extensão do TypeScript): `tsc --noEmit` aceita essa forma sem a flag, mas o loader nativo do Node (`--experimental-strip-types`) falha com `ERR_MODULE_NOT_FOUND` — Node não remapeia um especificador `.js` para o arquivo `.ts` real sem um loader ciente de TypeScript (`ts-node`/`tsx`), nenhum presente no projeto. Flag confirmada necessária pela evidência empírica, mantida sem alteração adicional de configuração. `web/tsconfig.tsbuildinfo` restaurado via `git checkout` após cada `tsc --noEmit`, confirmado fora do diff. `tsc --noEmit` e os 6 testes revalidados no estado final (6/6 passando, zero erro de tipo). Comandos locais fornecidos a Fabrício para `npm run lint`/`npm run build` (`next lint`/`next build`) — não executados neste sandbox, mesma limitação pré-existente de binário SWC ausente.

**P14.4.2 formalmente encerrado. O Incremento P14 como um todo permanece em andamento** — próximo incremento recomendado: **P14.4.3 — execução manual e auditada das ondas seguras remanescentes** (14 das 15 ondas do plano do P14.4.1, uma por vez, mesma disciplina de confirmação explícita e orçamento de requisições local já usada na Onda 1). Nenhuma migration/código adicional além do descrito, nenhuma nova chamada à JustTCG nesta rodada de encerramento, nenhum segredo visto/exibido/registrado.

Documentos atualizados nesta rodada: `database/migrations/3918_extend_printing_hierarchy_get_cards_pricing_summary.sql` (novo, `CONFIRMADO EXECUTADO`), `web/components/card/card-price-summary.tsx`, `web/lib/pricing/pricing-batch-client.ts`, `web/app/api/cards/pricing/batch/route.ts` (comentário), `web/lib/pricing/pricing-batch-client.test.ts` (novo), `docs/05f-pricing.md` (versão `1.47`), `docs/adr/ADR-029-pricing-domain-model.md` (revisão `1.30`), `docs/log.md`, este handoff.

**Nenhum commit ou push foi realizado nesta rodada.** Por instrução explícita de Fabrício, a decisão de commitar/pushar permanece com ele.
