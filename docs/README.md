# Project Mimikyu Documentation

Esta pasta contém a documentação oficial do Project Mimikyu.

## Status Atual do Projeto

| Campo | Valor |
|-------|-------|
| **Fase 1 — Arquitetura Conceitual** | Concluída |
| **Fase 2 — Modelo Lógico** | Em andamento, agora subdividida em Fases/Blocos próprios (ver `05-modelo-de-dados.md`, seção "Roteiro Consolidado — Fases e Blocos"). **Sub-Fase 1 — Catálogo Editorial: Bloco A (Modelo de Dados) concluído**, cobrindo `game`, `expansion`, `card_set`, `card`, `card_category`, `rarity`, `language`, `card_variant_type`/`card_variant`, `card_asset_type`/`card_asset`, `storage_bucket`, `asset_source`, `card_external_reference`, `asset_import_run`, `asset_import_failure` — todas criadas, homologadas e com roteiro completo em `05-modelo-de-dados.md`. **Reaberto pontualmente**: nova entidade `card_set_external_reference` (`240`/`241` CONFIRMADAS EXECUTADAS; `910` adiada até dados reais da TCGdex, `991` planejada), descoberta como lacuna real durante o Sprint B2.5 do Bloco B — o pipeline precisa saber qual identificador a TCGdex usa para um `card_set`, mesmo mapeamento que `card_external_reference` já resolve para `card`. Episódio real: um mapeamento de teste incorreto (`ME0`→`sv10pt5`, um Set oficial real da TCGdex) foi inserido e corrigido. **Bloco B (Pipeline de Importação — Edge Function `import-card-assets`) em andamento**: Sprints `B2.0`–`B2.4.1` **CONFIRMADOS CONCLUÍDOS** (ambiente local, primeira Edge Function, deploy e teste, consulta de execução por `run_code`, descoberta de `card_set`/`card` da execução, refatoração em camadas `services/database.ts`/`types.ts` com `index.ts` reduzido a orquestrador), cada um com Diário Técnico (Objetivo/Critério de Aceite/Resultado/Pendências Descobertas) registrado em `06-pipeline-importacao.md`. **Novo princípio de arquitetura para todas as Edge Functions futuras**: `index.ts` não conhece SQL/PostgreSQL/fontes externas diretamente, apenas orquestra chamadas a serviços especializados. Roteiro dos sprints restantes revisado: `B2.5` dividida em `B2.5A` (consulta à TCGdex) e `B2.5B` (extração de URL/download/validação de imagem), com possível sobreposição de escopo com `B2.6` sinalizada, não resolvida unilateralmente. **Marco: primeira comunicação externa real e bem-sucedida do Bloco B** — `external_set_id` reais da TCGdex descobertos para `ME0`-`ME5` via script administrativo (`scripts/discover-tcgdex-sets.ts`, executado com sucesso). Nova decisão de negócio em aberto (não resolvida): se `ME0` deve ser mapeado ao Set oficial `mee`/"Mega Evolution Energy" da TCGdex ou permanecer 100% interno — cross-referenciada com a pendência "escopo `ENERGY`". **Decisão de arquitetura declarada definitiva, formalizada em `adr/ADR-017-two-function-import-pipeline.md`**: o pipeline passa a ser dividido em duas Edge Functions — `sync-card-set` (nova, sincroniza o catálogo completo de cartas de um Set via TCGdex, populando `card_external_reference`, sem baixar imagens) e `import-card-assets` (papel redefinido: consome referências já sincronizadas, baixa imagens, grava `card_asset`) —, cada uma seguindo uma arquitetura fixa em camadas (`index.ts` orquestrador → `database.ts` → `tcgdex.ts`, este último reescrito como classe `TcgdexClient`). **Nenhuma implementação real desta nova arquitetura ainda existe** — próximo passo confirmado é o Sprint B3, implementando `sync-card-set` isoladamente. Nova disciplina declarada para esta fase: uma camada por vez (`tcgdex.ts` → `database.ts` → `index.ts`), tratando o repositório como software de produção. `tcgdex.ts` (classe `TcgdexClient`) já passou por revisão técnica e foi aprovado, mas segue **não deployado**; `index.ts` recebeu nota 9,5/10 em revisão, com melhorias sugeridas ainda não confirmadas como aplicadas. **Discrepância real, não resolvida**: a implementação segue dentro da estrutura de `import-card-assets/services/`, não em uma pasta própria `sync-card-set/` como o plano da `ADR-017` sugeria — não confirmado se será uma função compartilhada ou se a criação da função separada foi adiada. **Marco real: Query `910` (Seed Card Set External Reference) CONFIRMADA EXECUTADA (parcial)** — `ME1`/`ME2`/`ME2.5`/`ME3`/`ME4` mapeados com `external_set_id` reais da TCGdex, confirmados por consulta; `ME0` deliberadamente excluído (decisão de negócio pendente); `ME5` aguarda ser cadastrado como `card_set`. `index.ts`/`tcgdex.ts`/`scripts/discover-tcgdex-sets.ts` aprovados em revisão técnica; nova versão completa de `services/database.ts` (com `findCardSetExternalReference`) escrita como rascunho, ainda não deployada. Código confirmado (v1.2.1) versionado no repositório em `supabase/functions/import-card-assets/index.ts`, `types.ts` e `services/database.ts`; `database/seeds/910_seed_card_set_external_reference.sql` adicionado. **Sprint B3.2: primeira tentativa real de deploy da v1.3.0 (primeira chamada real Supabase→TCGdex) FALHOU** com um erro real de bundling (import `@supabase/supabase-js` sem mapeamento em `deno.json`), causa raiz diagnosticada, correção escrita (`supabase: any`, tipagem forte adiada). **Sprint B3.3 — marco real: deploy da v1.3.0 CONFIRMADO com sucesso** (`index.ts`+`database.ts`+`tcgdex.ts`, os três copiados ao repositório pela primeira vez juntos — `tcgdex.ts` nunca havia sido copiado antes). Nova convenção: `deno check` na pasta da função, depois `deploy` na raiz. **Invocação de ponta a ponta com resposta real da TCGdex ainda não confirmada** — três tentativas falharam (sintaxe PowerShell, script inexistente, HTTP 401 real). **Sprint B3.4: causa raiz do 401 diagnosticada definitivamente** (duas hipóteses reais descartadas por reteste — chave/header errados, depois `verify_jwt`; causa real: mecanismo de autenticação de `withSupabase({ auth: ["secret"] })` não satisfeito por Secret Key). Correção final (`index.ts` v1.3.1, sem `auth: ["secret"]`) escrita. **Sprint B3.5: correção real ao diagnóstico da B3.4 — a correção v1.3.1 foi aplicada e testada de ponta a ponta e o 401 PERSISTIU**, invalidando a hipótese de que o mecanismo de autenticação da biblioteca fosse a causa completa. Mais duas hipóteses descartadas por evidência real (nome/tipo da Secret Key; roteamento até o Edge Runtime); um bug real de metodologia de teste (placeholder `"SUA_SECRET_KEY"` nunca substituído) foi encontrado e corrigido, mas não era a causa raiz — o 401 persistiu mesmo com a chave real. **Sprint B3.6 — marco real: HTTP 401 definitivamente eliminado.** Um teste mínimo confirmou o problema em `withSupabase`/`@supabase/server`; a biblioteca foi abandonada por completo (decisão concordada por Fabrício), substituída por `Deno.serve()` + `@supabase/supabase-js` com cliente criado manualmente via `SUPABASE_SERVICE_ROLE_KEY`. Deploy real confirmado; teste real sem headers de autenticação retornou HTTP 500 em vez de 401 — **401 eliminado, confirmado**. Causa do 500 diagnosticada e corrigida: `GRANT` ausente em `card_set_external_reference` para `service_role` (RLS habilitado não substitui `GRANT` de tabela), corrigida por nova migration real (`250`) e reconfirmada por consulta real. `index.ts` (v2.0.0) e `deno.json` copiados ao repositório. **Sprint B3.7: progresso real além do GRANT — teste avançou para HTTP 404 (lógica de aplicação), confirmando infraestrutura 100% funcional.** Diagnóstico revelou que a execução de teste apontava para `ME0`, sem mapeamento externo. Fabrício resolveu definitivamente a pendência de longa data `ME0`↔`mee` (aberta desde a revisão `0.17`): são coleções diferentes (promocionais de Mega Evolução vs. Energias de Mega Evolução), sem relação. `ME0` removida de `card_set` por completo (Migration `251`, real, confirmada). **Sprint B3.8 — 🎉 MARCO REAL: primeira resposta de ponta a ponta da TCGdex confirmada, objetivo do Sprint B3 desde a revisão `0.18`.** Novo `asset_import_run` real criado para `ME1` (migration `252`, com um bug real de coluna obrigatória capturado e corrigido no mesmo ciclo); Edge Function reinvocada retornou `success: true` com dados reais da TCGdex: Set `me01`, "Mega Evolution", `188` cartas, logo/símbolo/metadados. **`B2.5A` do roteiro agora CONCLUÍDO.** Três propostas reais adiadas: Stored Procedure de orquestração, tipos `ENUM` nativos para `run_type`/`status`/`execution_context`, e reestruturação de pastas da Edge Function em camadas. Próximo foco: persistir as cartas retornadas pela TCGdex em `card`/`card_external_reference`. **Sprint B3.9: estrutura real de carta da TCGdex confirmada** (`id`/`localId`/`name`/`image`, sem mecânica de jogo — HP/ataques/fraquezas exigiriam consulta adicional). **Episódio real de perda de contexto da sessão pareada** (chegou a propor tabelas de mecânica de jogo e uma etapa intermediária contrariando decisões já tomadas), autocorrigido após Fabrício intervir com uma captura real do banco; fluxo de persistência realinhado à arquitetura existente (`card`→`card_external_reference`, imagens depois). **⚠️ Discrepância sinalizada, pendente de decisão de Fabrício**: a sessão pareada propôs criar `docs/architecture.md` como "especificação oficial" do projeto, aparentemente sem ciência de que esta documentação (`00`-`07`, ADRs, STDs) já cumpre esse papel — nenhum arquivo criado nesta revisão. Nova pendência carregada: possível mesmo `GRANT` ausente em outras tabelas do projeto — auditoria proposta, não executada. **Sprint B3.10: reconfirmação real da arquitetura de fluxo/responsabilidades das Edge Functions, sem mudança de decisão.** **Episódio real de perda de foco operacional** (distinto do episódio de perda de contexto de esquema do Sprint B3.9) — a sessão pareada tentou pivotar para testar o download de uma única imagem via PowerShell; Fabrício interrompeu diretamente: *"Não quero trabalhar para importar 1 imagem apenas. Esse não é o meu objetivo! [...] Precisamos finalizar o trabalho do catálogo editorial, garantindo a carga completa das cartas em nossa base [...] incluindo as imagens."* Em resposta, a sessão pareada propôs um novo faseamento (`FASE 1-6`/backlog `Sprint 1-2`), ainda **não reconciliado com o "Roteiro vigente" `B2.x`/`B3.x`** já existente. **Autocorreção real no mesmo ciclo**: a separação inicial entre "catálogo editorial" (sem imagens) e "imagens" (fase posterior) foi reconhecida pela própria sessão pareada como incorreta — a Query `880` (incluindo `card_asset`) segue sendo o ponto final real do Catálogo Editorial, como já documentado. Checklist real de seis pontos pré-`880` registrado (idempotência da importação de `card`, criação de `card_external_reference`, decisão sobre endpoint de carta individual, mapeamentos editoriais, infraestrutura de ativos, convenção de Storage). **⚠️ Nova discrepância sinalizada, NÃO resolvida unilateralmente**: proposta de `docs/adr/` com ADRs `001`-`005`, colidindo diretamente com os 17 ADRs reais já aprovados neste repositório — mesmo padrão do `docs/architecture.md`, terceiro sinal de que a sessão pareada opera sem visibilidade completa da estrutura já consolidada. Também sinalizada divergência de nome entre `download-card-assets` (usado nesta revisão) e `sync-card-set` (nome real em `ADR-017`). Nenhum código ou SQL novo executado neste sprint — todo o conteúdo é planejamento/reconciliação. **Sprint B3.11: escopo real do Bloco B expandido para as 5 coleções (`ME1`/`ME2`/`ME2.5`/`ME3`/`ME4`), decisão explícita de Fabrício.** **Quarto episódio real de perda de contexto, contido antes de qualquer execução**: a sessão pareada momentaneamente tratou `card`/`card_variant` como vazias, a preencher pela TCGdex — Fabrício corrigiu diretamente, lembrando que essas tabelas já têm 859/1.555 registros reais, marco fechado há dezenas de batches (Query `840`/`860`/`960`). **Correção arquitetural real**: o pipeline passa a **consultar** `card` (nunca inserir) — `card`/`card_variant` ficam congeladas, fora do escopo do Bloco B; apenas `card_external_reference` e `card_asset` serão populadas. Duas queries de auditoria real confirmaram, sem divergência, os totais e a estrutura de colunas já documentados (um erro real de colunas/`JOIN` inexistentes foi capturado por Fabrício e corrigido antes da auditoria final). Nova reformulação de roadmap (`FASE 1-4`/três Entregas) substitui a `FASE 1-6` do sprint anterior — terceiro framing paralelo em três batches, ainda não reconciliado com o "Roteiro vigente" `B2.x`/`B3.x`. **Sprint B3.12**: dependências de `card_external_reference` auditadas e confirmadas prontas (`asset_source`: 3 provedores; `card_set_external_reference`: 5 coleções mapeadas; `card_external_reference`: 0 registros, ponto de partida real); chave de correspondência `card`↔TCGdex validada (`card_set_id`+`collector_number`=`localId`); plano do próximo incremento travado (188 `UPSERT`s para a `ME1`), ainda não codificado. **A partir desta revisão, por pedido explícito de Fabrício, a documentação passa a registrar apenas o estado final e as decisões confirmadas de cada etapa, não mais o histórico de tentativas.** **Sprint B3.13**: código do Incremento 1 finalizado (`index.ts` v2.1.0 + `database.ts`, lookup em `Map`) — deploy/execução reais ainda não confirmados, código não copiado ao repositório; plano de validação definido (5 checagens), execução bloqueada por um problema real de ambiente local (Supabase CLI indisponível), resolvido via `npm install -g supabase`, verificação final pendente. ⚠️ Não confirmado se a validação rodará contra o ambiente local ou o projeto hospedado real. **Sprint B3.14**: resolvido — banco é o Supabase Cloud, não local. Deploy do Incremento 1 CONFIRMADO por saída real de terminal; `index.ts` (v2.1.0) e `database.ts` copiados ao repositório. Execução de ponta a ponta ainda não confirmada, pendente de verificar o secret `SUPABASE_SERVICE_ROLE_KEY` na Edge Function. **Sprint B3.15 — 🎉 MARCO REAL: Incremento 1 CONFIRMADO CONCLUÍDO.** Um bloqueio real (`permission denied for table card_external_reference` — mesmo gap de `GRANT` ausente já visto em `card_set_external_reference`) foi diagnosticado após corrigir o logging de erro em `database.ts`, e corrigido pela nova migration `253`. Reexecução confirmou `imported: 188`/`ignored: 0`/`total: 188`, reconfirmado por `COUNT(*)` no banco. **Sprint B3.16**: Incremento 2 (download de imagens) iniciado — fluxo definido (TCGdex → download → Storage → `card_asset`), teste controlado com uma única carta aprovado por Fabrício antes de escalar para as 188. Estrutura real de `card_asset` parcialmente confirmada; `card_asset_type` confirmada (`CARD_FRONT` escolhido, corrigindo uma suposição inicial de nome de tabela); `storage_bucket` ainda não confirmada. **Sprint B3.17**: `storage_bucket` confirmada — catálogo interno de metadados, não o bucket físico em si, mapeado 1:1 a `card_asset_type` (`card-front`/`card-back`/`artwork`); bucket físico `card-front` criado no Supabase Storage. **Sprint B3.18**: estrutura completa de `card_asset` confirmada (18 colunas; sem `card_external_reference_id` — a referência externa é só fonte de importação). Código do Incremento 2 (teste controlado com uma carta) escrito e **DEPLOYADO, CONFIRMADO** (`index.ts` v2.2.0 + `database.ts`). Execução real FALHOU — terceiro caso confirmado do mesmo gap de GRANT ausente para `service_role` (agora em `language`); correção proposta, ainda não confirmada executada. **Sprint B3.19**: cadeia de GRANTs resolvida (mais três casos reais: `card_asset_type`/`card_asset`/`expansion`, seis no total) — teste controlado do Incremento 2 CONFIRMADO CONCLUÍDO, primeira imagem real do projeto importada de ponta a ponta. **Sprint B3.20 — 🎉 MARCO REAL: Incremento 2 CONCLUÍDO para a `ME1`** — 188/188 imagens importadas, 0 falhas, refatoração (`services/storage.ts`) aplicada, bug real de regra de negócio corrigido (`external_url`). **Sprint B3.21**: pipeline replicado sem alteração de código — `ME2` (130/130) e `ME2.5` (295/295) CONCLUÍDAS, 0 falhas; total acumulado `613`/`613`/`613`, 3 de 5 coleções. Plano de duas fases definido (Fase 1: `en` nas 5 coleções; Fase 2: `pt-BR`). **Sprint B3.22 — 🎉 MARCO REAL: Fase 1 100% CONCLUÍDA** — `ME3` (124/124) e `ME4` (122/122) concluídas; catálogo editorial completo em inglês: `859` cartas/referências/imagens/`card_asset`, `0` falhas nas 5 coleções. **Sprint B3.23**: Fase 2 (`pt-BR`) iniciada com teste controlado na `ME1` — confirma que `card_asset` já suporta múltiplos idiomas por carta; ⚠️ discrepância real sinalizada em `card_external_reference` (`UNIQUE` sem dimensão de idioma), NÃO resolvida, aguardando confirmação prática antes de qualquer mudança. **Sprint B3.24 — 🎉 teste controlado da Fase 2 CONFIRMADO CONCLUÍDO**: bug real diferente do previsto corrigido (código de idioma da TCGdex, `pt` ≠ `pt-BR` interno); reexecução da `ME1` com `188/188`, `0` falhas; três validações reais confirmadas (Storage, `card_asset` duas linhas, imagem visualmente em português); pendência de `card_external_reference` segue sem resposta empírica. **Sprint B3.25**: Fase 2 escalada diretamente para `ME2`-`ME4`, sem novo teste controlado; `ME2` (`pt-BR`) preparada, execução ainda NÃO confirmada. **Sprint B3.26 — 🎉 MARCO REAL: Fase 2 100% CONCLUÍDA, Bloco B encerrado**: `ME2`/`ME2.5`/`ME3`/`ME4` (`pt-BR`) confirmadas executadas com sucesso por Fabrício. Catálogo Editorial completo nas duas línguas para as 5 coleções: `859` cartas, `1.555` variantes, `859` referências externas, `1.718` assets (`859` `en` + `859` `pt-BR`), `1.718` imagens, `0` falhas. Pendência crítica de `card_external_reference` (Sprint B3.23/24) respondida por evidência agregada (total permaneceu em `859`, confirmando colisão por `UPSERT` em vez de duplicação por idioma) — decisão sobre corrigir o modelo continua pendente de Fabrício. Migrations `259`-`262` copiadas ao repositório. **Sprint B3.27**: Fabrício define o próximo passo real do roadmap — modelagem de **Coleções** (banco de dados), corrigindo diretamente uma sugestão da sessão pareada de partir para a interface de usuário. Mapa de módulos registrado: `Catálogo Editorial` (✅ concluído) → `Coleções`/`Inventário`/`Usuários`/`Trocas`/`Decks`/`Marketplace`/`Estatísticas` (pendentes). Ideias especulativas de uma Edge Function orquestradora (`catalog-import`) e de um checkpoint de backup/Git/tag capturadas apenas como visão futura, sem decisão de adoção. **Bloco B (Pipeline de Importação) está CONCLUÍDO.** Depois disso, inicia a **Sub-Fase 2 — Coleções**, ainda sem modelagem iniciada. **Bloco A reaberto uma segunda vez**: investigação real encontrou o identificador oficial do Set promocional removido pela Migration `251` — não é `mee` (Energia), é **`MEP`** ("Mega Evolution Black Star Promos", TCGdex `mep`), Set irmão de `ME1`-`ME4` cobrindo toda a era Mega Evolution. O mecanismo de `ADR-015` (Set `PROMO`) permanece correto; só a convenção de código estava errada (código sintético "Expansion + `0`" em vez do identificador real). Novo `AP-018` adotado: nunca inventar códigos editoriais quando um identificador oficial existe ou pode ser pesquisado. **A questão sobre `Expansion`/"Series"/"Era" foi RESOLVIDA por decisão direta de Fabrício, sem necessidade da investigação cogitada**: "Na modelagem não alteramos a tabela expansion. Desconsidere qualquer informação sobre as possíveis entidades 'Series e Era'." — nenhuma mudança de schema. **Duas decisões reais adicionais, ainda NÃO executadas**: o catálogo será completado com `MEE` também (Set de Energias, antes deliberadamente excluído — Fabrício decidiu fazer o trabalho completo, critério único: "foi oficialmente publicada?"), e uma nova convenção de `release_order` quando existem Sets especiais (Energia primeiro, Promocional em seguida, regulares depois). Novo lema adotado por Fabrício para o ritmo do projeto: **"Primeiro fechar completamente um módulo. Depois documentar. Só então iniciar o próximo."** Roadmap completo (Fase 1 — Catálogo Editorial em conclusão, até Fase 7 — Interface) esboçado pela sessão pareada e endorsado por Fabrício; `ROADMAP.md` será o primeiro documento formal a ser criado quando a documentação recomeçar — ainda não existe. **`06-pipeline-importacao.md` dividido em três documentos** (a pedido de Fabrício, mesmo depois da reescrita anterior que já havia removido o histórico sprint a sprint): `06-pipeline-importacao.md` (arquitetura/estratégia), `operations/import-card-assets.md` (guia operacional de 8 passos), `history/pipeline-sprint-log.md` (diário histórico condensado). `03-documentation-architecture.md` atualizado formalizando `operations/` e `history/` como novos tipos de artefato; três pastas órfãs (`docs/pipelines/`, `docs/sprint/`, `docs/editorial/`, vazias) descobertas e sinalizadas para Fabrício decidir. **Correção da revisão `1.5`, ainda válida**: a antiga lista de "17 tabelas físicas pré-existentes" incluía `asset_source`, `asset_import_run` e `asset_import_failure` — evidências diretas obtidas durante este projeto (guardas defensivas das Queries `200`/`220`/`230`, que só executaram com sucesso porque as tabelas ainda não existiam, mais uma captura do Table Editor da revisão `0.48` de `05-modelo-de-dados.md`) mostram que as três foram, na verdade, criadas pelas Queries deste próprio projeto — não herdadas de antes da fase de documentação. `card_set_external_reference` permanece citada como pré-existente em `06-pipeline-importacao.md`, mas seu status também não foi confirmado por inspeção direta e deve ser tratado com a mesma cautela até ser verificado. Ver `05-modelo-de-dados.md`, seções "Asset Source" e "Arquitetura de Importação de Ativos", para o histórico completo dessa investigação. **Bloco A reaberto uma terceira vez**: Migrations `263`/`264` (CONFIRMADAS EXECUTADAS) ampliaram o domínio de `card_set.set_type` para incluir `ENERGY` e reorganizaram o `release_order` de `ME1`-`ME4` (`3`-`7`), liberando as posições `1`/`2` para `MEE`/`MEP` — precedidas por uma auditoria estrutural real que confirmou as constraints já documentadas. Refatoração de `set_type` em duas dimensões (natureza editorial vs. natureza do conteúdo) cogitada e deliberadamente adiada. `ADR-015` atualizada (revisão `1.6`) — depois, `1.7`. **`MEE`/`MEP` CONFIRMADOS EXECUTADOS (Migrations `265`–`268`)**: ambos cadastrados como `card_set` com dados editoriais reais (`MEP` via pesquisa direta na TCGdex — `mep`/"MEP Black Star Promos"/`60` cartas confirmadas; `MEE` via fontes oficiais da Pokémon); `MEP` com `card_set_external_reference` confirmada, `MEE` deliberadamente ainda sem referência externa. `AP-018` estendido (revisão `1.8`) para cobrir também `name`, não apenas `code` — discrepância real sinalizada, não resolvida: nomes já cadastrados de `ME1`-`ME4`/`ME2.5` estão em português, inconsistentes com esse princípio se aplicado retroativamente. Próximo passo planejado: validar o pipeline completo (cartas/variantes/referências/imagens) primeiro para `MEE`, depois `MEP` — ainda não iniciado. |
| **Última atualização** | 2026-07-24 |
| **Última revisão** | 1.46 |

Fase 1 entregou: princípios arquiteturais, delimitação do domínio (Pokémon TCG, não o universo Pokémon), estrutura do catálogo editorial, modelo do universo do colecionador, separação entre Set e Collection, e a estratégia de evolução incremental. Fase 2 transforma cada conceito já validado em modelo lógico e, em seguida, tabela física — uma entidade por vez, validada com dados reais antes de avançar para a próxima.

## Retomando este Projeto com uma Nova Sessão de IA

Este repositório é a única fonte de verdade do Project Mimikyu. Ele foi estruturado para que qualquer sessão de IA — inclusive uma sessão nova, sem memória de conversas anteriores — consiga retomar o projeto de forma confiável.

Ao iniciar uma nova sessão (por perda de contexto, início de uma nova fase, ou troca de ferramenta), utilize um prompt equivalente a este:

> Você está retomando o Project Mimikyu. O repositório oficial (`fabriciosouzasales/project-mimikyu`) é a única fonte de verdade — não presuma nenhuma decisão que não esteja explicitamente documentada nele, mesmo que pareça familiar. Antes de qualquer ação:
>
> 1. Leia `docs/README.md` (este documento), incluindo a seção "Status Atual do Projeto".
> 2. Leia, em ordem, `docs/00-project-charter.md` até o último documento numerado existente em `docs/`.
> 3. Leia todos os arquivos `docs/adr/ADR-NNN-*.md` presentes na pasta — não confie apenas no `ADR-INDEX.md`, que pode estar temporariamente desatualizado durante a fase de consolidação documental.
> 4. Leia todos os arquivos `docs/standards/STD-NNN-*.md`.
> 5. Leia `docs/architecture/ubiquitous-language.md` para o vocabulário oficial do domínio.
> 6. Depois de ler tudo, resuma a fase atual do projeto, as decisões já consolidadas e as pendências em aberto — e só então aguarde instruções para iniciar qualquer trabalho novo.
>
> Não avance com implementação ou com novas decisões de modelagem sem essa confirmação.

Este prompt funciona da mesma forma na fase de documentação e na fase de implementação — o que muda é apenas o papel assumido pela IA depois da confirmação. Decisões permanentes (ADRs) são imutáveis uma vez aprovadas; supersessões geram um novo número, preservando o histórico — por isso ler todos os ADRs, e não apenas os "vigentes", é seguro e recomendado.

## Core Documents

| Documento | Finalidade |
|------------|------------|
| [Project Charter](00-project-charter.md) | Define missão, visão, princípios e critérios de sucesso. |
| [Technical Identity](01-technical-identity.md) | Consolida a identidade técnica permanente do projeto. |
| [Architecture Principles](02-architecture-principles.md) | Define os princípios que orientam decisões arquiteturais. |
| [Documentation Architecture](03-documentation-architecture.md) | Define a organização e a governança da documentação. |
| [Domain Model](04-domain-model.md) | Define o modelo conceitual do domínio, anterior à modelagem lógica e física. *(Em elaboração)* |
| [Modelo de Dados](05-modelo-de-dados.md) | Modelo lógico e físico (SQL) de cada entidade, uma por vez, validado com dados reais. *(Em elaboração)* |
| [Pipeline de Importação](06-pipeline-importacao.md) | Estratégia de importação e sincronização de fontes externas. *(Em elaboração)* |
| [Catálogo Editorial](07-catalogo-editorial.md) | Estratégia de captura e disponibilização de dados do catálogo. *(Em elaboração)* |

## Documentation Areas

| Área | Finalidade |
|------|------------|
| [Architecture Decision Records](adr/ADR-INDEX.md) | Registra decisões arquiteturais e suas justificativas. |
| [Standards](standards/STD-INDEX.md) | Define padrões permanentes de implementação e documentação. |
| [Architecture](architecture/README.md) | Reúne visões, modelos, diagramas e descrições arquiteturais. |
| [Operações — Importar uma Nova Coleção](operations/import-card-assets.md) | Guia operacional passo a passo do pipeline `import-card-assets`. |
| [Diário Histórico — Pipeline de Importação](history/pipeline-sprint-log.md) | Histórico condensado de tentativas/bugs/sprints do pipeline de importação. |

## Governing Rule

> ADRs explain **why**. Standards define **how**.

Cada informação deve possuir um único local oficial. A documentação deve evoluir junto com o software e permanecer sincronizada com as decisões e a implementação.

---

## Revision History

| Versão | Descrição |
|---------|-----------|
| 1.0 | Estrutura inicial do documento de navegação. |
| 1.1 | Adicionadas as seções "Status Atual do Projeto" e "Retomando este Projeto com uma Nova Sessão de IA", garantindo que o repositório permaneça recuperável mesmo após perda total de contexto de uma sessão de IA. |
| 1.2 | Adicionado Modelo de Dados à tabela de Core Documents. Atualizado "Status Atual do Projeto": entidade Game concluída na Fase 2. |
| 1.3 | Corrigido "Status Atual do Projeto": o banco físico já possui as 17 tabelas originais com carga inicial de dados, construídas antes desta fase de consolidação documental — a documentação formal (05-modelo-de-dados.md) está sendo escrita retroativamente, e não define uma sequência de criação do zero. |
| 1.4 | Atualizado "Status Atual do Projeto": Game, Expansion e Set (`card_set`, incluindo o tipo `PROMO`) já têm o roteiro completo de `05-modelo-de-dados.md` concluído. Próxima entidade: Card. |
| 1.5 | Reescrito "Status Atual do Projeto" (estava muito desatualizado — ainda citava "próxima entidade: Card", já concluída há dezenas de ciclos). Adotada a nova estrutura de Fases/Blocos de `05-modelo-de-dados.md`: Sub-Fase 1 (Catálogo Editorial), Bloco A (Modelo de Dados) **concluído** — cobre Game/Expansion/Set/Card/Language/Rarity/Card Variant/Card Asset/Storage Bucket/Asset Source/Card External Reference/Asset Import Run/Asset Import Failure; Bloco B (Pipeline de Importação) e Bloco C (Carga Editorial) ainda não iniciados. **Corrigida a lista de "17 tabelas físicas pré-existentes"**: `asset_source`, `asset_import_run` e `asset_import_failure` removidas dessa lista — evidências diretas coletadas durante este projeto (guardas defensivas de Queries que só executaram porque as tabelas não existiam ainda, mais uma captura real do Table Editor) mostram que as três foram criadas pelas próprias Queries deste projeto, não herdadas de antes da fase de documentação; `card_set_external_reference` mantida na lista original, mas sinalizada como não verificada. |
| 1.6 | Atualizado "Status Atual do Projeto": **Bloco B (Pipeline de Importação) iniciado** — arquitetura da Edge Function `import-card-assets` especificada e roteiro de 12 sprints (`B2.1`–`B2.12`) definido, ambos detalhados em `06-pipeline-importacao.md` (não duplicados aqui). Código do Sprint B2.1 proposto, mas deploy ainda não confirmado — não tratado como concluído. |
| 1.7 | Atualizado "Status Atual do Projeto": **primeiro deploy real e primeira invocação remota confirmada de uma Edge Function** (Sprints B2.0-B2.2 concluídos); roteiro renumerado/consolidado para `B2.0`–`B2.8` (detalhes em `06-pipeline-importacao.md`); Sprint B2.3 com código publicado, teste pendente. Código versionado pela primeira vez em `supabase/functions/import-card-assets/index.ts`. |
| 1.8 | Atualizado "Status Atual do Projeto": Sprints `B2.0`–`B2.4` **CONFIRMADOS CONCLUÍDOS** (consulta de execução por `run_code` e descoberta de `card_set`/`card`, ambos com Diário Técnico registrado); Sprint `B2.4.1` (refatoração para services) com código escrito, deploy ainda não confirmado; `B2.5` dividida em `B2.5A`/`B2.5B`, com possível sobreposição de escopo com `B2.6` sinalizada em `06-pipeline-importacao.md`, não resolvida unilateralmente. Código confirmado (v1.2.0) atualizado em `supabase/functions/import-card-assets/index.ts`. |
| 1.9 | Atualizado "Status Atual do Projeto": Sprint `B2.4.1` **CONFIRMADO CONCLUÍDO** — deploy e reteste reais confirmados, mesmo comportamento da v1.2.0 sob `version: "1.2.1"`. Novo princípio de arquitetura registrado: `index.ts` de toda Edge Function futura deve apenas orquestrar, sem conhecer SQL/PostgreSQL/fontes externas diretamente. Código confirmado (v1.2.1) versionado em `supabase/functions/import-card-assets/index.ts`, `types.ts` e `services/database.ts`. |
| 1.10 | Atualizado "Status Atual do Projeto": Bloco A reaberto pontualmente — nova entidade `card_set_external_reference` (`240` CONFIRMADA EXECUTADA), documentada em `05-modelo-de-dados.md`. Sprint B2.5A em andamento: `services/tcgdex.ts` escrito e validado no editor, ainda não integrado/deployado/testado. |
| 1.11 | Atualizado "Status Atual do Projeto": `card_set_external_reference` com `241` (triggers) também CONFIRMADA EXECUTADA; episódio real de correção (`ME0`→`sv10pt5`) registrado; Seed `910` adiada até dados reais da TCGdex. Escopo final de B2.5A definido (`NO_EXTERNAL_SET_MAPPING` para Sets sem referência ativa). |
| 1.12 | Atualizado "Status Atual do Projeto": B2.5A com código escrito (services + rascunho v1.3.0 de `index.ts`), sem deploy confirmado — sprint pivotou para descoberta dos `external_set_id` reais via script administrativo, ainda não executado. |
| 1.13 | Atualizado "Status Atual do Projeto": marco — primeira comunicação externa real e bem-sucedida do Bloco B, `external_set_id` reais da TCGdex descobertos (`ME0`-`ME5`); nova decisão de negócio em aberto sobre `ME0`↔`mee` ("Mega Evolution Energy"), cross-referenciada com o "escopo `ENERGY`". |
| 1.14 | Atualizado "Status Atual do Projeto": decisão de arquitetura declarada definitiva pela sessão pareada — pipeline dividido em duas Edge Functions (`sync-card-set`/`import-card-assets`), formalizada em nova `ADR-017-two-function-import-pipeline.md`. Nenhuma implementação real ainda; próximo passo é o Sprint B3. |
| 1.15 | Atualizado "Status Atual do Projeto": nova disciplina de trabalho (uma camada por vez) declarada; `tcgdex.ts` revisado tecnicamente e aprovado, `index.ts` com nota 9,5/10 — nenhum dos dois deployado. Discrepância real sinalizada: implementação segue dentro de `import-card-assets/services/`, não em uma pasta `sync-card-set/` própria como o plano da `ADR-017` sugeria. |
| 1.16 | Atualizado "Status Atual do Projeto": marco real — Query `910` (Seed Card Set External Reference) CONFIRMADA EXECUTADA (parcial: `ME1`-`ME4`/`ME2.5`; `ME0` excluído por decisão pendente; `ME5` aguarda cadastro). `index.ts`/`tcgdex.ts`/`scripts/discover-tcgdex-sets.ts` aprovados em revisão técnica; nova versão completa de `services/database.ts` escrita como rascunho, ainda não deployada. Novo arquivo `database/seeds/910_seed_card_set_external_reference.sql`. |
| 1.17 | Atualizado "Status Atual do Projeto": primeira tentativa real de deploy da v1.3.0 (primeira integração real Supabase→TCGdex) FALHOU com erro real de bundling (`@supabase/supabase-js` sem mapeamento em `deno.json`); causa raiz diagnosticada, correção escrita (`supabase: any`); deploy de sucesso ainda não confirmado. |
| 1.18 | Atualizado "Status Atual do Projeto": marco real — deploy da v1.3.0 (`index.ts`+`database.ts`+`tcgdex.ts`) CONFIRMADO com sucesso; os três arquivos copiados ao repositório (primeira vez para `tcgdex.ts`). Nova convenção `deno check` + `deploy`. Invocação de ponta a ponta com resposta real da TCGdex ainda não confirmada (bloqueada por chave de autenticação incorreta). |
| 1.19 | Atualizado "Status Atual do Projeto": causa raiz do HTTP 401 diagnosticada definitivamente (mecanismo de autenticação de `withSupabase({ auth: ["secret"] })`, não a chave usada nem `verify_jwt`); correção final (`index.ts` v1.3.1, sem autenticação, temporário) escrita, ainda não deployada nem testada. |
| 1.20 | Atualizado "Status Atual do Projeto": correção da revisão `1.19` — `index.ts` v1.3.1 foi de fato deployado e testado, mas o HTTP 401 PERSISTIU; causa raiz continua desconhecida. Bug real de metodologia de teste (chave placeholder não substituída) encontrado e corrigido, mas não era a causa raiz. Teste de isolamento definitivo (Edge Function mínima sem `withSupabase`) proposto, ainda não executado. |
| 1.21 | Atualizado "Status Atual do Projeto": marco real — HTTP 401 definitivamente eliminado, `@supabase/server`/`withSupabase` abandonado, substituído por `@supabase/supabase-js` com cliente manual (`index.ts` v2.0.0, `deno.json`, copiados ao repositório pela primeira vez). GRANT ausente em `card_set_external_reference` para `service_role` descoberto e corrigido (migration `250`). Resposta final `success: true`/`tcgdex_set` ainda não explicitamente confirmada. |
| 1.22 | Atualizado "Status Atual do Projeto": progresso real além do GRANT (HTTP 404, lógica de aplicação); pendência de longa data `ME0`↔`mee` resolvida por decisão de negócio real de Fabrício (coleções diferentes, sem relação); `ME0` removida de `card_set` por completo (Migration `251`, real, confirmada). |
| 1.23 | Atualizado "Status Atual do Projeto": 🎉 MARCO REAL — primeira resposta de ponta a ponta da TCGdex confirmada através de `import-card-assets` (Set `me01`, "Mega Evolution", 188 cartas); `B2.5A` do roteiro CONCLUÍDO. Três propostas reais adiadas (Stored Procedure, `ENUM`s nativos, reestruturação de pastas). |
| 1.24 | Atualizado "Status Atual do Projeto": estrutura real de carta da TCGdex confirmada; episódio real de perda de contexto arquitetural da sessão pareada, autocorrigido; fluxo de persistência de `card`/`card_external_reference` realinhado. Sinalizada, sem resolver unilateralmente, a proposta de `docs/architecture.md` — possível duplicação da documentação já existente neste repositório. |
| 1.25 | Atualizado "Status Atual do Projeto": reconfirmação real da arquitetura de fluxo/Edge Functions; episódio real de perda de foco operacional, corrigido por Fabrício ("Não quero trabalhar para importar 1 imagem apenas [...] Precisamos finalizar o trabalho do catálogo editorial"); novo faseamento `FASE 1-6`/`Sprint 1-2` proposto, ainda não reconciliado com o Roteiro vigente `B2.x`/`B3.x`; autocorreção real de que a Query `880` (com `card_asset`) segue sendo o ponto final do Catálogo Editorial. Nova discrepância sinalizada, não resolvida: proposta de `docs/adr/` com ADRs `001`-`005` colidindo com os 17 ADRs reais já aprovados; divergência de nome `download-card-assets` vs. `sync-card-set` (`ADR-017`). |
| 1.26 | Atualizado "Status Atual do Projeto": escopo real do Bloco B expandido para 5 coleções (`ME1`/`ME2`/`ME2.5`/`ME3`/`ME4`); quarto episódio real de perda de contexto, contido antes de qualquer execução — `card`/`card_variant` já tinham 859/1.555 registros reais (marco fechado há dezenas de batches), momentaneamente tratadas como vazias pela sessão pareada, corrigido por Fabrício. Correção arquitetural real: o pipeline passa a consultar `card`, nunca inserir — `card`/`card_variant` congeladas, fora do escopo do Bloco B. Auditoria real (com um erro de colunas/`JOIN` capturado e corrigido) reconfirmou totais e estrutura sem divergência. Terceiro framing de roadmap paralelo (`FASE 1-4`/três Entregas) em três batches, ainda não reconciliado. |
| 1.27 | Atualizado "Status Atual do Projeto": Sprint B3.12 — dependências de `card_external_reference` auditadas e confirmadas prontas (`asset_source`, `card_set_external_reference`); `card_external_reference` confirmada vazia, ponto de partida real; chave de correspondência `card`↔TCGdex (`card_set_id`+`collector_number`=`localId`) validada com dados reais; plano do próximo incremento travado, ainda não codificado. **Mudança de processo, por pedido explícito de Fabrício**: a partir desta revisão, a documentação registra apenas o estado final e as decisões confirmadas de cada etapa — não mais o histórico de tentativas/correções da sessão pareada. |
| 1.28 | Atualizado "Status Atual do Projeto": estrutura de `card_external_reference` reconfirmada; código do Incremento 1 finalizado (`index.ts` v2.1.0 + `database.ts`) — deploy/execução reais ainda não confirmados, código não copiado ao repositório. |
| 1.29 | Atualizado "Status Atual do Projeto": plano de validação do Incremento 1 definido; execução bloqueada por problema real de ambiente local (Supabase CLI indisponível no terminal), resolvido via `npm install -g supabase`, verificação final pendente. Sinalizado, não resolvido: se a validação rodará local ou contra o projeto hospedado real. |
| 1.30 | Atualizado "Status Atual do Projeto": resolvido — banco é o Supabase Cloud; deploy do Incremento 1 CONFIRMADO por saída real de terminal; código copiado ao repositório (`index.ts` v2.1.0 + `database.ts`). Execução de ponta a ponta ainda não confirmada. |
| 1.31 | Atualizado "Status Atual do Projeto": 🎉 MARCO REAL — Incremento 1 CONFIRMADO CONCLUÍDO (`card_external_reference` 188/188 para a `ME1`). Bloqueio real (GRANT ausente) diagnosticado e corrigido pela migration `253`; logging de erro corrigido em `database.ts`. |
| 1.32 | Atualizado "Status Atual do Projeto": Incremento 2 (download de imagens) iniciado — fluxo definido, teste controlado aprovado por Fabrício; `card_asset`/`card_asset_type` parcialmente confirmadas; `storage_bucket` ainda não confirmada. |
| 1.33 | Atualizado "Status Atual do Projeto": `storage_bucket` confirmada (catálogo interno, não o bucket físico); bucket físico `card-front` criado no Storage; estrutura completa de `card_asset` confirmada (sem vínculo direto com `card_external_reference`). Código do Incremento 2 (teste controlado) DEPLOYADO, CONFIRMADO; execução real FALHOU — terceiro caso do gap de GRANT ausente para `service_role` (`language`), correção proposta, não confirmada executada. |
| 1.34 | Atualizado "Status Atual do Projeto": cadeia de GRANTs resolvida (seis casos reais no total); teste controlado do Incremento 2 CONFIRMADO CONCLUÍDO — primeira imagem real do projeto importada de ponta a ponta. |
| 1.35 | Atualizado "Status Atual do Projeto": 🎉 MARCO REAL — Incremento 2 CONCLUÍDO para a `ME1` (188/188 imagens, 0 falhas). Refatoração (`services/storage.ts`) e bug real de regra de negócio (`external_url`) corrigido. |
| 1.36 | Atualizado "Status Atual do Projeto": pipeline replicado sem alteração de código — `ME2` (130/130) e `ME2.5` (295/295) CONCLUÍDAS, 0 falhas. Plano de duas fases definido (Fase 1: `en`; Fase 2: `pt-BR`). Total acumulado: `613`/`613`/`613`, 3 de 5 coleções. |
| 1.37 | Atualizado "Status Atual do Projeto": 🎉 MARCO REAL — Fase 1 100% CONCLUÍDA (`ME3`/`ME4` concluídas). Catálogo editorial completo em inglês: `859` cartas/referências/imagens/`card_asset`, `0` falhas nas 5 coleções. |
| 1.38 | Atualizado "Status Atual do Projeto": Fase 2 (`pt-BR`) iniciada — teste controlado confirma `card_asset` multi-idioma; ⚠️ discrepância real sinalizada em `card_external_reference` (`UNIQUE` sem idioma), NÃO resolvida. |
| 1.39 | Atualizado "Status Atual do Projeto": 🎉 teste controlado da Fase 2 CONFIRMADO CONCLUÍDO — bug real do código de idioma da TCGdex corrigido; `ME1` `pt-BR` `188/188/0`; três validações confirmadas. Fase 2 escalada para `ME2`-`ME4`, `ME2` preparada, execução não confirmada. |
| 1.40 | Atualizado "Status Atual do Projeto": 🎉 MARCO REAL — Fase 2 100% CONCLUÍDA, Bloco B encerrado. `ME2`/`ME2.5`/`ME3`/`ME4` (`pt-BR`) confirmadas por Fabrício. Catálogo Editorial completo nas duas línguas: `859`/`1.555`/`859`/`1.718`/`1.718`/`0`. Pendência de `card_external_reference` respondida por evidência agregada (decisão de correção ainda pendente de Fabrício). Fabrício define o próximo módulo real: `Coleções`, não interface. |
| 1.41 | Atualizado "Status Atual do Projeto": Bloco A reaberto — identificador oficial real do Set promocional encontrado (`MEP`, não `mee`); mecanismo de `ADR-015` permanece correto, só a convenção de código estava errada; novo `AP-018` adotado (nunca inventar códigos editoriais). Recadastro de `MEP` planejado, não executado. Questão real e explicitamente aberta: se `Expansion` modela corretamente "Series"/"Era" — investigação pendente antes de qualquer mudança no banco. |
| 1.42 | Atualizado "Status Atual do Projeto": questão `Expansion`/"Series"/"Era" RESOLVIDA por decisão direta de Fabrício (sem mudança de schema). Duas novas decisões reais, não executadas: cadastro de `MEE` (Set de Energias) e convenção de `release_order` para Sets especiais (Energia=1, Promo=2). Novo lema de ritmo do projeto registrado. Roadmap completo (Fase 1-7) esboçado; `ROADMAP.md` planejado como primeiro documento formal futuro. |
| 1.43 | Atualizado "Status Atual do Projeto": `06-pipeline-importacao.md` dividido em três documentos (arquitetura, `operations/import-card-assets.md`, `history/pipeline-sprint-log.md`), a pedido de Fabrício. `03-documentation-architecture.md` atualizado com os novos tipos de artefato. Três pastas órfãs em `docs/` descobertas e sinalizadas, não removidas. |
| 1.44 | Adicionadas `operations/import-card-assets.md` e `history/pipeline-sprint-log.md` à tabela "Documentation Areas", completando a navegação para os dois novos tipos de artefato formalizados em `03-documentation-architecture.md`, revisão `1.5`. |
| 1.45 | Atualizado "Status Atual do Projeto": Migrations `263`/`264` (CONFIRMADAS EXECUTADAS) ampliaram `card_set.set_type` para incluir `ENERGY` e reorganizaram `release_order` de `ME1`-`ME4`, liberando `1`/`2` para `MEE`/`MEP`. `MEE`/`MEP` ainda não cadastrados. `ADR-015` atualizada (revisão `1.6`). |
| 1.46 | Atualizado "Status Atual do Projeto": `MEE`/`MEP` CONFIRMADOS EXECUTADOS (Migrations `265`–`268`), com `MEP` já mapeado à TCGdex via `card_set_external_reference`. `AP-018` estendido (revisão `1.8`) para cobrir `name`, não apenas `code`. Discrepância real sinalizada: nomes de `ME1`-`ME4`/`ME2.5` em português, inconsistentes com o princípio se aplicado retroativamente. `ADR-015` atualizada (revisão `1.7`). |
