# Project Mimikyu Documentation

Esta pasta contém a documentação oficial do Project Mimikyu.

## Status Atual do Projeto

### Placar Rápido

| Indicador | Estado |
|-------|-------|
| Fase 1 — Arquitetura Conceitual | Concluída |
| Card Sets | Última contagem confirmada e documentada: 7 (`MEE`/`MEP`/`ME1`-`ME4`/`ME2.5`, revisão `1.50` deste documento). Desde então, o Catálogo Editorial cresceu ativamente via ingestão TCGdex (`ADR-024`, Ciclo 2 — ver linha "Catálogo Editorial — Escrita e Ingestão" abaixo): pelo menos mais 11 Card Sets foram incorporados (`SV1`-`SV5`, `SV3.5`, `SVE`, `SVP`, `ME5`, entre outros observados na tela `/catalogo/card-sets`). **Total atual não confirmável apenas pelo repositório** (contagem física só existe no banco Supabase, fora do escopo de leitura desta auditoria) — ver o handoff vigente para a lista de Sets processados até a data dele. |
| Cards / Card Variants / Card Assets | Última contagem confirmada e documentada: `927` Cards / `1.653` Card Variants / `1.734` Card Assets (revisão `1.50`), cobrindo as 7 Card Sets originais. Cresceram junto com os novos Card Sets acima — **total atual não confirmável apenas pelo repositório.** |
| Sets com imagens (Card Asset) | Última situação documentada para as 7 Card Sets originais: `MEE` completa nos dois idiomas desde 2026-07-25; `MEP` sem pendência de canal — o canal de importação manual (`ADR-026`) está implementado e validado em produção (Coleção `SVE`, 2026-08-08), disponível para concluir `MEP` a qualquer momento. Uso do canal para `MEP` especificamente é item de dado, não de implementação — não bloqueia nenhuma frente do Catálogo Editorial. O pipeline de imagens (`import-card-assets`) evoluiu desde então (idioma dinâmico `en`+`pt-BR`, retry por download, aborto antecipado — ver `06-pipeline-importacao.md`) e vem sendo usado ativamente para os novos Card Sets da ingestão TCGdex; estado atual por Set não confirmável apenas pelo repositório. |
| Falhas na importação | Sem falhas conhecidas nas 7 Card Sets originais. Falhas pontuais por raridade não mapeada foram identificadas e corrigidas durante a ingestão dos novos Card Sets (ver linha "Raridades — ajustes de mapeamento", abaixo) — não há um total agregado atual confirmável apenas pelo repositório. |
| Catálogo Editorial | **Formalmente encerrado (2026-08-08)** — as cinco frentes (A. Modelo de Dados, B. Pipeline de Assets, C. Interface de Consulta, D. Escrita Administrativa, E. Ingestão Administrativa) declaradas Concluídas após auditoria dedicada de cada uma. Ver `ROADMAP.md`, seção "Catálogo Editorial — Frentes de Encerramento", para o critério e o detalhamento por frente. |
| Módulo Gerencial | **Formalmente encerrado (2026-08-09)** — as quatro frentes da Trilha 4 (`ROADMAP.md`) concluídas e aprovadas por Fabrício: Visão Geral (dashboard executivo com drill-down completo), Histórico de Importações (histórico unificado Cartas+Imagens, gráficos de tendência por pipeline), Log de Atualizações (V1 sobre `catalog_admin_action_log`, 3 gráficos semanais por categoria, tabela paginada/filtrada server-side) e Central de Relatórios (`/catalogo/relatorios`, 6 relatórios imprimíveis via `@media print`, sem motor de PDF). O Checklist por Coleção recebeu tratamento diferenciado (folha A4 com marca própria, checkbox antes do número, coluna "Cadastrada" removida) e virou a **baseline visual** dos outros 5 relatórios (`RelatorioFolha`/`RelatorioCabecalho`/`RelatorioRodape` compartilhados) depois de várias rodadas de refinamento. Fechamento formal da última frente (Central de Relatórios) motivado por uma inspeção geral final do módulo, que também corrigiu três achados soltos fora do escopo da Central em si: ícone ausente no título de "Log de Atualizações", botão "Ver todas" trocado por rolagem infinita nas galerias de Cartas/Coleções, e barras de "Cobertura por idioma" ampliadas no hub de Card Set. Fabrício validou o resultado final pela UI: "Considero pela UI o bloco concluído." **Duas evoluções aditivas em 2026-08-16, sem reabrir o encerramento formal**: eventos de Governança de Variantes humanizados no Log de Atualizações (rótulos de Entidade/Ação); pipeline VARIANTES (`import-card-variants`) passa a aparecer no Histórico de Importações unificado, lado a lado com Cartas/Imagens. Também em 2026-08-16, bug estrutural de paginação de impressão corrigido no template compartilhado (`RelatorioFolha`/`globals.css`) — rodapé `position: fixed` não reservava espaço em toda página, ocultando a última linha antes da quebra; corrigido com `box-decoration-break: clone` + `break-inside: avoid`, propagado automaticamente aos 8 relatórios. Ver `ROADMAP.md`, seção "Concluído", e `05e-catalogo-editorial.md` para o detalhamento completo. |
| Sub-Fase 2 — Coleções | Não iniciada. **Critério de entrada atualizado em 2026-08-16** — o bloco intermediário **Card Variant** está formalmente encerrado como fundação, e a fundação física de **Pricing/Market Data** agora também está completa (dez de dez entidades, Incremento P6, mesma data). Sequenciamento macro vigente: Catálogo Editorial → Card Variant (concluído) → Pricing (fundação, concluída) → Coleções. Início real permanece decisão de Fabrício, não automática só pela conclusão física de Pricing. |
| Bloco Card Variant (intermediário) | **Fundação encerrada por decisão explícita de Fabrício (2026-08-16)** — declarada base necessária e suficiente para as próximas frentes (Pricing e Collection). Trabalho real: governança formalizada (`ADR-028`, revisões `1.0`–`1.4`); taxonomia de `card_variant_type` com CRUD administrativo completo (`/catalogo/tipos-variacao`, três incrementos); pipeline "Importar Variantes" de ponta a ponta (staging, Edge Function `import-card-variants`, decisão/confirmação em lote, resolução administrativa de mapeamento externo, painel de conclusão); indicador de Card Variants na galeria de Cartas; dois incidentes reais corrigidos (SV10 — job preso por timeout de fetch sem `AbortController`; SV8.5 — erro genérico em duplicidade de combinação na fonte). Estado real confirmado por introspecção direta do Supabase em 2026-08-16: **4.718 Card Variants, 39 tipos canônicos, 2.433 de 7.104 Cards cobertas.** Evolução futura, explicitamente não bloqueante: Vintage/Promo Variant Modeling (30 combinações de `BASEP` sem mapeamento, mais um job `BASE1` — Coleção Básica 1999 — também `STAGED` com 410 linhas 100% sem mapeamento, achado desta mesma auditoria). Modelo físico consolidado em `docs/05b-cartas-e-raridade.md`. Ver `docs/log.md` (2026-08-14/15/16) e `ADR-028` para o detalhamento normativo. |
| Front-end (aplicação web) | Em desenvolvimento (`ADR-019`) — app shell, autenticação, Identidade e Acesso (Incrementos 1 "Meu Perfil" e 2 "Administração de Usuários") implementados e validados em produção; Catálogo Editorial — leitura (`ADR-022`, telas de Jogos/Expansões/Card Sets/Cartas/Visão Geral) implementada e validada visualmente em produção; **escrita administrativa (`ADR-023`) concluída (2026-08-07)**: infraestrutura comum concluída; `Game`, `Expansion`, `Card Set` e `Card` todos com o ciclo vertical completo (create/update/delete/deactivate/reactivate conforme aplicável, backend + tela + validação estrutural/funcional real) — `Card` foi o último a fechar, com atualização (`admin_update_card()`, Query `2114`) e cadastro/desativação/reativação (`admin_create_card()`/`admin_deactivate_card()`/`admin_reactivate_card()`, Queries `2115`-`2117`) implementados no mesmo dia. Telas de galeria `/catalogo/card-sets` (agrupada por Expansão) e `/catalogo/cartas` (leitura + cadastro + edição + desativação/reativação, toggle "Mostrar inativas") concluídas. Módulo Gerencial (Visão Geral com drill-down completo, Histórico de Importações unificado) implementado sobre essa base — ver linha "Módulo Gerencial", acima. **Auth Experience concluída (2026-08-16)**: Login V1 aprovado e congelado (fundação visual própria, `AuthHeroShell`), propagado a Cadastro/Recuperar Senha/Atualizar Senha. **Design System/UI interna consolidado (2026-08-16)**: arquitetura cromática validada em três rodadas de prova visual isolada e promovida a baseline de toda a aplicação autenticada (workspace off-white/grafite por tema, navegação sempre escura, dourado como accent); Form Controls consolidados (tokens `--control-*`, `Select` compartilhado substituindo os 18 usos de `<select>` cru) — ver `docs/standards/STD-004-frontend-standards.md` versão `1.3`. |
| Segurança/Performance | **RLS Admin Performance Hardening + Performance Foundation v1 (2026-08-14/15).** `is_admin()` convertida de `VOLATILE` para `STABLE`, 24 policies do Catálogo Editorial reescritas para `(select is_admin())` (Query `2134`, CONFIRMADO EXECUTADO E VALIDADO); quatro incrementos de eliminação de over-fetch/leitura redundante em `/catalogo`, `/catalogo/cartas` e `/catalogo/card-sets`; least privilege ampliado (`TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` revogados de `anon`/`authenticated` nas 24 tabelas do Catálogo Editorial, Query `2147`). Padrões formalizados nesta auditoria em `docs/standards/STD-001-database-standards.md` versão `1.19` (existiam apenas em `docs/log.md` desde a execução — gap de normatização fechado em 2026-08-16). |
| Pricing / Market Data | **Fundação física completa (`ADR-029`/`05f-pricing.md`); Incrementos P1–P6 — Fundação Física + Correspondência Externa + Auditoria Operacional + Produto Externo + Série Histórica de Câmbio + Observação Histórica de Preço executados (2026-08-16).** Dez entidades (`pricing_source` até `pricing_sync_run_call`) — quarto domínio independente de Catálogo/Ownership/Analytics; printing/condição/idioma/moeda/mercado como dimensões independentes; "Valor Brasil" depende de evidência em `pricing_observation`, nunca só do default da fonte. **Dez de dez entidades criadas no Supabase** (`pricing_source`, `card_condition`, `pricing_condition_mapping` — Incremento P1; `pricing_set_mapping`, `pricing_card_mapping` — Incremento P2; `pricing_sync_run`, `pricing_sync_run_call` — Incremento P3; `pricing_product` — Incremento P4; `pricing_fx_rate` — Incremento P5; `pricing_observation` — Incremento P6, com identidade market-aware `UNIQUE NULLS NOT DISTINCT` corrigida — todas ainda vazias, milhar `3000`–`3999`, formalizado em `STD-001`). **Fundação física concluída.** **Incremento P7 (2026-08-17)**: `JUSTTCG` cadastrada em `pricing_source` (Query `3700`), homologada para piloto técnico de produto/printing/condição/preço USD/histórico (Decisão A aprovada) — não homologada para PT-BR nem como preço do mercado brasileiro (Decisão B inconclusiva), `is_active = FALSE` até plano comercial compatível ser contratado. **Incrementos P8–P12 executados na sequência (2026-08-17→18), completando o arco de Pricing até a exibição ao usuário final.** P8 — primeiro conector real JustTCG (`scripts/sync-justtcg-pricing.ts`), condições canônicas semeadas, grants de escrita (Queries `3091`/`3701`/`3702`). P9 — ingestão PTAX (`scripts/sync-ptax-fx-rate.ts`, manual). P10 — projeção de leitura `pricing_observation_brl_equivalent`. P11 — contrato seguro de leitura por carta (`get_card_pricing_snapshot`, `SECURITY DEFINER STABLE`). **P12 — Exibição de Preços no Frontend, formalmente encerrado (2026-08-18)**: pill de preço em BRL nos três grids do catálogo, sobre `get_cards_pricing_summary` (migration `3903`, sem N+1) e hierarquia de printing Normal → Holofoil → Reverse Holofoil corrigida (migration `3904`); validação local completa (desktop/mobile, temas, toque, teclado, `typecheck`/`build`) aprovada por Fabrício; commits `6324b66`/`0dad413` sincronizados com `origin/main`. **A visualização de preços nunca chama a JustTCG** — lê só `pricing_observation` já sincronizada; sincronização e consulta seguem desacopladas. `JUSTTCG.is_active` permanece `FALSE` — Pricing ainda não é operacional em produção (falta plano comercial contratado e agendamento automático). **Incremento P13 — Orquestração Programada de Pricing, formalmente encerrado (2026-08-18→19).** P13.1 — fundação de schema (`FX_REFRESH`/`fx_source_code`/`confirmed_by` opcional/índices de concorrência, Migrations `3905`–`3907`, `ADR-031`), **P13.2 — Módulo Compartilhado e Execução Manual Auditável da PTAX, formalmente encerrado**: lógica extraída para `supabase/functions/_shared/pricing-ptax/` (núcleo puro, 12 arquivos), adapter/runner reescritos; motivado por auditoria pós-piloto real, migration `3909` tornou o PostgreSQL autoridade de `started_at`/`finished_at` de `pricing_sync_run`, reexecução idempotente auditada sem inversão temporal. **P13.3 — Edge Function `ptax-fx-refresh`, formalmente encerrado**: implementada sobre uma porta funcional (`PtaxSyncRunPort`/`supabase-adapter.ts` — abandonando uma tentativa anterior de mimetizar a API fluente do PostgREST), suíte offline registrada no runner nativo do Deno, **implantada em produção** (Supabase MCP, `qjfutqujxrbzgrtkpgkg`, `slug=ptax-fx-refresh`, `version=1`, `status=ACTIVE`, `verify_jwt=false`) e validada por piloto real autenticado — dois testes negativos (`HTTP 401`, zero efeito no banco) antes do positivo (`HTTP 200`/`COMPLETED`, run `e75e8fca-1c2e-498a-a473-18afa74c152d`, `inserted=0`/`unchanged=7`/`divergent=0`/`invalid=0`). **P13.4 — Agendamento Automático via `pg_cron`/`pg_net`/Vault, formalmente encerrado**: migration `3910` habilita `pg_cron`/`pg_net` e cria o Job único `ptax-fx-refresh-weekdays` (segunda a sexta, `22:00 UTC`/`19:00 America/Sao_Paulo`), chamando `net.http_post` diretamente com URL/`apikey` resolvidos por nome no Vault (nunca valor literal no comando). **Validação autônoma antecipada (2026-08-19)**: `pg_cron 1.6.4` sem overload de timestamp absoluto para one-shot — Job temporário protegido por data fixa na própria expressão cron, comando idêntico ao permanente (nunca tocado), disparado pelo próprio `pg_cron` sem chamada manual, evidência completa correlacionada (`HTTP 200`, zero run ativo residual, reexecução idempotente sem duplicidade), removido logo após a auditoria. **P13 formalmente encerrado.** Ver `docs/adr/ADR-031-scheduled-pricing-orchestration.md`, revisão `1.9`, e `docs/05f-pricing.md`, seção "Encerramento Formal do Incremento P13". **Pricing não parou em P13 — Incremento P14 (2026-08-19→20) permanece em andamento**: condição comercial satisfeita (`JUSTTCG.is_active = TRUE`, P14.1), descoberta+persistência em lotes (P14.2/P14.3) e expansão de cobertura por ondas com matching canônico por Set+número (P14.4.1–P14.4.4, run `66c9e878` — 53 mappings promovidos). **P14.4.4 formalmente encerrado (2026-08-20)** — próximo passo real: P14.4.3 (ondas seguras remanescentes). Ver `docs/05f-pricing.md`, revisão `1.48`, e `docs/adr/ADR-029-pricing-domain-model.md`, revisão `1.31`. |
| Pesquisa Global de Cartas (`ADR-030`) | **Implementada de ponta a ponta (2026-08-16/17), com correção de escopo no mesmo dia (revisão `1.1` do ADR)** — combobox no header (debounced, ARIA `aria-activedescendant`, teclado, até 8 sugestões) navegando para `/pesquisa?card=<uuid>` ou `?q=<termo>`; página `/pesquisa` autenticada com busca, filtros (Card Set/Categoria/Raridade — **sem Jogo nesta versão**, decisão de escopo), estado 100% na URL, ordenação fixa sem seletor de sort. Backend: duas funções `public.*` `SECURITY DEFINER STABLE` (`search_cards`/`search_card_filter_options`, milhar `4000`-`4999`, novo em `STD-001`) substituem qualquer ampliação de `SELECT` direto nas tabelas base do Catálogo (que seguem admin-only, `ADR-022` intacto) — `EXECUTE` revogado de `PUBLIC`/`anon`, concedido só a `authenticated`; validado com `SET LOCAL ROLE` real, não só pela conexão de service role. Índices `pg_trgm`/normalização de `collector_number` validados por `EXPLAIN (ANALYZE, BUFFERS)` mapeado individualmente aos 10 padrões de consulta, incluindo 22.104 linhas (volume sintético, `ROLLBACK`, zero resíduo). **Correção de escopo (2026-08-17, mesma data)**: as assinaturas originais (`4010`/`4020`) incluíam indevidamente um parâmetro de Jogo, fora do aprovado — corrigidas via `4030`/`4031` (`DROP FUNCTION` explícito + recriação sem Jogo, mesmo endurecimento), sem alterar retroativamente as originais; confirmado por `pg_proc` que não resta overload antigo executável. **Pendência transparente registrada nesta mesma rodada**: divergência pré-existente entre `database/migrations/` e o histórico de migrations do Supabase, descoberta durante a introspecção obrigatória deste incremento (não causada por ele) — `2050` e `3000`-`3090` sem arquivo no repo; `2098`-`2153` sem entrada no histórico do Supabase; a bookkeeping da Query `4002` ficou com texto incorreto por um bloqueio do classificador de segurança do Auto Mode. Registrada como débito explícito, não reconciliada (decisão de Fabrício). **Encerramento formal (2026-08-17, mesmo dia)**: além da correção de escopo, mais três rodadas corretivas no mesmo dia — busca por "número/total" (ex. "125/094"), preview de carta em `/pesquisa` estruturalmente compartilhado com `/catalogo/cartas` (`CardImagePreview`/`CardPreviewOverlay`, `HoloCard`), e o modo imersivo de pesquisa mobile reescrito como painel de tela cheia substituindo o header (referência funcional pkmn.gg). Testes de UI (teclado/leitor de tela/tema/mobile) e `npm run lint`/`build`, que não puderam ser executados no sandbox do agente (sem navegador/SWC), **foram validados e aprovados localmente por Fabrício**; commit e push das cinco commits do incremento já realizados (`git log`/`git status` confirmam árvore limpa, sincronizado com `origin/main`). Incremento formalmente encerrado. Ver `docs/adr/ADR-030-card-search-projection.md`, `ROADMAP.md`, `docs/development/HANDOFF-2026-08-17.md`, `docs/log.md`. |
| Catálogo Editorial — Ingestão (`ADR-024`) | Ciclo 1 (staging comum) e **Ciclo 2 (fluxo TCGdex completo) implementados, em uso ativo em produção e formalmente validados de ponta a ponta (2026-08-07)** — Edge Function `import-catalog-cards` processa Sets reais (`SV1`-`SV5`, `SV3.5`, `ME5`, entre outros) através da tela `/catalogo/importar-cartas` (staging → revisão administrativa → confirmação → `internal.write_card()`). Validação `2818` (somente leitura, sobre dado real de produção) **CONFIRMADO EXECUTADO E VALIDADO — 8 de 8 itens**, mesmo padrão de rigor da `2814` do Ciclo 1. Único achado real: bug em `admin_confirm_catalog_import()` v1.0 (job podia chegar a `COMPLETED` com linha de decisão nunca revisada) — corrigido pela Query `2082` v1.1 + Migration `2118` de reparo retroativo, ambas confirmadas executadas. Multi-provider (múltiplas fontes externas) confirmado como implementação futura, fora de escopo desta sprint (decisão explícita de Fabrício, 2026-08-07). **Ciclos 3/4 (processador de PDF) encerrados definitivamente (2026-08-08)** — decisão explícita de Fabrício, motivada pela taxa de sucesso já suficiente do canal TCGdex, pela direção futura de multi-provider e pelos canais manuais (`ADR-023`/`ADR-026`) já cobrirem o caso residual; embasada por prova técnica exploratória real que confirmou viabilidade mas quantificou custo de engenharia para robustez entre produtos. Com isso, a Frente E do Catálogo Editorial está concluída. Ver o handoff vigente para o detalhamento. |
| Raridades — ajustes de mapeamento | `RARITY_NAME_ALIASES` (mapa hardcoded em código) **substituído por cadastro self-service (2026-08-07)** — tabela `rarity_external_mapping` + tela `/catalogo/raridades`, Queries `2094`–`2113` (`ADR-024`, emenda "Raridade: mapeamento self-service e revalidação"). Corrigir ou cadastrar um mapeamento não exige mais deploy de código; revalidação sem reimportar (`svc_apply_catalog_import_revalidation()`) recalcula linhas já staged preservando decisões humanas já tomadas. Nenhuma raridade conhecida sem mapeamento no momento desta atualização — ver `05e-catalogo-editorial.md`, seção "Raridade — mapeamento self-service e revalidação". |
| Subciclo `Card` (ADR-023) | **Concluído (2026-08-07)** — as quatro operações administrativas de Card via UI: atualização (`admin_update_card()`, Query `2114`), cadastro (`admin_create_card()`, Query `2115`) e desativação/reativação (`admin_deactivate_card()`/`admin_reactivate_card()`, Queries `2116`/`2117`, soft delete real via `is_active`, sem cascata sobre `card_variant`/`card_asset`). Botões de ação rápida no grid (`/catalogo/cartas`): Editar+Desativar (Card ativa) / Editar+Reativar (Card inativa); botão "Nova Carta"; toggle "Mostrar inativas". Todas as Queries confirmadas executadas e validadas funcionalmente (validação `2817`, 15 cenários). Pendência isolada, deliberadamente fora deste fechamento: auditoria retroativa de `REVOKE ... FROM PUBLIC/anon` nas demais funções `admin_*` do módulo (gap de segurança descoberto durante esta rodada, corrigido nas três funções novas). |
| Próximo passo | **Incremento P14.4.4 — Matching Canônico, Reparo Real e Saneamento de Resíduos formalmente encerrado (2026-08-20)** — ver linha "Pricing / Market Data", acima; P14 como um todo permanece em andamento (próximo passo real dentro de Pricing: P14.4.3, ondas seguras remanescentes). Próxima frente comprometida: **Collection** (sequência estratégica aprovada — ver `ROADMAP.md`, seção "Next") — início ainda dependente de decisão explícita de Fabrício, não bloqueado pela continuação de P14. Card Variant está encerrado como fundação (governança, taxonomia, pipeline "Importar Variantes" — ver linha "Bloco Card Variant", acima); Pesquisa Global de Cartas, Auth Experience e Design System seguem formalmente encerrados. **Decisão real ainda em aberto, dependente de Fabrício**: iniciar Collection vs. continuar a expansão de cobertura de Pricing (P14.4.3) vs. retomar Card Variant (Vintage/Promo Variant Modeling, backlog não bloqueante). Débitos não bloqueantes registrados (sem cronograma): upload de logo em `/catalogo/card-sets/[code]`, dois achados de precisão da reconciliação de `docs/05b-cartas-e-raridade.md` (regex de validação de `code`; `GRANT`s da view `catalog_card_set_variant_coverage`), auditoria retroativa de `REVOKE ... FROM PUBLIC/anon` (task `#368`), divergência de rastreabilidade em `database/migrations/` (ver linha "Pesquisa Global de Cartas"), entre outros — ver `ROADMAP.md`, seção "Débitos Técnicos Registrados". **Correção do erro de autenticação `/login` (`AuthApiError: Invalid Refresh Token`, cookie stale) permanece fora do escopo de qualquer incremento fechado** — diagnosticada como benigna (limpeza de cookies locais), sem código alterado. Ver o handoff vigente para o estado detalhado. |
| Handoff vigente | [`development/HANDOFF-2026-08-18.md`](development/HANDOFF-2026-08-18.md) — mesmo arquivo, amendado ao longo do dia e no dia seguinte: encerramento formal do Incremento P12 (Exibição de Preços no Frontend), P13.1 (fundação de schema), P13.2 (módulo compartilhado e execução manual auditável da PTAX, correção de autoridade temporal via migration `3909`, reexecução idempotente auditada), P13.3 (Edge Function `ptax-fx-refresh` implementada, corrigida estruturalmente, implantada em produção e validada por piloto real autenticado — seção 13), P13.4 (agendamento automático via `pg_cron`/`pg_net`/Vault, migration `3910`, Job único `ptax-fx-refresh-weekdays`, chamada controlada real validada — seção 14), e a antecipação da validação autônoma do P13.4 (2026-08-19, Job temporário disparado pelo próprio `pg_cron` — seção 15) — **P13.4 e P13 formalmente encerrados nesta rodada.** Amendos seguintes (16–21) registram P14.1–P14.4.2 (condição comercial da JustTCG satisfeita, descoberta+persistência em lotes, executor de ondas com Onda 1 real, migration `3918` de hierarquia de printing, correção do primeiro hover). **Amendo 22 (2026-08-20)**: encerramento formal do Incremento P14.4.4 — matching canônico por Set+número, reparo real de 53 mappings (run `66c9e878`), migration `3920` (autoridade temporal de `confirmed_at`) e migration `3921` (saneamento do mapping legado `ext-teste`); ciclo exclusivamente documental, nenhum código/banco/migration alterado nesta rodada. Handoffs anteriores preservados em `history/development/` (inclui `HANDOFF-2026-08-17.md`, Pesquisa Global de Cartas). Ver `03-documentation-architecture.md` para o ciclo de vida deste tipo de artefato. |

Ver [`ROADMAP.md`](ROADMAP.md) para a trajetória completa (o que está concluído, em andamento, e a direção futura ainda não comprometida).

### Detalhamento

| Campo | Valor |
|-------|-------|
| **Fase 2 — Modelo Lógico** | Subdividida em Fases/Blocos próprios (ver `05-modelo-de-dados.md`, seção "Roteiro Consolidado — Fases e Blocos"). **Sub-Fase 1 — Catálogo Editorial, Bloco A (Modelo de Dados): concluído** — todas as entidades do catálogo (`game` até `asset_import_failure`, ver `05-modelo-de-dados.md` para a lista completa) criadas e homologadas para as 7 Card Sets, incluindo `card_variant` (ver placar acima). **Bloco B (Pipeline de Importação): concluído para as 5 coleções originais**, via uma única Edge Function (`import-card-assets`) — ver `ADR-018-single-function-import-pipeline.md` (a arquitetura de duas funções de `ADR-017` nunca foi implementada; `ADR-018` formaliza a realidade vigente). `MEE` já importada (`en`+`pt-BR`); `MEP` sem pendência de canal — o canal de importação manual (`ADR-026`) está implementado e validado, disponível para concluir `MEP` a qualquer momento (item de dado, não de implementação, não bloqueia nenhuma frente). **Bloco Card Variant (intermediário): fundação encerrada (2026-08-14/15/16)** — governança (`ADR-028`), taxonomia com CRUD administrativo e pipeline "Importar Variantes" completos; ver linha "Bloco Card Variant" no Placar Rápido, acima. Modelo físico consolidado em `docs/05b-cartas-e-raridade.md`, reconciliado contra o schema real do Supabase em 2026-08-16. **Sub-Fase 2 — Coleções**: ainda não iniciada; as cinco frentes do Catálogo Editorial, o Módulo Gerencial, o bloco Card Variant e a fundação física de Pricing/Market Data (dez de dez entidades, Incremento P6) estão concluídos — sequenciamento: Catálogo Editorial → Card Variant → Pricing (fundação, concluída) → Coleções, ver `ROADMAP.md`, seção "Now"/"Next". Início real permanece decisão de Fabrício. |
| **Catálogo Editorial — Escrita e Ingestão (`ADR-023`/`ADR-024`)** | `ADR-023` (escrita administrativa) **concluído (2026-08-07)** — infraestrutura comum, `Game`, `Expansion`, `Card Set` e `Card` com o ciclo vertical completo (create/update/delete/deactivate/reactivate conforme aplicável), todos validados pela própria interface; `Card` foi o último a fechar (Queries `2114`–`2117`). `ADR-024` (ingestão de Cards a partir de PDF/TCGdex) iniciada em 2026-08-01. **Ciclo 1 (infraestrutura comum de staging e confirmação) CONFIRMADO EXECUTADO E VALIDADO** — `catalog_import_job`/`catalog_import_row` (Queries `2060`/`2061`/`2070`/`2071`), três funções (`admin_start`/`decide`/`confirm_catalog_import`, Queries `2080`–`2082`), auditoria ampliada (Queries `2054`/`2055`) e duas validações (`2813` estrutural, `2814` funcional com dados sintéticos e `ROLLBACK`) — as 11 Queries confirmadas por Fabrício uma a uma no Supabase. **Ciclo 2 (fluxo TCGdex completo) implementado e em uso ativo em produção** — Edge Function `import-catalog-cards` (processador TCGdex) construída, deployada e usada para importar múltiplos Card Sets reais (`SV1`-`SV5`, `SV3.5`, `ME5`, entre outros) através da tela `/catalogo/importar-cartas`. **Fechamento documental do Ciclo 2 concluído (2026-08-07)** — nova seção própria "Ciclo 2 — Fluxo vertical completo via TCGdex" em `05e-catalogo-editorial.md`, no mesmo padrão do Ciclo 1; validação `2818` confirmada executada e validada, 8/8 itens. Ciclos 3 (prova técnica do processador de PDF) e 4 (fluxo PDF) **encerrados definitivamente (2026-08-08)** — decisão explícita de Fabrício, motivada pela taxa de sucesso já suficiente do canal TCGdex, pela direção futura de multi-provider e pelos canais manuais (`ADR-023`/`ADR-026`) já cobrirem o caso residual sem automação; embasada por prova técnica exploratória real (`ME04`/`ME02pt5`) que confirmou viabilidade técnica mas quantificou o custo de engenharia necessário para robustez entre produtos. Detalhamento completo no handoff vigente, em `05e-catalogo-editorial.md` e em `adr/ADR-024-catalog-card-ingestion-strategy.md`. |
| **Decisão registrada — Cartas de Energia no catálogo** | Categoria `ENERGY` somava `17` Cards reais nas 7 Card Sets originais (contagem não reconfirmada desde então). Resolvido em 2026-07-30: cartas de Energia ocupam posição oficial no Set e fazem parte do catálogo editorial numerado, como Pokémon e Trainer — decisão explícita e definitiva de Fabrício, formalizada em `adr/ADR-025-energy-as-catalog-card-category.md`, resolvendo a Open Decision `OD-001` de `04-domain-model.md`. |
| **Decisão de escopo registrada — múltiplas fontes externas (multi-provider)** | A instabilidade real observada na TCGdex durante a ingestão (HTTP 502 transitório, timeouts de download, respostas incompletas — ver handoff vigente) motiva a necessidade de avaliar uma estratégia de múltiplos provedores (metadados e/ou imagens), mas **nenhuma solução técnica foi decidida**: providers candidatos, prioridade, fallback, tratamento de erro por tipo (timeout/404/429/5xx), rate limit, telemetria, licenciamento e cobertura por idioma/Set permanecem em aberto. **Decisão de escopo confirmada por Fabrício (2026-08-07)**: multi-provider fica como implementação futura, fora desta sprint — resolve apenas a pergunta de prioridade, nenhuma das sub-decisões técnicas acima. Não é uma emenda ao `ADR-008-external-catalog-data-sources.md`. Ver `06-pipeline-importacao.md`, seção "Em Aberto" (revisão `1.7`). |
| **Histórico detalhado do pipeline** | Sprints `B2.0`-`B3.28` (setup, autenticação, GRANTs, Incrementos 1/2, `en`/`pt-BR`) condensados em `history/pipeline-sprint-log.md`. Este campo registra apenas o estado atual — não o histórico de tentativas (mudança de estilo aplicada na revisão `1.49`). O histórico operacional mais recente (Ciclo 2, correções de raridade, pipeline de imagens `en`+`pt-BR`) ainda não foi condensado neste arquivo — está no handoff vigente e nas revisões recentes de `05-modelo-de-dados.md`. |
| **Última atualização** | 2026-08-20 |
| **Última revisão** | 2.20 |

Fase 1 entregou: princípios arquiteturais, delimitação do domínio (Pokémon TCG, não o universo Pokémon), estrutura do catálogo editorial, modelo do universo do colecionador, separação entre Set e Collection, e a estratégia de evolução incremental. Fase 2 transforma cada conceito já validado em modelo lógico e, em seguida, tabela física — uma entidade por vez, validada com dados reais antes de avançar para a próxima.

## Retomando este Projeto com uma Nova Sessão de IA

Este repositório é a única fonte de verdade do Project Mimikyu. Ele foi estruturado para que qualquer sessão de IA — inclusive uma sessão nova, sem memória de conversas anteriores — consiga retomar o projeto de forma confiável.

Ao iniciar uma nova sessão (por perda de contexto, início de uma nova fase, ou troca de ferramenta), utilize um prompt equivalente a este:

> Você está retomando o Project Mimikyu. O repositório oficial (`fabriciosouzasales/project-mimikyu`) é a única fonte de verdade — não presuma nenhuma decisão que não esteja explicitamente documentada nele, mesmo que pareça familiar. Antes de qualquer ação:
>
> 1. Leia `CLAUDE.md` (raiz do repositório) — convenções operacionais para qualquer agente de IA neste projeto (fluxo por ciclo, pareamento de SQL, estilo de histórico, ambiente).
> 2. Leia `docs/INDEX.md` — catálogo de toda a documentação, um resumo de uma linha por documento; use-o para decidir o que ler em profundidade, em vez de ler tudo de imediato.
> 3. Leia `docs/README.md` (este documento), incluindo a seção "Status Atual do Projeto", e `docs/ROADMAP.md` para a trajetória macro.
> 4. Leia `docs/log.md` (últimas entradas) para o que aconteceu recentemente.
> 5. Leia o handoff vigente em `docs/development/`, quando existir — ele é operacional (estado executado, arquivos alterados, validações pendentes, checklist de retomada), não normativo.
> 6. A partir daí, aprofunde apenas nos documentos numerados, ADRs (`docs/adr/ADR-INDEX.md`) e Standards (`docs/standards/STD-INDEX.md`) relevantes ao trabalho em questão — `docs/INDEX.md` existe justamente para evitar ter que ler tudo de uma vez.
> 7. Depois de ler o necessário, resuma a fase atual do projeto, as decisões já consolidadas e as pendências em aberto — e só então aguarde instruções para iniciar qualquer trabalho novo.
>
> Não avance com implementação ou com novas decisões de modelagem sem essa confirmação.

Este prompt funciona da mesma forma na fase de documentação e na fase de implementação — o que muda é apenas o papel assumido pela IA depois da confirmação. Decisões permanentes (ADRs) são imutáveis uma vez aprovadas; supersessões geram um novo número, preservando o histórico. **Atualizado em 2026-08-06** como parte da adequação ao padrão LLM Wiki (ver `CLAUDE.md`): o ritual anterior ("leia 00 até o último documento numerado, cada ADR, cada Standard") foi substituído por uma orientação em camadas via `CLAUDE.md`/`docs/INDEX.md`, mais barata para uma sessão nova.

## Core Documents

| Documento | Finalidade |
|------------|------------|
| [Roadmap](ROADMAP.md) | Consolida a trajetória macro do projeto (concluído/em andamento/direção futura). |
| [Project Charter](00-project-charter.md) | Define missão, visão, princípios e critérios de sucesso. |
| [Technical Identity](01-technical-identity.md) | Consolida a identidade técnica permanente do projeto. |
| [Architecture Principles](02-architecture-principles.md) | Define os princípios que orientam decisões arquiteturais. |
| [Documentation Architecture](03-documentation-architecture.md) | Define a organização e a governança da documentação. |
| [Domain Model](04-domain-model.md) | Define o modelo conceitual do domínio, anterior à modelagem lógica e física. *(Aprovado, com pendências identificadas — ver seção "Open Decisions")* |
| [Modelo de Dados](05-modelo-de-dados.md) | Índice + metodologia do modelo lógico e físico (SQL) — dividido por área em `05a`–`05e` desde 2026-08-06 (ver `docs/INDEX.md`). *(Ativo e evolutivo)* |
| [Pipeline de Importação](06-pipeline-importacao.md) | Estratégia de importação e sincronização de fontes externas. *(Implementado — ver `06-pipeline-importacao.md`, seção "Em Aberto", para itens de evolução futura não bloqueantes)* |
| [Catálogo Editorial](07-catalogo-editorial.md) | Estratégia de captura e disponibilização de dados do catálogo. *(Concluído — `ADR-023` e `ADR-024` (Ciclos 1/2) concluídos e validados; canal PDF (Ciclos 3/4) encerrado definitivamente por decisão explícita)* |

## Documentation Areas

| Área | Finalidade |
|------|------------|
| [CLAUDE.md](../CLAUDE.md) | Schema do agente — convenções operacionais para qualquer LLM que trabalhe neste repositório (raiz, fora de `docs/`). |
| [Índice](INDEX.md) | Catálogo único de toda a documentação, uma linha por documento. |
| [Log](log.md) | Log cronológico enxuto de eventos do projeto. |
| [Architecture Decision Records](adr/ADR-INDEX.md) | Registra decisões arquiteturais e suas justificativas. |
| [Standards](standards/STD-INDEX.md) | Define padrões permanentes de implementação e documentação. |
| [Architecture](architecture/README.md) | Reúne visões, modelos, diagramas e descrições arquiteturais. |
| [Operações — Importar uma Nova Coleção](operations/import-card-assets.md) | Guia operacional passo a passo do pipeline `import-card-assets`. |
| [Diário Histórico — Pipeline de Importação](history/pipeline-sprint-log.md) | Histórico condensado de tentativas/bugs/sprints do pipeline de importação. |
| [Development — Handoffs](development/) | Documentos temporais de continuidade operacional entre sessões de desenvolvimento (ver `03-documentation-architecture.md` para o ciclo de vida). |

## Governing Rule

> ADRs explain **why**. Standards define **how**.

Cada informação deve possuir um único local oficial. A documentação deve evoluir junto com o software e permanecer sincronizada com as decisões e a implementação.

---

## Revision History

| Versão | Descrição |
|---------|-----------|
| 1.0 | Estrutura inicial do documento de navegação. |
| 1.1 | Adicionadas as seções "Status Atual do Projeto" e "Retomando este Projeto com uma Nova Sessão de IA", garantindo que o repositório permaneça recuperável mesmo após perda total de contexto de uma sessão de IA. |
| 1.2 | Adicionado Modelo de Dados à tabela de Core Documents. Atualizado "Status Atual do Projeto": entidade Game concluída na Fase 2. |
| 1.3 | Corrigido "Status Atual do Projeto": o banco físico já possui as 17 tabelas originais com carga inicial de dados, construídas antes desta fase de consolidação documental — a documentação formal (05-modelo-de-dados.md) está sendo escrita retroativamente, e não define uma sequência de criação do zero. |
| 1.4 | Atualizado "Status Atual do Projeto": Game, Expansion e Set (`card_set`, incluindo o tipo `PROMO`) já têm o roteiro completo de `05-modelo-de-dados.md` concluído. Próxima entidade: Card. |
| 1.5 | Reescrito "Status Atual do Projeto" (estava muito desatualizado — ainda citava "próxima entidade: Card", já concluída há dezenas de ciclos). Adotada a nova estrutura de Fases/Blocos de `05-modelo-de-dados.md`: Sub-Fase 1 (Catálogo Editorial), Bloco A (Modelo de Dados) **concluído** — cobre Game/Expansion/Set/Card/Language/Rarity/Card Variant/Card Asset/Storage Bucket/Asset Source/Card External Reference/Asset Import Run/Asset Import Failure; Bloco B (Pipeline de Importação) e Bloco C (Carga Editorial) ainda não iniciados. **Corrigida a lista de "17 tabelas físicas pré-existentes"**: `asset_source`, `asset_import_run` e `asset_import_failure` removidas dessa lista — evidências diretas coletadas durante este projeto (guardas defensivas de Queries que só executaram porque as tabelas não existiam ainda, mais uma captura real do Table Editor) mostram que as três foram criadas pelas próprias Queries deste projeto, não herdadas de antes da fase de documentação; `card_set_external_reference` mantida na lista original, mas sinalizada como não verificada. |
| 1.6 | Atualizado "Status Atual do Projeto": **Bloco B (Pipeline de Importação) iniciado** — arquitetura da Edge Function `import-card-assets` especificada e roteiro de 12 sprints (`B2.1`–`B2.12`) definido, ambos detalhados em `06-pipeline-importacao.md` (não duplicados aqui). Código do Sprint B2.1 proposto, mas deploy ainda não confirmado — não tratado como concluído. |
| 1.7 | Atualizado "Status Atual do Projeto": **primeiro deploy real e primeira invocação remota confirmada de uma Edge Function** (Sprints B2.0-B2.2 concluídos); roteiro renumerado/consolidado para `B2.0`–`B2.8` (detalhes em `06-pipeline-importacao.md`); Sprint B2.3 com código publicado, teste pendente. Código versionado pela primeira vez em `supabase/functions/import-card-assets/index.ts`. |
| 1.8 | Atualizado "Status Atual do Projeto": Sprints `B2.0`–`B2.4` **CONFIRMADOS CONCLUÍDOS** (consulta de execução por `run_code` e descoberta de `card_set`/`card`, ambos com Diário Técnico registrado); Sprint `B2.4.1` (refatoração para services) com código escrito, deploy ainda não confirmado; `B2.5` dividida em `B2.5A`/`B2.5B`, com possível sobreposição de escopo com `B2.6` sinalizada em `06-pipeline-importacao.md`, não resolvida unilateralmente. Código confirmado (v1.2.0) atualizado em `supabase/functions/import-card-assets/index.ts`. |
| 1.9 | Atualizado "Status Atual do Projeto": Sprint `B2.4.1` **CONFIRMADO CONCLUÍDO** — deploy e reteste reais confirmados, mesmo comportamento da v1.2.0 sob `version: "1.2.1"`. Novo princípio de arquitetura registrado: `index.ts` de toda Edge Function futura deve apenas orquestrar, sem conhecer SQL/PostgreSQL/fontes externas diretamente. Código confirmado (v1.2.1) versionado em `supabase/functions/import-card-assets/index.ts`, `types.ts` e `services/database.ts`. |
| 1.10 | Atualizado "Status Atual do Projeto": Bloco A reaberto pontualmente — nova entidade `card_set_external_reference` (`240` CONFIRMADA EXECUTADA), documentada em `05-modelo-de-dados.md`. Sprint B2.5A em andamento: `services/tcgdex.ts` escrito e validado no editor, ainda não integrado/deployado/testado. |
| 1.11 | Atualizado "Status Atual do Projeto": `card_set_external_reference` com `241` (triggers) também CONFIRMADA EXECUTADA; episódio real de correção (`ME0`→`sv10pt5`) registrado; Seed `910` adiada até dados reais da TCGdex. Escopo final de B2.5A definido (`NO_EXTERNAL_SET_MAPPING` para Sets sem referência ativa). |
| 1.12 | Atualizado "Status Atual do Projeto": B2.5A com código escrito (services + rascunho v1.3.0 de `index.ts`), sem deploy confirmado — sprint pivotou para descoberta dos `external_set_id` reais via script administrativo, ainda não executado. |
| 1.13 | Atualizado "Status Atual do Projeto": marco — primeira comunicação externa real e bem-sucedida do Bloco B, `external_set_id` reais da TCGdex descobertos (`ME0`-`ME5`); nova decisão de negócio em aberto sobre `ME0`↔`mee` ("Mega Evolution Energy"), cross-referenciada com o "escopo `ENERGY`". |
| 1.14 | Atualizado "Status Atual do Projeto": decisão de arquitetura declarada definitiva pela sessão pareada — pipeline dividido em duas Edge Functions (`sync-card-set`/`import-card-assets`), formalizada em nova `ADR-017-two-function-import-pipeline.md`. Nenhuma implementação real ainda; próximo passo é o Sprint B3. |
| 1.15 | Atualizado "Status Atual do Projeto": nova disciplina de trabalho (uma camada por vez) declarada; `tcgdex.ts` revisado tecnicamente e aprovado, `index.ts` com nota 9,5/10 — nenhum dos dois deployado. Discrepância real sinalizada: implementação segue dentro de `import-card-assets/services/`, não em uma pasta `sync-card-set/` própria como o plano da `ADR-017` sugeria. |
| 1.16 | Atualizado "Status Atual do Projeto": marco real — Query `910` (Seed Card Set External Reference) CONFIRMADA EXECUTADA (parcial: `ME1`-`ME4`/`ME2.5`; `ME0` excluído por decisão pendente; `ME5` aguarda cadastro). `index.ts`/`tcgdex.ts`/`scripts/discover-tcgdex-sets.ts` aprovados em revisão técnica; nova versão completa de `services/database.ts` escrita como rascunho, ainda não deployada. Novo arquivo `database/seeds/910_seed_card_set_external_reference.sql`. |
| 1.17 | Atualizado "Status Atual do Projeto": primeira tentativa real de deploy da v1.3.0 (primeira integração real Supabase→TCGdex) FALHOU com erro real de bundling (`@supabase/supabase-js` sem mapeamento em `deno.json`); causa raiz diagnosticada, correção escrita (`supabase: any`); deploy de sucesso ainda não confirmado. |
| 1.18 | Atualizado "Status Atual do Projeto": marco real — deploy da v1.3.0 (`index.ts`+`database.ts`+`tcgdex.ts`) CONFIRMADO com sucesso; os três arquivos copiados ao repositório (primeira vez para `tcgdex.ts`). Nova convenção `deno check` + `deploy`. Invocação de ponta a ponta com resposta real da TCGdex ainda não confirmada (bloqueada por chave de autenticação incorreta). |
| 1.19 | Atualizado "Status Atual do Projeto": causa raiz do HTTP 401 diagnosticada definitivamente (mecanismo de autenticação de `withSupabase({ auth: ["secret"] })`, não a chave usada nem `verify_jwt`); correção final (`index.ts` v1.3.1, sem autenticação, temporário) escrita, ainda não deployada nem testada. |
| 1.20 | Atualizado "Status Atual do Projeto": correção da revisão `1.19` — `index.ts` v1.3.1 foi de fato deployado e testado, mas o HTTP 401 PERSISTIU; causa raiz continua desconhecida. Bug real de metodologia de teste (chave placeholder não substituída) encontrado e corrigido, mas não era a causa raiz. Teste de isolamento definitivo (Edge Function mínima sem `withSupabase`) proposto, ainda não executado. |
| 1.21 | Atualizado "Status Atual do Projeto": marco real — HTTP 401 definitivamente eliminado, `@supabase/server`/`withSupabase` abandonado, substituído por `@supabase/supabase-js` com cliente manual (`index.ts` v2.0.0, `deno.json`, copiados ao repositório pela primeira vez). GRANT ausente em `card_set_external_reference` para `service_role` descoberto e corrigido (migration `250`). Resposta final `success: true`/`tcgdex_set` ainda não explicitamente confirmada. |
| 1.22 | Atualizado "Status Atual do Projeto": progresso real além do GRANT (HTTP 404, lógica de aplicação); pendência de longa data `ME0`↔`mee` resolvida por decisão de negócio real de Fabrício (coleções diferentes, sem relação); `ME0` removida de `card_set` por completo (Migration `251`, real, confirmada). |
| 1.23 | Atualizado "Status Atual do Projeto": 🎉 MARCO REAL — primeira resposta de ponta a ponta da TCGdex confirmada através de `import-card-assets` (Set `me01`, "Mega Evolution", 188 cartas); `B2.5A` do roteiro CONCLUÍDO. Três propostas reais adiadas (Stored Procedure, `ENUM`s nativos, reestruturação de pastas). |
| 1.24 | Atualizado "Status Atual do Projeto": estrutura real de carta da TCGdex confirmada; episódio real de perda de contexto arquitetural da sessão pareada, autocorrigido; fluxo de persistência de `card`/`card_external_reference` realinhado. Sinalizada, sem resolver unilateralmente, a proposta de `docs/architecture.md` — possível duplicação da documentação já existente neste repositório. |
| 1.25 | Atualizado "Status Atual do Projeto": reconfirmação real da arquitetura de fluxo/Edge Functions; episódio real de perda de foco operacional, corrigido por Fabrício ("Não quero trabalhar para importar 1 imagem apenas [...] Precisamos finalizar o trabalho do catálogo editorial"); novo faseamento `FASE 1-6`/`Sprint 1-2` proposto, ainda não reconciliado com o Roteiro vigente `B2.x`/`B3.x`; autocorreção real de que a Query `880` (com `card_asset`) segue sendo o ponto final do Catálogo Editorial. Nova discrepância sinalizada, não resolvida: proposta de `docs/adr/` com ADRs `001`-`005` colidindo com os 17 ADRs reais já aprovados; divergência de nome `download-card-assets` vs. `sync-card-set` (`ADR-017`). |
| 1.26 | Atualizado "Status Atual do Projeto": escopo real do Bloco B expandido para 5 coleções (`ME1`/`ME2`/`ME2.5`/`ME3`/`ME4`); quarto episódio real de perda de contexto, contido antes de qualquer execução — `card`/`card_variant` já tinham 859/1.555 registros reais (marco fechado há dezenas de batches), momentaneamente tratadas como vazias pela sessão pareada, corrigido por Fabrício. Correção arquitetural real: o pipeline passa a consultar `card`, nunca inserir — `card`/`card_variant` congeladas, fora do escopo do Bloco B. Auditoria real (com um erro de colunas/`JOIN` capturado e corrigido) reconfirmou totais e estrutura sem divergência. Terceiro framing de roadmap paralelo (`FASE 1-4`/três Entregas) em três batches, ainda não reconciliado. |
| 1.27 | Atualizado "Status Atual do Projeto": Sprint B3.12 — dependências de `card_external_reference` auditadas e confirmadas prontas (`asset_source`, `card_set_external_reference`); `card_external_reference` confirmada vazia, ponto de partida real; chave de correspondência `card`↔TCGdex (`card_set_id`+`collector_number`=`localId`) validada com dados reais; plano do próximo incremento travado, ainda não codificado. **Mudança de processo, por pedido explícito de Fabrício**: a partir desta revisão, a documentação registra apenas o estado final e as decisões confirmadas de cada etapa — não mais o histórico de tentativas/correções da sessão pareada. |
| 1.28 | Atualizado "Status Atual do Projeto": estrutura de `card_external_reference` reconfirmada; código do Incremento 1 finalizado (`index.ts` v2.1.0 + `database.ts`) — deploy/execução reais ainda não confirmados, código não copiado ao repositório. |
| 1.29 | Atualizado "Status Atual do Projeto": plano de validação do Incremento 1 definido; execução bloqueada por problema real de ambiente local (Supabase CLI indisponível no terminal), resolvido via `npm install -g supabase`, verificação final pendente. Sinalizado, não resolvido: se a validação rodará local ou contra o projeto hospedado real. |
| 1.30 | Atualizado "Status Atual do Projeto": resolvido — banco é o Supabase Cloud; deploy do Incremento 1 CONFIRMADO por saída real de terminal; código copiado ao repositório (`index.ts` v2.1.0 + `database.ts`). Execução de ponta a ponta ainda não confirmada. |
| 1.31 | Atualizado "Status Atual do Projeto": 🎉 MARCO REAL — Incremento 1 CONFIRMADO CONCLUÍDO (`card_external_reference` 188/188 para a `ME1`). Bloqueio real (GRANT ausente) diagnosticado e corrigido pela migration `253`; logging de erro corrigido em `database.ts`. |
| 1.32 | Atualizado "Status Atual do Projeto": Incremento 2 (download de imagens) iniciado — fluxo definido, teste controlado aprovado por Fabrício; `card_asset`/`card_asset_type` parcialmente confirmadas; `storage_bucket` ainda não confirmada. |
| 1.33 | Atualizado "Status Atual do Projeto": `storage_bucket` confirmada (catálogo interno, não o bucket físico); bucket físico `card-front` criado no Storage; estrutura completa de `card_asset` confirmada (sem vínculo direto com `card_external_reference`). Código do Incremento 2 (teste controlado) DEPLOYADO, CONFIRMADO; execução real FALHOU — terceiro caso do gap de GRANT ausente para `service_role` (`language`), correção proposta, não confirmada executada. |
| 1.34 | Atualizado "Status Atual do Projeto": cadeia de GRANTs resolvida (seis casos reais no total); teste controlado do Incremento 2 CONFIRMADO CONCLUÍDO — primeira imagem real do projeto importada de ponta a ponta. |
| 1.35 | Atualizado "Status Atual do Projeto": 🎉 MARCO REAL — Incremento 2 CONCLUÍDO para a `ME1` (188/188 imagens, 0 falhas). Refatoração (`services/storage.ts`) e bug real de regra de negócio (`external_url`) corrigido. |
| 1.36 | Atualizado "Status Atual do Projeto": pipeline replicado sem alteração de código — `ME2` (130/130) e `ME2.5` (295/295) CONCLUÍDAS, 0 falhas. Plano de duas fases definido (Fase 1: `en`; Fase 2: `pt-BR`). Total acumulado: `613`/`613`/`613`, 3 de 5 coleções. |
| 1.37 | Atualizado "Status Atual do Projeto": 🎉 MARCO REAL — Fase 1 100% CONCLUÍDA (`ME3`/`ME4` concluídas). Catálogo editorial completo em inglês: `859` cartas/referências/imagens/`card_asset`, `0` falhas nas 5 coleções. |
| 1.38 | Atualizado "Status Atual do Projeto": Fase 2 (`pt-BR`) iniciada — teste controlado confirma `card_asset` multi-idioma; ⚠️ discrepância real sinalizada em `card_external_reference` (`UNIQUE` sem idioma), NÃO resolvida. |
| 1.39 | Atualizado "Status Atual do Projeto": 🎉 teste controlado da Fase 2 CONFIRMADO CONCLUÍDO — bug real do código de idioma da TCGdex corrigido; `ME1` `pt-BR` `188/188/0`; três validações confirmadas. Fase 2 escalada para `ME2`-`ME4`, `ME2` preparada, execução não confirmada. |
| 1.40 | Atualizado "Status Atual do Projeto": 🎉 MARCO REAL — Fase 2 100% CONCLUÍDA, Bloco B encerrado. `ME2`/`ME2.5`/`ME3`/`ME4` (`pt-BR`) confirmadas por Fabrício. Catálogo Editorial completo nas duas línguas: `859`/`1.555`/`859`/`1.718`/`1.718`/`0`. Pendência de `card_external_reference` respondida por evidência agregada (decisão de correção ainda pendente de Fabrício). Fabrício define o próximo módulo real: `Coleções`, não interface. |
| 1.41 | Atualizado "Status Atual do Projeto": Bloco A reaberto — identificador oficial real do Set promocional encontrado (`MEP`, não `mee`); mecanismo de `ADR-015` permanece correto, só a convenção de código estava errada; novo `AP-018` adotado (nunca inventar códigos editoriais). Recadastro de `MEP` planejado, não executado. Questão real e explicitamente aberta: se `Expansion` modela corretamente "Series"/"Era" — investigação pendente antes de qualquer mudança no banco. |
| 1.42 | Atualizado "Status Atual do Projeto": questão `Expansion`/"Series"/"Era" RESOLVIDA por decisão direta de Fabrício (sem mudança de schema). Duas novas decisões reais, não executadas: cadastro de `MEE` (Set de Energias) e convenção de `release_order` para Sets especiais (Energia=1, Promo=2). Novo lema de ritmo do projeto registrado. Roadmap completo (Fase 1-7) esboçado; `ROADMAP.md` planejado como primeiro documento formal futuro. |
| 1.43 | Atualizado "Status Atual do Projeto": `06-pipeline-importacao.md` dividido em três documentos (arquitetura, `operations/import-card-assets.md`, `history/pipeline-sprint-log.md`), a pedido de Fabrício. `03-documentation-architecture.md` atualizado com os novos tipos de artefato. Três pastas órfãs em `docs/` descobertas e sinalizadas, não removidas. |
| 1.44 | Adicionadas `operations/import-card-assets.md` e `history/pipeline-sprint-log.md` à tabela "Documentation Areas", completando a navegação para os dois novos tipos de artefato formalizados em `03-documentation-architecture.md`, revisão `1.5`. |
| 1.45 | Atualizado "Status Atual do Projeto": Migrations `263`/`264` (CONFIRMADAS EXECUTADAS) ampliaram `card_set.set_type` para incluir `ENERGY` e reorganizaram `release_order` de `ME1`-`ME4`, liberando `1`/`2` para `MEE`/`MEP`. `MEE`/`MEP` ainda não cadastrados. `ADR-015` atualizada (revisão `1.6`). |
| 1.46 | Atualizado "Status Atual do Projeto": `MEE`/`MEP` CONFIRMADOS EXECUTADOS (Migrations `265`–`268`), com `MEP` já mapeado à TCGdex via `card_set_external_reference`. `AP-018` estendido (revisão `1.8`) para cobrir `name`, não apenas `code`. Discrepância real sinalizada: nomes de `ME1`-`ME4`/`ME2.5` em português, inconsistentes com o princípio se aplicado retroativamente. `ADR-015` atualizada (revisão `1.7`). |
| 1.47 | Atualizado "Status Atual do Projeto": Migrations `269`–`271` (CONFIRMADAS EXECUTADAS) — `metadata` de `card_set_external_reference` padronizada para `{}` (nova regra em `STD-001`, revisão `1.14`); referência externa de `MEE` confirmada; data de lançamento de `MEE` corrigida para `2025-09-25`. Camada `Expansion → Card Set → Card Set External Reference` completa para `MEE`/`MEP`. Plano definido (não executado) para evoluir `840 - Seed Card` de `v2.1` para `v2.2`. |
| 1.48 | Atualizado "Status Atual do Projeto": 🎉 MARCO REAL — Query `840` evoluída para `v2.2` e Query `940` para `v2.1`, AMBAS CONFIRMADAS EXECUTADAS ("Já executei as duas queries. Sem erros!"). Catálogo `card` passa de `859` para `927` Cards, cobrindo as sete Card Sets da Expansion `ME`. Discrepância `ENERGY` agora soma 17 Cards reais. Plano definido, não executado, para estender `Card Variant` a `MEE`/`MEP` (série `860A`-`860G` + `960`). |
| 1.49 | **Auditoria de qualidade documental conduzida por Fabrício (2026-07-24)**, confirmada por inspeção real do repositório: "Status Atual do Projeto" reescrito de uma célula única com ~48 revisões de narrativa sprint-a-sprint acumulada (duplicando o que já existia, de forma condensada, em `history/pipeline-sprint-log.md`) para um resumo enxuto do estado final atual — consistente com a disciplina de "apenas o estado final" já aplicada a `04-domain-model.md` e `06-pipeline-importacao.md`. Nenhuma informação foi perdida: o histórico completo permanece em `history/pipeline-sprint-log.md` (até a revisão que originou aquele arquivo) e nas revisões anteriores desta própria tabela, abaixo. `ADR-INDEX.md`/`STD-INDEX.md` permanecem deliberadamente desatualizados por decisão explícita de Fabrício nesta rodada — não fazem parte desta revisão. |
| 1.50 | Atualizado "Status Atual do Projeto": 🎉 MARCO REAL — `860A - Seed Card Variant MEE`, `860B - Seed Card Variant MEP` (ambas v1.0) e `960 - Validate Card Variant` v2.1, TODAS CONFIRMADAS EXECUTADAS. Card Variant passa a cobrir as 7 Card Sets: `927` Cards, `1.653` Card Variants, status `COMPLETE`. Plano anterior de renomear a antiga `860A` (ME1) para `860C` foi abandonado por decisão explícita de Fabrício — ver `05-modelo-de-dados.md`, revisão `0.63`, para o detalhamento completo. |
| 1.51 | **Auditoria completa da documentação (2026-07-24), a pedido explícito de Fabrício, antes de retomar o desenvolvimento.** Verificação cruzada de todas as contagens (`859`/`927`/`1.555`/`1.653`), status de ADRs, links internos, e conteúdo de `04-domain-model.md`/`07-catalogo-editorial.md`/`architecture/`/`database/README.md`. Achados e correções: (a) contradição interna nesta própria célula — a frase "`card_variant` parcial... `MEE`/`MEP` pendentes... ainda não executado" havia sobrevivido à revisão `1.50`, contradizendo a frase seguinte, que já registra o marco `927`/`1.653`/`COMPLETE`; removida a frase obsoleta. (b) nova pasta órfã encontrada, `docs/glossary/` (mesmo perfil de `pipelines/`/`editorial/`/`sprint/`, nunca documentada) — registrada em `03-documentation-architecture.md`, revisão `1.8`, mesma recomendação de remoção manual. Nenhuma outra inconsistência encontrada: colisões de numeração de Query (`900`, `970`) e a discrepância `ENERGY` já estavam corretamente sinalizadas; ADRs `009`/`010`/`016`/`017`/`018` com status mutuamente consistentes; menções a `859`/`5 Card Sets` remanescentes em `05-modelo-de-dados.md`/`06-pipeline-importacao.md`/ADRs/`operations/`/`history/` são todas legítimas (escopo do pipeline de imagens, que ainda não cobre `MEE`/`MEP`) — nenhuma foi alterada. `ADR-INDEX.md`/`STD-INDEX.md`/`ROADMAP.md` permanecem deliberadamente fora do escopo desta auditoria, por decisão explícita de Fabrício. |
| 1.52 | **Segunda auditoria (2026-07-24), conduzida por Fabrício sobre o resultado da revisão `1.51`, com 5 achados — todos corrigidos no mesmo ciclo.** (1) `ADR-INDEX.md` reativado (versão `2.0`): catálogo completo com os 18 ADRs reais, antes limitado a `ADR-001`/`002`. (2) `STD-INDEX.md` corrigido (versão `2.0`): `STD-002`/`STD-003` já existiam aprovados havia dezenas de ciclos, mas o índice ainda os listava como "Planned" com título errado para `STD-002` ("SQL Conventions" — o real é "Domain Modeling"). (3) `docs/ROADMAP.md` criado — primeira fonte única de verdade da trajetória do projeto (Now/Next/Later), sem adotar nenhuma das múltiplas propostas de roadmap não reconciliadas ao longo do projeto; item correspondente em `06-pipeline-importacao.md` marcado como resolvido. (4) Linguagem informal ("alucinou geral") na revisão `0.64` de `05-modelo-de-dados.md` substituída por descrição neutra do episódio, mantendo os fatos técnicos. (5) Esta célula de status reestruturada em "Placar Rápido" (tabela de indicadores) + "Detalhamento" (prosa), reduzindo a densidade de uma única célula longa; resume prompt da seção "Retomando este Projeto" atualizado para incluir `ROADMAP.md` e refletir que `ADR-INDEX.md`/`STD-INDEX.md` agora são confiáveis. **Mudança de regra, decidida por Fabrício nesta revisão**: os índices e o roadmap deixam de ser "congelados até o fim da fase de documentação" e passam a ser mantidos ativamente a partir de agora — a documentação do passado do projeto está encerrada; tudo documentado daqui em diante é sobre novas atualizações. `docs/sprint/` e `docs/glossary/` seguem pendentes de remoção manual por Fabrício (Claude não pode deletar arquivos neste ambiente). |
| 1.53 | **Exclusão de `docs/sprint/` e `docs/glossary/` confirmada por Fabrício (2026-07-24)** — as quatro pastas órfãs identificadas ao longo do projeto (`pipelines/`, `editorial/`, `sprint/`, `glossary/`) foram todas removidas. Ver `03-documentation-architecture.md`, revisão `1.10`, para o registro completo. |
| 1.54 | **Decisão real de Fabrício (2026-07-25): iniciar o front-end (aplicação web) agora, sem esperar a modelagem de Coleções — `ADR-019-web-application-as-primary-interface.md` criada.** React/Next.js adotado como interface principal do produto; Power Apps/SharePoint/Power BI descartados da arquitetura-alvo. "Placar Rápido" ganhou a linha "Front-end (aplicação web)"; "Próximo passo" atualizado para incluir a concepção funcional dos dois módulos iniciais (Gestão de Usuários, Catálogo Editorial). Bug real de `asset_import_run` nunca transicionar de `PENDING` corrigido e testado em produção nesta mesma revisão (v2.6.0) — ver `05-modelo-de-dados.md`. |
| 1.55 | **Auditoria de qualidade documental conduzida por Fabrício (2026-07-26), antes de retomar o desenvolvimento — Prioridade 1 (sincronização imediata) corrigida no mesmo ciclo.** Achados: (a) linha "Front-end (aplicação web)" ainda dizia "nenhum código ainda", contradizendo `web/` e o próprio `ROADMAP.md` (Incrementos 1 e 2 de Identidade e Acesso concluídos e validados em produção) — corrigida para refletir o estado real; (b) linha "Sets com imagens" ainda dizia "5 de 7 — `MEE`/`MEP` pendentes", desatualizada desde que `MEE` foi concluída (revisão `1.4` do `ROADMAP.md`) — corrigida para "6 de 7 — `MEP` pendente"; (c) "Próximo passo" reescrito para citar o script manual (`import-manual-assets.ts`, não mais o pipeline `import-card-assets`, já resolvido para `MEE`/`MEP` não terem asset na fonte) e a abertura da documentação própria do frontend. (d) a mesma desatualização se repetia na prosa de "Detalhamento" ("`MEE`/`MEP` ainda não importadas"), contradizendo a correção do item (b) — sincronizada. Achados de `README.md` (raiz) e `01-technical-identity.md` corrigidos em conjunto, fora desta tabela — ver revisões próprias de cada documento. Prioridades 2–5 da auditoria (área `docs/frontend/`, `web/README.md`, `STD-004`) explicitamente adiadas para ciclo próprio, por decisão de Fabrício. |
| 1.56 | **Fundação de autorização e logo do Catálogo Editorial concluída (2026-07-26) — Queries `273`-`277`, `ADR-022`.** "Placar Rápido": linha "Front-end (aplicação web)" atualizada para refletir que o Catálogo Editorial já tem sua fundação de banco pronta (autorização admin-only nas 10 tabelas usadas pela Visão Geral, coluna `card_set.logo_storage_path`, função administrativa e bucket de Storage dedicado), mas nenhuma tela foi implementada ainda; "Próximo passo" reescrito para priorizar a implementação da tela `/catalogo` sobre essa fundação. Detalhamento completo em `05-modelo-de-dados.md`, seção "Autorização do Catálogo Editorial" (nova) e seção "Set" (logo). |
| 1.57 | **Tela Visão Geral (`/catalogo`) implementada no mesmo dia (2026-07-26), sobre a fundação da revisão `1.56`.** "Placar Rápido": "Front-end" atualizado para refletir os quatro blocos implementados (Estado do Catálogo, Card Sets navegável, Cartas por Raridade, Atividade Recente) e a guarda de admin nas seis rotas do módulo; "Próximo passo" reescrito para priorizar validação visual em `npm run dev` e conferência dos indicadores com Fabrício — os blocos foram reconstruídos a partir do resumo de uma compactação de contexto no meio da sessão, sem acesso ao wireframe verbatim originalmente aprovado. Detalhamento completo em `05-modelo-de-dados.md`, seção "Frontend — Visão Geral" (nova). |
| 1.58 | **Reconciliação documental (2026-07-26), motivada por auditoria externa conduzida por Fabrício sobre o repositório após a sessão que produziu `development/HANDOFF-2026-07-26.md`.** "Placar Rápido"/"Detalhamento" estavam dessincronizados: ainda descreviam o Catálogo Editorial só pela fundação de leitura (`ADR-022`) e pela tela Visão Geral, sem registrar a escrita administrativa (`ADR-023`) — que já tinha, na prática, infraestrutura comum, `Game` e `Expansion` concluídos (backend + tela + validação funcional real) e `Card Set` definido como próximo ciclo. Corrigidos: "Front-end", "Próximo passo" (agora aponta para o handoff vigente em vez de repetir seu conteúdo), nova linha "Handoff vigente" no Placar Rápido, nova linha "Catálogo Editorial — Escrita e Ingestão" no Detalhamento. Passo 7 (leitura do handoff vigente) adicionado ao prompt de retomada de sessão, entre Standards/linguagem ubíqua e o resumo final — deliberadamente depois dos documentos normativos, por ser operacional. Status genéricos "Em elaboração" de `04`-`07` substituídos por descrições específicas do estado real de cada um. Discrepância `ENERGY` agora referencia a Open Decision `OD-001` formalizada em `04-domain-model.md` nesta mesma rodada, em vez de ficar apenas descrita em prosa sem responsável/gatilho. Nova entrada "Development — Handoffs" em "Documentation Areas". Rodada de reconciliação completa (README/ROADMAP/arquitetura documental/STD-001) documentada em conjunto — ver revisões correspondentes de cada documento. |
| 1.59 | **Correção direcionada (2026-07-30), a pedido de Fabrício — sessão exclusiva de correção e sincronização documental.** "Card Assets carregados" corrigido de `1.718` para `1.734` (contagem real confirmada no Supabase, refletindo `MEE` completa em `en`+`pt-BR`). Linha "Discrepância em aberto" (Categoria `ENERGY`) substituída por "Decisão registrada — Cartas de Energia no catálogo": `OD-001` foi resolvida por decisão explícita e definitiva de Fabrício — cartas de Energia ocupam posição oficial no Set e fazem parte do catálogo numerado, como Pokémon e Trainer — formalizada em `adr/ADR-025-energy-as-catalog-card-category.md`. Nenhuma alteração física no banco; a categoria `ENERGY` e os 17 Cards que a utilizam já existiam. |
| 1.60 | **Reconciliação documental (2026-07-31), motivada pelo pedido explícito de Fabrício de recuperar o ponto exato de retomada a partir do Handoff.** O handoff vigente até então (`development/HANDOFF-2026-07-26.md`) estava desatualizado: citava como "próxima tarefa" o ciclo de `Card Set` com Queries `2035`/`2036`/`2806`, nunca de fato usadas — o ciclo real foi implementado com outra numeração (`2048`–`2050`/`2811`) e nem esse handoff nem esta seção registravam o trabalho concluído entre 2026-07-26 e 2026-07-31 (exclusão real de `Expansion`, logo de Expansão, redesenho completo de Coleções, atualização/exclusão real de `Card Set`, logo de Card Set, ajuste de contraste no tema escuro, altura fixa da logo). Novo `development/HANDOFF-2026-07-31.md` criado, substituindo o anterior (preservado como histórico). "Front-end", "Próximo passo" e "Catálogo Editorial — Escrita e Ingestão" reescritos para refletir o estado real verificado no repositório: módulo `ADR-023` em ~75% (`Game`/`Expansion`/`Card Set` concluídos, `Card` é o próximo ciclo), três confirmações de interface real ainda pendentes (validação funcional de `Card Set`, exclusão de `Game`, tela completa de `Expansion`). |
| 1.61 | **Sincronização de fim de dia (2026-07-31, mesmo dia, segunda metade)**, a pedido explícito de Fabrício: "paramos por aqui... deixe registrado na documentação o ponto exato de retorno aos trabalhos." Entre a revisão `1.60` e esta, Fabrício conduziu uma longa rodada de QA visual ponto a ponto sobre a galeria de leitura de Cartas (`/catalogo/cartas`), já reescrita antes da revisão `1.60` mas não documentada nela — nenhuma Query de banco nova, só apresentação: transição de ampliação com morph real (View Transitions API), cabeçalho da Coleção redesenhado (logo + metadados + alternador de idioma de imagem PT/EN), 5 indicadores agregados substituindo a antiga barra "Recentes", filtro hierárquico Jogo→Expansão→Coleção, filtros de Raridade/Categoria convertidos de checkboxes para chips multi-seleção, e correção da seleção padrão (agora sempre cai num Card Set com cartas cadastradas). "Front-end", "Próximo passo" e "Catálogo Editorial — Escrita e Ingestão" atualizados para descrever esse trabalho; `docs/development/HANDOFF-2026-07-31.md` estendido com uma nova Seção 2b e checklist de retomada atualizado. Percentual do `ADR-023` inalterado (~75%) — o próximo passo real de escopo continua sendo a criação/edição administrativa de `Card`, ainda não iniciada. |
| 1.62 | **Início do dia (2026-08-01), reorganização do menu secundário do Catálogo — primeiro passo do subciclo Card criação/variações/importação de imagens.** Pedido de Fabrício: "1. Reorganize o bloco de Operações, deixando os itens: 'Importar Cartas' e 'Importar Imagens' 2. No bloco Gerencial, inclua o item Log de Atualizações." `nav-config.ts` (bloco "Operações") trocou a categorização por método de importação (stubs nunca implementados) por categorização pelo que é importado ("Importar Cartas"/"Importar Imagens"); bloco "Gerencial" ganhou "Log de Atualizações" (trilha de `catalog_admin_action_log`, `ADR-023`), distinto do "Histórico de Importações" já existente. Três páginas stub novas (`ComingSoonPage`), rotas antigas (`importacao-manual`/`-pdf`/`-api`) sem item de menu mas ainda no repositório — remoção pendente de autorização de Fabrício. Mudança só de navegação, sem Query nova; detalhe completo em `05-modelo-de-dados.md`, revisão `1.25`. |
| 1.63 | **Botão "Importar Cartas" na tela Cartas + primeira estrutura da página Importar Cartas (2026-08-01, mesmo dia).** Pedido de Fabrício: botão na tela Cartas seguindo o padrão das demais páginas de cadastro, direcionando para `/catalogo/importar-cartas`; nessa página, duas opções — "importar via PDF (lista de verificação oficial)" e "importar via API (TCGDex), para as coleções ainda sem cartas cadastradas". `/catalogo/importar-cartas` deixou de ser `ComingSoonPage` genérica: novo componente `ImportarCartasView` com dois cartões ("Importar via PDF"/"Importar via API (TCGDex)"), ambos ainda "Em construção" — escopo desta rodada é só a estrutura visual, por decisão explícita de Fabrício. O modelo do checklist em PDF, mencionado no pedido, não veio anexado nesta rodada; Fabrício confirmou que reenvia depois — o detalhamento do fluxo PDF (e da integração TCGDex) fica para uma rodada própria. Detalhe completo em `05-modelo-de-dados.md`, revisão `1.26`. |
| 1.64 | **Ajuste fino: indicador "Variações" removido da tela Cartas (2026-08-01, mesmo dia).** Pedido de Fabrício: "após a inserção do botão 'Importar Cartas' a harmonia visual da página foi impactada... remova o indicador VARIAÇÕES". `CartasStats` passa de 5 para 4 indicadores (Cartas/Imagens/Sem Cartas/Sem Imagens); o dado (`totalVariacoes`) continua calculado, só não é mais exibido. Detalhe completo em `05-modelo-de-dados.md`, revisão `1.27`. |
| 1.65 | **Correção: espaço entre o botão "Importar Cartas" e a área de busca da tela Cartas padronizado com as demais telas de cadastro (2026-08-01, mesmo dia).** Pedido de Fabrício, com prints comparando Cartas e Coleções: "padronize o espaço do botão e da tabela abaixo". O botão tinha ficado fora do agrupamento de espaçamento que Jogos/Expansões/Coleções sempre usaram entre o botão de ação e o `Card` de conteúdo — corrigido, sem mudança visual em mais nada. Detalhe completo em `05-modelo-de-dados.md`, revisão `1.28`. |
| 1.66 | **`ADR-024` (ingestão de Cards) iniciada — Ciclo 1 escrito e proposto (2026-08-01, mesmo dia).** Fabrício pediu o retorno ao Catálogo Editorial com foco em ingestão de Cards, anexando o modelo real do checklist em PDF (`cri_web_cardlist_ME05_br.pdf`); após duas rodadas de plano técnico revisado (sem nenhuma escrita, por pedido explícito) e um ajuste final de 4 pontos (`progress_step` como enum estável; `category_confidence` como coluna própria; regra de classificação de categoria única entre PDF/TCGdex; resumo de análise na tela de Revisão), Fabrício aprovou o plano e autorizou o início da implementação. Ciclo 1 (infraestrutura comum de staging e confirmação, primeiro dos quatro ciclos verticais acordados) escrito: dez Queries (`2054`, `2060`/`2061`, `2070`/`2071`, `2080`–`2082`, `2813`/`2814`), todas com status `PROPOSTA`, aguardando execução e confirmação de Fabrício. Gap documental reconciliado no mesmo commit: `05-modelo-de-dados.md` nunca havia registrado as Queries `2051`–`2053`/`2812` (já confirmadas executadas desde 2026-07-31) na lista de Sequência do módulo — corrigido. Detalhe completo em `05-modelo-de-dados.md`, seção "Ciclo 1 — Infraestrutura comum de staging e confirmação". |
| 1.67 | **Ciclo 1 de `ADR-024` executado, corrigido e validado (2026-08-01, mesmo dia).** Fabrício aprovou o plano com 5 alinhamentos finais antes de iniciar a execução: manter o ritual de uma Query por vez; **não alterar o modelo de dados** — `category_source`/`category_confidence` voltam para dentro de `normalized_data` (revertido nas Queries `2070`/`2071`/`2814` e na documentação, sem nunca terem sido executados como coluna física); confirmar a regra de concorrência de conflito como desenhada; confirmar a ausência de `language_id`; autorizar a atualização de documentação sem reescrever histórico. As 10 Queries (mais uma `2055`, ver abaixo) foram então apresentadas e executadas uma a uma, cada uma confirmada por Fabrício antes da próxima. Dois problemas reais surgiram na própria execução, não hipotéticos: (1) a Query `2054` ampliou só duas das três constraints de `catalog_admin_action_log` — `ck_catalog_admin_action_log_entity_type_valid` foi esquecida, bloqueando a primeira chamada real de `admin_start_catalog_import()` com um erro de constraint; corrigida com a Query `2055`, isolada, e reconciliada no arquivo canônico da Query `2010` (v1.3); (2) a validação funcional (`2814`) inicialmente tentava chamar as três funções diretamente no SQL Editor, que roda sem sessão JWT — `is_admin()`/`auth.uid()` nunca resolvem `true` ali (mesma limitação já registrada na Query `1860`); corrigido simulando a sessão do primeiro administrador real via `set_config('request.jwt.claim(s)...', true)`, escopo local à transação de teste. Com essas duas correções, `2814` passou por todos os cenários funcionais (abertura de job, bloqueio de fingerprint duplicado, decisão de linhas, confirmação com os três desfechos `INSERTED`/`UPDATED`/`UNCHANGED`, bloqueio de reconfirmação) com dados sintéticos e `ROLLBACK`, sem deixar resíduo. **Ciclo 1 do `ADR-024` está confirmado executado e validado — o próximo passo é aguardar autorização explícita de Fabrício para iniciar o Ciclo 2 (fluxo TCGdex completo).** Detalhe completo em `05-modelo-de-dados.md`, seção "Ciclo 1 — Infraestrutura comum de staging e confirmação". |
| 1.68 | **Auditoria de reconciliação documental (2026-08-02), a pedido explícito de Fabrício: "vamos parar para organizar a casa... avançamos muito sem atualizar a documentação do projeto."** Consultado o repositório local (`main`, HEAD `be575d4bcd20154fbac3a2758aca1e0da0513898` — sem acesso de rede a `origin/main` neste ambiente, ver relatório da auditoria) e os arquivos de implementação relacionados, não a memória de sessões anteriores. Achados corrigidos nesta revisão: (a) "Placar Rápido" ainda descrevia o catálogo com `7` Card Sets/`927` Cards como se fosse o total atual — na realidade, o Ciclo 2 do `ADR-024` (ver item c) incorporou pelo menos mais `11` Card Sets (`SV1`-`SV5`/`SV3.5`/`SVE`/`SVP`/`ME5`) desde então; corrigido para citar a última contagem documentada e sinalizar que o total atual não é confirmável apenas pelo repositório (sem acesso ao banco físico nesta auditoria); (b) linha "Front-end" ainda descrevia `Card` (criação/edição) como "próximo ciclo" do `ADR-023` — desatualizado desde que Fabrício redirecionou o foco para `ADR-024` em 2026-08-01; corrigido para "pausado", não "próximo"; (c) **contradição central encontrada**: esta própria seção dizia "aguardando autorização explícita de Fabrício para iniciar o Ciclo 2", mas o Ciclo 2 (fluxo TCGdex completo) está de fato implementado e em uso ativo em produção — Edge Function `import-catalog-cards` real, tela `/catalogo/importar-cartas` completa, múltiplos Card Sets já importados; corrigido para refletir o estado real, com uma pendência explícita registrada (fechamento formal/validação final do Ciclo 2 nunca foi feito, diferente do Ciclo 1); (d) handoff vigente apontava para `HANDOFF-2026-07-31.md`, defasado em relação a toda a implementação do Ciclo 2 — novo `HANDOFF-2026-08-02.md` criado (ver `03-documentation-architecture.md`), handoffs anteriores movidos para `history/development/`, conforme o ciclo de vida já documentado e nunca antes aplicado; (e) três raridades citadas por Fabrício como potencialmente ainda pendentes (`Shiny rara`/`Brilhante Ultra Rara`/`ACE SPEC Raro`) foram verificadas diretamente em `database/seeds/830_seed_rarity.sql` (v1.5) e `supabase/functions/import-catalog-cards/services/normalize.ts` — as três têm nome canônico, código estável, alias confirmado contra dados reais de produção e símbolo visual definidos; registradas como resolvidas nesta revisão, não como pendência (ver relatório da auditoria para a justificativa completa desta divergência em relação ao pedido original); (f) nova pendência arquitetural registrada, sem solução: estratégia de múltiplas fontes externas (multi-provider), motivada pela instabilidade real observada na TCGdex — sem alterar `ADR-008`. Nenhum código, SQL ou Edge Function foi alterado nesta rodada — escopo exclusivamente documental (`README.md`, `docs/**/*.md`, `database/README.md`), conforme instrução explícita de Fabrício. |
| 1.69 | **Adequação ao padrão LLM Wiki (2026-08-06)**: prompt de retomada reescrito para liderar com `CLAUDE.md`/`docs/INDEX.md` em vez do ritual "leia 00 até o último numerado"; `Core Documents`/`Documentation Areas` atualizados com os 3 artefatos novos e a divisão de `05-modelo-de-dados.md` em `05a`–`05e`. Ver `CLAUDE.md`, `docs/INDEX.md`, `docs/log.md` e `03-documentation-architecture.md` revisão `1.12`. |
| 1.70 | **Fechamento documental da rodada de Raridade/Mapeamento e da edição de Card (2026-08-07).** Duas frentes implementadas e validadas em produção ao longo do dia, cujos 21 Queries (`2094`–`2114`) foram executados sem o arquivo canônico correspondente ter sido escrito no mesmo ciclo — gap real de disciplina, corrigido nesta rodada de fechamento (todos os arquivos reconstruídos a partir do estado real lido em produção via `pg_get_functiondef()`/`pg_get_constraintdef()`, nunca da memória de sessão, por exigência de `CLAUDE.md`). (1) "Raridades — ajustes de mapeamento" reescrita: `RARITY_NAME_ALIASES` (hardcoded) substituído por `rarity_external_mapping` + tela `/catalogo/raridades`, com revalidação sem reimportação — ver `adr/ADR-024-catalog-card-ingestion-strategy.md` revisão `1.2` e `05e-catalogo-editorial.md`, nova seção "Raridade — mapeamento self-service e revalidação". (2) Nova linha "Edição administrativa de Card": `admin_update_card()` (Query `2114`) confirmada executada e validada funcionalmente por Fabrício. "Próximo passo" ajustado para refletir que a edição de Card já não é mais pendência, só criação/desativação. Fecha a task de documentação pendente desde a rodada de Raridade (registrada no handoff vigente). |
| 1.71 | **Subciclo `Card` do `ADR-023` concluído (2026-08-07, mesmo dia, rodada seguinte).** Pedido explícito de Fabrício: "Vamos avançar com o Resto do subciclo `Card` — criação e desativação/reativação administrativa (edição já está pronta)". `admin_create_card()` (Query `2115`), `admin_deactivate_card()` (`2116`) e `admin_reactivate_card()` (`2117`) confirmadas executadas e validadas funcionalmente (validação `2817`, 15 cenários) — linha "Edição administrativa de Card" (revisão `1.70`) renomeada para "Subciclo `Card` (ADR-023)" e reescrita para refletir o ciclo vertical completo; "Front-end" e "Próximo passo" atualizados para remover `Card` como pendência do `ADR-023`. Descoberta real durante a validação: `GRANT EXECUTE ... TO authenticated` não revoga o `EXECUTE` implícito herdado de `PUBLIC` — gap de segurança corrigido nas três funções novas, auditoria retroativa nas demais funções `admin_*` do módulo adiada por decisão explícita de Fabrício (item futuro separado). Ver `adr/ADR-023-catalog-editorial-write-authorization.md` revisões `1.7`/`1.8` e `05e-catalogo-editorial.md`, nova seção. |
| 1.72 | **Fechamento documental do Ciclo 2 (`ADR-024`), 2026-08-07, mesmo dia, rodada seguinte.** Pedido de Fabrício: seguir o plano após confirmar que multi-provider fica fora de escopo desta sprint (decisão de escopo, não arquitetural). Nova seção "Ciclo 2 — Fluxo vertical completo via TCGdex" em `05e-catalogo-editorial.md`, consolidando o que estava disperso desde 2026-08-01 nas revisões `1.1`–`1.50` desta tabela (antes da divisão de 2026-08-06) — fecha o gap "Ciclo 2 ainda não tem uma seção estruturada equivalente ao Ciclo 1", sinalizado pela auditoria de 2026-08-02 (revisão `1.68`, item c) e nunca corrigido até agora. Nova validação `2818` (somente leitura, sobre dado real de produção — diferente da `2814` do Ciclo 1, que usou dado sintético) escrita e apresentada a Fabrício, aguardando execução; resolve de quebra uma divergência real encontrada nesta auditoria (cabeçalho do arquivo canônico da Query `2092` v1.3 ainda dizia "PROPOSTA", contradizendo a Revision History e o uso real em produção). Linhas "Catálogo Editorial — Ingestão" e "Pendência arquitetural — múltiplas fontes externas" reescritas para refletir os dois fechamentos; "Próximo passo" reescrito para apontar a confirmação da `2818` como o item imediato. Também corrigida, de passagem, uma stale na tabela "Detalhamento" (linha "Catálogo Editorial — Escrita e Ingestão" ainda descrevia `Card` como pendente/pausado, apesar de a revisão `1.71` já ter fechado esse subciclo no mesmo dia). Ver `adr/ADR-024-catalog-card-ingestion-strategy.md` revisão `1.3`, `adr/ADR-INDEX.md` revisão `2.11`, `06-pipeline-importacao.md` revisão `1.7` e `05e-catalogo-editorial.md`. |
| 1.73 | **Ciclo 2 (`ADR-024`) formalmente validado de ponta a ponta, 2026-08-07, mesmo dia, rodada seguinte.** Fabrício rodou os 8 itens da validação `2818`: 5 confirmados limpos direto, item 4 devolveu divergência real (job podia chegar a `COMPLETED` com linha de decisão nunca revisada) — investigada, diagnosticada e corrigida na mesma rodada pela Query `2082` v1.1 + Migration `2118` (reparo retroativo dos 2 jobs reais afetados), ambas confirmadas executadas; os 2 jobs reabertos por sua vez já estavam superados por reimportações posteriores bem-sucedidas dos mesmos Card Sets (decisão de Fabrício: deixá-los como estão, sem consequência prática). Itens 3/5/6 confirmados limpos na sequência, fechando os 8 de 8. Linha "Catálogo Editorial — Ingestão" e "Próximo passo" reescritas para CONFIRMADO EXECUTADO E VALIDADO. Também corrigida, de passagem, uma stale real na tabela "Detalhamento" (`ROADMAP.md` Trilha 2/Frente D ainda descrevia o subciclo `Card` como "pausado, não iniciado"/"~75%", desatualizado desde a revisão `1.71` deste mesmo documento). Ver `adr/ADR-024-catalog-card-ingestion-strategy.md` revisões `1.4`–`1.6`, `05e-catalogo-editorial.md` e `ROADMAP.md` revisão `1.16`. |
| 1.74 | **Novo `ADR-026` (Manual Local-File Asset Import Channel, 2026-08-07)**, mesmo dia, rodada seguinte. Fabrício perguntou em qual ADR o mecanismo de importação manual de imagens (`scripts/import-manual-assets.ts`, `asset_source` `MANUAL`, em produção desde 2026-07-24 para `MEE`/`MEP`) estava documentado — resposta: nenhum, gap real de decisão arquitetural nunca formalizada. Novo ADR criado, e decisão de prioridade confirmada na mesma rodada: concluir `MEP` por este canal é mais prioritário do que iniciar o Ciclo 3 de `ADR-024` (prova técnica do processador de PDF). "Próximo passo" reescrito para refletir essa prioridade. Ver `adr/ADR-026-manual-local-file-asset-import-channel.md`, `adr/ADR-INDEX.md` revisão `2.12` e `ROADMAP.md` revisão `1.17`. |
| 1.75 | **Backend SQL do segundo ponto de entrada via UI para o canal Manual (`ADR-026`, emenda), 2026-08-07/08.** Pedido de Fabrício: evoluir `/catalogo/importar-imagens` para suportar `Fonte = API`/`Manual`, upload direto do navegador para `card-front`, sem campo de texto para caminho local. Plano técnico apresentado e aprovado com 3 ajustes de segurança antes da implementação (menor privilégio nas políticas de Storage, validação de invariantes na função de persistência com a chave natural de `card_asset` confirmada em produção antes de escrever o upsert, `run_id` na auditoria por lote). Quatro Queries CONFIRMADAS EXECUTADAS E VALIDADAS uma a uma (`2119` políticas admin-only em `storage.objects`, só `INSERT`/`DELETE`; `2120` `admin_persist_manual_card_asset()`; `2121` amplia `catalog_admin_action_log`; `2122` `admin_log_manual_card_asset_import_batch()`), validação consolidada `2819` (4 itens) fechando o backend. Achado real durante a implementação: `card_set` não tem `game_id` direto — corrigido para resolver via `card → card_set → expansion → game`. "Próximo passo" reescrito para refletir o núcleo compartilhado/Server Actions/UI como o que falta. Ver `adr/ADR-026-manual-local-file-asset-import-channel.md` revisão `1.1`, `adr/ADR-INDEX.md` revisão `2.13`. |
| 1.76 | **Frontend do segundo ponto de entrada via UI para o canal Manual (`ADR-026`, emenda), 2026-08-08, mesmo dia, rodada seguinte.** Núcleo compartilhado `web/lib/catalogo/manual-asset-import/core.ts` extraído de `scripts/import-manual-assets.ts` (que passou a importá-lo, comportamento inalterado); Server Actions `persistirImagemManual()`/`fecharLoteImportacaoManual()`; manifesto `getCartasParaImportacaoManual()`; componente `ImportarImagensManualPicker` integrado a `ImportarImagensView` via novo `FonteToggle` (`API`/`Manual`), modo `API` preservado integralmente. Upload direto navegador→Storage com path sempre novo, rollback seguro, auditoria em lote com `run_id`. `tsc --noEmit` limpo. "Próximo passo" reescrito: falta só a validação funcional em produção real. Ver `adr/ADR-026-manual-local-file-asset-import-channel.md` revisão `1.1`. |
| 1.77 | **Validação funcional em produção real confirmada, 2026-08-08, mesmo dia, rodada seguinte.** Fabrício testou o fluxo completo com a Coleção `SVE`: 6 arquivos de energia básica importados com sucesso pela UI, visíveis na galeria com a imagem correta (`#007` fora do lote testado, de propósito). Fecha a emenda "Segundo ponto de entrada via UI" de `ADR-026` — backend, frontend e validação real todos confirmados. "Próximo passo" reescrito: falta só usar o canal para concluir `MEP` de fato (item de dado, não de implementação). Ver `adr/ADR-026-manual-local-file-asset-import-channel.md` revisão `1.1`. |
| 1.78 | **Encerramento do canal PDF (Ciclos 3/4 de `ADR-024`), decisão explícita de Fabrício, 2026-08-08, mesmo dia, rodada seguinte.** Fabrício avaliou que, com o canal Manual via UI concluído e validado (revisão `1.77`), o custo de automatizar o canal PDF não traz benefício real: a taxa de sucesso do canal TCGdex já é suficiente, a direção futura de multi-provider tende a cobrir lacunas remanescentes com mais confiabilidade do que interpretar layout gráfico de checklist, e os canais manuais já existentes (`ADR-023`/`ADR-026`) cobrem o caso residual sem automação. Decisão embasada por uma prova técnica exploratória real (fora do repositório) contra dois checklists oficiais (`ME04`, `ME02pt5`, ambos em `assets/reference-sources/`): confirmou viabilidade técnica de extrair raridade/acabamento por geometria vetorial e cor, sem OCR/IA visual, mas também quantificou que a robustez cai de forma real em checklists maiores/mais densos — exigindo detecção dinâmica de grade de linhas/colunas por produto, engenharia real, não um ajuste incremental. "Placar Rápido" (linha "Catálogo Editorial — Ingestão"), "Próximo passo" e "Detalhamento" (linha "Catálogo Editorial — Escrita e Ingestão") atualizados; Frente E do Catálogo Editorial passa a "Concluído". Corrigidas de passagem duas stales pré-existentes na linha de Core Documents (`ADR-023` ainda descrito como "~75%, pausada", desatualizado desde 2026-08-07). Nenhuma mudança de schema ou código de produção. Ver `adr/ADR-024-catalog-card-ingestion-strategy.md` revisão `1.7` e `ROADMAP.md` revisão `1.21`. |
| 1.79 | **Novo handoff `HANDOFF-2026-08-08.md`, 2026-08-08, mesmo dia, rodada seguinte.** `HANDOFF-2026-08-02.md` estava significativamente desatualizado — não refletia o fechamento formal do Ciclo 2 de `ADR-024` (2026-08-07), a conclusão do subciclo `Card` de `ADR-023` (2026-08-07), a emenda de Raridade self-service (2026-08-07), a criação de `ADR-026` e seu segundo ponto de entrada via UI (2026-08-07/08), nem o encerramento definitivo do canal PDF (2026-08-08) — todos ocorridos depois da data daquele handoff. Novo handoff consolida o estado real de todas essas frentes, mais a limpeza de UI das telas `/catalogo/importar-cartas`/`/catalogo/importar-imagens` (seletor de Fonte API/PDF removido, layout e altura dos controles ajustados) feita nesta mesma sessão, a pedido de Fabrício, como pré-requisito explícito antes de "seguir com a documentação". `HANDOFF-2026-08-02.md` movido para `history/development/`, ponteiro "Handoff vigente" atualizado. Ver `docs/development/HANDOFF-2026-08-08.md`, `docs/INDEX.md` revisão `1.2`. |
| 1.80 | **Reconciliação documental de fechamento (2026-08-08), mesmo dia, rodada seguinte — a pedido explícito de Fabrício, ao final da sequência de auditorias que produziu o Módulo Gerencial (Sprint Gerencial 1), a matriz de 4 frentes, a auditoria da Frente C e a auditoria de planejamento.** "Placar Rápido": duas correções de premissa — `MEP` deixa de ser tratado como pendência (a pendência histórica real era a existência do canal manual, resolvida por `ADR-026`, não a falta de imagens em si); Frente C sem nenhum critério de aceite formal em aberto (auditoria dedicada não encontrou pendência real). Novas linhas "Catálogo Editorial" (formalmente encerrado, cinco frentes concluídas) e "Módulo Gerencial" (Visão Geral/Histórico de Importações concluídos; Log de Atualizações/Central de Relatórios pendentes). "Sub-Fase 2 — Coleções" atualizada: critério de entrada satisfeito, sem ordem de prioridade decidida frente ao Módulo Gerencial. "Próximo passo"/"Handoff vigente" reescritos para refletir o Módulo Gerencial como frente ativa. "Detalhamento" e "Core Documents" sincronizados com as mesmas correções. Detalhamento completo da reconciliação em `ROADMAP.md` revisão `1.22`. |
| 1.81 | **Fechamento da sessão (2026-08-08) — Fabrício pausou o trabalho e pediu o registro do ponto de retomada.** "Próximo passo" atualizado: o hub de Card Set (`/catalogo/card-sets/[code]`), único achado da auditoria da Frente C na revisão `1.80`, foi implementado e teve sua V1 aprovada e encerrada por Fabrício na mesma janela, depois de várias rodadas de refinamento visual — deixa de aparecer como débito, só o upload de logo permanece. Ponto real de retomada explicitado: decisão de prioridade entre Módulo Gerencial e Sub-Fase 2 — Coleções, nenhuma outra pendência de arquitetura/dado em aberto. `docs/development/HANDOFF-2026-08-08.md` ganhou nova seção 11 (hub de Card Set) e checklist de retomada atualizado; `ROADMAP.md` revisão `1.24`; `docs/05e-catalogo-editorial.md`, seção "Hub de Card Set", reescrita para o estado final. Nenhuma mudança de schema ou código nesta rodada — escopo exclusivamente documental. |
| 1.82 | **Ritual de retomada (2026-08-09) — divergência real encontrada e corrigida na linha "Módulo Gerencial".** Antes de retomar o desenvolvimento funcional, o ponto de retomada trazido por Fabrício foi confrontado contra o estado real do código (não só a documentação): Catálogo Editorial, Visão Geral e V1 do hub de Card Set seguem concluídos como já registrado, mas "Histórico de Importações" estava marcado como concluído desde a revisão `1.80` sem nunca ter recebido a revisão de UX/layout (mesmo modelo do bloco "Atividade Recente" da Visão Geral) que Fabrício lembrava ter definido como direção — a tela ainda era uma `<table>` simples sem busca, com "Card Set" em vez de "Coleção". Sinalizado explicitamente, não corrigido em silêncio. Implementado nesta rodada: `ImportacoesTable` (`web/components/catalogo/importacoes-table.tsx`) substitui a tabela antiga. Linha "Módulo Gerencial" corrigida para refletir o estado real — backend do Histórico de Importações concluído (2026-08-08), UX/layout implementado (2026-08-09), pendente validação visual de Fabrício antes de considerar esta frente encerrada. `tsc --noEmit` confirmado limpo. Ver `ROADMAP.md` revisão `1.26`. |
| 1.83 | **Histórico de Importações aprovado e encerrado (2026-08-09), mesmo dia, rodadas seguintes.** Depois da revisão `1.82`, mais três rodadas de ajuste (gráficos de tendência por pipeline com popover ao clicar, cor exata `#3FCF8E` na barra de sucesso, eixo X semanal) — todas validadas visualmente e aprovadas por Fabrício. Linha "Módulo Gerencial" atualizada: Histórico de Importações passa de "pendente validação visual" para concluída, ao lado da Visão Geral. Restam pendentes, sem mudança nesta rodada: Log de Atualizações, Central de Relatórios, e a decisão de prioridade frente à Sub-Fase 2 — Coleções. Ver `ROADMAP.md` revisão `1.30`. |
| 1.84 | **Log de Atualizações V1 implementada e aprovada (2026-08-09), mesmo dia, rodada seguinte.** Primeira leitura de `catalog_admin_action_log` desde a criação da tabela — proposta técnica revisada (classificação semântica dos 21 `action` reais em Cadastro/Alteração/Exclusão/Outras) e segunda revisão independente via persona ECC database/security-reviewer (primeira aplicação real do piloto formalizado em `ROADMAP.md` revisão `1.25`) antes da execução — achou e corrigiu um gap real de resolução de `entity_label` para 4 dos 7 `entity_type`. Backend: índice, function de classificação, 4 functions de escrita corrigidas para gravar `card_set_name`/`card_set_code` no momento do evento, function de listagem paginada/filtrada server-side e function de agregação semanal (Queries `2125`–`2128`) — todas CONFIRMADAS EXECUTADAS por Fabrício. Frontend: 3 gráficos semanais por categoria, filtros server-side (busca/Entidade/Ação/Usuário), tabela Data\|Quem\|Entidade\|Registro\|Ação\|Detalhes com Dialog de Detalhes a partir do metadata já resolvido. Linha "Módulo Gerencial" atualizada: Log de Atualizações passa de "pendente" para concluída; resta pendente, sem mudança nesta rodada: Central de Relatórios. `tsc --noEmit` confirmado limpo. Ver `ROADMAP.md` revisão `1.31` e `05e-catalogo-editorial.md`, seção "Log de Atualizações". |
| 1.85 | **Fabrício encerra formalmente Histórico de Importações e Log de Atualizações (2026-08-09), mesmo dia, rodada seguinte.** Ajuste visual pós-V1 no Log de Atualizações antes do encerramento (janela de exibição 12→10 semanas, cor única `#3FCF8E`, correção de um bug de largura do eixo introduzido pela redução de largura das barras, interação trocada de popover-ao-clicar para tooltip-ao-passar-o-mouse — retroativo também a `ImportacoesTendencia`, para as duas telas ficarem consistentes). Uma proposta de prefixar o código da Coleção no "Registro" da tabela foi apresentada seguindo o ritual de query mas abortada por Fabrício antes da execução (`admin_list_catalog_action_log()` permanece na v1.0). Linha "Módulo Gerencial" atualizada: as duas frentes passam de "concluída" para "formalmente encerrada por Fabrício" — só resta Central de Relatórios na Trilha 4, com a decisão de prioridade frente à Sub-Fase 2 — Coleções ainda em aberto. `tsc --noEmit` confirmado limpo. Ver `ROADMAP.md` revisão `1.32` e `05e-catalogo-editorial.md`, seção "Log de Atualizações". |
| 1.86 | **Central de Relatórios implementada (2026-08-09), mesmo dia, rodada seguinte — Fabrício escolheu esta frente em vez da Sub-Fase 2 — Coleções.** Hub `/catalogo/relatorios` com os 6 relatórios previstos, todos imprimíveis (`window.print()` nativo, `@media print`, sem motor de PDF) — primeiro uso real de impressão no projeto, `AppShell`/`Sidebar`/`Header` ganharam `print:hidden`/`print:overflow-visible`. Nenhuma mudança de schema/RPC: 4 relatórios (Cartas/Imagens pendentes por Coleção, Qualidade do Catálogo, Cobertura Geral) reaproveitam `catalog_card_set_metrics`/`catalog_card_set_image_coverage` (Query 2123/2124); os outros 2 (Checklist por Coleção, Resumo da Coleção) reaproveitam `getCartasCompletas()`/`getCardSetByCode()`. Definição de "Qualidade do Catálogo" (única sem especificação prévia) e arquitetura confirmadas com Fabrício antes da implementação. `tsc --noEmit` confirmado limpo. Linha "Módulo Gerencial" atualizada: Central de Relatórios implementada, pendente validação visual de Fabrício — só então a Trilha 4 fica formalmente encerrada por completo. Ver `ROADMAP.md` revisão `1.33` e `05e-catalogo-editorial.md`, seção "Central de Relatórios". |
| 1.87 | **Checklist por Coleção redesenhado (2026-08-09), mesmo dia, rodada seguinte — tratamento diferenciado a pedido de Fabrício, modelo oficial de referência anexado.** Sinalizado como o relatório de maior valor para colecionadores e futuro primeiro ponto de acesso de usuários comuns (política de acesso explicitamente adiada). Estrutura funcional do modelo replicada (cabeçalho com duas logos, lista em colunas com checkbox, legenda de raridade), nunca a marca/conteúdo do modelo — logo do Mimikyu + logo real da Coleção. Folha A4 (`max-w-[210mm]`, novo `@page { size: A4; margin: 10mm }` em `globals.css`), lista em 3 colunas CSS, checkbox ANTES do número (pedido explícito, ao contrário do modelo anexado), coluna "Cadastrada" removida e relatório restrito a Cartas ativas — nenhuma query nova. `tsc --noEmit` confirmado limpo; validação visual (inclusive impressão real) ainda pendente, junto com o restante da Central de Relatórios. Linha "Módulo Gerencial" atualizada. Ver `ROADMAP.md` revisão `1.34` e `05e-catalogo-editorial.md`, seção "Checklist por Coleção — tratamento diferenciado, modelo oficial anexado". |
| 1.88 | **Ajustes pós-validação visual do Checklist por Coleção (2026-08-09), mesmo dia, rodada seguinte** — três pedidos de Fabrício a partir de captura de tela real (folha de `ME3`, 124 Cartas): título movido para dentro do cabeçalho entre as duas logos e renomeado para "Lista de Verificação"; zebra striping branco/`#F7F5ED` por linha (`print-color-adjust: exact` explícito); contagem de colunas deixou de ser fixa em 3 — Coleções com mais de ~132 Cartas não caberiam numa única folha A4 — passa a ser calculada (`Math.ceil(cartas.length / 46)`), constante calibrada visualmente pela folha de `ME3`, sujeita a ajuste. `tsc --noEmit` confirmado limpo; validação visual desta rodada ainda pendente. Ver `ROADMAP.md` revisão `1.35` e `05e-catalogo-editorial.md`, seção "Checklist por Coleção", subseção "Ajustes pós-validação visual". |
| 1.89 | **Mais um ajuste no Checklist por Coleção (2026-08-09), mesmo dia, rodada seguinte** — a partir de captura de tela real de uma Coleção grande (`ME2.5`, 295 Cartas, 7 colunas): subtítulo empilhado sob o título dentro do cabeçalho (ganho de espaço vertical real — a altura do cabeçalho já é dominada pela logo da Coleção); espaço entre colunas reduzido de `1.5rem` para `0.5rem`. `tsc --noEmit` confirmado limpo; validação visual ainda pendente. Ver `ROADMAP.md` revisão `1.36` e `05e-catalogo-editorial.md`, seção "Checklist por Coleção", subseção "Mais uma rodada de ajuste". |
| 1.90 | **Mudança de direção no Checklist por Coleção — teto de colunas (2026-08-09), mesmo dia, teste explícito de Fabrício.** Coleções grandes continuavam ruins visualmente mesmo com o espaço entre colunas reduzido (7+ colunas espremidas). Teto explícito de colunas por página (testado em 5); Coleções pequenas usam só as colunas que precisarem; Coleções que exigem mais que o teto passam a imprimir em mais de uma folha A4 — aceito explicitamente por Fabrício, abandonando o objetivo de "sempre uma única folha" para Coleções muito grandes. `tsc --noEmit` confirmado limpo; validação visual ainda pendente. Ver `ROADMAP.md` revisão `1.37` e `05e-catalogo-editorial.md`, seção "Checklist por Coleção", subseção "Mudança de direção — teto de colunas". |
| 1.91 | **Teto de colunas do Checklist por Coleção ajustado de 5 para 4 (2026-08-09), mesmo dia, rodada seguinte.** Pedido direto de Fabrício — só o valor de `MAX_COLUNAS_A4` mudou; `MIN_COLUNAS_A4 = 2` e a aceitação de relatório multi-página para Coleções grandes seguem inalteradas. `tsc --noEmit` confirmado limpo; validação visual ainda pendente. Ver `ROADMAP.md` revisão `1.38`. |
| 1.92 | **Checklist por Coleção — fontes reduzidas e cabeçalho repetido a cada página (2026-08-09), mesmo dia, rodada seguinte.** Fontes reduzidas um passo em toda a folha; lista trocada de CSS multi-coluna para uma tabela HTML real (`<table>`/`<thead>`/`<tbody>`) — `<thead>` repete nativamente o cabeçalho (logos+título) em toda página impressa, em todos os navegadores principais, sem os riscos de `position: fixed` ou de alterar a margem global do `@page`. `tsc --noEmit` confirmado limpo; validação visual ainda pendente. Ver `ROADMAP.md` revisão `1.39` e `05e-catalogo-editorial.md`, seção "Checklist por Coleção", subseção "Mudança estrutural — cabeçalho repetido a cada página + fontes reduzidas". |
| 1.93 | **Checklist por Coleção aprovado — vira baseline visual da Central de Relatórios (2026-08-09), mesmo dia, rodada seguinte.** Fabrício aprovou o Checklist ("Visualmente excelente") com três ajustes finais (subtítulo sem associar a posse registrada, título de volta a 18px em negrito) e pediu que ele virasse a baseline dos outros 5 relatórios. `RelatorioFolha`/`RelatorioCabecalho`/`RelatorioRodape` extraídos e reaplicados em Cartas pendentes, Imagens pendentes, Qualidade do Catálogo, Cobertura Geral e Resumo da Coleção — cabeçalho repetido via `<thead>` nos 4 relatórios tabulares, cores fixas `neutral-*` em vez de tokens de tema, e `print:hidden` adicionado ao `PageHeader` de tela dos 5 (faltava antes). `tsc --noEmit` confirmado limpo; validação visual dos 5 relatórios ainda pendente. Ver `ROADMAP.md` revisão `1.40` e `05e-catalogo-editorial.md`, seção "Checklist por Coleção", subseção "Checklist por Coleção aprovado — vira baseline visual da Central de Relatórios". |
| 1.94 | **Ajustes finais na baseline visual da Central de Relatórios (2026-08-09), mesmo dia, rodada seguinte — sinalizada por Fabrício como a última rodada.** Zebra striping e cores fixas `neutral-*` — que a revisão `1.93` já afirmava aplicadas, mas só o Resumo da Coleção tinha de fato recebido — corrigidas nos 4 relatórios tabulares. Linha de totais em "Qualidade do Catálogo" (soma por coluna) e em "Cobertura Geral" (soma de Cadastradas/Com imagem + percentual geral recalculado). Rodapé renomeado para "Gerado por MMKYU Collector" e fixado no rodapé físico de cada folha impressa (nunca mais "no meio da página" em relatórios curtos). Contador "Página X de Y" investigado e adiado por decisão explícita de Fabrício — navegadores não expõem paginação real nativamente durante a impressão. `tsc --noEmit` confirmado limpo. Ver `ROADMAP.md` revisão `1.41` e `05e-catalogo-editorial.md`, seção "Checklist por Coleção", subseção "Ajustes finais na baseline". |
| 1.95 | **Cabeçalho em 3 linhas no Checklist/Resumo + linha de totais em "Imagens pendentes por Coleção" (2026-08-09), mesmo dia, rodada seguinte.** `RelatorioCabecalho` ganhou a prop `identificacaoColecao`: identificação da Coleção em destaque na linha 1, nome fixo do relatório na linha 2, subtítulo na linha 3 (antes concatenados num único título, truncando cedo em Coleções com nome longo) — só nos relatórios de uma Coleção só (Checklist, Resumo); os 4 relatórios multi-Coleção mantêm o cabeçalho de 2 linhas original. Linha de totais adicionada em "Imagens pendentes por Coleção", mesmo padrão de "Qualidade do Catálogo"/"Cobertura Geral". `tsc --noEmit` confirmado limpo. Ver `ROADMAP.md` revisão `1.42` e `05e-catalogo-editorial.md`, seção "Checklist por Coleção", subseção "Últimos ajustes". |
| 1.96 | **Ícone ausente em "Log de Atualizações" + rolagem infinita em Cartas/Coleções (2026-08-09), mesmo dia, rodada seguinte — dois achados de Fabrício numa inspeção geral do Catálogo Editorial.** `PageTitle` do Log de Atualizações ganhou o wrapper de ícone (`ScrollText`) que faltava desde a V1. Botão "Ver todas as N cartas" removido de `CartasGallery` e `CardSetCartasGrid`; novo hook `useInfiniteReveal` revela lotes de cartas via `IntersectionObserver` conforme a rolagem, mesmo tamanho de lote de antes. `tsc --noEmit` confirmado limpo. Ver `ROADMAP.md` revisão `1.43` e `05e-catalogo-editorial.md`, subseções "Ícone do título ausente, corrigido" e "Paginação por rolagem infinita". |
| 1.97 | **Barras de "Cobertura por idioma" ampliadas no hub de Card Set (2026-08-09), mesmo dia, rodada seguinte.** Coluna de cada idioma alargada de `w-40` para `w-56` (`app/catalogo/card-sets/[code]/page.tsx`) — pedido de Fabrício para aproveitar melhor o espaço sobrando no painel "Estado do Set" em Coleções com só 2 idiomas. `tsc --noEmit` confirmado limpo. Ver `ROADMAP.md` revisão `1.44` e `05e-catalogo-editorial.md`, seção "Hub de Card Set". |
| 1.98 | **Módulo Gerencial (Trilha 4) formalmente encerrado (2026-08-09) — Fabrício: "Considero pela UI o bloco concluído."** Central de Relatórios, última das quatro frentes ainda pendente, aprovada visualmente. Linhas "Módulo Gerencial", "Sub-Fase 2 — Coleções", "Fase 2 — Modelo Lógico", "Próximo passo" e "Handoff vigente" reescritas para refletir o fechamento (nenhuma decisão de priorização entre Módulo Gerencial e Sub-Fase 2 — Coleções permanece em aberto — só a decisão de quando começar a segunda). Novo `development/HANDOFF-2026-08-09.md` consolida tudo desde `HANDOFF-2026-08-08.md` (Histórico de Importações, Log de Atualizações, Central de Relatórios — com todas as rodadas de ajuste visual — e os três achados finais de uma inspeção geral do módulo); `HANDOFF-2026-08-08.md` movido para `history/development/`. Ver `ROADMAP.md` revisão `1.45` e `05e-catalogo-editorial.md`, seção "Central de Relatórios", subseção "Encerramento formal". |
| 1.99 | **Decisão de sequenciamento macro: Card Variant como bloco intermediário entre Catálogo Editorial e Coleções (2026-08-09), mesmo dia, rodada seguinte.** Fabrício determinou que Coleções não deve iniciar imediatamente após o Catálogo Editorial (que permanece encerrado, escopo `Game → Expansion → Card Set → Card` inalterado) — falta uma camada intermediária, Card Variant, que representa as formas colecionáveis específicas de cada Card, necessária para Collections referenciar uma variante inequívoca (não a Card genérica) ao registrar posse. Novo sequenciamento: Catálogo Editorial → Card Variant → Coleções. Não é domínio novo — `card_variant_type`/`card_variant`/triggers/validações/seeds já existem; o bloco começará por auditoria do existente. Linhas "Sub-Fase 2 — Coleções", nova linha "Bloco Card Variant", "Fase 2 — Modelo Lógico" e "Próximo passo" atualizadas para refletir o novo critério de entrada. Nenhuma implementação/alteração de Card Variant nesta rodada — só o planejamento; escopo detalhado a partir de 2026-08-13, após auditoria técnica final do Catálogo Editorial. Ver `ROADMAP.md` revisão `1.46`. |
| 2.00 | **Auditoria documental completa (2026-08-16) — este documento estava congelado desde a revisão `1.99`/2026-08-09, sem refletir nenhum dos seis fronts reais que aconteceram desde então.** Confrontado contra `docs/log.md` (entradas de 2026-08-14 a 2026-08-16), `ADR-028` e o código real: (1) Bloco Card Variant corrigido de "planejamento registrado, ainda não iniciado" para "substancialmente implementado (governança, taxonomia com CRUD administrativo, pipeline Importar Variantes completo), pausado por instrução explícita de Fabrício" — sem alterar o critério de entrada de Coleções, que permanece como decisão pendente de confirmação, não resolvida por esta auditoria; (2) nova frente Auth Experience (Login V1 aprovado e congelado, propagado a Cadastro/Recuperar/Atualizar Senha) e consolidação do Design System (arquitetura cromática promovida a baseline, Form Controls) adicionadas — nunca haviam sido registradas neste documento; (3) Módulo Gerencial ganhou nota das duas evoluções aditivas (humanização do Log de Atualizações, pipeline VARIANTES no Histórico de Importações) e do bug estrutural de paginação de impressão corrigido; (4) novas linhas "Segurança/Performance" (RLS Admin Performance Hardening, Performance Foundation v1) e "Pricing / Market Data" (discovery em andamento, nenhuma decisão tomada); (5) "Próximo passo" reescrito com as três decisões reais ainda pendentes de Fabrício; (6) "Handoff vigente" atualizado para `development/HANDOFF-2026-08-16.md` (`HANDOFF-2026-08-09.md` movido para `history/development/`). Achado registrado, não corrigido nesta rodada (fica como débito em `ROADMAP.md`): `docs/05b-cartas-e-raridade.md` (modelo físico de Card Variant) não acompanhou nenhum dos incrementos reais de 2026-08-14/15 — reconstituição exige conferência migration a migration, maior que uma correção de reconciliação. Nenhuma implementação, migration ou mudança de código nesta rodada — escopo exclusivamente documental. Ver `ROADMAP.md` revisão `1.48`, `docs/standards/STD-001-database-standards.md` versão `1.19` e `docs/log.md`, entrada `lint` de 2026-08-16. |
| 2.01 | **Reconciliação com decisões formais de Fabrício (2026-08-16), segunda rodada de auditoria — motivada por rejeição explícita do encerramento anterior devido à inconsistência crítica de `docs/05b-cartas-e-raridade.md`.** Bloco Card Variant corrigido de "substancialmente implementado, pausado" para **fundação encerrada** (necessária e suficiente para Pricing e Collection; Vintage/Promo segue backlog não bloqueante) — estado real confirmado por introspecção direta do Supabase: 4.718 Card Variants, 39 tipos canônicos, 2.433 de 7.104 Cards cobertas; achado novo, job `BASE1` (`STAGED`, 410 linhas sem mapeamento) nunca antes documentado. Sub-Fase 2 — Coleções: critério de entrada reescrito — depende agora também da fundação de Pricing/Market Data (sequência Catálogo Editorial → Card Variant → Pricing (fundação) → Coleções, aprovada por Fabrício). Pricing/Market Data: de "discovery, sem próximo passo definido" para "próxima frente comprometida da sequência aprovada". "Próximo passo" e "Handoff vigente" reescritos. `docs/05b-cartas-e-raridade.md` reconstruído nesta mesma rodada (não mais um débito em aberto) — dois achados de precisão reais (regex de validação de `code`; `GRANT`s da view `catalog_card_set_variant_coverage`) substituem o item de "desatualizado" em `ROADMAP.md`. Ver `ROADMAP.md` revisão `1.49` e `docs/log.md`, entrada de 2026-08-16. |
| 2.02 | **Modelagem conceitual e lógica do domínio Pricing concluída (`ADR-029`/`05f-pricing.md`, 2026-08-16, mesma data, ciclo seguinte) — desacoplada da homologação da JustTCG, que segue interrompida por limitação de cota, não aprovada nem reprovada.** Linha "Pricing / Market Data" reescrita: de "discovery concluído, fundação a definir" para "modelagem concluída (dez entidades), implementação física ainda não iniciada". Nenhuma tabela criada no Supabase — decisão de modelagem, não implementação. Nenhuma mudança na sequência estratégica nem no critério de entrada de Coleções. Ver `ROADMAP.md` revisão `1.50`, `docs/adr/ADR-INDEX.md` revisão `2.17` e `docs/log.md`, entrada `docs` de 2026-08-16. |
| 2.03 | **Incremento P1 — Fundação Física de Pricing executada (2026-08-16, mesma data, ciclo seguinte).** Linha "Pricing / Market Data" reescrita: `pricing_source`, `card_condition` e `pricing_condition_mapping` criadas no Supabase (milhar `3000`–`3999`, formalizado em `STD-001`); as demais sete entidades permanecem propostas; nenhuma fonte cadastrada, nenhuma condição semeada. Nenhuma mudança na sequência estratégica nem no critério de entrada de Coleções (a fundação de Pricing ainda não está completa — só iniciada). Ver `ROADMAP.md` revisão `1.51`, `docs/standards/STD-001-database-standards.md` versão `1.20`, `docs/development/HANDOFF-2026-08-16.md` seção 14 e `docs/log.md`, entrada `feature` de 2026-08-16. |
| 2.04 | **Incremento P2 — Correspondência Externa de Pricing executada (2026-08-16, mesma data, ciclo seguinte).** Linha "Pricing / Market Data" reescrita: `pricing_set_mapping` e `pricing_card_mapping` criadas no Supabase, ambas vazias; cinco entidades do domínio agora implementadas, cinco restantes permanecem propostas. Corrigido, nesta mesma rodada, um campo "Última revisão" desatualizado (`2.02`, deveria já refletir `2.03` desde a revisão anterior) — divergência de sincronização entre o campo de cabeçalho e a tabela de Revision History, sem efeito sobre o conteúdo. Ver `ROADMAP.md` revisão `1.52`, `docs/standards/STD-001-database-standards.md` versão `1.21`, `docs/development/HANDOFF-2026-08-16.md` seção 15 e `docs/log.md`, entrada `feature` de 2026-08-16. |
| 2.05 | **Incremento P3 — Auditoria Operacional de Sincronização executada (2026-08-16, mesma data, ciclo seguinte).** Linha "Pricing / Market Data" reescrita: `pricing_sync_run` e `pricing_sync_run_call` criadas no Supabase, ambas vazias; sete entidades do domínio agora implementadas, três restantes permanecem propostas. Ver `ROADMAP.md` revisão `1.53`, `docs/standards/STD-001-database-standards.md` versão `1.22`, `docs/development/HANDOFF-2026-08-16.md` seção 16 e `docs/log.md`, entrada `feature` de 2026-08-16. |
| 2.06 | **Incremento P4 — Produto Externo de Pricing executado (2026-08-16, mesma data, ciclo seguinte).** Linha "Pricing / Market Data" reescrita: `pricing_product` criada no Supabase, vazia; oito entidades do domínio agora implementadas, duas restantes permanecem propostas. Ver `ROADMAP.md` revisão `1.54`, `docs/standards/STD-001-database-standards.md` versão `1.23`, `docs/development/HANDOFF-2026-08-16.md` seção 17 e `docs/log.md`, entrada `feature` de 2026-08-16. |
| 2.07 | **Incremento P5 — Série Histórica de Taxas de Câmbio executado (2026-08-16, mesma data, ciclo seguinte).** Linha "Pricing / Market Data" reescrita: `pricing_fx_rate` criada no Supabase, vazia; nove entidades do domínio agora implementadas, resta apenas `pricing_observation` como proposta. Ver `ROADMAP.md` revisão `1.55`, `docs/standards/STD-001-database-standards.md` versão `1.25`, `docs/development/HANDOFF-2026-08-16.md` seção 18 e `docs/log.md`, entrada `feature` de 2026-08-16. |
| 2.08 | **Incremento P6 — Observação Histórica de Preço executado (2026-08-16, mesma data, ciclo seguinte) — fundação física de Pricing concluída.** Linha "Pricing / Market Data" reescrita: `pricing_observation` criada no Supabase (identidade market-aware `UNIQUE NULLS NOT DISTINCT` corrigida), vazia; dez de dez entidades do domínio agora implementadas — fundação física completa, domínio ainda não operacional. Linhas "Sub-Fase 2 — Coleções" e "Próximo passo" atualizadas: fundação de Pricing concluída, início de Coleções permanece decisão de Fabrício. Ver `ROADMAP.md` revisão `1.56`, `docs/standards/STD-001-database-standards.md` versão `1.26`, `docs/development/HANDOFF-2026-08-16.md` seção 19 e `docs/log.md`, entrada `feature` de 2026-08-16. |
| 2.09 | **Incremento Pesquisa Global de Cartas executado de ponta a ponta (2026-08-16/17).** Nova linha "Pesquisa Global de Cartas (`ADR-030`)" no Placar Rápido: combobox no header + página `/pesquisa` (filtros, estado na URL, ordenação fixa), projeção segura `public.search_cards()`/`public.search_card_filter_options()` (milhar `4000`-`4999`, novo em `STD-001`) preservando `ADR-022` intacto, `EXECUTE` restrito a `authenticated`, validado por `SET LOCAL ROLE` real e `EXPLAIN (ANALYZE, BUFFERS)` a 22.104 linhas sintéticas com `ROLLBACK`. "Próximo passo"/"Handoff vigente" atualizados: novo `development/HANDOFF-2026-08-17.md` (anterior movido para `history/development/`). **Divergência pré-existente descoberta durante a introspecção obrigatória deste incremento, não causada por ele**: `database/migrations/` diverge do histórico de migrations do Supabase (`2050`/`3000`-`3090` sem arquivo no repo; `2098`-`2153` sem entrada no histórico) — Fabrício optou explicitamente por seguir o incremento e registrar a dívida, sem reconciliar tudo nesta rodada. Testes de UI (teclado/leitor de tela/tema/mobile) e `npm run lint`/`build` pendentes de validação local — sandbox do agente não tem navegador nem o binário SWC. Ver `docs/adr/ADR-030-card-search-projection.md`, `ROADMAP.md` revisão `1.58`, `docs/standards/STD-001-database-standards.md` versão `1.27`, `docs/standards/STD-004-frontend-standards.md` versão `1.4` e `docs/log.md`. |
| 2.10 | **Rodada corretiva — remoção do filtro de Jogo do contrato público de Pesquisa Global de Cartas (2026-08-17), mesmo dia, rejeição explícita do encerramento anterior por Fabrício.** `p_game_code` havia sido implementado em `search_cards()`/`search_card_filter_options()` apesar da decisão prévia de que esta versão não teria filtro de Jogo — divergência de escopo real (frontend chegou a expor um `<Select>` de Jogo, contradizendo a própria documentação do incremento anterior). Corrigido de ponta a ponta sem alterar migrations já executadas: `4030`/`4031` (`DROP FUNCTION` + `CREATE OR REPLACE` com o mesmo endurecimento — `SECURITY DEFINER`, `search_path=''`, `REVOKE` de `PUBLIC`/`anon`, `GRANT EXECUTE` só a `authenticated`); confirmado via `pg_proc` que não resta overload antigo. Frontend (Route Handlers, tipos, `pesquisa-view.tsx`) atualizado no mesmo ciclo — zero referências residuais a "Jogo"/`game`. Segurança revalidada integralmente (autenticado funciona, `anon`/sessão nula bloqueados, tabelas editoriais inacessíveis, cartas inativas ausentes, grants efetivos corretos, advisors sem novos achados). Performance revalidada com mapeamento individual aos 10 padrões de consulta pedidos por Fabrício (antes descritos de forma agregada como "7 padrões"). Linha "Pesquisa Global de Cartas (`ADR-030`)" no Placar Rápido reescrita para refletir o contrato sem Jogo. UI visual, `npm run lint`/`build` seguem pendentes de validação local — mesma limitação de sandbox já registrada na revisão `2.09`; checklist objetivo de homologação local adicionado ao handoff vigente. Ver `docs/adr/ADR-030-card-search-projection.md` revisão `1.1`, `docs/adr/ADR-INDEX.md` revisão `2.19`, `ROADMAP.md` revisão `1.59`, `docs/standards/STD-001-database-standards.md` versão `1.28`, `docs/standards/STD-004-frontend-standards.md` versão `1.5`, `docs/development/HANDOFF-2026-08-17.md` e `docs/log.md`. |
| 2.11 | **Rodada corretiva 2 — busca por "número/total" não retornava resultado (2026-08-17), mesmo dia, achado da própria homologação local pedida na revisão `2.10`.** Pesquisar "125/094" (formato número/total exibido nos cards do catálogo) em `/pesquisa` retornava "Nenhuma carta encontrada", apesar de a carta existir — o tier "número" de `search_cards()` só reconhecia `collector_number` isolado, nunca o formato combinado. Corrigido pela Query `4032` (`CREATE OR REPLACE` sobre a mesma assinatura da `4030`, sem `DROP`): passa a comparar `collector_number` e `collector_total` juntos quando a busca contém uma barra com dígitos dos dois lados, sem expressão regular (mesmo contorno do classificador de segurança já usado na Query `4002`). Validado via RPC real como `authenticated`; regressão do número isolado confirmada intacta; `anon` continua bloqueado; catálogo (`pg_proc`) confirma uma única assinatura de `search_cards()`. A primeira tentativa de aplicação foi negada pelo classificador de segurança do Auto Mode (não por conteúdo malicioso, ver handoff); o trabalho foi pausado e reportado a Fabrício, que autorizou nova tentativa — bem-sucedida. Ver `docs/adr/ADR-030-card-search-projection.md` revisão `1.2`, `docs/standards/STD-001-database-standards.md` versão `1.29`, `docs/development/HANDOFF-2026-08-17.md` e `docs/log.md`. |
| 2.12 | **Pesquisa Global de Cartas formalmente encerrada (2026-08-17, mesmo dia).** Depois das rodadas corretivas 3 (preview de carta estruturalmente compartilhado com `Cartas`) e 4 (modo imersivo de pesquisa mobile reescrito, referência funcional pkmn.gg), Fabrício confirmou em chat que a validação local (UI/teclado/tema/mobile, `npm run lint`/`build`) foi executada e aprovada, e que commit + push das cinco commits do incremento já foram realizados (`git log`/`git status` confirmam árvore limpa, sincronizado com `origin/main`). Linhas "Pesquisa Global de Cartas"/"Próximo passo"/"Handoff vigente" reescritas para remover a linguagem de pendência. Nenhuma migration/código alterado nesta rodada — escopo exclusivamente documental. Ver `ROADMAP.md` revisão `1.60`, `docs/development/HANDOFF-2026-08-17.md` e `docs/log.md`. |
| 2.13 | **Incremento P7 — Homologação Condicionada da JustTCG executado (2026-08-17).** Linha "Pricing / Market Data" reescrita: `JUSTTCG` cadastrada em `pricing_source` (Query `3700`), homologada para piloto técnico (Decisão A aprovada), não homologada para PT-BR/"Valor Brasil" (Decisão B inconclusiva), `is_active = FALSE` até plano comercial contratado. Linha "Próximo passo" reescrita no mesmo sentido. Ver `docs/adr/ADR-029-pricing-domain-model.md` revisão `1.9` e `docs/05f-pricing.md` versão `1.9` para o detalhamento completo. |
| 2.14 | **Incremento P8 — Conector JustTCG e Piloto Controlado executado (2026-08-17).** Linha "Pricing / Market Data" reescrita: primeiro conector real (`scripts/sync-justtcg-pricing.ts`, standalone Deno, server-side, manual), grants de escrita em `pricing_set_mapping`/`pricing_card_mapping` (Query `3091`) e seeds de condição/de-para JustTCG (`3701`/`3702`); piloto real não executado (`JUSTTCG_API_KEY` ausente no ambiente do agente). Ver `docs/adr/ADR-029-pricing-domain-model.md` revisão `1.10` e `docs/05f-pricing.md` versão `1.10` para o detalhamento completo. |
| 2.15 | **Incremento P12 — Exibição de Preços no Frontend formalmente encerrado (2026-08-18), a pedido explícito de Fabrício.** Linhas "Pricing / Market Data", "Próximo passo" e "Handoff vigente" reescritas: P8–P11 (conector JustTCG, PTAX, projeção cambial, contrato de leitura por carta) resumidos como já executados; P12 encerrado com as 14 evidências aprovadas por Fabrício (grids/desktop/mobile/temas/toque/teclado/BRL-USD/popover/fallback de printing/`has_pricing=false`/migration `3904`/segurança-grants-advisors/testes transacionais/typecheck-build), migrations `3903`/`3904` registradas, commits `6324b66`/`0dad413` confirmados sincronizados com `origin/main`. Registrado explicitamente: PTAX pode ser automatizada; JustTCG permanece `is_active = FALSE` até contratação comercial; a visualização de preços nunca chama a JustTCG; sincronização e consulta seguem desacopladas. Próximo incremento recomendado: **P13 — Orquestração Programada de Pricing**. Correção de autenticação de `/login` mantida fora do escopo. Handoff vigente passa a ser `development/HANDOFF-2026-08-18.md`; `HANDOFF-2026-08-17.md` movido para `history/development/`. Nenhuma alteração de código/banco/migration nesta rodada — escopo exclusivamente documental. Ver `docs/05f-pricing.md` versão `1.30`, `docs/adr/ADR-029-pricing-domain-model.md` revisão `1.23`, `ROADMAP.md` revisão `1.62` e `docs/log.md`. |
| 2.16 | **Incremento P13.2 formalmente encerrado (2026-08-18, mesmo dia), a pedido explícito de Fabrício, após auditoria pós-idempotência real.** Linhas "Pricing / Market Data", "Próximo passo" e "Handoff vigente" reescritas: P13.1 (fundação de schema) e P13.2 (módulo compartilhado da PTAX — 12 arquivos —, adapter/runner reescritos, migration `3909` — PostgreSQL passa a ser autoridade de `started_at`/`finished_at` de `pricing_sync_run` —, reexecução idempotente auditada sem inversão temporal) registrados como concluídos; **P13 como um todo permanece em andamento**, não declarado concluído. Próximo passo real: **P13.3 — Edge Function `ptax-fx-refresh`**. Nenhuma alteração de código/banco/migration nesta rodada — escopo exclusivamente documental. Ver `docs/adr/ADR-031-scheduled-pricing-orchestration.md`, revisão `1.5`, `docs/05f-pricing.md`, versão `1.36`, `ROADMAP.md`, revisão `1.63`, e `docs/log.md`. |
| 2.17 | **Incremento P13.3 formalmente encerrado (2026-08-18, mesmo dia), a pedido explícito de Fabrício.** Linhas "Pricing / Market Data", "Próximo passo" e "Handoff vigente" reescritas: Edge Function `ptax-fx-refresh` implementada sobre uma porta funcional (`PtaxSyncRunPort`/`supabase-adapter.ts`, abandonando uma tentativa anterior de mimetizar a API fluente do PostgREST), suíte offline registrada no runner nativo do Deno, **implantada em produção** via Supabase MCP (`qjfutqujxrbzgrtkpgkg`, `slug=ptax-fx-refresh`, `version=1`, `status=ACTIVE`, `verify_jwt=false`) e validada por piloto real autenticado (dois testes negativos `HTTP 401`/zero efeito no banco, depois `HTTP 200`/`COMPLETED`, run `e75e8fca-1c2e-498a-a473-18afa74c152d`, `inserted=0`/`unchanged=7`/`divergent=0`/`invalid=0`) registrada como concluída; **P13 como um todo permanece em andamento**, não declarado concluído. Próximo passo real: **P13.4 — habilitação de `pg_cron`/`pg_net`, Vault secret do lado Postgres, e o Job de agendamento real**. Nenhuma migration, Cron/Vault, código JustTCG ou alteração de frontend nesta rodada — escopo exclusivamente documental sobre trabalho já executado em ciclos anteriores da mesma frente. Ver `docs/adr/ADR-031-scheduled-pricing-orchestration.md`, revisões `1.6`/`1.7`, `docs/05f-pricing.md`, versão `1.37`, `ROADMAP.md`, revisão `1.64`, e `docs/log.md`. |
| 2.18 | **Incremento P13.4 — Agendamento Automático via `pg_cron`/`pg_net`/Vault, implementado e validado manualmente (2026-08-18, mesmo dia), a pedido explícito de Fabrício.** Linhas "Pricing / Market Data", "Próximo passo" e "Handoff vigente" reescritas: migration `3910` habilita `pg_cron`/`pg_net` e cria o Job único `ptax-fx-refresh-weekdays` (segunda a sexta, `22:00 UTC`/`19:00 America/Sao_Paulo`), comando chamando `net.http_post` diretamente com URL/`apikey` resolvidos por nome no Vault (nunca valor literal); chamada controlada real pelo mesmo caminho confirmou `HTTP 200`/`COMPLETED` (run `5ee0b0c5-0d08-4889-908d-9d288123e1ae`, `inserted=0`/`unchanged=7`/`divergent=0`/`invalid=0`). Segurança confirmada (schema `cron`/Vault bloqueados a papéis client-facing). **P13.4 implementado e validado manualmente — P13 como um todo permanece em andamento, aguardando a primeira execução autônoma do Cron (prevista `2026-08-19`, `22:00 UTC`) antes de qualquer encerramento formal.** Nenhum código JustTCG ou alteração de frontend nesta rodada. Ver `docs/adr/ADR-031-scheduled-pricing-orchestration.md`, revisão `1.8`, `docs/05f-pricing.md`, versão `1.38`, e `docs/log.md`. |
| 2.19 | **Incremento P13 — Orquestração Programada de Pricing formalmente encerrado (2026-08-19), a pedido explícito de Fabrício: antecipação da validação autônoma do P13.4.** Linhas "Pricing / Market Data", "Próximo passo" e "Handoff vigente" reescritas: `pg_cron 1.6.4` confirmado sem overload de timestamp absoluto para one-shot (tentativa real retornou `ERRO 22023`); fallback do próprio pedido usado — Job temporário `ptax-fx-refresh-oneoff-20260819-0027utc`, protegido por data fixa na própria expressão cron (`27 0 19 8 *`), comando idêntico ao permanente (nunca tocado), disparado pelo próprio `pg_cron` sem chamada manual, evidência completa correlacionada (`cron.job_run_details`→`net._http_response`→`pricing_sync_run`→`pricing_sync_run_call`, `HTTP 200`, zero run ativo residual, reexecução idempotente sem duplicidade) e removido logo após a auditoria. **P13.4 e P13 formalmente encerrados.** Próxima frente comprometida: Collection. Nenhum código JustTCG ou alteração de frontend nesta rodada. Ver `docs/adr/ADR-031-scheduled-pricing-orchestration.md`, revisão `1.9`, `docs/05f-pricing.md`, versão `1.39`, `ROADMAP.md`, revisão `1.65`, e `docs/log.md`. |
| 2.20 | **Correção de contradição objetiva + encerramento formal do Incremento P14.4.4 (2026-08-20), a pedido explícito de Fabrício.** Campo "Última revisão" corrigido (estava desatualizado em `2.15` desde antes da revisão `2.16`, sem nunca ter sido sincronizado com a tabela de Revision History real). Linhas "Pricing / Market Data", "Próximo passo" e "Handoff vigente" corrigidas: o texto "P13 encerrado, próxima frente é Collection" nunca foi atualizado ao longo de todo o Incremento P14 (condição comercial da JustTCG satisfeita — P14.1 —, descoberta+persistência em lotes — P14.2/P14.3 —, expansão de cobertura por ondas com matching canônico por Set+número — P14.4.1–P14.4.4). **P14.4.4 formalmente encerrado nesta rodada** — regra de matching reescrita (Set externo `CONFIRMED` + `collector_number` normalizado, nome nunca decide), reparo real (run `66c9e878-2469-453c-86ab-e31875f68f79`, 54 avaliados/53 promovidos, 316 produtos/316 observações, único `NOT_FOUND` restante `BASE1` Machamp `#008`), migration `3920` (autoridade temporal de `confirmed_at` transferida ao servidor) e migration `3921` (saneamento do mapping legado `ext-teste`, `external_card_id` canônico comprovado por 3 requisições controladas). **P14 como um todo permanece em andamento** — próximo passo real, por reconciliação da documentação existente: P14.4.3. **Collection continua sendo a próxima frente comprometida**, início ainda dependente de decisão explícita de Fabrício. Nenhuma migration/código alterado nesta rodada — escopo exclusivamente documental. Ver `docs/05f-pricing.md`, versão `1.48`, `docs/adr/ADR-029-pricing-domain-model.md`, revisão `1.31`, `ROADMAP.md`, revisão `1.66`, e `docs/log.md`. |
