# STD-001 — Database Standards

| Campo | Valor |
|--------|-------|
| **Documento** | STD-001 |
| **Título** | Database Standards |
| **Versão** | 1.42 |
| **Status** | Aprovado |
| **Objetivo** | Definir os padrões permanentes utilizados na modelagem e implementação do banco de dados do Project Mimikyu. |
| **Escopo** | Todas as entidades, tabelas, relacionamentos e scripts SQL do projeto. |

---

# Purpose

Este documento define os padrões oficiais de banco de dados do Project Mimikyu.

Toda decisão relacionada à modelagem deverá obedecer às regras descritas neste documento.

Este documento representa a "Constituição" do banco de dados.

---

# 1. Technical Language

Toda a modelagem será desenvolvida utilizando inglês.

Inclui:

- nomes de tabelas;
- colunas;
- constraints;
- índices;
- funções;
- triggers;
- views.

Comentários e cabeçalhos explicativos dentro dos scripts SQL (descrição, regras de negócio, autor, data) são escritos em **português**, como o restante da documentação do projeto — apenas os identificadores técnicos (nomes de objetos do banco) seguem o inglês. Ver Seção 10 (Migration Standards) para o modelo de cabeçalho.

---

# 2. Naming Conventions

O banco de dados é tratado como um produto do projeto, não apenas como suporte da aplicação — por isso a nomenclatura abaixo é uma convenção permanente e única para todo o projeto, nunca misturando estilos entre tabelas ou módulos.

## Tabelas

Nome no singular, em inglês, minúsculas, no formato `snake_case`.

Exemplos: `game`, `expansion`, `set`, `card`, `collection`, `collection_item`.

## Colunas

Minúsculas, no formato `snake_case`. Nunca `camelCase`, `PascalCase` ou notação húngara.

Proibido: `gameId`, `GameID`, `idGame`.

Correto: `game_id`.

## Colunas Reservadas Comuns

`id`, `status`, `created_at`, `updated_at`, `deleted_at`, `created_by`, `updated_by`, `deleted_by` (ver Seção 4 — Audit Model) possuem significado padronizado em todo o projeto e não devem ser reutilizadas com outro propósito.

## Palavras Reservadas do SQL

Quando o nome conceitual de uma entidade colide com uma palavra reservada do SQL (ex.: `SET` no PostgreSQL), a tabela física deve adotar um nome qualificado que evite a ambiguidade — mesmo que o conceito de domínio continue com seu nome original na documentação e na aplicação.

Exemplo: o conceito **Set (Set)** — ver `04-domain-model.md` — é implementado fisicamente como a tabela `card_set`.

---

# 3. Data Types

## Tipos de Texto

- `VARCHAR(n)` — quando houver um limite de negócio claro e conhecido (ex.: um código com tamanho máximo definido).
- `TEXT` — quando o conteúdo for naturalmente livre, sem limite de negócio conhecido.

Não adotaremos limites arbitrários por tradição (ex.: `VARCHAR(255)` em todos os campos de texto, sem relação com uma regra de negócio real).

## Identificadores Técnicos

`UUID` para toda chave primária técnica (ver Seção 5 — Primary Keys).

## Datas de Auditoria

`TIMESTAMPTZ` (timestamp com fuso horário) para todos os campos de data/hora de auditoria (`created_at`, `updated_at`, e demais quando existirem). Nunca armazenar apenas data local ou horário sem fuso.

## Colunas `JSONB` de `metadata`

Quando uma entidade tiver uma coluna `metadata JSONB` para guardar informação adicional de uma fonte externa (ex.: `card_set_external_reference.metadata`, `card_external_reference.metadata`), **`metadata` nunca deve duplicar um atributo que já existe em uma coluna relacional** — nem da própria tabela, nem de uma tabela relacionada acessível por `JOIN`. `metadata` existe apenas para propriedades específicas da fonte externa, sem equivalente relacional, e que não justificam uma coluna própria.

Nasceu de um caso real: o cadastro de `MEP` em `card_set_external_reference` inicialmente guardou em `metadata` quatro campos (`official_code`, `external_name`, `release_date`, `card_count_at_registration`) — três já existiam como colunas relacionais (`card_set.code`, `card_set.name`, `card_set.release_date`, acessíveis por `JOIN`) e o quarto (`card_count_at_registration`) ficaria desatualizado rapidamente, por natureza (o `card_set.total_set_size` já é a fonte de verdade para a contagem atual, ver `05-modelo-de-dados.md`, seção "Set", "Migration `265`–`268`"). Padronizado para `metadata = '{}'::JSONB`, mesmo padrão já usado por todos os demais registros da tabela.

---

# 4. Audit Model

## Padrão Mínimo

Toda entidade de negócio deverá possuir obrigatoriamente:

```text
id (UUID)
created_at (TIMESTAMPTZ)
updated_at (TIMESTAMPTZ)
```

`updated_at` deve ser atualizado automaticamente pelo banco (ex.: função/trigger reutilizável), sem depender de aplicações ou importadores lembrarem de alterá-lo manualmente.

## Campos Adicionais (sob demanda)

Os campos `created_by` e `updated_by` **não** são adicionados por padrão a todas as tabelas. Só devem ser adicionados quando houver necessidade real: múltiplos administradores, edição colaborativa, necessidade concreta de rastrear alterações, ou governança sobre o catálogo (Princípio da Simplicidade Inicial — ver AP-004).

`deleted_at`/`deleted_by` seguem a mesma lógica de "sob demanda" — ver Seção 8 (Logical Delete).

---

# 5. Primary Keys

Toda tabela possui uma chave primária técnica chamada `id`, do tipo UUID (ver Seção 4 — Audit Model).

O `id` é a identidade técnica permanente do registro: não deve ser reaproveitado, reordenado, nem carregar significado de negócio.

Chaves de negócio (ex.: código do Set; Set + Número da Card) representam a identidade de domínio (ver `04-domain-model.md`) e podem receber restrições de unicidade próprias, mas não substituem o `id` técnico como chave primária da tabela.

## Versão do UUID

Para o MVP, utiliza-se **UUID v4**, gerado pelo próprio banco (ex.: `gen_random_uuid()`, extensão `pgcrypto` no PostgreSQL). UUID v7 (com ordenação temporal) é preferível quando a camada tecnológica oferecer suporte adequado, mas não é um requisito do MVP — essa escolha é puramente técnica, não afeta o domínio, e pode ser revista futuramente sem impacto conceitual.

## Identidade de Negócio (code)

Além do `id` técnico, uma entidade pode possuir um `code`: um identificador curto, estável e legível, usado por pessoas, integrações ou regras editoriais (ex.: `game.code = POKEMON`; `card_set.code = ME2.5`). O `code` nunca substitui o `id` como chave primária, mas normalmente recebe uma restrição de unicidade própria.

### Código Internacional, Nome Localizável

O `code` é editorial e internacional — não muda entre idiomas (ex.: a Expansion `SV` continua sendo `SV` em qualquer idioma). O `name` pode ser localizado futuramente, quando houver necessidade concreta, seguindo o mesmo padrão já estabelecido para conteúdo editorial em Card Translation (ver `04-domain-model.md` e ADR-007). Este padrão — código internacional, nome localizável — tende a se repetir em todo o catálogo editorial.

---

# 6. Foreign Keys

Toda chave estrangeira segue o padrão `<entidade_referenciada>_id`, referenciando a coluna `id` (UUID) da tabela relacionada.

Exemplos: `game_id` (em `expansion`), `set_id` (em `card`), `card_id` (em `card_finish`, `collection_entry`).

---

# 7. Indexes

*Documentação pendente.*

---

# 8. Logical Delete

A exclusão lógica generalizada (`deleted_at` em todas as tabelas) **não** é adotada por padrão. Soft delete generalizado aumenta a complexidade do sistema: todas as consultas precisam ignorar registros excluídos; índices e restrições de unicidade ficam mais difíceis; relacionamentos podem manter dados aparentemente ativos; erros de implementação podem fazer registros "apagados" reaparecerem.

Exclusão lógica (`deleted_at`/`deleted_by`) só deve ser aplicada onde houver necessidade concreta de restauração ou preservação histórica.

Para o catálogo editorial, o padrão recomendado é um campo `status`, representando um estado de negócio — não uma exclusão:

```text
status: ACTIVE, INACTIVE
```

ou estados específicos do domínio, quando fizerem sentido:

```text
status: ANNOUNCED, RELEASED, CANCELLED
```

Isso não é soft delete — é estado de negócio, com semântica própria de cada entidade.

---

# 9. SQL Standards

Regras estruturais importantes devem existir no próprio banco de dados, e não apenas na camada de aplicação. A aplicação oferece mensagens amigáveis; o banco garante que dados inválidos não sejam persistidos, mesmo em caso de falha ou bypass da aplicação.

Exemplos de restrições implementadas no banco:

- unicidade de códigos de negócio (ex.: `game.code` não pode se repetir — `UNIQUE`);
- obrigatoriedade de relacionamentos hierárquicos (ex.: uma Expansion deve pertencer a um Game; uma Card deve pertencer a um Card Set — `NOT NULL` + `FOREIGN KEY`);
- valores numéricos com regra de negócio (ex.: uma ordem de lançamento deve ser positiva — `CHECK`);
- formato de campos de negócio (ex.: um `code` deve seguir um padrão normalizado — `CHECK` com expressão regular).

## Row Level Security (RLS)

Toda tabela do schema `public` deve ter Row Level Security habilitado no momento da criação (`ALTER TABLE ... ENABLE ROW LEVEL SECURITY`), já que esse schema é exposto pela API automática do Supabase (ver `01-technical-identity.md`) a clientes anônimos ou autenticados. Políticas de acesso específicas são criadas posteriormente, apenas quando houver necessidade concreta — até lá, o RLS habilitado sem políticas impede qualquer leitura ou escrita externa à administração direta do banco.

**Padrão de performance para função de checagem usada em policy (2026-08-14).** Toda função usada dentro de uma política de RLS (ex.: `is_admin()`) deve ser `STABLE`, nunca `VOLATILE` — o planner do Postgres só pode cachear o resultado por linha quando a função é marcada como estável; `VOLATILE` força reavaliação por linha varrida, mesmo quando o resultado não muda dentro da mesma transação. Toda referência a essa função dentro do corpo de uma política deve ser escrita como subconsulta escalar entre parênteses — `(select is_admin())`, nunca `is_admin()` solto — para que o planner resolva o `InitPlan` uma única vez por execução da query, em vez de uma vez por linha (otimização documentada oficialmente pelo Supabase para RLS; sem o parêntese, o ganho de `STABLE` não se realiza da mesma forma em todos os planos de execução). Aplicado retroativamente em 24 policies do Catálogo Editorial que já existiam com o padrão antigo (`is_admin()` solto, função `VOLATILE`) — ver Query `2134`, CONFIRMADO EXECUTADO E VALIDADO (segurança e performance), `docs/log.md`, 2026-08-14. Toda política nova a partir desta revisão já nasce no formato `(select <função>())`.

**Least privilege em `GRANT`s de tabela (2026-08-15).** Além de `SELECT`/`INSERT`/`UPDATE`/`DELETE` concedidos apenas quando uma tela ou função realmente precisa (nunca `ALL PRIVILEGES` por padrão), as roles de aplicação (`anon`/`authenticated`) nunca devem reter os privilégios `TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` que o Postgres concede por padrão a qualquer role com algum `GRANT` na tabela — nenhum desses quatro é necessário para o uso normal via PostgREST/RPC, e cada um representa uma superfície de ataque desnecessária (`TRUNCATE` apaga a tabela inteira sem passar por RLS de linha; `TRIGGER`/`REFERENCES` permitem alterar constraints/triggers da tabela). Revogados explicitamente (`REVOKE TRUNCATE, REFERENCES, TRIGGER, MAINTAIN ON <tabela> FROM anon, authenticated;`) nas 24 tabelas do Catálogo Editorial — ver Query `2147`, CONFIRMADO EXECUTADO, `docs/log.md`, 2026-08-15. Toda tabela nova a partir desta revisão já nasce com esses quatro privilégios revogados das roles de aplicação, junto com o `GRANT` mínimo necessário.

## Schema `internal` — Rotinas Não Expostas pela API

Além do schema `public` (exposto pela API automática do Supabase, ver acima), o projeto reserva o schema `internal` para rotinas auxiliares `SECURITY DEFINER` que nunca devem ser um contrato RPC público — a lógica de validação/persistência real que uma ou mais funções administrativas reutilizam internamente, não algo que o frontend chama diretamente. Formalizado a partir de `ADR-023-catalog-editorial-write-authorization.md`, mas não é exclusivo do Catálogo Editorial — é a convenção do projeto, a partir de agora, para qualquer módulo que precise de lógica interna não exposta.

**Finalidade.** Concentrar lógica que precisa rodar com os privilégios elevados de uma função `SECURITY DEFINER`, mas que não representa, por si só, uma operação que o frontend deveria poder invocar — evita que essa lógica fique duplicada entre várias funções públicas, e evita depender de convenção de nome (ex.: um prefixo `_`) para ficar fora de alcance, o que **não é** um mecanismo de controle de acesso em PostgreSQL.

**Objetos permitidos.** Apenas funções e procedures `SECURITY DEFINER` de uso interno — nunca tabelas, views, ou qualquer objeto que precisaria ser consultado diretamente por um cliente. Um dado que o frontend precisa ler sempre passa por uma função pública em `public` ou por RLS em `public`, nunca por acesso direto a algo dentro de `internal`.

**Nunca exposto pela API.** `internal` não deve constar entre os schemas expostos pela API do Supabase (por padrão, apenas `public`/`graphql_public`) — PostgREST não alcança objetos fora dos schemas configurados como expostos, independentemente de `GRANT`. Esta é a primeira camada de proteção, não a única.

**Revogação explícita de privilégios.** Toda função em `internal` revoga `EXECUTE` de `PUBLIC`, `anon` e `authenticated` explicitamente (`REVOKE EXECUTE ON FUNCTION internal.<função>(...) FROM PUBLIC, anon, authenticated;`) — defesa em profundidade, que não depende só do isolamento de schema.

**Critério de `GRANT`.** Uma função em `internal` só é chamada por outra função `SECURITY DEFINER` do projeto (chamada interna, dentro do mesmo contexto de execução) — nunca recebe `GRANT EXECUTE` para `authenticated`, `anon` ou `service_role` diretamente. Se uma role de aplicação precisa acionar a lógica, ela sempre passa por uma função pública em `public`, nunca chama `internal` diretamente.

**`search_path` explícito e seguro.** Toda função em `internal` declara `SET search_path = ''`, mesmo padrão já exigido de qualquer função `SECURITY DEFINER` do projeto — nunca depende do `search_path` de sessão de quem a invoca.

**Referências qualificadas por schema.** Toda referência a tabela, view, função ou tipo dentro de uma função em `internal` usa o nome totalmente qualificado (ex.: `public.card`, `public.card_set`), nunca um nome que dependeria de `search_path` para ser resolvido.

**Funções RPC públicas vs. rotinas internas reutilizáveis.** Uma função pública (schema `public`, exposta via RPC do PostgREST) é um contrato de API — parâmetros, mensagens de erro e efeitos colaterais (ex.: o que ela audita) fazem parte desse contrato, e podem diferir entre funções públicas distintas mesmo quando compartilham a mesma lógica de persistência. Uma rotina em `internal` nunca é, ela mesma, um contrato de API — existe só para ser reaproveitada por uma ou mais funções públicas, evitando duplicar validação e proteção de campos sensíveis entre elas. Uma função pública pode chamar várias rotinas internas; uma rotina interna nunca deve assumir que só existe uma função pública chamando-a.

Precedente: `internal.write_card()` (Catálogo Editorial — Escrita e Ingestão, ver `ADR-023`), reutilizada por `admin_create_card()`, `admin_update_card()` e, em `ADR-024`, por `admin_confirm_catalog_import()`.

---

# 10. Migration Standards

## Ferramenta

Migrations são sempre executadas via SQL — nunca pelo menu visual de criação de tabelas (Table Editor) do Supabase. O menu visual é adequado para protótipos, mas o Project Mimikyu depende de constraints, triggers, funções, índices e checks — recursos mais bem organizados, documentados e rastreáveis em SQL.

A execução em si pode ocorrer por dois caminhos, ambos igualmente válidos: diretamente pelo ambiente de desenvolvimento autorizado (hoje, Claude Code integrado ao Supabase via MCP, usando `apply_migration`/`execute_sql`) ou, excepcionalmente, pelo SQL Editor do Supabase. A governança descrita nesta seção — cabeçalho oficial, numeração, validação, versionamento em `database/` — é a mesma independentemente de qual caminho executou a Query; nenhum dos dois dispensa o registro em `database/` nem a validação. Este Standard não vincula permanentemente o processo a uma ferramenta específica — se o ambiente de desenvolvimento autorizado mudar no futuro, só esta nota precisa ser atualizada.

A ideia inicial era organizar migrations em arquivos numerados (`000_initial_database.sql`, `001_create_game.sql`, ...). Na prática, essa organização evoluiu para **Queries nomeadas e numeradas dentro do próprio fluxo de execução** (SQL Editor ou ambiente de desenvolvimento autorizado), seguindo a mesma lógica sequencial.

## Padrão Oficial de Queries SQL do Project Mimikyu

A sequência de Queries — independentemente de qual ferramenta as executou — é tratada como o diário de construção do Project Mimikyu: qualquer pessoa deve conseguir acompanhar a evolução do banco apenas lendo essa sequência, mesmo anos depois.

Toda Query deve possuir:

- **Número** — dentro da faixa oficial correspondente (ver "Faixas de Numeração", abaixo);
- **Nome** — curto, descritivo, em inglês (ex.: `Create Game table`);
- **Descrição** — em português, explicando o objetivo da Query;
- **Cabeçalho** — bloco de comentário no início do script (ver modelo abaixo);
- **SQL** — o script propriamente dito;
- **Validação** — uma ou mais consultas que confirmam que a execução funcionou corretamente (ver "Validação", abaixo).

### Faixas de Numeração (Esquema Legado — Congelado)

| Faixa | Finalidade |
|-------|------------|
| 000–099 | Infraestrutura do banco (extensões, funções compartilhadas, configurações globais) |
| 100–199 | Catálogo Editorial |
| 200–299 | Evoluções e migrations complementares do Catálogo Editorial — esquema legado (inclui as migrations `250`–`277`, já executadas) |
| 300–399 | (histórico — sem Queries executadas) |
| 400–499 | (histórico — sem Queries executadas) |
| 500–599 | (histórico — sem Queries executadas) |
| 600–699 | (histórico — sem Queries executadas) |
| 700–799 | (histórico — sem Queries executadas) |
| 800–899 | Dados iniciais (Seeds) — das entidades numeradas em 000–299 |
| 900–999 | Validações e consultas — das entidades numeradas em 000–299 |

Deixar intervalos entre os grupos permitiu inserir novas migrations sem perder a organização. Exemplo real (entidade Game): `000 - Enable pgcrypto`, `001 - Create updated_at function`, `100 - Create Game Table`, `101 - Create Game Trigger`, `800 - Seed Game`, `900 - Validate Game`.

**Todo o intervalo `000–999` está congelado para novas implementações** — cobre exclusivamente as Queries já executadas do Catálogo Editorial, suas evoluções (`200`–`277`) e a infraestrutura inicial. Nenhuma faixa acima, usada ou não, está reservada para futuros módulos (Coleções, Inventário, Aquisições, Armazenamento, Analytics, Views ou qualquer outro) — os nomes que aparecem nesta tabela refletem apenas o planejamento original, não uma reserva vigente. Todo módulo novo, a partir de Identidade e Acesso, segue o Modelo Modular de Numeração abaixo. **Importante**: "congelado para novas implementações" significa que nenhuma *nova entidade* ganha um bloco de dez nessa faixa — ela permanece legitimamente aberta para *evoluções de controle de acesso/permissão de entidades já existentes* do Catálogo Editorial (precedente: `250`/`253`/`254`/`272`, e mais recentemente `273`–`277`, que adicionam autorização admin-only e a coluna de logo em `card_set` — ver `ADR-022`). Uma evolução dessa natureza nunca abre um novo bloco de dez por entidade; recebe o próximo número sequencial livre na faixa.

### Modelo Modular de Numeração (a partir de 4 dígitos)

Todo módulo novo recebe um milhar inteiro (`X000`–`X999`), sem colisão com o esquema legado ou com outros módulos:

| Faixa (relativa ao milhar `X000`) | Finalidade |
|---|---|
| `X000–X699` | Estrutura do módulo: tabelas, triggers, functions, Storage, e evoluções futuras — inclusive extensões conceitualmente distintas que pertençam ao mesmo módulo |
| `X700–X799` | Seeds |
| `X800–X899` | Validações |
| `X900–X999` | Reserva |

Dentro de `X000–X699`, cada entidade persistente continua ocupando um **bloco de dez** (`X0` cria a tabela, `X1` cria o trigger, `X2`/`X3`... reservados para evoluções da mesma entidade) — o mesmo princípio do esquema legado, descrito a seguir. Objetos sem tabela própria dentro de `X000–X699` (funções administrativas, funções internas, políticas transversais) não ocupam um bloco de dez — recebem o próximo número sequencial livre, associados ao objeto correspondente pelo nome e pelo cabeçalho da Query, mesma flexibilidade já aplicada a Seed/Validação sem correspondência 1:1 (ver "Offset", abaixo).

**Alteração de entidade legada que só serve a um módulo novo.** Quando um módulo novo precisa alterar uma entidade já existente do esquema legado (ex.: adicionar uma coluna em `card`), e essa alteração não tem propósito independente do módulo novo — só existe para viabilizar a funcionalidade que está sendo construída —, a Query correspondente é numerada dentro do milhar do módulo novo, nunca na faixa legada (`000`–`299`), mesmo que a tabela alterada seja legada. O critério é o **propósito da mudança**, não a idade da tabela: se a alteração resolve uma necessidade do próprio Catálogo Editorial legado (ex.: `card_set.logo_storage_path`, `ADR-022`), permanece em `200`–`299`; se resolve uma necessidade de um módulo novo construído sobre entidades legadas (ex.: `card.is_active`, necessário apenas para `admin_deactivate_card()`/`admin_reactivate_card()`, `ADR-023`), numera-se no milhar do módulo novo.

**Offset `+700`/`+800`:** deixa de ser obrigatório no modelo modular. Quando uma Seed ou Validação corresponde diretamente a uma única entidade e o número resultante do deslocamento não colide com nada já numerado, ele pode ser usado como conveniência. Nos demais casos — funções sem tabela própria, triggers transversais, Storage, validações sem correspondência 1:1 — Seed e Validação recebem o **próximo número sequencial livre** dentro de suas faixas, associados ao objeto correspondente pelo nome e pelo cabeçalho da Query, não pela aritmética do número.

**Módulos definidos:** `1000–1999` = Identidade e Acesso, incluindo papéis e permissões futuros (permanecem no mesmo milhar, não abrem um novo). `2000–2999` = Catálogo Editorial — Escrita e Ingestão (`ADR-023`/`ADR-024`, ver subseção própria abaixo). `3000–3999` = Pricing (`ADR-029`, ver subseção própria abaixo). `4000–4999` = Pesquisa de Cartas (`ADR-030`, ver subseção própria abaixo). `5000–5084` = Collections (`ADR-011`/domínio de Coleções — ver `docs/05d-colecoes-e-usuarios.md`; nota: comprometido de fato desde 2026-08-30, esta seção não registrava explicitamente o módulo até esta revisão — gap de documentação corrigido aqui, sem renumerar nenhuma Query já `CONFIRMADO EXECUTADO`). `6000–6999` = Pokémon Catalog Foundation (`ADR-011` v1.2, ver subseção própria abaixo). Nenhum outro milhar está comprometido como decisão definitiva — novos módulos recebem milhar próprio quando efetivamente aprovados, não por reserva antecipada.

### Módulo: Catálogo Editorial — Escrita e Ingestão (`2000`–`2999`)

Comprometido nesta revisão como o próximo milhar do Modelo Modular de Numeração, cobrindo a implementação de `ADR-023-catalog-editorial-write-authorization.md` e `ADR-024-catalog-card-ingestion-strategy.md` — tratados como um único módulo de numeração, mesmo padrão já usado por Identidade e Acesso (`1000`–`1999`), que também cobre múltiplos ADRs do mesmo domínio.

Dentro de `2000–2699` (estrutura):

- `catalog_import_job` e `catalog_import_row` (`ADR-024`) são entidades persistentes — cada uma ocupa um bloco de dez, mesma regra do modelo modular.
- A alteração de `card.is_active` (`ADR-023`) é numerada aqui, não na faixa legada `200`–`299` — pela regra geral acima ("Alteração de entidade legada que só serve a um módulo novo"): `is_active` não tem propósito independente do Catálogo Editorial já existente, só viabiliza `admin_deactivate_card()`/`admin_reactivate_card()`.
- A criação do schema `internal` (Seção 9) é numerada aqui, por ser a primeira necessidade real que a motivou — módulos futuros que também usem `internal` reaproveitam o schema já existente, sem precisar de nova Query de criação.
- As demais funções deste módulo — `internal.write_card()`; `admin_create_game()`/`admin_update_game()`; `admin_create_expansion()`/`admin_update_expansion()`; `admin_create_card_set()`/`admin_update_card_set()`; `admin_create_card()`/`admin_update_card()`/`admin_deactivate_card()`/`admin_reactivate_card()`; a tabela de auditoria editorial (bloco de dez, por ser entidade persistente); e as funções de staging/confirmação de `ADR-024` (ex. `admin_confirm_catalog_import()`) — não têm tabela própria (exceto a auditoria) e recebem o próximo número sequencial livre dentro de `2000–2699`, pela regra geral de objetos sem tabela (acima).

`2700–2799`: Seeds — reservado por consistência com o modelo modular; nenhum dado inicial conhecido a semear neste módulo até o momento.

`2800–2899`: Validações — toda Query de validação estrutural e de dados deste módulo.

`2900–2999`: Reserva.

A escolha do número exato de cada Query específica dentro destas faixas acontece no momento da implementação, seguindo a ordem real em que cada peça for modelada e aprovada — mesmo princípio já registrado para o esquema legado (ver nota sobre Rarity/Card, na subseção seguinte).

### Módulo: Pricing (`3000`–`3999`)

Comprometido no Incremento P1 — Fundação Física de Pricing (2026-08-16), cobrindo a implementação de `ADR-029-pricing-domain-model.md` — ver `docs/05f-pricing.md` para o modelo lógico e físico completo do domínio. Estendido no Incremento P2 — Correspondência Externa de Pricing (2026-08-16, mesmo dia), no Incremento P3 — Auditoria Operacional de Sincronização (2026-08-16, mesmo dia), no Incremento P4 — Produto Externo de Pricing (2026-08-16, mesmo dia), no Incremento P5 — Série Histórica de Taxas de Câmbio (2026-08-16, mesmo dia), no Incremento P6 — Observação Histórica de Preço (2026-08-16, mesmo dia), que encerra a fundação física do domínio.

Dentro de `3000–3699` (estrutura), dez entidades foram implementadas — fundação física completa:

- `pricing_source` (`3000` tabela, `3001` trigger, `3002` grant de `service_role`, corrigindo uma omissão detectada na própria validação do incremento) — `CONFIRMADO EXECUTADO` (Incremento P1).
- `card_condition` (`3010`–`3019` tabela/trigger) — `CONFIRMADO EXECUTADO` (Incremento P1). **Nota de numeração, decisão explícita de Fabrício (2026-08-16):** `card_condition` é uma referência conceitualmente compartilhada e neutra, não exclusiva de Pricing (ver `docs/05f-pricing.md`) — sua posição dentro do milhar de Pricing registra apenas o ciclo que realizou sua primeira implementação física, não pertencimento de domínio. Um módulo próprio de "Referências Compartilhadas" foi cogitado (candidato `4000`–`4999`) e deliberadamente não criado neste incremento — permanece livre e não reservado, mesmo princípio geral desta seção (milhar comprometido só quando efetivamente aprovado). Entidades compartilhadas futuras não devem ser automaticamente numeradas em `3000`–`3999` só por este precedente; cada caso deve ser avaliado quando existir um conjunto real de entidades e responsabilidades que justifique um módulo próprio.
- `pricing_condition_mapping` (`3020`–`3029` tabela/trigger) — `CONFIRMADO EXECUTADO` (Incremento P1), referenciando `pricing_source` e `card_condition`.
- `pricing_set_mapping` (`3030`–`3039` tabela/trigger) — `CONFIRMADO EXECUTADO` (Incremento P2), correspondência `card_set` ↔ fonte externa, tabela vazia (nenhuma fonte homologada).
- `pricing_card_mapping` (`3040`–`3049` tabela/trigger) — `CONFIRMADO EXECUTADO` (Incremento P2), correspondência `card` ↔ fonte externa, tabela vazia. `service_role` recebeu apenas `SELECT` nas duas tabelas do Incremento P2 — capacidade de escrita deliberadamente adiada para um incremento futuro de sincronização. **Atualização (Query `3091`, Incremento P8, 2026-08-17):** `service_role` passou a ter também `INSERT` (nunca `UPDATE`/`DELETE`) em ambas — ver nota abaixo.
- `pricing_sync_run` (`3080`–`3089`: `3080` tabela + índices + RLS + grants, `3081` trigger) — `CONFIRMADO EXECUTADO` (Incremento P3), auditoria de alto nível de execuções de sincronização, tabela vazia. Índices reformulados por decisão explícita do incremento (composto `(pricing_source_id, started_at DESC)` + parcial `(pricing_source_id) WHERE status IN ('RECEIVED','PROCESSING')`, confirmados por `EXPLAIN (ANALYZE, BUFFERS)`, em vez do par genérico de índices isolados cogitado inicialmente). `service_role` recebeu `SELECT`/`INSERT` e `UPDATE` restrito por coluna (só campos operacionais do ciclo de vida — `id`/`pricing_source_id`/`run_type`/`triggered_by`/`started_at`/`created_at` permanecem inalteráveis), sem `GRANT DELETE`. **Atualização (Queries `3082`/`3083`, correção pós-P8, 2026-08-17):** coluna `confirmed_by UUID NOT NULL` (sem `FK`) acrescentada, validada por trigger `BEFORE INSERT` `SECURITY DEFINER`/`search_path=''` (`validate_pricing_sync_run_confirmed_by()`) contra `admin_user` — nunca por função RPC com parâmetro UUID livre, padrão que `ADR-021-administrative-role-model.md` já registra como avaliado e rejeitado; `EXECUTE` revogado de todos os papéis, inclusive `service_role` (disparo do trigger não depende desse privilégio). Ver nota abaixo.
- `pricing_sync_run_call` (`3090`–`3099`: `3090` tabela + RLS + grants, sem trigger — tabela append-only) — `CONFIRMADO EXECUTADO` (Incremento P3), log granular de chamadas HTTP, tabela vazia. Nenhum índice isolado em `sync_run_id` criado — a `UNIQUE (sync_run_id, sequence_number)` já cobre a leitura ordenada por `sequence_number`, confirmado por `EXPLAIN (ANALYZE, BUFFERS)`. `service_role` recebeu apenas `SELECT`/`INSERT` — `UPDATE`/`DELETE` confirmados bloqueados por teste real.
- `pricing_product` (`3050`–`3059`: `3050` tabela + índices + RLS + grants, `3051` trigger, `3052` índice adicional na FK `language_id`, corrigindo um achado do advisor de performance detectado na própria validação do incremento) — `CONFIRMADO EXECUTADO` (Incremento P4), produto/impressão reportado pela fonte, tabela vazia. Nenhum índice isolado em `pricing_card_mapping_id` criado — já coberto pelo prefixo da própria `UNIQUE (pricing_card_mapping_id, external_product_id)`. `service_role` recebeu `SELECT`/`INSERT` e `UPDATE` restrito por coluna (`source_printing_label`/`language_status`/`language_id`/`card_variant_id`/`is_active`) — mais restritivo que P1–P3: este incremento revoga explicitamente `TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` de `service_role` (privilégios que `pg_default_acl` concede por padrão em tabelas criadas por `postgres`, deixados intocados nos incrementos anteriores).
- **Correção retroativa (Query `3053`, mesmo ciclo do Incremento P4):** auditoria somente leitura confirmou que as sete tabelas dos Incrementos P1–P3 (`pricing_source`, `card_condition`, `pricing_condition_mapping`, `pricing_set_mapping`, `pricing_card_mapping`, `pricing_sync_run`, `pricing_sync_run_call`) nunca tiveram `TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` revogados de `service_role` — mesmo default de `pg_default_acl` corrigido em `pricing_product`, agora estendido retroativamente. Uma única migration, sete tabelas, sem alteração de RLS/policies/estrutura/índices/dados; `SELECT`/`INSERT`/`UPDATE` (inclusive os grants de coluna restritos de `pricing_sync_run`) preservados integralmente, validados por `has_table_privilege` e smoke test transacional real.
- **Concessão de escrita (Query `3091`, Incremento P8, 2026-08-17):** `GRANT INSERT` (só `INSERT`, nunca `UPDATE`/`DELETE`) a `service_role` em `pricing_set_mapping`/`pricing_card_mapping`, resolvendo a pendência explícita registrada desde o Incremento P2. Numericamente após `3090`, mas conceitualmente não pertence ao bloco de dez de nenhuma entidade específica — mesmo padrão de correção transversal já usado em Query `3053`.
- **Validação administrativa sem RPC de UUID livre (Queries `3082`/`3083`, correção pós-P8, 2026-08-17):** `service_role` nunca teve `SELECT` em `admin_user`, de propósito (`ADR-021`) — o conector de P8 fazia esse `SELECT` diretamente, causando falha real em produção. Corrigido dentro do bloco de dez de `pricing_sync_run` (evolução da própria entidade, não correção transversal): `3082` adiciona `pricing_sync_run.confirmed_by`; `3083` cria `validate_pricing_sync_run_confirmed_by()` (`SECURITY DEFINER`, `search_path=''`, `RETURNS TRIGGER` — impede chamada direta; `EXECUTE` revogado de `PUBLIC`/`anon`/`authenticated`/`service_role`) e o trigger `BEFORE INSERT` correspondente. Alternativa inicialmente proposta (função `is_admin_user(uuid)` com `EXECUTE` para `service_role`) identificada como conflitante com `ADR-021` antes de qualquer execução e descartada por decisão explícita de Fabrício — nenhuma revisão do `ADR-021` foi necessária.

- `pricing_fx_rate` (`3060`: tabela + `UNIQUE` reordenada servindo como índice único + RLS + policy + grants; sem trigger, tabela append-only sem `updated_at`) — `CONFIRMADO EXECUTADO` (Incremento P5), série histórica de taxas de câmbio, tabela vazia. `UNIQUE` de unicidade semântica reordenada para `(from_currency, to_currency, rate_source_code, rate_date)` em relação à hipótese original de `05f-pricing.md` — a própria constraint serve como índice para os três padrões de consulta reais, dispensando um índice adicional. `service_role` recebeu apenas `SELECT`/`INSERT`, com `UPDATE`/`DELETE`/`TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` revogados explicitamente — mesmo padrão restritivo de `pricing_product`/Query `3053`, não confiando nos defaults de `pg_default_acl`. `authenticated` apenas `SELECT` via `pricing_admin_select`, `anon` sem privilégios — decisão que corrige a hipótese original de leitura pública cogitada em `05f-pricing.md`.

- `pricing_observation` (`3070`: tabela + identidade market-aware `UNIQUE NULLS NOT DISTINCT` + índices + RLS + policy + grants; sem trigger, tabela append-only sem `updated_at`) — `CONFIRMADO EXECUTADO` (Incremento P6), observação histórica de preço, décima e última entidade física do domínio, tabela vazia. Identidade corrigida em relação à hipótese original de `05f-pricing.md`: `UNIQUE NULLS NOT DISTINCT (pricing_product_id, condition_id, price_type, currency_code, market_label, observed_at)` em vez de `UNIQUE (pricing_product_id, condition_id, price_type, observed_at)` — correção obrigatória exigida pelo próprio pedido do incremento, para não colidir fatos distintos de mercados/moedas diferentes no mesmo instante; suporte a `NULLS NOT DISTINCT` confirmado em PostgreSQL 17.6 antes do DDL. Três índices (`ix_pricing_observation_latest_lookup`, `ix_pricing_observation_condition_id`, `ix_pricing_observation_sync_run_id` parcial) — nenhum índice parcial dedicado a "Valor Brasil" foi criado, decisão validada empiricamente contra 80.000 linhas sintéticas mostrando o índice geral já suficiente. Decisão explícita de não particionar neste incremento. `service_role` recebeu apenas `SELECT`/`INSERT`, com `UPDATE`/`DELETE`/`TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` revogados explicitamente — mesmo padrão restritivo de `pricing_product`/`pricing_fx_rate`. `authenticated` apenas `SELECT` via `pricing_admin_select`, `anon` sem privilégios. `pricing_product_id`/`condition_id` em `ON DELETE RESTRICT`, `sync_run_id` em `ON DELETE SET NULL`, ambos validados fisicamente.

**Fundação física de Pricing completa — dez de dez entidades `CONFIRMADO EXECUTADO`.** Domínio **operacional em produção** desde o Incremento P14.1 (`JUSTTCG.is_active = TRUE`, 2026-08-19): refresh de preços automático via dispatcher durável por Set, PTAX ativa como fonte de câmbio, módulo administrativo "Valor de Mercado" completo (11 rotas) — ver "Estado Atual do Pricing — Síntese Executiva", início de `docs/05f-pricing.md`, para o detalhamento vigente. Este parágrafo registra apenas a numeração `3000`–`3999` e o estado da fundação física (P1–P6); não é o lugar para o status operacional corrente do domínio.

`3700–3799`: Seeds — `3700` `CONFIRMADO EXECUTADO` (Incremento P7, 2026-08-17): primeira linha de `pricing_source`, `JUSTTCG`, homologada para piloto técnico (Decisão A aprovada; Decisão B/idioma inconclusiva), `is_active = FALSE` (não produção, plano comercial pendente — detalhe completo em `ADR-029`, revisão `1.9`). `3701`/`3702` `CONFIRMADO EXECUTADO` (Incremento P8, 2026-08-17): cinco condições canônicas (`card_condition`) confirmadas pela prova técnica real e o de-para correspondente para `JUSTTCG` (`pricing_condition_mapping`) — detalhe completo em `ADR-029`, revisão `1.10`. Demais faixas de `3700`–`3799` seguem livres.

`3800–3899`: Validações — `3800` executada no Incremento P1 (validação consolidada de 12 itens); `3810` reservada para o Incremento P2 (validação consolidada de 15 itens); `3820` reservada para o Incremento P3 (validação consolidada de 16 itens funcionais + 4 planos de execução `EXPLAIN (ANALYZE, BUFFERS)`); `3830` reservada para o Incremento P4 (validação consolidada de 19 itens funcionais + 4 planos de execução `EXPLAIN (ANALYZE, BUFFERS)`); `3840` reservada para o Incremento P5 (validação consolidada de 22 itens funcionais + 3 planos de execução `EXPLAIN (ANALYZE, BUFFERS)`); `3850` reservada para o Incremento P6 (validação consolidada de 32 itens funcionais + 5 planos de execução `EXPLAIN (ANALYZE, BUFFERS)` sobre 80.000 linhas sintéticas) — todas executadas de forma transacional com `ROLLBACK`, sem migration física associada, mesmo padrão de "número de diário", não uma Query aplicada ao schema.

`3900–3999`: `3900` `CONFIRMADO EXECUTADO` (Incremento P10 — Projeção Cambial PTAX, 2026-08-17): `public.pricing_observation_brl_equivalent`, view (`security_invoker = true`, sem trigger, sem grant de escrita) que projeta observações `currency_code = 'USD'` para equivalente em BRL via `LEFT JOIN LATERAL` contra a última `pricing_fx_rate` (`BCB_PTAX`, `rate_date <=` data UTC de `observed_at`) — não pertence ao bloco de dez de nenhuma entidade específica (transversal a `pricing_observation`/`pricing_fx_rate`, mesmo padrão de Query `3053`/`3091`), primeiro uso real desta faixa antes reservada. Detalhe completo em `ADR-029`, revisão `1.15`, e `05f-pricing.md`, seção "Incremento P10".

`3901` `CONFIRMADO EXECUTADO` (Incremento P11 — Contrato Seguro de Leitura de Preços por Card, 2026-08-17): `public.get_card_pricing_snapshot(p_card_id uuid)`, função `SECURITY DEFINER STABLE` (`search_path = ''`, sem tabela própria) — mesmo padrão de segurança de `search_cards()`/`search_card_filter_options()` (`ADR-030`, módulo `4000`–`4999`): verificação explícita de `auth.uid() IS NOT NULL`, nunca `is_admin()`; `REVOKE ALL FROM PUBLIC, anon` + `GRANT EXECUTE TO authenticated` (`service_role` deliberadamente sem `EXECUTE`, já lê as tabelas base direto). Primeiro caminho de leitura de Pricing para usuário autenticado não-administrador — contorna `pricing_admin_select`/`card_condition_admin_select` por desenho, mas só através de uma projeção curada (snapshot mais recente por produto/condição/tipo de preço/mercado, nunca `external_product_id`/`raw_payload`/histórico completo), com filtros de negócio (mapping `CONFIRMED`, produto ativo, fonte ativa) aplicados no corpo da função. Registrada em `3900`–`3999` (não em `4000`–`4999`) por pertencer ao domínio Pricing, não à Pesquisa de Cartas — mesmo critério geral desta seção (a faixa segue o domínio do dado, não o padrão de segurança usado).

`3902` `CONFIRMADO EXECUTADO` (mesmo incremento, mesmo dia): `ix_pricing_observation_snapshot_lookup (pricing_product_id, condition_id, price_type, market_label, observed_at DESC)` em `pricing_observation` — índice adicional motivado por achado real de `EXPLAIN (ANALYZE, BUFFERS)` durante a validação da Query `3901`: `ix_pricing_observation_latest_lookup` (Query `3070`, Incremento P6) não cobre `market_label`, coluna adicional do agrupamento desta função; sem o índice novo, o `DISTINCT ON` caía num `Sort` externo em disco sobre todo o histórico de cada produto. Mesmo precedente de índice acrescentado após achado real de validação já usado na Query `3052` (Incremento P4). Não pertence ao bloco de dez de `pricing_observation` (evolução funcional da entidade motivada por um consumidor novo, não uma correção transversal — mas registrada aqui, ao lado da `3901`, por nascerem do mesmo incremento). Detalhe completo em `ADR-029`, revisão `1.16`, e `05f-pricing.md`, seção "Contrato Seguro de Leitura de Preços por Card (Incremento P11)".

`3903` `CONFIRMADO EXECUTADO` (Incremento P12 v4, 2026-08-18): `public.get_cards_pricing_summary(p_card_ids uuid[])`, variante em lote de `3901` — mesmo padrão de segurança (`SECURITY DEFINER STABLE`, `search_path = ''`, verificação explícita de `auth.uid() IS NOT NULL`, `REVOKE ALL FROM PUBLIC, anon` + `GRANT EXECUTE TO authenticated`), dois guards de validação de entrada adicionais (array vazio/nulo, mais de 100 elementos — ambos `ERRCODE 22023`). Elimina o N+1 servidor→Postgres que a primeira versão do endpoint batch do frontend ainda tinha (`get_card_pricing_snapshot` chamada uma vez por carta via `Promise.all`, N consultas reais): esta função resolve o lote inteiro numa única consulta SQL (`DISTINCT ON` por `card_id` + `LEFT JOIN LATERAL` PTAX), validado com `EXPLAIN (ANALYZE, BUFFERS)` em 100 `card_id` reais — 12,9ms, um único `Function Scan`, sem laço. Retorno deliberadamente mínimo (`card_id`, `has_pricing`, `brl_amount`, `fx_status`), sob regra de seleção fixa e determinística — condição NM, printing "Normal", `price_type` MARKET, pedido literal de Fabrício ("valor BRL padrão NM/Normal") — não um "melhor preço" heurístico nesta revisão específica; ausência de fallback entre printings corrigida na revisão seguinte (`3904`, abaixo). Detalhe completo em `ADR-029`, revisão `1.20`, e `05f-pricing.md`, seção "Painel de Preços da Carta (Incremento P12)".

`3904` `CONFIRMADO EXECUTADO` (Incremento P12 Rodada 8, correção pós-teste "Reverse-only", 2026-08-18): `public.get_cards_pricing_summary(p_card_ids uuid[])` recriada via `DROP FUNCTION` + `CREATE FUNCTION` (não `CREATE OR REPLACE` — a assinatura de retorno muda, nova coluna `printing_label`) — a `3903` não foi editada. Corrige um bug real, não uma limitação deliberada: `3903` não tinha nenhum fallback de printing, hardcoded a `source_printing_label = 'Normal'`, achado provado por teste transacional dedicado ("Reverse-only", dado real do Bulbasaur ME1 #001). Implementa a hierarquia aprovada por Fabrício — Normal → Holofoil → Reverse Holofoil, sempre dentro de NM+MARKET — com `has_pricing=true` se qualquer um dos três tiver preço elegível; `printing_label` (texto cru) devolve ao chamador qual printing foi escolhido. Bug de implementação real (`ERROR: 42702: column reference "card_id" is ambiguous` — identificador não qualificado ambíguo contra o próprio parâmetro `RETURNS TABLE(card_id uuid, ...)` da função) encontrado e corrigido no mesmo ciclo, antes de qualquer validação reportada, qualificando as referências com o alias `cbp`. Mesmo padrão de segurança e grants de `3903`, revalidado sem alteração; performance revalidada com `EXPLAIN (ANALYZE, BUFFERS)` em 100 `card_id` reais — 10,44ms, comparável aos 12,9ms de `3903`, sem regressão. Quatro cenários obrigatórios validados com dado real, todos transacionais com `ROLLBACK`, zero resíduo (Bulbasaur Normal → `Normal`; Alakazam/Mega Gardevoir ex/Mega Venusaur ex Holofoil-only → `Holofoil`; Bulbasaur com Normal/Holofoil desativados → `Reverse Holofoil`; carta sem mapping → `has_pricing=false`). Detalhe completo em `ADR-029`, revisão `1.22`, e `05f-pricing.md`, revisão `1.29`, seção "Rodada 8 do Incremento P12".

`3905`–`3907` `CONFIRMADO EXECUTADO` (Incremento P13.1 — Fundação de Orquestração Programada, 2026-08-18): evolução do bloco de dez de `pricing_sync_run` (`3080`/`3081`/`3082`/`3083`, acima) para suportar execuções agendadas (`SCHEDULED`) de PTAX, sem nenhuma extensão habilitada, Vault secret criado, Edge Function implantada ou chamada externa nesta rodada — só a fundação de schema. `3905`: `ck_pricing_sync_run_type` estendido com `FX_REFRESH` (não reaproveita `PRICE_REFRESH`); nova coluna `fx_source_code TEXT` (identidade cambial explícita, alinhada ao domínio já usado por `pricing_fx_rate.rate_source_code`, valor real `'BCB_PTAX'` — não é `FK`, mesmo motivo de `rate_source_code` também não ser); `pricing_source_id` passa a `NULL`able; novo `CHECK` `ck_pricing_sync_run_source_identity` correlacionando `run_type`↔(`pricing_source_id` XOR `fx_source_code`); guarda `ck_pricing_sync_run_fx_source_code_not_blank`. `3906`: `confirmed_by` passa a `NULL`able; novo `CHECK` `ck_pricing_sync_run_confirmed_by_by_trigger` (`MANUAL` exige `confirmed_by` preenchido, `SCHEDULED` exige `NULL`); `validate_pricing_sync_run_confirmed_by()` recriada via `CREATE OR REPLACE` (mesma assinatura, `SECURITY DEFINER`/`search_path=''` preservados, `EXECUTE` continua revogado de todos os papéis) para só consultar `admin_user` quando `confirmed_by IS NOT NULL`. `3907`: dois índices únicos parciais para aquisição atômica de execução ativa (`RECEIVED`/`PROCESSING`) — `ux_pricing_sync_run_active_price_per_source_type (pricing_source_id, run_type) WHERE ... AND pricing_source_id IS NOT NULL` e `ux_pricing_sync_run_active_fx_per_source_type (fx_source_code, run_type) WHERE ... AND fx_source_code IS NOT NULL`; cada índice exclui `NULL` da própria coluna por predicado, então `NULLS NOT DISTINCT` não é necessário aqui (diferente de `pricing_observation`/Query `3070`). Nenhum `GRANT` novo necessário — `service_role` já tinha `INSERT`/`SELECT` em nível de tabela (cobre colunas novas automaticamente); nenhuma coluna nova ganhou `UPDATE`, mesmo padrão restritivo já aplicado a `pricing_source_id`/`run_type`/`triggered_by`. Onze cenários validados transacionalmente com `ROLLBACK`, zero resíduo (5 linhas reais de produção antes e depois, intocadas); `get_advisors` sem nenhum achado novo referenciando `pricing_sync_run`/`fx_source_code`. Detalhe completo em `ADR-031` e `05f-pricing.md`, seção "Incremento P13.1".

`3908` `CONFIRMADO EXECUTADO` (Incremento P13.1, correção pós-auditoria, 2026-08-18, mesmo dia). Achado real da auditoria final do P13.1: `pricing_source.code`/`pricing_fx_rate.rate_source_code` já exigem formato normalizado (`ck_pricing_source_code_format`/`ck_pricing_fx_rate_source_code_format`: maiúsculas, começa por letra, só `[A-Z0-9_]`) — `fx_source_code` (`3905`) só tinha `NOT NULL`/não-branco, sem essa exigência de formato. `3905` não foi editada (aditiva, instrução explícita de Fabrício): novo `CHECK` `ck_pricing_sync_run_fx_source_code_format` replicando exatamente o mesmo padrão das duas colunas irmãs. Oito cenários validados transacionalmente com `ROLLBACK` antes da aplicação (`'BCB_PTAX'` aceito; minúsculas/espaço interno/hífen/início por dígito/string vazia/só espaços/`NULL` em `FX_REFRESH` rejeitados) — todos com o resultado esperado; zero linha real afetada (nenhuma tinha `fx_source_code` preenchido). `get_advisors` sem achado novo. **Achado adicional da mesma auditoria, também corrigido no mesmo ciclo**: as migrations `3905`–`3907`, embora `CONFIRMADO EXECUTADO` no Supabase, estavam ausentes de `database/migrations/` — quebra da convenção vigente desde a Query `3700` (todo `CONFIRMADO EXECUTADO` ganha arquivo físico correspondente). Os quatro arquivos (`3905`–`3908`) foram versionados neste mesmo ciclo, com o SQL idêntico ao efetivamente aplicado.

`3909` `CONFIRMADO EXECUTADO` (Incremento P13.2, correção pós-piloto, 2026-08-18, mesmo dia — retroativamente formalizada nesta seção durante o Incremento P13.4, mesmo padrão de reconciliação já usado para `3905`–`3908`). Auditoria pós-piloto real encontrou `pricing_sync_run_call.called_at` de uma execução aparecendo antes do `started_at` do próprio run — causa-raiz: `started_at`/`finished_at` eram capturados por `new Date().toISOString()` no relógio do processo cliente, não pelo `now()` do Postgres. Trigger dedicado `set_pricing_sync_run_server_timestamps()` (`BEFORE INSERT OR UPDATE`, nunca consolidado em `set_updated_at()`): `started_at` sempre `now()` do servidor em todo `INSERT`; `finished_at` sempre `now()` do servidor na primeira transição para status terminal (`COMPLETED`/`COMPLETED_WITH_ERRORS`/`FAILED`/`CANCELLED`); runs já terminais nunca têm `finished_at` recalculado; nenhuma linha histórica alterada/backfilled. Nenhum `GRANT` novo necessário. Dez cenários transacionais (`ROLLBACK`) antes da aplicação real; `get_advisors` sem achado novo. Detalhe completo em `ADR-031` e `05f-pricing.md`, seção "Incremento P13.2 — Correção de Autoridade Temporal e Ordenação da Telemetria".

`3910` `CONFIRMADO EXECUTADO` (Incremento P13.4 — Agendamento Automático via `pg_cron`/`pg_net`/Vault, 2026-08-18, mesmo dia). Não pertence ao bloco de dez de nenhuma entidade específica — habilita duas extensões (`pg_cron WITH SCHEMA pg_catalog` + os dois `GRANT` oficiais de instalação a `postgres`; `pg_net`) e cria, de forma idempotente, o único `cron.job` do projeto (`ptax-fx-refresh-weekdays`, `0 22 * * 1-5`, `22:00 UTC`/`19:00 America/Sao_Paulo`), cujo comando chama `net.http_post` diretamente com `url`/`apikey` resolvidos por nome via `vault.decrypted_secrets` (nomes `ptax_fx_refresh_url`/`ptax_fx_refresh_secret`, nunca valor literal no comando), `body='{}'`, `timeout_milliseconds=60000`. Testado transacionalmente (`BEGIN`/`ROLLBACK`) antes da aplicação real; sem função auxiliar, tabela, trigger, política ou `GRANT` além do estritamente necessário. Job único confirmado em produção; uma chamada controlada real pelo mesmo caminho confirmou `HTTP 200`/`COMPLETED`. Detalhe completo em `ADR-031`, revisão `1.8`, e `05f-pricing.md`, seção "Incremento P13.4". Demais faixas de `3900`–`3999` seguem livres.

### Módulo: Pesquisa de Cartas (`4000`–`4999`)

Comprometido no Incremento "Pesquisa Global de Cartas" (2026-08-17, ver `ADR-030-card-search-projection.md`) — sucede o candidato "Referências Compartilhadas" cogitado e deliberadamente não criado durante o Incremento P1 de Pricing (revisão `1.20`, acima): aquele candidato permanecia livre e não reservado, exatamente o critério geral desta seção ("milhar comprometido só quando efetivamente aprovado"); este incremento é a primeira aprovação real de um módulo para `4000`–`4999`, e seu propósito é Pesquisa de Cartas, não Referências Compartilhadas — `card_condition` (numerada em `3010`–`3019` durante Pricing P1) não migra de milhar por conta desta decisão.

Nenhuma tabela nova. Dentro de `4000–4699` (estrutura):

- `4000` — `CREATE EXTENSION pg_trgm` (schema `extensions`) — `CONFIRMADO EXECUTADO`.
- `4001` — índice GIN trigram sobre `lower(card.name)` (`ix_card_name_trgm`) — `CONFIRMADO EXECUTADO`. Alteração de entidade legada (`card`) que só serve a este módulo novo — numerada aqui, não na faixa `200`–`299`, pela mesma regra geral já aplicada a `card.is_active` (`ADR-023`, ver acima).
- `4002` — índice funcional sobre `card.collector_number` normalizado (`ix_card_collector_number_normalized`) — `CONFIRMADO EXECUTADO`. **Nota de rastreabilidade:** o texto de DDL originalmente proposto (`regexp_replace(collector_number, '^0+', '')`) foi bloqueado de forma reproduzível pelo classificador de segurança do Auto Mode em toda chamada MCP que o continha (`apply_migration` e uma tentativa de `UPDATE` de correção); o índice foi criado com sucesso via `execute_sql` usando `ltrim(collector_number, '0')` (semanticamente equivalente), que não aciona o bloqueio. O registro de histórico do Supabase (`supabase_migrations.schema_migrations`) para esta versão ficou com `statements = 'select 1;'` em vez do DDL real — o arquivo `database/migrations/4002_...sql` reflete o DDL real e correto; a correção do bookkeeping interno do Supabase não foi tentada (uma segunda tentativa de `UPDATE` também foi bloqueada) e fica registrada como pendência, não escondida.
- `4010` — `public.search_cards()` (`SECURITY DEFINER STABLE`, sem tabela própria) — `CONFIRMADO EXECUTADO`. **Substituída pela `4030`** (mesmo dia, correção de escopo — ver abaixo); assinatura original incluía `p_game_code`, fora do escopo aprovado. Projeção segura de busca (nome/número/código de Card Set, filtros de Categoria/Raridade, relevância fixa), verificação explícita de `auth.uid()`, `REVOKE ALL FROM PUBLIC, anon` + `GRANT EXECUTE TO authenticated`. Ver `ADR-030` para o contrato completo e a decisão de não usar o par `internal`+wrapper.
- `4020` — `public.search_card_filter_options()` (`SECURITY DEFINER STABLE`, sem tabela própria) — `CONFIRMADO EXECUTADO`. **Substituída pela `4031`** (mesmo dia, correção de escopo — ver abaixo); assinatura original incluía `p_game_code`/lista `games`, fora do escopo aprovado. Opções de filtro (Card Set/Categoria/Raridade) para a UI de `/pesquisa`, mesmo padrão de segurança de `4010`.
- `4030` — `public.search_cards()` **corrigida** (`SECURITY DEFINER STABLE`, sem tabela própria) — `CONFIRMADO EXECUTADO`. `DROP FUNCTION` explícito da assinatura da `4010` + `CREATE OR REPLACE` sem `p_game_code`/colunas de Jogo — sem filtro de Jogo nesta versão (decisão de escopo). Mesmo endurecimento de segurança; simplificação colateral: dispensa os `JOIN`s a `expansion`/`game`. Ver `ADR-030`, revisão `1.1`.
- `4031` — `public.search_card_filter_options()` **corrigida** (`SECURITY DEFINER STABLE`, sem tabela própria) — `CONFIRMADO EXECUTADO`. `DROP FUNCTION` explícito da assinatura da `4020` + `CREATE OR REPLACE` sem parâmetro nem lista `games`. Mesmo endurecimento de segurança. Ver `ADR-030`, revisão `1.1`.
- `4032` — `public.search_cards()` **corrigida novamente** (`SECURITY DEFINER STABLE`, sem tabela própria) — `CONFIRMADO EXECUTADO`. Assinatura idêntica à `4030` (sem `DROP`, só `CREATE OR REPLACE`); corrige achado da homologação local de Fabrício: busca por "número/total" (ex.: "125/094", formato exibido nas telas do catálogo) não retornava resultado — o tier "número" só reconhecia `collector_number` isolado. Passa a comparar `collector_number` e `collector_total` juntos quando a busca contém uma barra com dígitos dos dois lados, via `strpos`/`substr`/`translate` (sem expressão regular, mesma técnica de contorno do classificador de segurança já usada na `4002`). Ver `ADR-030`, revisão `1.2`.

`4700–4799`: Seeds — nenhuma; módulo não introduz vocabulário novo.

`4800–4899`: Validações — nenhuma migration física associada (validação transacional com `ROLLBACK`, mesmo padrão de "número de diário" já usado em Pricing); resultado completo no relatório do incremento (`docs/log.md`, 2026-08-17).

`4900–4999`: Reserva.

### Módulo: Pokémon Catalog Foundation (`6000`–`6999`)

Comprometido no Incremento `COLLECTIONS-PHYSICAL-INCREMENT-02G-IMPLEMENTATION-01` (2026-09-04), cobrindo a implementação física de `ADR-011-pokemon-tcg-domain-scope.md` (v1.2) — catálogo global e canônico de Pokémon Generation/Species, base para a futura Pokédex de Collections (`docs/05d-colecoes-e-usuarios.md`, seção "Collection Pokédex Reference / REFERENCE_POSITION"). Milhar próprio, não `5000`–`5999` (Collections): estas entidades são canônicas do universo Pokémon, não do domínio Collections — o vínculo entre os dois módulos acontece por referência lógica (Pokédex Position → Pokémon Species), não por numeração compartilhada.

Dentro de `6000–6699` (estrutura), duas entidades persistentes implementadas — fundação inicial do módulo:

- `pokemon_generation` (`6000` tabela, `6001` triggers) — `CONFIRMADO EXECUTADO`. Catálogo global das gerações da Pokédex nacional (`code` = `GENERATION_I`/`GENERATION_II`/..., `ordinal_number` inteiro separado sem limite superior). **Decisão congelada:** sem `game_id` — entidade canônica do universo Pokémon, não do TCG; o vínculo com o Game acontece depois, em Card → Species, fora do escopo desta rodada. Governança de identidade protege `id`/`code`/`ordinal_number`/`created_at`; `canonical_name`/`is_active` permanecem corrigíveis administrativamente.
- `pokemon_species` (`6010` tabela + índice `ix_pokemon_species_generation_id`, `6011` triggers) — `CONFIRMADO EXECUTADO`. Catálogo global das espécies Pokémon, identificadas por `national_dex_number` (`UNIQUE`, `CHECK > 0`). Mesma decisão de ausência de `game_id`. Governança de identidade protege apenas `id`/`created_at` — `national_dex_number`/`generation_id`/`canonical_name` são dado editorial canônico, corrigível via reconciliação administrativa (decisão congelada explícita: UUID é identidade técnica imutável, `national_dex_number` não).
- `pokemon_species_external_reference` (`6020` tabela, `6021` triggers) — `CONFIRMADO EXECUTADO`. Evidência de integração externa (PokéAPI etc.), mesmo padrão conceitual de `card_set_external_reference` (Query `240`/`241`), sem `language_id`. `UNIQUE(pokemon_species_id, asset_source_id)` e `UNIQUE(asset_source_id, external_species_id)`; `pokemon_species_id` em `ON DELETE CASCADE`, `asset_source_id` em `ON DELETE RESTRICT`. **RLS completamente fechado** — sem `catalog_admin_select`, sem `GRANT` direto a `authenticated` — decisão congelada: esta tabela é evidência de integração, não dado self-service (diferente de `card_variant_type_external_mapping`, Query `2140`, que nasce com policy por servir curadoria self-service). Nenhum índice além dos dois `UNIQUE`/PK — decisão explícita de não antecipar índices especulativos.

`6700–6799`: Seeds — `6700` `CONFIRMADO EXECUTADO`: linha `POKEAPI` em `public.asset_source` (`source_order = 3`, `source_type = 'API'`) — PokéAPI é representada via `asset_source`, nunca via coluna solta (`pokeapi_id`) ou mecanismo polimórfico genérico, mesmo princípio já aplicado às fontes existentes (`POKEMON_TCG_API`/`TCGDEX`).

`6800–6899`: Validações — nenhuma migration física associada. Validação estrutural (18 itens: tabelas, colunas/tipos/defaults, PK/FK/UNIQUE/CHECK, índice, triggers ativos, RLS habilitado, ausência de policy/grant a `anon`/`authenticated`) e 13 testes comportamentais executados transacionalmente com `ROLLBACK` (mesmo padrão de "número de diário" já usado em Pricing/Pesquisa de Cartas) — resultado completo no relatório do incremento (`docs/log.md`, 2026-09-04).

`6900–6999`: Reserva.

**Fora de escopo desta rodada** (decisão explícita, não antecipada): `pokemon_form`, `pokemon_variety`, `pokedex`, `pokedex_position`, `card_primary_species`, `collection_pokedex_reference`, pipeline de sincronização real com PokéAPI/TCGdex, `internal.write_pokemon_species()` (só se justifica quando houver ≥2 canais de escrita convergentes), admin UI, frontend.

**Gap de documentação registrado nesta rodada, não bloqueante:** `pricing_source` (Query `3000`/`3001`/`3002`, `CONFIRMADO EXECUTADO` conforme texto desta seção acima) não foi localizada como arquivo em `database/schema/` durante a auditoria física deste incremento — possível lacuna de promoção canônica do módulo Pricing, não investigada a fundo por estar fora do escopo de `02G`. Registrado aqui como pendência do domínio Pricing, a ser resolvida em rodada própria.

### Bloco por Entidade e Regra de Deslocamento (offset) — esquema legado

Dentro do Catálogo Editorial (100–199), cada entidade ocupa um bloco de 10 números: `X0` cria a tabela (`Create <Entity> Table`), `X1` cria o trigger (`Create <Entity> Trigger`), com os números seguintes reservados para evoluções futuras da mesma entidade (ex.: `X2`, `X3`...).

O Seed e a Validação de uma entidade são derivados do número da sua Query de criação de tabela por um deslocamento fixo:

```text
Seed      = (número de criação da tabela) + 700
Validate  = (número de criação da tabela) + 800
```

Exemplo real:

```text
Game       100 - Create Game Table       →  800 - Seed Game       →  900 - Validate Game
           101 - Create Game Trigger

Expansion  110 - Create Expansion Table  →  810 - Seed Expansion  →  910 - Validate Expansion
           111 - Create Expansion Trigger

Set        120 - Create Set Table        →  820 - Seed Set        →  920 - Validate Set
Rarity     130 - Create Rarity Table     →  830 - Seed Rarity     →  930 - Validate Rarity
Card       140 - Create Card Table       →  840 - Seed Card       →  940 - Validate Card
```

Essa regra elimina qualquer ambiguidade sobre qual número usar para o Seed ou a Validação de uma nova entidade.

> **Nota:** o número de uma entidade é atribuído na ordem em que ela é efetivamente modelada e aprovada, não necessariamente na ordem inicialmente imaginada — Rarity, por exemplo, precisou ser criada antes de Card por dependência de chave estrangeira (`card.rarity_id → rarity.id`), e por isso ocupa `130` em vez do `140` inicialmente cogitado para Card (ver `05-modelo-de-dados.md`, seções Rarity e Card).

### Seeds

Cada entidade possui sua própria Query de Seed (faixa 800–899), separada da Query que cria a tabela (faixa correspondente ao módulo, ex. 100–199). Estrutura e dados iniciais são responsabilidades diferentes.

Seeds devem ser **idempotentes** — executáveis múltiplas vezes sem criar registros duplicados, usando `INSERT ... ON CONFLICT (<coluna_única>) DO NOTHING` em vez de um `INSERT` simples:

```sql
INSERT INTO public.game (code, name)
VALUES ('POKEMON', 'Pokémon Trading Card Game')
ON CONFLICT (code) DO NOTHING;
```

Isso garante que reinstalar o banco do zero baste executar novamente todas as Seeds, sem falhas.

Quando um Seed depende de uma entidade relacionada, a chave estrangeira é resolvida por um `SELECT` no código de negócio da entidade relacionada — nunca um UUID fixo no script, já que o UUID real varia a cada execução:

```sql
INSERT INTO public.expansion (game_id, code, name, release_order)
SELECT game.id, 'ME', 'Mega Evolution', 1
FROM public.game
WHERE game.code = 'POKEMON'
ON CONFLICT (game_id, code) DO NOTHING;
```

Quando uma Seed precisa inserir mais de um registro relacionado à mesma entidade pai, um `CROSS JOIN` com uma lista `VALUES` é uma alternativa válida ao padrão de um `SELECT` por registro — resolvendo a chave estrangeira uma única vez e reaproveitando-a para todas as linhas (ver exemplo real em `05-modelo-de-dados.md`, seção Set — Query `820 - Seed Card Set`).

**Regra de confiabilidade dos dados de Seed:** uma Seed nunca deve inserir dados estimados como se fossem oficiais. Se a existência de um registro é conhecida, mas algum de seus atributos de negócio (quantidades, datas, contagens) ainda não foi validado contra uma fonte confiável, o registro fica de fora da Seed até que a validação aconteça — não se insere um valor aproximado apenas para completar o cadastro. Exemplo real: ao popular `card_set`, o Set `ME3` (`Equilíbrio Perfeito`) foi inicialmente deixado fora de uma versão preliminar da Seed `820` por falta de quantidades confirmadas; a Seed só foi de fato executada depois que Fabrício forneceu as folhas oficiais de verificação de todos os Sets, eliminando a necessidade de estimativa (ver `05-modelo-de-dados.md`, seção Set — "Fontes Primárias").

**Cuidado com `ON CONFLICT ... DO NOTHING` em correções:** essa cláusula garante que reexecutar uma Seed não crie duplicatas, mas ela também significa que **não atualiza** uma linha já existente. Se uma Seed for corrigida (ex.: um nome ou uma data errada) depois de já ter sido executada com sucesso, simplesmente reexecutar a versão corrigida não corrige os dados já gravados — é necessário um `UPDATE` explícito ou uma nova migration. Esse risco só se aplica a Seeds já executadas; corrigir uma Seed antes de sua primeira execução é seguro.

**`DO NOTHING` vs. `DO UPDATE`:** o padrão (`DO NOTHING`) é adequado para Seeds de carga inicial única. Para Seeds que representam o **estado atual e reaplicável de uma entidade** — por exemplo, o conjunto completo de Card Sets de uma Expansion, incluindo o Set promocional cuja quantidade cresce ao longo do tempo — prefira `ON CONFLICT ... DO UPDATE`, atualizando as colunas relevantes. Assim, reexecutar a Seed não apenas evita duplicidade, mas também corrige os registros existentes caso a fonte oficial seja atualizada. Critério: se a Seed representa "os dados iniciais que só existem uma vez", use `DO NOTHING`; se representa "o retrato mais atual e correto que conhecemos desta entidade", use `DO UPDATE`.

**Bloco `DO $$ ... END $$` com verificação explícita de pré-requisito:** quando a ausência de uma entidade-pai deve ser um erro visível — não um `INSERT` que silenciosamente insere zero linhas — a Seed pode ser escrita como um bloco PL/pgSQL, resolvendo a chave estrangeira uma única vez em uma variável e usando `RAISE EXCEPTION` caso o pré-requisito não exista:

```sql
DO $$
DECLARE
    v_game_id UUID;
BEGIN
    SELECT id INTO v_game_id FROM public.game WHERE code = 'POKEMON';

    IF v_game_id IS NULL THEN
        RAISE EXCEPTION 'Game POKEMON não está cadastrado.';
    END IF;

    INSERT INTO public.rarity (game_id, code, name, display_order)
    VALUES (v_game_id, 'COMMON', 'Comum', 1)
    ON CONFLICT (game_id, code) DO UPDATE SET
        name = EXCLUDED.name,
        display_order = EXCLUDED.display_order;
END;
$$;
```

Alternativa válida ao padrão `INSERT ... SELECT ... WHERE` já documentado acima — usar quando a falha silenciosa (zero linhas inseridas, sem aviso) for um risco maior do que a verbosidade de um bloco `DO`. Exemplo real: `830 - Seed Rarity`.

### Convenções de Apresentação da Query

Além do cabeçalho oficial (ver "Modelo de Cabeçalho", abaixo), cada Query deve ser acompanhada de uma **descrição resumida** — uma linha curta, sem o detalhamento completo do cabeçalho, usada para organizar e indexar visualmente o SQL Editor do Supabase à medida que o número de Queries cresce (dezenas ou centenas de scripts). Ao apresentar uma Query durante o par de execução, a resposta deve começar por `Query: NNN - Título` seguido de um `Objetivo:` de uma ou duas frases, antes do SQL propriamente dito — convenção adotada a partir da entidade Rarity, a pedido de Fabrício ("Isso me ajuda a manter o SQL Editor organizado e padronizado").

### Registro em `database/`

Depois que uma Query for executada e confirmada no Supabase, o mesmo SQL (com o cabeçalho oficial completo) deve ser copiado como arquivo `.sql` para o subdiretório correspondente em `database/` (fora de `docs/`), organizada pela mesma faixa de numeração desta seção — ver `database/README.md`. Isso garante que o histórico de execução exista de forma versionada no Git, não apenas dentro do Supabase ou embutido em prosa em `05-modelo-de-dados.md`. Este passo é posterior à execução — nunca usar `database/` como fonte para rodar migrations automaticamente (a execução real continua manual, via SQL Editor).

### Validação

Toda alteração estrutural recebe sua própria Query de validação (faixa 900–999), reutilizável sempre que a tabela correspondente for alterada novamente (ex.: `900 - Validate Game`, `901 - Validate Expansion`). As Queries 900+ funcionam, na prática, como um conjunto de testes manuais do banco.

Nunca se assume que uma operação funcionou apenas porque o Supabase retornou "Success" — cada tipo de mudança tem sua própria forma de confirmação: criar uma tabela é validado com um `SELECT`; criar um índice é validado confirmando sua existência; criar um trigger é validado confirmando que está corretamente associado à tabela.

**Padrão de cinco categorias para toda Query `9xx - Validate`:** a partir da entidade Card Set, toda Query de validação passa a cobrir cinco categorias, transformando-a em um verdadeiro teste de integridade, não apenas uma consulta de inspeção. Uma primeira proposta agrupava isso em três seções (estrutural / dados / regras derivadas); na prática, ao aplicar o padrão em Card Set, ele se mostrou mais útil dividido em cinco:

1. **Dados persistidos** — os valores realmente gravados nas colunas, com JOIN até as entidades relacionadas para leitura por código de negócio, não por UUID.
2. **Regras de negócio derivadas** — valores que não existem como coluna na tabela, mas fazem parte do domínio (ex.: `secret_set_size = total_set_size - base_set_size` em Card Set). Inclui também consultas de resumo/contagem por categoria (ex.: quantos Sets de cada `set_type` existem).
3. **Inconsistências** — consultas que devem retornar **zero linhas** quando tudo está correto (ex.: lacunas na sequência editorial, um Set promocional com data diferente da esperada, mais de um Set promocional na mesma Expansion). Esse é o formato mais forte de teste: o "resultado esperado" é a ausência de resultado.
4. **Constraints** — confirmar via `information_schema.table_constraints`/`check_constraints` que as constraints esperadas existem, com a definição exata de cada `CHECK`.
5. **Trigger** — confirmar via `information_schema.triggers` que o trigger de `updated_at` está associado à tabela correta.

Nem toda entidade precisará das cinco categorias completas (a categoria "inconsistências" só se aplica quando há regras condicionais, como as de Card Set promocional) — mas a ordem e a nomenclatura das categorias usadas devem seguir este padrão.

**Migrations que alteram constraints e dados existentes juntas devem ser transacionais:** envolver a Query inteira em `BEGIN; ... COMMIT;`, garantindo que uma falha em qualquer etapa não deixe o banco em estado intermediário (ex.: constraint alterada mas dados ainda não ajustados). Exemplo real: `122 - Adapt Card Set for Promo`, que remove e recria uma constraint, executa dois `UPDATE`s em sequência e adiciona uma nova constraint, tudo dentro de uma única transação.

### Princípio da Fonte Canônica (Autoria de Scripts SQL)

**Cada Query do repositório deve representar a forma correta e definitiva de criar aquela entidade em uma instalação nova do sistema.** Consequências diretas:

- não se mantêm migrations corretivas permanentes no fluxo padrão de instalação — quando o modelo de uma entidade evolui (ex.: um novo `set_type`, uma nova constraint), a **Query original de criação é atualizada em lugar** (mesmo número, nova `Versão` no cabeçalho), passando a já nascer correta;
- o histórico de como o modelo evoluiu passa a pertencer ao **controle de versão (Git)**, não à sequência de Queries — quem precisar consultar uma versão anterior de uma Query usa o histórico do repositório, não uma migration antiga ainda "ativa";
- uma migration que altera um banco **já existente** (ex.: `122 - Adapt Card Set for Promo`) continua registrada e preservada — não é apagada — mas passa a ser classificada como **histórica**: aplicável apenas a instalações antigas que já passaram por aquele estado intermediário, e explicitamente **fora** do fluxo de instalação limpa;
- o repositório deve sempre conseguir reconstruir o banco do zero executando apenas as Queries `CANÔNICA`s, na ordem de suas faixas — nunca dependendo de uma sequência de migrations históricas.

Este princípio foi adotado a partir da consolidação da entidade Card Set (Queries `120`/`820`, ver `05-modelo-de-dados.md`, seção Set): a Query `120 - Create Card Set Table` e a Query `820 - Seed Card Set` foram atualizadas em lugar (para `Versão 2.0`) para já nascerem com suporte nativo a `PROMO`, e as Queries `122`/`821` (que originalmente introduziram esse suporte em um banco já existente) foram reclassificadas como históricas.

**Atenção:** atualizar a Query canônica no repositório não executa nada automaticamente contra o banco físico. Se o banco atual já foi construído pelo caminho antigo (Query original + migration histórica), ele deve ser conferido individualmente contra a nova versão canônica — pode haver uma diferença estrutural entre "o que o banco atual tem" e "o que a Query canônica descreveria em uma instalação nova" até que essa conferência seja feita (ver nota concreta em `05-modelo-de-dados.md`, seção Set, sobre o índice único parcial de Card Set).

**Nota de reconciliação (`ADR-024`):** `ADR-024-catalog-card-ingestion-strategy.md` registra um princípio homônimo — "Princípio da Fonte Canônica" — aplicado a um objeto diferente. Ali, a fonte canônica é o próprio banco de dados do Project Mimikyu, autoridade sobre os **dados editoriais** do catálogo: nenhuma fonte externa (PDF oficial, TCGdex, integrações futuras) grava diretamente nas tabelas canônicas — cada uma propõe, a validação administrativa decide. A ideia comum aos dois usos é a mesma (existe sempre uma única fonte com autoridade final; tudo o que vem de fora dela é, na melhor das hipóteses, um candidato a ser incorporado, nunca um fato consumado), mas os objetos regidos são diferentes: aqui, scripts SQL (qual Query é a forma correta de criar uma estrutura); em `ADR-024`, dados editoriais (qual fonte tem autoridade sobre o conteúdo do catálogo). Os dois nomes são mantidos deliberadamente iguais — a analogia é intencional —, mas qualificados por contexto: "Princípio da Fonte Canônica (Autoria de Scripts SQL)" aqui, "Princípio da Fonte Canônica (Dados Editoriais)" em `ADR-024`. Qualquer citação futura sem qualificação deve especificar a qual dos dois usos se refere.

### Modelo de Cabeçalho

```sql
/*
================================================================
Projeto.....: Project Mimikyu
Query.......: 100 - Create Game table
Versão......: 1.0
Status......: CANÔNICA
Autor.......: [nome]
Data........: AAAA-MM-DD

Descrição...:
[o que este script cria ou altera]

Regras de Negócio:
- [regra 1]
- [regra 2]
================================================================
*/
```

O campo **Versão** identifica a evolução do próprio script (ex.: uma Query canônica que evolui de `1.0` para `2.0` preserva o número da Query, apenas incrementando a versão).

O campo **Status** identifica o papel da Query frente ao Princípio da Fonte Canônica (acima):

- **CANÔNICA** — representa a forma correta e mais atual de criar aquela estrutura; é o que uma instalação nova deve executar.
- **MIGRATION** — uma alteração histórica, já aplicada a um banco que existia antes da versão canônica correspondente; não faz parte do fluxo de instalação limpa, mas é preservada como registro.

## Execução

Uma etapa por vez, sempre validada antes de avançar: instalar extensão → validar → criar função → validar → criar tabela → validar. Isso facilita identificar exatamente onde algo deu errado, em vez de executar dezenas de comandos de uma só vez.

## Rollback de Alterações Estruturais

Toda alteração estrutural (tabela, trigger, function, política de RLS, configuração de Storage) segue o mesmo princípio de rollback: a reversão padrão **nunca remove nem desabilita imediatamente** o objeto alterado. O procedimento padrão é:

1. Impedir que a aplicação continue gerando novos dados dependentes do comportamento a ser revertido — a forma concreta desse bloqueio é específica de cada mudança e deve ser documentada onde a mudança é descrita (`05-modelo-de-dados.md`, ADR do módulo), não neste Standard.
2. Reverter o deploy da aplicação (frontend/Edge Function) que depende do objeto.
3. Preservar dados e infraestrutura já existentes no banco — tabelas, triggers e functions permanecem intactos, sem `DROP` nem `DISABLE`.

A remoção definitiva de um objeto de banco é um **processo separado de abandono arquitetural**, com Queries próprias, nomeadas e revisadas individualmente — nunca o procedimento padrão de rollback.

---

# Revision History

| Versão | Descrição |
|---------|-----------|
| 1.0 | Criação inicial do documento. |
| 1.1 | Preenchidas as Seções 2 (Naming Conventions), 5 (Primary Keys) e 6 (Foreign Keys): tabelas em `snake_case` singular, colunas em `snake_case` (nunca camelCase/PascalCase), chave primária `id` (UUID) como identidade técnica, chave estrangeira no padrão `<entidade_referenciada>_id`. |
| 1.2 | Adicionada nota sobre palavras reservadas do SQL na Seção 2 (ex.: Set → `card_set`). Preenchida a Seção 3 (Data Types: VARCHAR vs. TEXT, UUID, TIMESTAMPTZ). Refinada a Seção 4 (Audit Model): `created_by`/`updated_by`/`deleted_at`/`deleted_by` deixam de ser obrigatórios por padrão, passando a ser adicionados apenas sob necessidade concreta (Princípio da Simplicidade Inicial, AP-004); padrão mínimo agora é `id`/`created_at`/`updated_at`. Adicionada à Seção 5 a versão do UUID (v4 no MVP, v7 quando houver suporte adequado) e a distinção entre identidade técnica (`id`) e identidade de negócio (`code`). Preenchida a Seção 8 (Logical Delete: preferir `status` de negócio a soft delete generalizado) e a Seção 9 (SQL Standards: restrições de integridade também no banco, não só na aplicação). |
| 1.3 | Refinada a Seção 1 (Technical Language): cabeçalhos e comentários explicativos dos scripts SQL passam a ser em português; apenas identificadores técnicos seguem o inglês. Adicionado à Seção 9 o requisito de Row Level Security (RLS) habilitado em toda tabela do schema `public`. Preenchida a Seção 10 (Migration Standards): uso exclusivo do SQL Editor (nunca o menu visual), Padrão Oficial de Queries SQL (Número/Nome/Descrição/Cabeçalho/SQL/Validação, com modelo de cabeçalho incluindo Versão) e execução validada passo a passo. |
| 1.4 | Adicionada à Seção 5 a regra "código internacional, nome localizável" (code nunca muda entre idiomas; name pode ser localizado, mesmo padrão de Card Translation/ADR-007). Substituída, na Seção 10, a numeração sequencial simples pela convenção oficial de Faixas de Numeração (000–999, por módulo do projeto); adicionadas as subseções Seeds (idempotência via `ON CONFLICT ... DO NOTHING`, Query própria por entidade) e Validação (Queries de validação reutilizáveis na faixa 900–999; nunca assumir que "Success" significa que o resultado está correto). |
| 1.5 | Adicionada à Seção 10 a regra de bloco por entidade e deslocamento fixo (Seed = criação da tabela + 700; Validate = criação da tabela + 800), confirmada com Game e Expansion. Adicionada orientação para Seeds com dependência de entidade relacionada: resolver a chave estrangeira por `SELECT` no código de negócio, nunca por UUID fixo no script. |
| 1.6 | Adicionada à Seção 10 (Seeds) a alternativa de `CROSS JOIN` com `VALUES` para inserir múltiplos registros relacionados à mesma entidade pai em uma única Query, confirmada com Card Set. Adicionada a regra de confiabilidade dos dados de Seed: nunca inserir dados estimados como oficiais — registros com atributos ainda não validados ficam de fora até a validação (exemplo real: Set `ME3` deixado fora de uma versão preliminar da Seed `820` por falta de quantidades confirmadas). |
| 1.7 | Atualizado o exemplo de `ME3`: a Seed `820` foi de fato executada com todos os cinco Sets (`ME1`–`ME4`) após Fabrício fornecer as folhas oficiais de verificação de cada um. Adicionado alerta permanente: `ON CONFLICT ... DO NOTHING` não atualiza linhas já existentes — corrigir uma Seed já executada exige `UPDATE` explícito ou nova migration, não apenas reexecutar a Query. |
| 1.8 | Adicionado à Seção 10 (Validação) o padrão de três seções para toda Query `9xx - Validate`: validação estrutural, validação dos dados persistidos e validação das regras de negócio derivadas (valores que não existem como coluna, mas fazem parte do domínio — ex.: `secret_set_size`). Adotado a partir da entidade Card Set. |
| 1.9 | Revisado o padrão de validação (1.8) de três para cinco categorias, após aplicação real em Card Set: dados persistidos, regras derivadas, inconsistências (consultas que devem retornar zero linhas), constraints, trigger. Adicionada recomendação de envolver migrations que alteram constraints e dados juntas em uma transação explícita (`BEGIN`/`COMMIT`), com `122 - Adapt Card Set for Promo` como exemplo real. Adicionada à Seção 10 (Seeds) a distinção entre `DO NOTHING` (carga inicial única) e `DO UPDATE` (Seeds que representam o estado atual e reaplicável de uma entidade). |
| 1.10 | Adicionada à Seção 10 a subseção "Registro em `database/`": toda Query executada e confirmada deve ser copiada como arquivo `.sql` versionado em `database/`, fora de `docs/` — registrado durante auditoria de saúde do repositório (2026-07-23). |
| 1.11 | Adicionado o "Princípio da Fonte Canônica": Queries de criação/seed representam sempre a forma correta e mais atual para uma instalação nova; migrations que alteram um banco já existente são preservadas, mas reclassificadas como históricas, fora do fluxo de instalação limpa. Adicionado o campo `Status` (`CANÔNICA`/`MIGRATION`) ao Modelo de Cabeçalho oficial. Aplicado pela primeira vez em Card Set (`120`/`820` atualizadas para v2.0; `122`/`821` reclassificadas). |
| 1.12 | Atualizado o exemplo real da Seção 10 (Bloco por Entidade e Regra de Deslocamento): inserida a entidade Rarity em `130` (Seed `830`, Validate `930`), criada antes de Card por dependência de chave estrangeira (`card.rarity_id`). Card deslocada de `130` para `140` (Seed `840`, Validate `940`). Adicionada nota explicando que a numeração segue a ordem real de modelagem/aprovação, não a ordem originalmente cogitada. |
| 1.13 | Adicionado à Seção 10 (Seeds) o padrão de bloco `DO $$ ... END $$` com verificação explícita de pré-requisito via `RAISE EXCEPTION`, alternativa ao padrão `INSERT ... SELECT ... WHERE` para quando a ausência de uma entidade-pai deve ser um erro visível (exemplo real: `830 - Seed Rarity`). Adicionada nova subseção "Convenções de Apresentação da Query": toda Query deve ter uma descrição resumida para organizar o SQL Editor, e a apresentação de cada Query no par de execução deve começar por `Query: NNN - Título` + `Objetivo:`, a pedido de Fabrício. |
| 1.14 | Adicionada à Seção 3 a subseção "Colunas `JSONB` de `metadata`": `metadata` nunca deve duplicar um atributo já existente em coluna relacional (própria ou de tabela relacionada) — existe apenas para propriedades específicas de uma fonte externa, sem equivalente relacional. Nasceu de um caso real: `card_set_external_reference.metadata` de `MEP` guardava campos já cobertos por `card_set` (`code`/`name`/`release_date`) e um campo (contagem de cartas) que ficaria desatualizado rapidamente — corrigido para `{}`, mesmo padrão dos demais registros. |
| 1.15 | Motivada pelo incremento de Identidade e Acesso (User Profile, ver ADR-020): a Seção 10 (Faixas de Numeração) foi reclassificada como **esquema legado e congelado** — `200`–`299` passa a ser descrita como "Evoluções e migrations complementares do Catálogo Editorial" (contém as migrations `250`–`271`, já executadas), e todo o intervalo `000`–`999` é declarado congelado para novas implementações, sem reserva antecipada para Coleções/Inventário/Aquisições/etc. Adicionado o **Modelo Modular de Numeração**: todo módulo novo recebe um milhar inteiro (`X000`–`X999`), com `X000–X699` para estrutura, `X700–X799` para Seeds, `X800–X899` para validações e `X900–X999` de reserva; o deslocamento `+700`/`+800` deixa de ser obrigatório, valendo como conveniência quando não colide. `1000–1999` aprovado para Identidade e Acesso (incluindo papéis/permissões futuros, sem abrir milhar próprio). Adicionada a subseção "Rollback de Alterações Estruturais" (Seção 10): rollback padrão nunca remove/desabilita objetos imediatamente — bloqueia a origem de novos dados, reverte a aplicação e preserva banco; remoção definitiva é processo separado de abandono arquitetural. Corrigido também o campo **Versão** do cabeçalho, que permanecia em `1.0` desde a criação do documento, dessincronizado do Revision History (já em `1.14`). |
| 1.16 | Motivada pela fundação de autorização e logo do Catálogo Editorial (Queries `273`–`277`, ver `ADR-022`): corrigida a faixa `200`–`299` na tabela de Faixas de Numeração e no texto de congelamento, de "inclui as migrations `250`–`271`" para "`250`–`277`" (desatualizada desde a Query `272`, cuja execução não havia gerado esta atualização). Esclarecido explicitamente o que "congelado para novas implementações" significa nesta faixa: nenhuma *nova entidade* abre um bloco de dez novo nela, mas ela permanece legitimamente aberta para *evoluções de controle de acesso/permissão de entidades já existentes* do Catálogo Editorial — precedente confirmado por `250`/`253`/`254`/`272` e reforçado por `273`–`277`, que recebem o próximo número sequencial livre, nunca um bloco de dez próprio. |
| 1.17 | **Incremento de governança, a pedido de Fabrício, antes de iniciar migrations/funções de `ADR-023`/`ADR-024` — resolve as duas pendências deixadas em aberto por aqueles ADRs.** (1) Nova subseção na Seção 9: schema `internal` formalizado como convenção permanente do projeto para rotinas `SECURITY DEFINER` não expostas por API — finalidade, objetos permitidos (só funções, nunca tabelas/views), nunca exposto pela API, `EXECUTE` revogado explicitamente de `PUBLIC`/`anon`/`authenticated`, critério de `GRANT` (só entre funções `SECURITY DEFINER`, nunca para roles de aplicação), `search_path` explícito, referências qualificadas por schema, e a distinção entre função RPC pública (contrato de API) e rotina interna reutilizável (nunca um contrato por si só). (2) Seção 10: adicionada regra geral ao Modelo Modular de Numeração — alteração de entidade legada que só serve a um módulo novo (ex.: `card.is_active`) numera-se no milhar do módulo novo, não na faixa legada `200`–`299`, mesmo que a tabela alterada seja legada; e objetos sem tabela própria dentro de `X000–X699` não ocupam bloco de dez, recebem o próximo número sequencial livre. Comprometido o milhar `2000`–`2999` = Catálogo Editorial — Escrita e Ingestão (`ADR-023`/`ADR-024`), com subseção própria detalhando a numeração de `catalog_import_job`/`catalog_import_row` (bloco de dez cada), `card.is_active`, schema `internal`, funções administrativas/internas/de staging (sequenciais, sem bloco de dez), Seeds (`2700`–`2799`), Validações (`2800`–`2899`) e Reserva (`2900`–`2999`). (3) Reconciliado "Princípio da Fonte Canônica": heading da Seção 10 qualificado para "(Autoria de Scripts SQL)", com nota explícita distinguindo-o do princípio homônimo de `ADR-024` ("Dados Editoriais") — mesma ideia geral (uma única fonte de autoridade final), objetos diferentes (scripts SQL vs. dados editoriais); nomes mantidos deliberadamente iguais, mas sempre qualificados por contexto a partir de agora. Nenhuma migration, função ou tabela criada nesta revisão — só governança documental, conforme solicitado. |
| 1.18 | **Corrigida a Seção 10 ("Ferramenta"), motivada por auditoria externa conduzida por Fabrício (2026-07-26).** A redação anterior afirmava que migrations são "sempre executadas via SQL Editor (Supabase)" — desatualizada frente ao fluxo real já consolidado no projeto durante a implementação de `ADR-023` (Claude Code executando e validando Queries diretamente no Supabase via MCP, `apply_migration`/`execute_sql`; ver `docs/development/HANDOFF-2026-07-26.md`). Reescrita para descrever dois caminhos de execução igualmente válidos — ambiente de desenvolvimento autorizado ou, excepcionalmente, o SQL Editor — mantendo intacta a proibição permanente do menu visual (Table Editor) para alterações estruturais, e deixando claro que a governança desta seção (cabeçalho, numeração, validação, versionamento em `database/`) não muda conforme o caminho. Mesma correção aplicada em conjunto a `database/README.md` e `docs/03-documentation-architecture.md` (linha `database/` da tabela de Artifact Responsibilities) — ver revisões próprias de cada um. |
| 1.19 | **Auditoria documental (2026-08-16) — dois padrões permanentes já implementados e confirmados, mas nunca registrados como Standard, formalizados na Seção "Row Level Security (RLS)".** (1) Padrão de performance para função de checagem usada em policy: função `STABLE` (nunca `VOLATILE`) + referência sempre entre parênteses como subconsulta escalar (`(select is_admin())`, nunca solta) — aplicado retroativamente em 24 policies do Catálogo Editorial (Query `2134`, CONFIRMADO EXECUTADO E VALIDADO, 2026-08-14). (2) Least privilege em `GRANT`s de tabela: `TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` revogados das roles de aplicação (`anon`/`authenticated`) em toda tabela — nenhum dos quatro é necessário via PostgREST/RPC (Query `2147`, CONFIRMADO EXECUTADO, 2026-08-15). Nenhuma migration nova criada nesta revisão — ambos os padrões já estavam executados em produção; esta entrada só fecha o gap de documentação normativa (STD-001 é o "como" permanente, o achado já existia em `docs/log.md` desde 2026-08-14/15, mas nunca havia virado regra a seguir para código futuro). |
| 1.20 | **Incremento P1 — Fundação Física de Pricing (2026-08-16).** Comprometido o milhar `3000`–`3999` = Pricing (`ADR-029`), com subseção própria (mesmo padrão de `1000`–`1999`/`2000`–`2999`) detalhando a numeração de `pricing_source` (`3000`/`3001`/`3002`, CONFIRMADO EXECUTADO), `card_condition` (`3010`–`3019`, CONFIRMADO EXECUTADO) e `pricing_condition_mapping` (`3020`–`3029`, CONFIRMADO EXECUTADO), Seeds (`3700`–`3799`, nenhuma executada), Validações (`3800`–`3899`, `3800` executada) e Reserva (`3900`–`3999`). Registrada, na mesma subseção, a decisão explícita de Fabrício sobre a numeração de `card_condition` — referência conceitualmente compartilhada e neutra, cuja posição em `3000`–`3999` registra apenas o ciclo de implementação, não pertencimento de domínio; o módulo "Referências Compartilhadas" (candidato `4000`–`4999`) foi cogitado e deliberadamente não criado, permanecendo livre e não reservado; entidades compartilhadas futuras não devem ser automaticamente numeradas em `3000`–`3999` só por este precedente. |
| 1.21 | **Incremento P2 — Correspondência Externa de Pricing (2026-08-16, mesmo dia).** Subseção "Módulo: Pricing" atualizada: `pricing_set_mapping` (`3030`–`3039`, CONFIRMADO EXECUTADO) e `pricing_card_mapping` (`3040`–`3049`, CONFIRMADO EXECUTADO) criadas, ambas vazias; `3810` registrado em Validações para a validação consolidada de 15 itens do incremento (transacional, sem migration física associada). Restam cinco entidades `Proposto, ainda não executado`. `service_role` recebeu apenas `SELECT` nas duas novas tabelas — capacidade de escrita deliberadamente adiada para um incremento futuro de sincronização, decisão explícita de Fabrício. |
| 1.22 | **Incremento P3 — Auditoria Operacional de Sincronização (2026-08-16, mesmo dia).** Subseção "Módulo: Pricing" atualizada: `pricing_sync_run` (`3080`–`3089`, CONFIRMADO EXECUTADO) e `pricing_sync_run_call` (`3090`–`3099`, CONFIRMADO EXECUTADO) criadas, ambas vazias; `3820` registrado em Validações para a validação consolidada de 16 itens funcionais + 4 planos de execução do incremento. Restam três entidades `Proposto, ainda não executado`. Índices de ambas as tabelas reformulados por decisão explícita do incremento, deviando do par genérico de índices isolados cogitado inicialmente, em favor do menor conjunto que atende às consultas reais — confirmado por `EXPLAIN (ANALYZE, BUFFERS)` sobre volume sintético. `service_role` recebeu grants mais restritivos que o modelo genérico das versões anteriores: `UPDATE` restrito por coluna em `pricing_sync_run` (só campos operacionais), sem `GRANT DELETE`; apenas `SELECT`/`INSERT` em `pricing_sync_run_call` (tabela append-only). |
| 1.23 | **Incremento P4 — Produto Externo de Pricing (2026-08-16, mesmo dia).** Subseção "Módulo: Pricing" atualizada: `pricing_product` (`3050`–`3059`, CONFIRMADO EXECUTADO) criada, vazia; `3830` registrado em Validações para a validação consolidada de 19 itens funcionais + 4 planos de execução do incremento. Restam duas entidades `Proposto, ainda não executado`. Índices reformulados por decisão explícita do incremento (nenhum isolado em `pricing_card_mapping_id`, já coberto pela `UNIQUE`; adicionados identidade externa, vínculo `card_variant` parcial e composto `card_variant`+idioma confirmado parcial), confirmados por `EXPLAIN (ANALYZE, BUFFERS)` sobre volume sintético — um índice adicional na FK `language_id` (`3052`) incorporado após achado real do advisor de performance, mesmo padrão de correção pós-validação já usado em `3002` no Incremento P1. `service_role` recebeu grants mais restritivos que P1–P3: além de `UPDATE` restrito por coluna (só campos enriquecíveis) e nenhum `GRANT DELETE`, este incremento revoga explicitamente `TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` de `service_role` — privilégios que `pg_default_acl` concede por padrão em tabelas criadas por `postgres` e que os três incrementos anteriores haviam deixado intocados para esse papel. |
| 1.24 | **Correção retroativa de segurança — sete tabelas de P1–P3 (2026-08-16, mesmo dia, mesmo ciclo do Incremento P4).** Auditoria somente leitura, sem presumir os relatórios anteriores, confirmou via `has_table_privilege`/`information_schema.role_table_grants` que `pricing_source`, `card_condition`, `pricing_condition_mapping`, `pricing_set_mapping`, `pricing_card_mapping`, `pricing_sync_run` e `pricing_sync_run_call` nunca tiveram `TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` revogados de `service_role` — mesmo default de `pg_default_acl` já corrigido em `pricing_product` (revisão `1.23`), agora estendido retroativamente via Query `3053` (migration única, sete tabelas, sem bloco de dez próprio — mesmo padrão de correção transversal de Query `2147`, Catálogo Editorial). `anon`/`authenticated` auditados e confirmados sem excesso. `SELECT`/`INSERT`/`UPDATE` preservados integralmente, incluindo os grants de coluna restritos de `pricing_sync_run`; nenhuma alteração de RLS/policies/estrutura/índices/migrations históricas/dados. Validado por `has_table_privilege` pós-correção e por smoke test transacional real (`ROLLBACK` forçado) confirmando zero impacto funcional. Advisors de segurança e performance sem achado novo. |
| 1.25 | **Incremento P5 — Série Histórica de Taxas de Câmbio (2026-08-16, mesmo dia, ciclo seguinte à correção retroativa).** Subseção "Módulo: Pricing" atualizada: `pricing_fx_rate` (`3060`, CONFIRMADO EXECUTADO) criada, vazia; `3840` registrado em Validações para a validação consolidada de 22 itens funcionais + 3 planos de execução do incremento. Resta uma única entidade `Proposto, ainda não executado` (`pricing_observation`). `UNIQUE` de unicidade semântica reordenada — `(from_currency, to_currency, rate_source_code, rate_date)` em vez da ordem original cogitada em `05f-pricing.md` — para servir, ela mesma, como o único índice necessário aos três padrões de consulta reais, confirmado por `EXPLAIN (ANALYZE, BUFFERS)` sobre volume sintético de 7.830 linhas, sem `Seq Scan` nem `Sort` adicional. `service_role` recebeu apenas `SELECT`/`INSERT`, com `UPDATE`/`DELETE`/`TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` revogados explicitamente — mesmo padrão restritivo já usado em `pricing_product` (revisão `1.23`) e na correção retroativa (revisão `1.24`), não confiando nos defaults de `pg_default_acl`. `authenticated` apenas `SELECT` via policy administrativa, `anon` sem privilégios — decisão que corrige a hipótese original de leitura pública cogitada em `05f-pricing.md`. |
| 1.26 | **Incremento P6 — Observação Histórica de Preço (2026-08-16, mesmo dia, ciclo seguinte ao Incremento P5) — décima e última entidade física do domínio, fundação física de Pricing completa.** Subseção "Módulo: Pricing" atualizada: `pricing_observation` (`3070`, CONFIRMADO EXECUTADO) criada, vazia; `3850` registrado em Validações para a validação consolidada de 32 itens funcionais + 5 planos de execução sobre 80.000 linhas sintéticas. Zero entidades restantes `Proposto, ainda não executado` — dez de dez `CONFIRMADO EXECUTADO`. **Correção obrigatória de identidade**, exigida pelo próprio pedido do incremento: `UNIQUE (pricing_product_id, condition_id, price_type, observed_at)` da hipótese original de `05f-pricing.md` substituída por `UNIQUE NULLS NOT DISTINCT (pricing_product_id, condition_id, price_type, currency_code, market_label, observed_at)` — corrige a insuficiência frente à semântica multi-mercado/multi-moeda já aprovada em `ADR-029`; suporte a `NULLS NOT DISTINCT` confirmado em PostgreSQL 17.6 antes do DDL. Índices reformulados (nenhum isolado em `pricing_product_id`, já líder da identidade; leitura recente dedicada; FK `condition_id` com índice próprio; `sync_run_id` parcial); nenhum índice parcial dedicado a "Valor Brasil" criado, decisão validada empiricamente sobre 80.000 linhas sintéticas mostrando o índice geral de leitura recente já suficiente. Decisão explícita de não particionar neste incremento, com checkpoint de capacidade registrado antes do DDL. `service_role` recebeu apenas `SELECT`/`INSERT`, com `UPDATE`/`DELETE`/`TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` revogados explicitamente — mesmo padrão restritivo de `pricing_product`/`pricing_fx_rate`. `authenticated` apenas `SELECT` via `pricing_admin_select`, `anon` sem privilégios. `pricing_product_id`/`condition_id` em `ON DELETE RESTRICT`, `sync_run_id` em `ON DELETE SET NULL`, ambos validados fisicamente. Pricing ainda não é operacional: seguem pendentes fonte homologada, condições canônicas semeadas, mappings reais, integração/sincronização, ingestão PTAX, frontend e Analytics/Item Valuation. |
| 1.27 | **Incremento "Pesquisa Global de Cartas" (2026-08-17, ver `ADR-030`).** Comprometido o milhar `4000`–`4999` = Pesquisa de Cartas, sucedendo o candidato "Referências Compartilhadas" cogitado e não criado em Pricing P1 (revisão `1.20`) — primeira aprovação real deste milhar, para um propósito diferente do candidato original; `card_condition` não muda de milhar. Nova subseção "Módulo: Pesquisa de Cartas" detalhando `4000` (`CREATE EXTENSION pg_trgm`), `4001` (índice trigram em `lower(card.name)`), `4002` (índice funcional em `collector_number` normalizado), `4010`/`4020` (`public.search_cards()`/`public.search_card_filter_options()`, `SECURITY DEFINER STABLE` sem tabela própria, sem par `internal`+wrapper — decisão justificada em `ADR-030` por violar a própria convenção `internal` desta norma, Seção 9), Seeds (`4700`–`4799`, nenhuma), Validações (`4800`–`4899`, transacional) e Reserva (`4900`–`4999`). Todas `CONFIRMADO EXECUTADO`, exceto uma ressalva de rastreabilidade registrada na própria subseção (`4002`: bloqueio reproduzível do classificador de segurança do Auto Mode ao padrão de regex `'^0+'`, contornado com `ltrim()`; registro de histórico do Supabase para essa versão ficou com texto incorreto, arquivo do repositório correto). Também motivada por este incremento: descoberta (não causada por ele) de uma divergência pré-existente de rastreabilidade entre `database/migrations/` e o histórico real do Supabase — faixa `3000`–`3090`/`2050` aplicadas sem arquivo no repositório; `2098`–`2153` com arquivo mas sem entrada no histórico do Supabase — sinalizada a Fabrício em chat, registrada como pendência em `ADR-030` e `docs/log.md`, sem reconciliação completa neste ciclo por decisão explícita dele. |
| 1.28 | **Correção de escopo — remoção do filtro de Jogo (2026-08-17, mesmo dia, rodada de aceite).** `4010`/`4020` incluíam indevidamente `p_game_code` (parâmetro/coluna/lista `games`), fora do escopo aprovado para esta versão da Pesquisa Global de Cartas. Duas Queries corretivas, sem alterar retroativamente `4010`/`4020` já `CONFIRMADO EXECUTADO`: `4030` (`DROP FUNCTION` da assinatura da `4010` + `CREATE OR REPLACE` sem Jogo, dispensando os `JOIN`s a `expansion`/`game`) e `4031` (idem para `4020`, sem parâmetro nem lista `games`) — mesmo endurecimento de segurança das originais. Confirmado por `pg_proc` que não resta overload antigo executável em nenhuma das duas funções. Ver `ADR-030`, revisão `1.1`. |
| 1.29 | **Correção funcional — busca por "número/total" (2026-08-17, mesmo dia, achado da homologação local de Fabrício).** Pesquisar "125/094" (formato número/total exibido no catálogo) em `/pesquisa` não retornava resultado — o tier "número" de `search_cards()` só reconhecia `collector_number` isolado. Query `4032` (`CREATE OR REPLACE` sobre a mesma assinatura da `4030`, sem `DROP`): quando a busca contém uma barra com dígitos dos dois lados, `collector_number` e `collector_total` passam a ser comparados juntos, via `strpos`/`substr`/`translate` — sem expressão regular, mesmo contorno do classificador de segurança do Auto Mode já usado na `4002`. Regressão do número isolado confirmada intacta. Ver `ADR-030`, revisão `1.2`. |
| 1.30 | **Incremento P7 — Homologação Condicionada da JustTCG (2026-08-17).** Primeiro `Query` real da faixa `3700`–`3799` (Seeds) do módulo Pricing: `3700` (`CONFIRMADO EXECUTADO`) insere `JUSTTCG` em `pricing_source` — homologada para piloto técnico de produto/printing/condição/preço USD/histórico (Decisão A aprovada), não homologada para PT-BR/"Valor Brasil" (Decisão B inconclusiva), `is_active = FALSE` (piloto condicionado, sem consumidor operacional). Sem alteração de estrutura/RLS/grants/migrations históricas — `INSERT` único, `ON CONFLICT (code) DO NOTHING`, idempotência confirmada por reexecução real. `3700` é o primeiro arquivo físico a existir em `database/migrations/` dentro de toda a faixa `3000`–`3999` (a divergência de rastreabilidade já registrada em `ADR-030` cobria só `2098`–`2153`/`2050`/`3000`–`3090` — este arquivo nasce corretamente versionado, sem repetir o gap). Ver `ADR-029`, revisão `1.9`, e `docs/05f-pricing.md`, seção "Incremento P7". |
| 1.31 | **Incremento P8 — Conector JustTCG e Piloto Controlado (2026-08-17).** `3091` (`CONFIRMADO EXECUTADO`, transversal, não pertence ao bloco de dez de nenhuma entidade — mesmo padrão de `3053`): `GRANT INSERT` (só `INSERT`, nunca `UPDATE`/`DELETE`) a `service_role` em `pricing_set_mapping`/`pricing_card_mapping`, resolvendo a pendência explícita registrada desde o Incremento P2. `3701`/`3702` (`CONFIRMADO EXECUTADO`, faixa `3700`–`3799` Seeds): cinco condições canônicas confirmadas pela prova técnica real (`card_condition`) e o de-para correspondente restrito a `pricing_source.code = 'JUSTTCG'` (`pricing_condition_mapping`). Primeiro script de conector real do domínio (`scripts/sync-justtcg-pricing.ts`, standalone Deno, mesmo precedente de `scripts/import-manual-assets.ts`) — piloto real não executado nesta rodada (`JUSTTCG_API_KEY` ausente no ambiente de execução do agente); lógica validada por harness `node` equivalente, já que o runtime Deno não está disponível neste sandbox de execução. Ver `ADR-029`, revisão `1.10`, e `docs/05f-pricing.md`, seção "Incremento P8". |
| 1.32 | **Correção pós-P8 — validação administrativa sem RPC de UUID livre (2026-08-17).** Erro real em produção local (`permission denied for table admin_user`) expôs que o conector de P8 fazia `SELECT` direto em `admin_user`, tabela sem `SELECT` para `service_role` de propósito (`ADR-021`). Correção inicialmente proposta (função `is_admin_user(uuid)` `SECURITY DEFINER` com `EXECUTE` para `service_role`) identificada como conflitante com `ADR-021` — que já registra esse padrão como alternativa avaliada e rejeitada — antes de qualquer execução; divergência sinalizada explicitamente a Fabrício, que optou por preservar o ADR integralmente. `3082`/`3083` (`CONFIRMADO EXECUTADO`, dentro do bloco de dez de `pricing_sync_run`, não transversal): coluna `confirmed_by UUID NOT NULL` (sem `FK`) e trigger `BEFORE INSERT` `SECURITY DEFINER`/`search_path=''` (`validate_pricing_sync_run_confirmed_by()`, `EXECUTE` revogado de todos os papéis) validando `NEW.confirmed_by` contra `admin_user` como efeito colateral do primeiro `INSERT` do piloto — nunca uma função RPC isolada com parâmetro UUID. `requireAdmin()` (causa raiz) removida de `scripts/sync-justtcg-pricing.ts`. Testado via `SET ROLE service_role`: UUID admin real insere, UUID aleatório falha com erro genérico, zero resíduo. `ADR-021` não sofreu nenhuma revisão. Ver `ADR-029`, revisão `1.11`, e `docs/05f-pricing.md`, revisão `1.11`. |
| 1.33 | **Reconciliação retroativa da faixa `3000`–`3090` da divergência de rastreabilidade registrada na revisão `1.27` (2026-08-17, ciclo posterior, pedido explícito de Fabrício).** Os 22 arquivos de migration de Pricing (P1–P6) confirmadamente aplicados no Supabase mas ausentes do repositório foram extraídos verbatim de `supabase_migrations.schema_migrations` (`statements[1]`, somente leitura — nenhuma migration reexecutada, nenhum histórico do Supabase alterado) e criados em `database/migrations/` com nome e SQL preservados, mais um cabeçalho de reconstrução informando origem e data de recuperação; cada corpo recuperado foi comparado por `md5()` contra o statement armazenado antes de ser aceito (manifest de 22/22 correspondências exatas). Varredura de segredos sem achado. A migration `2050` e a faixa `2098`–`2153` (mencionadas na mesma divergência original) seguem não reconciliadas. Ver `ADR-030`, `docs/ROADMAP.md` ("Débitos Técnicos Registrados") e `docs/development/HANDOFF-2026-08-17.md`, seção 4. |
| 1.34 | **Incremento P10 — Projeção Cambial PTAX (2026-08-17), a pedido explícito de Fabrício.** Primeiro uso real da faixa `3900`–`3999` (Reserva) do módulo Pricing: `3900` (`CONFIRMADO EXECUTADO`), view `public.pricing_observation_brl_equivalent` — projeta observações `currency_code = 'USD'` de `pricing_observation` para equivalente em BRL via `LEFT JOIN LATERAL` contra a última `pricing_fx_rate` (`BCB_PTAX`) com `rate_date <=` data UTC de `observed_at`; `round(preço * taxa, 2)`; sem taxa aplicável, `equivalent_brl_amount` nulo e `fx_status = 'FX_RATE_UNAVAILABLE'`; rótulo fixo embutido na própria projeção (`equivalent_label = 'Equivalente em BRL pela PTAX Venda'`, nunca "Valor Brasil"). Sem migration/alteração em `pricing_observation`/`pricing_fx_rate`. `security_invoker = true` (mesmo padrão já usado por `catalog_card_set_metrics` e as demais views do Catálogo Editorial) — a RLS de ambas as tabelas base (`pricing_admin_select`) é aplicada ao papel que efetivamente consulta a view, não ao seu dono; `GRANT SELECT` só a `authenticated`/`service_role`, idêntico ao já concedido nas tabelas base — nenhuma ampliação de acesso. Não pertence ao bloco de dez de nenhuma entidade específica — transversal a `pricing_observation`/`pricing_fx_rate`, mesmo padrão de correção/extensão transversal já usado em `3053`/`3091`. Validado com dado real de produção (32 observações, todas convertidas corretamente) e com fixtures sintéticas transacionais (`BEGIN`/`ROLLBACK`): data exata, sábado/domingo resolvendo para a sexta-feira anterior, data anterior à primeira taxa cadastrada (`FX_RATE_UNAVAILABLE`), e impossibilidade estrutural de dupla conversão (observação `BRL` inserida na mesma transação nunca aparece na projeção). Segurança revalidada com `SET LOCAL ROLE`: `anon` bloqueado (`insufficient_privilege`), `authenticated` não-admin recebe zero linhas, `authenticated` admin e `service_role` recebem as mesmas linhas que já viam nas tabelas base. Plano de execução confirmado com volume sintético (20.000 observações/522 taxas, transacional): `Index Scan`/`Index Scan Backward` em ambos os lados do `LATERAL` no padrão de consulta real (por produto/condição/tipo), sem `Seq Scan` no caminho quente. `get_advisors` (segurança e performance): zero achado novo referenciando a nova view. Ver `ADR-029`, revisão `1.15`, e `docs/05f-pricing.md`, seção "Incremento P10". |
| 1.35 | **Incremento P11 — Contrato Seguro de Leitura de Preços por Card (2026-08-17), a pedido explícito de Fabrício.** `3901` (`CONFIRMADO EXECUTADO`), função `public.get_card_pricing_snapshot(p_card_id uuid)` — `SECURITY DEFINER STABLE`, `search_path = ''`, verificação explícita de `auth.uid() IS NOT NULL` (nunca `is_admin()`), mesmo padrão de segurança de `search_cards()`/`search_card_filter_options()` (`ADR-030`); `REVOKE ALL FROM PUBLIC, anon` + `GRANT EXECUTE TO authenticated` (`service_role` deliberadamente sem `EXECUTE`). Primeiro caminho de leitura de Pricing para usuário autenticado não-administrador — contorna `pricing_admin_select`/`card_condition_admin_select` por desenho, só através de projeção curada (snapshot mais recente por produto/condição/tipo de preço/mercado, nunca `external_product_id`/`raw_payload`/histórico completo), com filtros de negócio (mapping `CONFIRMED`, produto ativo, fonte ativa) aplicados no corpo da função. `3902` (`CONFIRMADO EXECUTADO`, mesmo incremento): índice `ix_pricing_observation_snapshot_lookup (pricing_product_id, condition_id, price_type, market_label, observed_at DESC)` em `pricing_observation`, acrescentado após achado real de `EXPLAIN (ANALYZE, BUFFERS)` — `ix_pricing_observation_latest_lookup` (Incremento P6) não cobria `market_label`, novo campo de agrupamento; sem o índice, o `DISTINCT ON` caía num `Sort` externo em disco sobre todo o histórico do produto (mesmo precedente de índice pós-achado real já usado na Query `3052`, Incremento P4). Validado com `SET LOCAL ROLE` real (não a conexão de service role do MCP): `anon` bloqueado por `insufficient_privilege`; `authenticated` sem JWT bloqueado pela própria função; `authenticated` não-admin recebe exatamente o mesmo resultado que `authenticated` admin; leitura direta das tabelas base por `authenticated` permanece em zero linhas; enumeração por UUID indistinguível entre carta inexistente e carta sem dado de Pricing; exclusão seletiva por mapping/produto validada com dado real do piloto P8. Performance: ~166ms em volume sintético extremo (50.000 observações para uma carta), ~27ms em volume realista (~1.000 observações/produto) — o desempate por `created_at`/`id`, não coberto por índice, ainda força um `Sort` interno residual no cenário extremo, achado registrado como sinal a reavaliar no futuro, não resolvido nesta rodada. `get_advisors`: único achado novo é `authenticated_security_definer_function_executable` (`INFO`, esperado, mesmo padrão de `search_cards()`/funções `admin_*`). `JUSTTCG` permanece `is_active = FALSE` — contrato confirmado retornando zero linhas em produção. Correção de processo registrada: uma transação de teste terminou com `COMMIT` em vez de `ROLLBACK` por erro do agente (ativação temporária de `pricing_source.is_active`), revertida em menos de um minuto, sem consequência de dado. Ver `ADR-029`, revisão `1.16`, e `docs/05f-pricing.md`, seção "Contrato Seguro de Leitura de Preços por Card (Incremento P11)". |
| 1.36 | **Incremento P12 v4 — Resumo de Preço em Lote sem N+1 (2026-08-18), a pedido explícito de Fabrício.** `3903` (`CONFIRMADO EXECUTADO`), função `public.get_cards_pricing_summary(p_card_ids uuid[])` — variante em lote de `3901` (Incremento P11), mesmo padrão de segurança (`SECURITY DEFINER STABLE`, `search_path = ''`, `auth.uid() IS NOT NULL`, `REVOKE ALL FROM PUBLIC, anon` + `GRANT EXECUTE TO authenticated`), com dois guards de validação de entrada adicionais (array vazio/nulo e mais de 100 elementos, ambos `ERRCODE 22023`). Motivada por um N+1 real que a primeira versão do endpoint batch do frontend (`POST /api/cards/pricing/batch`, sem migration, registrado no ciclo anterior) ainda tinha: resolvia o N+1 cliente→servidor com uma única requisição do navegador, mas chamava `3901` uma vez por carta em paralelo no servidor, ainda N consultas reais ao Postgres. Esta função resolve o lote inteiro numa única consulta SQL — CTE `input_ids` (`DISTINCT unnest`) `LEFT JOIN` CTE `candidate` (`DISTINCT ON (card_id)`, uma observação por carta sob regra fixa) `LEFT JOIN LATERAL` PTAX (mesma lógica de `3901`, restrita a `currency_code = 'USD'`) — `LEFT JOIN` garante exatamente uma linha de saída por `card_id` de entrada, mesmo sem nenhuma observação correspondente. Retorno deliberadamente mínimo (`card_id`, `has_pricing`, `brl_amount`, `fx_status`), pedido literal de Fabrício ("retorno mínimo... valor BRL padrão NM/Normal"): seleção fixa e determinística — condição NM (`card_condition.code = 'NM'`), printing "Normal" (`pricing_product.source_printing_label = 'Normal'`), `price_type = 'MARKET'` — não uma heurística de "melhor preço". Consequência real desta regra, nesta revisão específica: cartas cujo único printing catalogado é Holofoil (ex.: "ex" quase sempre holo-exclusivas) não satisfaziam a condição e por isso não tinham `has_pricing = true` por esta função, mesmo com preço real sob outro printing — confirmado com os 6 cards piloto do Incremento P8 (Abra/Arcanine/Bulbasaur resolviam; Alakazam/Mega Gardevoir ex/Mega Venusaur ex são Holofoil-only e não resolviam). **Corrigido na revisão `3904` — ver `1.37`, abaixo**: tratado como bug real (ausência de fallback entre printings), não limitação deliberada. Validado com `SET LOCAL ROLE`/`SET LOCAL request.jwt.claims` real: `anon` bloqueado por `permission denied` (sem `GRANT EXECUTE`); `authenticated` sem `sub` no JWT bloqueado pela própria função (`PRICING_SUMMARY_REQUIRES_AUTHENTICATION`); array vazio e array de 101 elementos rejeitados pelos guards de entrada; simulação transacional (`BEGIN`/`ROLLBACK`, `pricing_source.is_active` temporariamente `TRUE`) confirmou os valores reais esperados, incluindo `R$ 1,14` para Bulbasaur — mesmo número que Fabrício já havia citado organicamente antes desta validação rodar, forte sinal de correção independente. Performance: `EXPLAIN (ANALYZE, BUFFERS)` com 100 `card_id` reais — 12,9ms, um único `Function Scan`, sem laço. `get_advisors`: único achado novo é `authenticated_security_definer_function_executable` (`WARN`, esperado, mesmo padrão de `3901`); nenhum achado de performance referenciando a função nova. `JUSTTCG` permanece `is_active = FALSE` — confirmado inalterado ao final da rodada (zero resíduo). Endpoint `POST /api/cards/pricing/batch` reescrito para chamar esta função uma única vez no modo live (antes, `Promise.all` por carta); `CardPriceSummary` estendido ao terceiro grid do Catálogo (Coleções, `card-set-cartas-grid.tsx`), completando os três grids pedidos (Coleções/Catálogo de Cartas/Pesquisa). Ver `ADR-029`, revisão `1.20`, e `docs/05f-pricing.md`, seção "Painel de Preços da Carta (Incremento P12)". |
| 1.37 | **Incremento P12 Rodada 8 — hierarquia de printing na função de resumo, correção de bug real (2026-08-18), a pedido explícito de Fabrício.** `3904` (`CONFIRMADO EXECUTADO`), `public.get_cards_pricing_summary(p_card_ids uuid[])` recriada via `DROP FUNCTION` + `CREATE FUNCTION` (não `CREATE OR REPLACE` — muda a assinatura de retorno, nova coluna `printing_label`); `3903` não foi editada. Um teste transacional dedicado ("Reverse-only", dado real do Bulbasaur ME1 #001) provou que `3903` não tinha nenhum fallback de printing — hardcoded a `source_printing_label = 'Normal'`; a ausência de resumo para cartas Holofoil-only, registrada na revisão `1.36` como "consequência real da regra, não bug", era na verdade sintoma do mesmo bug. Implementa a hierarquia aprovada — Normal → Holofoil → Reverse Holofoil, sempre dentro de NM+MARKET — com `has_pricing=true` se qualquer um dos três tiver preço elegível; `printing_label` (texto cru) devolve ao chamador qual printing foi escolhido. Bug de implementação real (`ERROR: 42702: column reference "card_id" is ambiguous` — identificador não qualificado ambíguo contra o próprio parâmetro `RETURNS TABLE(card_id uuid, ...)` da função) encontrado e corrigido no mesmo ciclo, antes de qualquer validação reportada, qualificando as referências com o alias `cbp`. Mesmo padrão de segurança e grants de `3903`, revalidado sem alteração; `EXPLAIN (ANALYZE, BUFFERS)` em 100 `card_id` reais — 10,44ms, comparável aos 12,9ms de `3903`, sem regressão; `get_advisors` sem achado novo além do esperado. Quatro cenários obrigatórios validados com dado real, todos transacionais com `ROLLBACK`, zero resíduo: Bulbasaur Normal → `Normal`; Alakazam/Mega Gardevoir ex/Mega Venusaur ex (Holofoil-only) → `Holofoil`; Bulbasaur com Normal/Holofoil temporariamente desativados → `Reverse Holofoil`; carta sem mapping → `has_pricing=false`. Contrato frontend atualizado (`PricingLiveSummary`/Route Handler ganham `printingLabel`; `CardPriceSummary` inclui o printing resolvido no `aria-label` do pill). Ver `ADR-029`, revisão `1.22`, e `docs/05f-pricing.md`, revisão `1.29`, seção "Rodada 8 do Incremento P12". |
| 1.38 | **Incremento P13.1 — Fundação de Orquestração Programada de Pricing (2026-08-18), a pedido explícito de Fabrício.** `3905`–`3907` (`CONFIRMADO EXECUTADO`), evolução do bloco de dez de `pricing_sync_run` para suportar execuções `SCHEDULED` de PTAX — nenhuma extensão habilitada, Vault secret criado, Edge Function implantada, chamada externa ou código JustTCG nesta rodada. `3905`: `FX_REFRESH` adicionado a `ck_pricing_sync_run_type` sem reaproveitar `PRICE_REFRESH`; nova coluna `fx_source_code TEXT` (identidade cambial explícita, alinhada ao domínio de `pricing_fx_rate.rate_source_code`, sem `FK` — mesmo motivo de `rate_source_code`); `pricing_source_id` passa a `NULL`able; novo `CHECK ck_pricing_sync_run_source_identity` (FX_REFRESH ⟺ `pricing_source_id IS NULL` ∧ `fx_source_code IS NOT NULL`; demais run_types ⟺ o inverso). `3906`: `confirmed_by` passa a `NULL`able; novo `CHECK ck_pricing_sync_run_confirmed_by_by_trigger` (`MANUAL` ⟹ `confirmed_by` preenchido; `SCHEDULED` ⟹ `NULL`) — nenhum administrador sintético criado; `validate_pricing_sync_run_confirmed_by()` recriada via `CREATE OR REPLACE` (mesma assinatura/`SECURITY DEFINER`/`search_path=''`/`EXECUTE` revogado preservados) para só consultar `admin_user` quando `confirmed_by IS NOT NULL`. `3907`: dois índices únicos parciais (`ux_pricing_sync_run_active_price_per_source_type`/`ux_pricing_sync_run_active_fx_per_source_type`, estados ativos `RECEIVED`/`PROCESSING`) garantindo aquisição atômica de execução — nunca advisory lock; cada índice exclui `NULL` da própria coluna por predicado, então `NULLS NOT DISTINCT` não é necessário aqui (diferente de `pricing_observation`/`3070`). Nenhum `GRANT` novo — `service_role` já tinha `INSERT`/`SELECT` em nível de tabela, cobrindo `fx_source_code` automaticamente; coluna nova sem `UPDATE`, mesmo padrão restritivo de `pricing_source_id`/`run_type`/`triggered_by`. Onze cenários validados transacionalmente com `ROLLBACK` (MANUAL com/sem admin real, SCHEDULED com/sem `confirmed_by`, FX_REFRESH sem `fx_source_code`, FX_REFRESH com `pricing_source_id`, run de preço sem `pricing_source_id`, duas execuções ativas conflitantes — cambial e de preço —, estado terminal liberando nova execução, fluxo existente da JustTCG sem regressão), 5 linhas reais de produção intocadas, zero resíduo; `get_advisors` sem achado novo referenciando `pricing_sync_run`/`fx_source_code`. `ADR-030` já em uso (Card Search Projection) — arquitetura registrada em `ADR-031`, criado nesta rodada. Ver `ADR-031` e `docs/05f-pricing.md`, seção "Incremento P13.1". |
| 1.39 | **Auditoria final do P13.1 (2026-08-18, mesmo dia) — dois achados reais, ambos corrigidos no mesmo ciclo.** (1) `fx_source_code` (`3905`) não exigia o formato normalizado já usado por `pricing_source.code`/`pricing_fx_rate.rate_source_code` (maiúsculas, `^[A-Z][A-Z0-9_]*$`) — `3905` não foi editada (aditiva); `3908` (`CONFIRMADO EXECUTADO`) adiciona `ck_pricing_sync_run_fx_source_code_format`, replicando o mesmo padrão. Oito cenários validados transacionalmente com `ROLLBACK` antes da aplicação; zero linha real afetada. (2) `3905`–`3907`, embora `CONFIRMADO EXECUTADO` no Supabase, estavam ausentes de `database/migrations/` — quebra da convenção vigente desde a Query `3700`; os quatro arquivos (`3905`–`3908`) foram versionados retroativamente neste ciclo, SQL idêntico ao efetivamente aplicado. `get_advisors` sem achado novo. Ver `ADR-031`, revisão `1.1`, e `docs/05f-pricing.md`, seção "Incremento P13.1". |
| 1.40 | **Incremento P13.4 — Agendamento Automático via `pg_cron`/`pg_net`/Vault (2026-08-18, mesmo dia), a pedido explícito de Fabrício.** `3910` (`CONFIRMADO EXECUTADO`): primeiro uso real de extensões de agendamento/rede assíncrona no projeto — habilita `pg_cron` (`WITH SCHEMA pg_catalog` + os dois `GRANT` oficiais de instalação a `postgres`) e `pg_net`, e cria de forma idempotente o único `cron.job` (`ptax-fx-refresh-weekdays`, `0 22 * * 1-5`), cujo comando chama `net.http_post` diretamente com `url`/`apikey` resolvidos por nome via `vault.decrypted_secrets` (nunca valor literal no comando). Testado transacionalmente (`BEGIN`/`ROLLBACK`) antes da aplicação real; chamada controlada real pelo mesmo caminho confirmou `HTTP 200`/`COMPLETED`. **Registro retroativo de `3909`** (P13.2, correção de autoridade temporal de `pricing_sync_run.started_at`/`finished_at` via trigger dedicado) na mesma rodada, fechando um gap de rastreabilidade encontrado durante a introspecção deste incremento — mesmo padrão de reconciliação já usado para `3905`–`3908`. `get_advisors` (performance) sem achado novo relacionado a `cron`/`net`/`pricing_sync_run`. Ver `ADR-031`, revisão `1.8`, e `docs/05f-pricing.md`, seção "Incremento P13.4". |
| 1.41 | **Correção de texto stale (2026-08-24), micro-rodada de governança documental pedida por Fabrício.** O parágrafo de fechamento do Módulo Pricing (acima) ainda descrevia o domínio como "ainda não operacional" — verdadeiro em 2026-08-16 (fim da fundação física, P1–P6), obsoleto desde P14.1 (2026-08-19, `JUSTTCG.is_active = TRUE`) e P15/dispatcher (refresh automático em produção). Corrigido para apontar ao estado vigente em `docs/05f-pricing.md` em vez de restar uma alegação desatualizada neste Standard — nenhuma mudança de padrão/regra de banco, só da frase de status. Nenhum código/migration/Supabase alterado. |
| 1.42 | **Incremento `COLLECTIONS-PHYSICAL-INCREMENT-02G-IMPLEMENTATION-01` — Pokémon Catalog Foundation (2026-09-04).** Comprometido o milhar `6000`–`6999` = Pokémon Catalog Foundation (`ADR-011` v1.2), com subseção própria (mesmo padrão de `1000`–`1999`/`2000`–`2999`/`3000`–`3999`/`4000`–`4999`) detalhando `pokemon_generation` (`6000`/`6001`, `CONFIRMADO EXECUTADO`, sem `game_id` — decisão congelada), `pokemon_species` (`6010`/`6011`, `CONFIRMADO EXECUTADO`, mesma decisão, `national_dex_number` corrigível administrativamente por desenho), `pokemon_species_external_reference` (`6020`/`6021`, `CONFIRMADO EXECUTADO`, RLS completamente fechado — evidência de integração, não dado self-service), Seed `6700` (linha `POKEAPI` em `asset_source`, `CONFIRMADO EXECUTADO`), Validações (`6800`–`6899`, transacional, sem migration física) e Reserva (`6900`–`6999`). Também corrigido nesta revisão: a "Módulos definidos" (Seção 10) passou a registrar explicitamente `5000–5084` = Collections — gap de documentação pré-existente desde 2026-08-30 (o módulo já estava `CONFIRMADO EXECUTADO` em `database/schema/`, mas esta seção nunca o declarava), corrigido sem renumerar nenhuma Query já executada. Gap não bloqueante registrado na subseção do novo módulo: `pricing_source` (`3000`–`3002`, `CONFIRMADO EXECUTADO` conforme revisão `1.20` acima) não localizada como arquivo em `database/schema/` durante a auditoria física deste incremento — possível lacuna de promoção canônica do módulo Pricing, não investigada a fundo, fora do escopo de `02G`. Validação estrutural de 18 itens e 13 testes comportamentais executados ao vivo via `BEGIN...ROLLBACK`, zero resíduo. Precedida por uma rodada de auditoria física sem alteração de banco (`COLLECTIONS-PHYSICAL-INCREMENT-02G-MODELING-AUDIT-01`) e staging auditado em `database/proposals/2026-09-04-02g-pokemon-catalog-foundation/`. Nenhuma decisão conceitual/lógica reaberta (LDM-175 a LDM-185 permanecem intactas). Ver `docs/05d-colecoes-e-usuarios.md`, revisão `1.9`, e `docs/log.md`. |